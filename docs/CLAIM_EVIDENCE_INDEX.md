# Claim vs Evidence Index

Every substantive claim seLe4n makes in public — in the README, the
specification, the website — with the command that checks it and the artefact
that carries it. If a claim is not in this table, it is not a claim the project
stands behind.

**This is an index, not a record.** What each version changed is in
[`CHANGELOG.md`](../CHANGELOG.md); what is in flight is in
[`REGISTERED_DEBT.md`](REGISTERED_DEBT.md).

## 1. Proof surface

| Claim | Where it is made | Check it with | Artefact |
|-------|------------------|---------------|----------|
| Zero `sorry` and zero `axiom` in the production proof surface | README, `SELE4N_SPEC.md` | `./scripts/test_tier0_hygiene.sh`; `python3 scripts/check_module_axioms.py` | `SeLe4n/`, `Main.lean` |
| Proofs are not vacuous one-liners | `SELE4N_SPEC.md` | `python3 scripts/check_proof_depth.py` | proof bodies across `SeLe4n/` |
| Every kernel transition is an executable pure function returning explicit success or failure | README, `SELE4N_SPEC.md` | `./scripts/test_tier2_negative.sh` | `SeLe4n/Kernel/*/Operations*.lean` |
| Every golden fixture is listed in `tests/fixtures/README.md`'s `## Files` table **and** compared by the gate its own row names — one fixture per row, so the membership question and the consumer question are about the same file | `tests/fixtures/README.md` | `python3 scripts/scenario_catalog.py check-fixture-index` (Tier 0); `python3 -m unittest scripts.tests.test_scenario_catalog` (Tier 0) | `fixture_table_rows` enforces the row shape once and both checks read it, so neither can proceed on a row the other refused; `check_fixture_index` reconciles the directory against the table in both directions over PARSED CELLS, and `check_fixture_consumers` validates each row's `Used by` claim per fixture KIND (a manifest names `MANIFEST_CONSUMER`'s own discovery; every other fixture names a repository path that mentions it in the code view and consumes the name it binds it to).  `TestFixtureRowShape` witnesses both controls and every rejecting case, with each conjunct separately mutation-tested — dropping the one-fixture count, the `.sha256`-as-fixture refusal, the companion match and the hash-cell count each fail a different set, so none is rescued by a partner |
| The kernel model is deterministic — the same input trace yields the same output | `SELE4N_SPEC.md` | `./scripts/test_tier2_determinism.sh` | `tests/fixtures/main_trace_smoke.expected` |
| Named theorems and invariants still exist and still say what the docs claim | this file | `./scripts/test_full.sh` (Tier 3) | `scripts/test_tier3_invariant_surface.sh` |
| Every artefact **this file** names as evidence exists in the tree | this file | `./scripts/check_claim_evidence_citations.py` (Tier 0; `--self-test`, 9 cases) | both sides derived — every backticked identifier here against every name the tree declares in Lean, Rust, assembly, Python and shell, plus file-named artefacts; `CITATION_EXEMPTIONS` carries the names the index discusses without owning, each with its reason |
| The proof surface introduces no `sorry`, no `axiom` and no `native_decide` | `SELE4N_SPEC.md`, `CLAUDE.md`, `SMP_FOUNDATIONS_PLAN.md` §6.3 | `./scripts/test_tier0_hygiene.sh` | the forbidden-marker scan, read over the comment-free code view so a docstring explaining why `decide` is used instead cannot trip it; `native_decide` admits `Lean.ofReduceBool` and would take the Lean compiler and runtime into the TCB, so it is an axiom under another name |
| Production never imports staged modules | `SELE4N_SPEC.md` | `./scripts/check_production_staging_partition.sh` | `scripts/staged_module_allowlist.txt` (64 modules) |
| Every kernel entry the HAL links against is defined — by the built archive, a defined symbol in assembly `build.rs` assembles, or a reconciled expected-unresolved entry — and the boot entry, once exported, calls the checked platform boot | `SELE4N_SPEC.md`, `CLAUDE.md` | `./scripts/check_kernel_entry_exports.py` (Tier 1, after `lake build SeLe4n:static`; `--self-test`, 22 cases) for the link half; `lake build SeLe4n.Testing.BootEntryContract` (Tier 1) for the boot-entry half | `nm --defined-only` over `.lake/build/lib/libseLe4n_SeLe4n.a`; every HAL `extern "C"` declaration is required (`classify_link_requirements`), so a rename on either side fails; `asm_definitions_in` (outside cpp conditionals) ∧ `assembled_sources_in` (the live builder chain) ∩ the assembled archive's `nm` definitions when present; the boot entry's contract is decided by the **elaborator** rather than by a scan (`bootEntryContractViolations`, `isApprovedBootApplication`, `expectedBootEntryType`), so aliasing, shadowing and attribute spelling are not questions it can get wrong |
| A hardware boot without a verified deployment labeling context fails closed | `SELE4N_SPEC.md` §6.7, `CLAUDE.md` | `lake exe syscall_dispatch_suite` (SD-043/SD-044) | `Platform.FFI.bootAndInitialiseFromPlatform` (mandatory context; guard before commit), `insecureLabelingContextBootError` |
| The labeling a hardware boot installs is bound in the platform contract, admitted and `LabelingContextValid` by construction, its declared separation witnesses are installed threads of the boot state, neither witness is the binding's boot VSpace root's id, and the hardware entry boots the binding's own configuration at the RPi5 binding | `SELE4N_SPEC.md` §6.7, `CLAUDE.md` | `lake exe syscall_dispatch_suite` (SD-046, SD-055, SD-056); Tier 3 | `PlatformBinding.deploymentLabeling : DeploymentLabeling`, `PlatformBinding.labeling_admitted`, `PlatformBinding.labeling_valid`, `rpi5_deploymentLabeling` (`rfl`), `rpi5LowerWitnessIndex_ne_bootVSpaceRoot`, `PlatformBinding.witnessesOffBootVSpaceRoot` (class obligation), `witnesses_ne_bootVSpaceRoot`, `bootAndInitialisePlatform_eq_checked_boot`, `bindPlatformConfig`, `PlatformBinding.bindMachineConfig`, `PlatformBinding.boundConfig_declaredCoreCount`, `rpi5BoundMachineConfig`, `rpi5BoundMachineConfig_mem_family`, `rpi5PlatformConfigFromDtb_ok_binds_detected_variant`, `bootAndInitialiseRPi5`, `bootAndInitialiseRPi5_bound_config`, `bootAndInitialisePlatform_checked_declaredCoreCount`, `declaredWitnessesInstalled`, `uninstalledSeparationWitnessBootError` |
| The admitted separation witness names neither the sentinel nor an idle thread | `SELE4N_SPEC.md` §6.7 | `lake exe information_flow_suite` | `separationWitnessAdmissible`, `separationWitnessAdmissible_idleThreadId`, `isInsecureDefaultContext_false_implies_witness_not_idle`, `upperWitnessIndex` |
| The insecure-context guard decides non-triviality rather than sampling for it | `SELE4N_SPEC.md` §6.7 | `lake exe information_flow_suite` | `isInsecureDefaultContext_false_implies_labelNonTriviality`, `deploymentLabelingContext_valid` |
| Every core the binding declares boots with its idle thread enqueued on its own run queue, the RPi5 binding declares every model core, no binding declares more cores than the model has, and an undeclared core's idle slot is absent after the boot | `SELE4N_SPEC.md` §6.5.5, §6.7, `CLAUDE.md` | `lake exe smp_idle_suite`, `lake exe syscall_dispatch_suite` (SD-045, SD-055, SD-056), `lake exe smp_foundations_suite` | `bootFromPlatformCheckedWithIdleThreads_idle_available` (production), `…_idleThreadEnqueuedOnCore` (staged, discharges `schedulerNoStall_smp`'s `hIdle`); `PlatformBinding.declaredCores`, `PlatformBinding.coreCountLe` (class obligation), `declaredCores_length`, `mem_declaredCores_iff`, `bootCoreModelId_mem_declaredCores`, `bootFromPlatformCheckedWithIdleThreadsFor_allCores`, `…For_undeclared_idle_absent`, `rpi5_cores_eq_allCores`, `bootAndInitialisePlatform_rpi5_all_cores` |
| The boot run queue is exactly the idle singleton on every core, so per-core selection succeeds with no hypothesis beyond the boot and first selects idle | `SELE4N_SPEC.md` §6.5.5, `CLAUDE.md` | `lake exe smp_idle_suite`; Tier 3 | `bootFromPlatformCheckedWithIdleThreads_runQueueOnCore_eq`, `…_runQueueOnCore_wellFormed`, `…_runnable_resolve` (production); `…_chooseThreadOnCore_succeeds`, `…_chooseThreadOnCore_idle` (staged) |
| The production boot state is `threadStateConsistent` — a boot-state theorem; the inactive-flag relation the live decisions read is `threadInactiveFlagConsistent`, and its preservation is registered debt — and the checked boot reserves the idle slots and every reference to them — an untyped's allocation and ancestry records and a TCB's own thread id included — with its own diagnostic, and stores every boot TCB under its own thread id | `SELE4N_SPEC.md` §6.5.5, `CLAUDE.md`, `docs/REGISTERED_DEBT.md` | `lake exe smp_idle_suite`; Tier 3 | `queuedIdleThread`, `bootFromPlatformCheckedWithIdleThreads_threadStateConsistent`, `…_threadInactiveFlagConsistent`, `idleSlotsReserved`, `bootObjectReferencesReservedIdleSlot`, `bootFromPlatformChecked_ok_idleSlotsFreshAt` |
| No syscall can act on a per-core idle TCB: a capability naming one resolves like an empty slot at the single resolution every invoked capability passes through | `SELE4N_SPEC.md` §6.5.5, `CLAUDE.md` | `lake exe syscall_dispatch_suite` (SD-054); Tier 3 | `capTargetsReservedIdleObject`, `syscallResolveCap_ok_not_reserved` |
| Every syscall the **hardware** path serves is capability gated: a successful flow-checked dispatch implies the caller held a capability carrying the syscall's required right, on the executing core and on the state the caller trapped in, through both gate shapes and out to the exported ABI seam | `SELE4N_SPEC.md` §6.5.5, GitBook 3 | `lake build SeLe4n.Platform.FFI`; Tier 3 | `dispatchSyscallChecked_requires_right`, `syscallEntryChecked_implies_capability_held`, `…_of_pre_state`, `syscallDispatchFromAbi_implies_capability_held`, `dispatchWithCapChecked_audit_success_requires_right`, `resolveCapAddress_congr_objects` |
| The deployment source carries the policy fields a binding configures, with fail-closed defaults and no bearing on validity | `SELE4N_SPEC.md` §6.7 | `lake exe information_flow_suite`; Tier 3 | `DeploymentLabeling.{memoryOwnership,endpointPolicy,declassificationPolicy,auditMonitorClearance}`, `deploymentLabelingContext_policy_fields`, `deploymentLabelingContext_valid` |
| Every hardware seam consults the per-core Lean-runtime readiness gate, over an export inventory derived from the comment-free Lean view | `CLAUDE.md`, `rust/sele4n-hal/src/kernel_entry.rs` | `cargo build -p sele4n-hal` (build.rs derivation; `verify_lean_export_collector`, 6 token-preserving mutations) | `scan_lean_upcalls_readiness_gated`, `lean_code_view`, `lean_exports_in`; `LEAN_UPCALLS_OUTSIDE_THE_GATE` holds one entry, the boot install |
| On a not-ready core every `SVC` halts, before the prefilters and the `x7` narrowing | `CLAUDE.md` | `cargo build -p sele4n-hal` (`svc_arm_readiness_gate_status`), `cargo test` (`readiness_gate_before_mark`, `…_after_mark`) | the gate is a top-level statement of the SVC arm ahead of the `dispatched` binding, on the executing core, ending in the diverging halt |
| The lock-order tripwire halts the holder, never a waiter | `CLAUDE.md` | `cargo test -p sele4n-hal` (`tripwire_trips_on_own_round_lock_hold_only`, `round_lock_records_its_owner`) | `round_lock_held_by`, `assert_not_holding_round_lock(core_id)` |
| Each release-surviving tripwire halts on its declared failure predicate — the whole condition, not a token of it — in every profile, ends in `fatal_halt` rather than any divergence, and dominates the operation it protects | `CLAUDE.md`, `docs/REGISTERED_DEBT.md` | `cargo build -p sele4n-hal` (build.rs self-check with polarity and dominance mutations) | `release_surviving_tripwire_status`, `tripwire_dominates_protected_operation`, `RELEASE_SURVIVING_TRIPWIRES`, `verify_release_surviving_tripwire_polarity` |
| A Lean symbol is linker-visible only under `feature = "hw_target"`, decided over the parsed `cfg` predicate | `CLAUDE.md` | `cargo build -p sele4n-hal` (build.rs self-check, 12 token-preserving mutations) | `lean_extern_gating_status`, `cfg_predicate_entailment`, `header_is_linker_visible` |

## 2. Kernel invariants

| Claim | Where it is made | Check it with | Artefact |
|-------|------------------|---------------|----------|
| `ipcInvariantFull` (20 conjuncts) is machine-checked end to end: no bundle in the family assumes a conjunct on its own post-state | `SELE4N_SPEC.md`, GitBook 12 | `python3 scripts/check_ipc_invariant_dethreading.py` | `SeLe4n/Kernel/IPC/Invariant/` |
| The bundle carries across a whole syscall dispatch | `SELE4N_SPEC.md`, GitBook 12 | `lake build SeLe4n.Kernel.API` | `dispatchCapabilityOnly_preserves_ipcInvariantFull` (production); `dispatchWithCap_…` / `dispatchSyscall_…` (staged, `IPC/Invariant/DispatchPayoff.lean`) |
| The dispatch payoff's quiescence packs are inhabited, not vacuous | GitBook 12 | `lake build SeLe4n.Kernel.IPC.Invariant.DispatchPayoff` | `…Quiescence_inhabited` family |
| Capability derivation is acyclic and complete, so revocation terminates and is total | `SELE4N_SPEC.md` | `lake build SeLe4n.Kernel.Capability.Invariant` | `capabilityInvariantBundle` |
| Twelve cross-subsystem predicates hold, including blocking-graph acyclicity | `SELE4N_SPEC.md` | `lake build SeLe4n.Kernel.CrossSubsystem` | `crossSubsystemInvariant` |
| Slot uniqueness and waiter uniqueness are structural, not state predicates | GitBook 12 | `lake build SeLe4n.Model.Object.Structures` | `UniqueSlotMap`, `NoDupList ThreadId` |
| The SchedContext donation chain is well formed: the stack is **doubly linked**, every `prev` link is answered by the frame it names, every head is answered by its context, and each context's head walks a terminating chain — preserved through a frame stated over the projections the walk reads, not assumed | `SELE4N_SPEC.md` §8.12.7 | `lake build SeLe4n.Kernel.IPC.Invariant.Reachability`; `lake exe smp_ipc_suite` (§3.15) | `donationChainWellFormed`, `donationChainFrom`, `replyStackLinks?`, `schedContextStackHead?`, `donationChainFrame`, `donationChainWellFormed_of_frame`, `donationChainFrom_mem`, `not_mem_donationChainFrom_of_unlinked`, `ReplyStackLink`, `Reply.wellFormed`, `Reply.isFree`, `ipcReachable`, `returnDonatedSchedContext_preserves_donationChainWellFormed`, `donationChainFrom_head_not_mem_tail`, `donationChainFrom_congr_on_chain`, `donationChainWitness_pop_wellFormed`, `donationChainWitness_pop_chain`, `donateSchedContext_preserves_donationChainWellFormed`, `donationHeadPush_preserves_donationChainWellFormed`; **no longer vacuous since WS-OD OD4.1 (`v0.35.2`)** — a depth-2 Call builds a two-frame stack, and every writer of `Reply.prev`, `Reply.next` and `SchedContext.scReply` (the push, the pop, the middle-frame splice, and the freshening/consumption paths) carries a frame or a preservation theorem |
| The donation return is a reply-stack **pop**, and at the bottom of the stack it is the pre-OD3 return character for character — so the structure is live and the behaviour is unchanged | `SELE4N_SPEC.md` §8.12.7 | `lake build SeLe4n.Kernel.IPC.Operations.Endpoint`; `lake exe smp_ipc_suite` (§3.16) | `returnDonatedSchedContext`, `returnDonatedSchedContext_eq_legacy_of_none`, `returnDonatedSchedContext_ok_storeChain`, `donationReturnBinding`, `donationHeadOf?`, `storeDonationHeadPop`, `storeDonationHeadClear`, `storeReplyReHead`, `returnDonatedSchedContext_post_scReply`; the head validation's own obligation is `donationHeadResolves`, established from `donationChainWellFormed` or across any chain-object-free step |
| The pop's depth-≥ 2 arm owes the outer caller's shape, and says so where the binding widens rather than where the arm becomes reachable | `SELE4N_SPEC.md` §8.12.7 | `lake build SeLe4n.Kernel.IPC.Invariant.Defs` | `donationReturnOuterValid`, `donationReturnOuterValid_none`, `returnDonatedSchedContext_donationOwnerValid_at_target`, `returnDonatedSchedContext_preserves_donationOwnerUnique`, `returnDonatedSchedContext_preserves_donationBudgetTransfer`; generalised at WS-OD OD4.3 (`v0.35.2`): `returnDonatedSchedContext_establishes_ipcInvariantFull_of_except` no longer takes `newOwner? = none`, which is what let OD4.4 thread a resolved owner through all six call sites |
| Donation is **transitive**: a caller already holding a donated context passes it on, so seL4-MCS's passive-server pattern works at call depth ≥ 2 | `SELE4N_SPEC.md` §8.12.7 | `lake build SeLe4n.Kernel.IPC.Invariant.DonationPreservation`; `lake exe smp_ipc_suite` (§3.18) | `callDonationSchedContext?_of_donated_caller`, `applyRendezvousCallDonation_donated_donor_pushes`, `donateSchedContext`, `storeDonationFramePush`, `applyCallDonationOnCore_preserves_ipcInvariantFull`, `applyCallDonationOnCore_donated_caller_migrates_and_preserves_affinity` |
| The workstream's own closure theorem: a passive server reached at call depth ≥ 2 holds a SchedContext whose bound thread is itself — the improvement stated positively, not as the absence of a regression | `SELE4N_SPEC.md` §8.12.7 | `lake build SeLe4n.Kernel.IPC.Invariant.DonationPreservation` | `passiveServerHoldsDonatedContext_atCallDepthTwo`, `passiveServerHoldsDonatedContext_onCore` |
| A **reused** Reply object cannot redirect a donation: the freshening refuses a Reply carrying a stack link in either direction, and the pop refuses to read past a below-head frame whose own **upward** link does not answer the head that reached it | `SELE4N_SPEC.md` §8.12.7 | `lake build SeLe4n.Model.State`; `lake exe smp_ipc_suite` (§3.18) | `SystemState.linkReply`, `Reply.isFree`, `linkReply_getReply?_caller_some`, `replyStackOuterCaller?`, `replyStackOuterCaller?_of_consumed_frame`, `lifecyclePreRetypeCleanup_reply_refuses_live_stack_frame`, `lifecyclePreRetypeCleanup_schedContext_refuses_stack_head` |
| A frame cut out of the **middle** of a stack is removed in `O(1)` on three objects, so no frame is left on a stack with its caller gone — the pinning defect the single-linked encoding could not avoid | `SELE4N_SPEC.md` §8.12.7 | `lake build SeLe4n.Kernel.Lifecycle.Invariant.CancellationReplyShape`; `lake exe smp_ipc_suite` (§3.18 runs the splice's *writing* arm and, as its paired negative, the same consume with the splice omitted) | `spliceReplyFrameOut`, `spliceThreadReplyFrameOut`, `spliceReplyFrameOut_preserves_donationChainWellFormed`, `spliceThreadReplyFrameOut_preserves_donationChainWellFormed`, `Reply.consumed`, `consumeReplyLink_preserves_donationChainWellFormed` |
| `maxLockSetSize` is a **derived** figure, and a cut that widens a footprint pays for it visibly: the pop now *writes* the frame below the head and the suspend pipeline's second pop reads and writes one further down, so `maxLockSetSize` moved and the RPi5 per-lock cost and envelope moved with it | `SELE4N_SPEC.md`, `CLAUDE.md`, GitBook 12 | `python3 scripts/check_lock_ceiling_figures.py`; `lake build SeLe4n.Testing.LockFootprintBoundCensus`; `lake exe smp_wcrt_suite` | `maxLockSetSize`, `admissibleCriticalSection`, `admissibleCriticalSection_rpi5Tick`, `rpi5Tick_refuses_sixty_micro_sections`, `lockSet_tcbSuspendOnCore_size_le_seventeen`, `lockSet_endpointReplyRecvOnCore_size_le_eighteen` (unconditional since WS-HP HP6.2; the sharper seventeen it stood beside is retired there, its owner merge occurring on no state the head-driven trigger reaches); the figure gate holds every prose copy to the Lean sources, so a stale one is a build failure |
| A cancelled **middle** caller's frame is **spliced out** of the stack (WS-HP HP6.8, `v0.35.45`; it severed up to `v0.35.44`) — the policy is a named constant with theorems reading it, not a field read nobody decided, and the flip had to change the constant, the `_eq` lemma and both theorems that read it | `SELE4N_SPEC.md` §8.12.7 and §8.12.11, `SCHEDCONTEXT_DONATION_CHAIN_PLAN.md` §3.4, `DONATION_POP_TRIGGER_PLAN.md` HP6.8 | `lake build SeLe4n.Kernel.Lifecycle.Invariant.CancellationReplyShape`; `lake exe smp_ipc_suite` (§3.20 asserts the live constant); Tier 3 (the retired value is a negative, token-preserving: the constructor stays and only the constant's right-hand side moves) | `CancelledMiddleCallerPolicy`, `cancelledMiddleCallerPolicy`, `cancelledMiddleCallerPolicy_eq`, `replyStackOuterCaller?_follows_policy` (concluding `.ok (some outer)` where it concluded `.ok none`), `cancelledMiddleCaller_splices_at_cut` (giving `.donated scId outer` where `…_severs_at_cut` gave `.bound scId`), `cancelledCallerDonation?_none_below_the_cut`; `severAtCut_pop_leaves_no_head` is **deleted**, its first conjunct having been the policy constant at the old value |
| **The frozen execution surface's reply-stack removal splices, not severs** (WS-HP HP8, `v0.35.47`) — `FrozenOps` holds the live `Reply` record and is reached by neither library root, so a severed removal there lost the frames below a cut exactly as the live one did; the sever's names are deleted rather than kept beside a spliced body | `SELE4N_SPEC.md` §8.12.13, `DONATION_POP_TRIGGER_PLAN.md` HP8.1–HP8.4, `CLAUDE.md` | `lake exe frozen_ops_suite` (13 leg + 3 operation differentials, `FO-043` the depth-3 witness — mutation-verified in both directions: the pre-HP8 sever fails it while every control passes); `lake build SeLe4n.Testing.ReplyStackWriteCensus` (24 sites, 6 frozen mirrors); Tier 3 (the sever's names are refused tree-wide) | `frozenSpliceFrameBelow?`, `frozenSpliceReplyFrameStores`, `frozenSpliceReplyFrameOut`, `frozenSpliceReplyFrameOutOrSelf`, `frozenSpliceReplyFrameStores_eq_sever_of_no_frame_below` (the equality that makes every pre-HP8 scenario answer as it did).  **The scope is stated**: the splice's third store (`rid.prev := none`) is *not observable through the reply composite* — `Reply.consumed` clears the same field on a frame heading nothing, measured by deleting the store and finding the suite still green — so `FO-043`'s last half drives the primitive directly, which is the only place that deletion fails |
| **The frozen SchedContext bind and unbind agree with their live counterparts** (PR #897 review, `v0.35.96`) — on the *recorded origin*, on the *refusal set* and on the *priority write*.  `SchedContext.donationOrigin` is a field of the **live** record and `Model.freeze` copies it verbatim, so a frozen state taken mid-chain carries one; WS-HP HP10.4 clears it at three live sites and both frozen mirrors kept it, so `frozenDonationOriginRecipient?` could still name a thread whose loan had ended.  Sweeping the **operation** rather than the field found three more: the live `schedContextBind` refuses four things and the mirror refused one, so on three concrete states it *succeeded where the kernel refuses*, and it did not propagate `sc.priority`, so every frozen post-bind state falsified `boundThreadPriorityConsistent` — a mirror producing states the live kernel cannot reach.  *A field added to a shared record is a sweep of both surfaces, and the unit of the sweep is the operation* | `SELE4N_SPEC.md` §8.12.5, `CLAUDE.md` | `lake exe frozen_ops_suite` (`FO-047` the origin and the priority write, with the PAYOFF half asserting the resolver names the origin before the bind and nobody after; `FO-047b` the unbind; `FO-047c` the three refusals, each paired with a control that the live side really refuses, plus a NEGATIVE that a frame whose upward link nothing answers is still admitted by both); seven mutations, each reverting one production change, all decisive; Tier 3 (nine anchors, the last the frozen twin of a negative the live predicate has carried since `v0.35.5`) | `frozenSchedContextBind`, `frozenSchedContextUnbind`, `frozenReplyFrameOnLiveStack`, `frozenDonationOriginRecipient?`, `schedContextBind`, `schedContextUnbind`, `replyFrameOnLiveStack` |
| **The donation pop's three stated coherence hypotheses are retired** (WS-HP HP7, `v0.35.46`) — under the head-driven trigger the resolver reads the context off the answered frame's own `.head` link and validates that context's `scReply` against the *same* frame, so what a caller used to supply is a consequence of the trigger firing; the third was not merely unused but **false on reachable states** since the splice went live | `SELE4N_SPEC.md` §8.12.12, `DONATION_POP_TRIGGER_PLAN.md` HP7.1–HP7.4, gitbook 12 | `lake build SeLe4n.Platform.Staged`; `lake exe smp_cross_core_reply_suite` (computes the retired reading beside the live one on the agreeing shape and at an orphan head); `python3 scripts/check_ipc_invariant_dethreading.py`; Tier 3 (each retired symbol is a **negative** tree-wide, so a reintroduction fails rather than a deleted pin reporting PASS over nothing) | `answeredFrameHeadContext?_head_is_answered_reply`, `answeredFrameHeadContext?_donationHeadOf`, `answeredFrameHeadContext?_boundThread`, `answeredReplyFrameAbove?_none_of_headContext`, `replyFrameHeadIsBound`, `replyFrameHeadHolderDonation` (the fourth stated fact, **live** — twelve-plus consumers, its docstring's claim that HP7 retires it corrected), `bindingDrivenReplyServerDonation?` (the retired reading, private to the witness that refutes it).  The nine deleted declarations are enumerated in `SELE4N_SPEC.md` §8.12.12 and the plan's HP7.3 row rather than here: an evidence column names what *discharges* a claim, and a deleted symbol discharges nothing |
| **A middle removal preserves the donation accounting at reply-stack depth ≥ 3** (WS-HP HP6, `v0.35.45`) — the reservation leaves the cut **owed outward** and the pop that answers the bottom frame delivers it home, where the sever settled it `.bound` on a thread strictly inside the chain and left its owner `.unbound` for good.  An **improvement on seL4-MCS**, not parity: `reply_remove`'s non-head branch writes zero into the frame above at master, 13.0.0, 12.1.0, 12.0.0 and 11.0.0, so upstream strands the reservation at depth ≥ 3 too.  The removal is **three** stores — the frame above takes the cut frame's downward link, the frame below links back up at it, and the cut frame's own `prev` is cleared (seL4's `reply_unlink` downward half) — and the third is what makes `donationChainWellFormed` survive the removal outright rather than transiently; it is free, because the cut frame's lock was already a declared write member on both paths, so `maxLockSetSize` stays at 23.  The below side **degenerates to the sever** rather than refusing, so the removal's refusal set is exactly the pre-WS-HP one and a stale upward link is still reachable, which is why the reciprocity checks stay.  **Depth 2 is not closed** — both policies write `none` into the frame above a bottom frame — and that §3.20's depth-two halves and the golden trace pass byte-identically is the measurement that the change is confined to depth ≥ 3 | `SELE4N_SPEC.md` §8.12.11, `CLAUDE.md`, `docs/REGISTERED_DEBT.md` (the depth-≥ 3 half of the donation-accounting row), GitBook 12 | `lake build SeLe4n.Kernel.IPC.Invariant.DonationPreservation`; `lake exe smp_ipc_suite` (§3.22 inverted from COST to PAYOFF, with two negatives spelling the retired sever's values and the **second** pop measured delivering the reservation home; §3.20 byte-identical); `lake exe sele4n` (the golden trace, byte-identical); `lake build SeLe4n.Testing.ReplyStackWriteCensus`; Tier 3 | `spliceFrameBelow?`, `spliceFrameBelow?_eq_some`, `spliceFrameBelow?_ne_cut`, `spliceFrameBelow?_above_ne_cut`, `spliceReplyFrameStores`, `spliceReplyFrameStores_cases`, `spliceReplyFrameStores_splice_values`, `spliceReplyFrameOut_eq_sever_of_no_frame_below`, `spliceReplyFrameOut_preserves_donationChainWellFormed`, `removeCallerReplyFrame_preserves_donationChainWellFormed`, `removeCallerReplyFrame_splices_reciprocally`, `removeCallerReplyFrame_getSchedContext?_eq`, `donationAccountingPreserved_atCallDepthThree`, `spliceFrameBelow?_mem_replyFrameBelow?`, `spliceFrameBelow?_mem_answeredReplyFrameBelow?`, `spliceFrameBelow?_mem_cancelSplicedFrameBelow?`, `replyFrameBelow?_eq_prev_of_frame_above`, `donationChainWalk_exists_of_splice`, `storeObject_reply_stackLinks_preserves_ipcInvariantFull`, `spliceReplyFrameOutOrSelf_preserves_reply_caller_and_headLink` |
| **The splice COMPOSES — a middle removal at reply-stack depth 4 still delivers the reservation to its owner** (WS-HP HP9, `v0.35.48`).  Depth 4 is the shallowest stack with **two** frames below a cut, and that is the point: the splice writes the frame below's `next` and leaves its own `prev` untouched, so at depth 3 “the frame above reconnects” and “the frames below survive” are the *same* statement and nothing distinguishes a reconnection from a transitive one.  Three successive pops then carry the reservation from the innermost server out to the original owner, which is the transitive form of `donationAccountingPreserved_atCallDepthThree`.  **The scope is stated**: a token-preserving mutation of the splice's *store shape* — reverting to the sever, or clobbering the frame below's own `prev` — **fails to elaborate**, because `spliceReplyFrameStores_cases` pins the three stores as a theorem, so the store shape needs no witness and this row's subject is the **composition** rather than the shape.  **Depth 2 stays open** (HP10): both policies write `none` into the frame above a bottom frame, so nothing at depth ≤ 2 can measure this claim in either direction | `SELE4N_SPEC.md` §8.12.14, `CLAUDE.md`, `docs/REGISTERED_DEBT.md` (the donation-accounting row's depth-≥ 3 half), GitBook 12 | `lake exe smp_ipc_suite` (§3.23: a four-frame stack cut at its third frame, the frame below the cut asserted **untouched**, a negative refusing the retired sever's value, and three `returnDonatedSchedContextResolved` pops each taken at the owner the **previous** pop's `replyStackOuterCaller?` resolved, so the chain follows production rather than the fixture, ending `.bound` on the owner); `lake exe sele4n` (the golden trace, byte-identical); Tier 3 | `spliceReplyFrameStores_cases` (what makes the store shape a theorem rather than a witness), `spliceReplyFrameStores_splice_values`, `removeCallerReplyFrame_splices_reciprocally`, `donationAccountingPreserved_atCallDepthThree` (the depth-3 statement §3.23 composes), `returnDonatedSchedContextResolved`, `replyStackOuterCaller?`, `donationRecipientAcceptable` (whose docstring carries the three upstream facts this workstream rests on, each with the revisions it was read at) |
| **A depth-2 out-of-order removal delivers the reservation to its owner — WHEN THE TWO GUARDS ADMIT IT, which is not every reachable state** (WS-HP HP10.9, `v0.35.53`; scope corrected at `v0.35.141`).  **The "at every reply-stack depth" form of this claim is RETRACTED**: `donationOriginRebindable` refuses an origin that is `.blockedOnReply`, as a proxy for "some live `.donated _ origin` binding names it", and a client answered out of order and re-called is reply-blocked while owning nothing — its Call is `.unbound`, so it donated nothing.  The pop then falls back to the answered caller and **transfers** the reservation to the intermediate caller, erasing `donationOrigin` with it (`tests/SmpIpcSuite.lean` §3.25's COST group, driven through the live pop, with its awake-client CONTROL).  The depth-≥ 3 row above stands; this row's theorem stands as stated, under its hypotheses.  Re-opened in `docs/REGISTERED_DEBT.md` table C.  At depth 2 the frame the removal takes off the stack **is** the bottom, so nothing sits below it to reconnect and `cancelledMiddleCallerPolicy` writes the same `none` into the frame above whichever value it holds (`removeCallerReplyFrame_clears_prev_of_bottom_frame`) — which is why HP6's splice provably could not reach this loss, and upstream has it too (`reply_pop` donates to the answered frame's own `replyTCB`).  What closes it is the reservation's **origin** recorded on the `SchedContext` by the *first* push and read in place of stack reachability, so a delegate answering the client out of order no longer costs that client its reservation.  The theorem **derives** the reachability answer that names the wrong thread (`replyStackOuterCaller? = .ok none`, read off the removal) and **hypothesises** the two guards and, since `v0.35.61`, the origin's existence at the post-removal state (the resolver names only a thread it can resolve, so a stale origin falls back instead of reaching the pop's own lookup as a refusal — `replyDonationRecipient_resolves`); one guard cannot be derived at all: the owner is `.blockedOnReply` on exactly the reply being answered until the reply leg's wake.  **No mutation of the production code is available** — the origin write, the resolver, the three reply-path pops and the dispatch's recipient are each pinned as theorems, so a mutation fails to *elaborate* rather than failing the suite, exactly as the depth-4 row records for the splice's stores — so the decisive comparison is a differential *within* the suite: one function applied to a chain whose first push recorded an origin and to one that predates the field, a single field apart and opposite outcomes.  The same cut gave HP10.4's production write its **first** measurement, every earlier fixture having set the field by hand.  §3.22, §3.23 and the golden trace are byte-identical, which is *structural* rather than lucky: at depth ≥ 3 the pop sits at a `some` arm where `replyDonationRecipient_eq_of_outer_some` makes the redirect the identity by theorem | `SELE4N_SPEC.md` §8.12.15, `CLAUDE.md`, `docs/REGISTERED_DEBT.md` (the donation-accounting row's depth-2 half), GitBook 12 | `lake build SeLe4n.Kernel.IPC.Invariant.DonationPreservation`; `lake build SeLe4n.Kernel.IPC.Invariant.Structural.DualQueueMembership`; `lake exe smp_ipc_suite` (§3.20 inverted from COST to PAYOFF, driving the live `.reply` spine, with the origin-versus-no-origin differential and both directions of HP10.4's push; §3.22 and §3.23 byte-identical); `lake exe sele4n` (the golden trace, byte-identical); Tier 3 | `donationAccountingPreserved_atCallDepthTwo`, `replyDonationRecipient_resolves`, `removeCallerReplyFrame_clears_prev_of_bottom_frame`, `replyDonationRecipient`, `replyDonationRecipient_eq_of_outer_some` (what makes the depth-≥ 3 identity structural), `donationOriginRecipient?`, `donationOriginRecipient?_eq_some_iff`, `donationOriginRebindable`, `donationOriginRebindable_no_owner`, `donationFirstPush`, `replyStackOuterCaller?_of_bottom_head`, `spliceFrameBelow?`, `spliceReplyFrameOut_eq_sever_of_no_frame_below`, `removeCallerReplyFrame_getSchedContext?_eq`, `returnDonatedSchedContext_getTcb?_char` |
| **Both** removal paths run seL4's `reply_remove`: the answered frame comes off its reply stack before its caller link is consumed, so the delegated out-of-order reply no longer leaves a frame below the head with a stale `prev`.  A frame that *heads* a scheduling context keeps its links deliberately — `Reply.consumed`, because the pop validates the head by them — so the step's own statement is the relaxed `…_head_preserves_donationChainWellFormedExcept`, and it is the **transition** (`endpointReplyCrossCoreDispatch`) whose donation pop discharges that transient and leaves no frame on a stack with its caller gone | `SELE4N_SPEC.md` §8.12.8, GitBook 12 | `lake build SeLe4n.Kernel.IPC.Operations.Endpoint`; `lake exe smp_ipc_suite` (§3.20 answers a depth-2 chain's outer caller through a delegated reply capability, with the same consume minus the splice as its paired negative); `lake exe smp_cross_core_reply_suite` (§3.9, both directions); Tier 3 | `removeCallerReplyFrame`, `removeCallerReplyFrame_eq_consume_of_no_frame_above`, `removeCallerReplyFrame_isOk`, `spliceReplyFrameOutOrSelf`, `spliceReplyFrameOutOrSelf_cases`, `removeCallerReplyFrame_preserves_donationChainWellFormed`, `removeCallerReplyFrame_head_preserves_donationChainWellFormedExcept`, `spliceReplyFrameOutOrSelf_unreferenced`, `spliceThreadReplyFrameOut_unreferenced` |
| The reply leg's head case is **stated**, not hidden: a frame that heads a scheduling context keeps its links when its caller is consumed, so the leg's post-state satisfies the chain invariant relaxed at that one key — and the donation pop that follows it in the same transition discharges it.  Since WS-HP HP4 (`v0.35.38`) the composite carries **no** coherence fact relating the relaxed frame to the popped context: the pop reads that same frame, so they name one context by construction.  What survives is `replyFrameHeadIsBound`, strictly weaker and vacuous wherever the frame heads nothing | `SELE4N_SPEC.md` §8.12.8 | `lake build SeLe4n.Kernel.IPC.CrossCore.EndpointReplyDispatchInvariant`; `lake build SeLe4n.Kernel.IPC.Invariant.FaultPreservation` | `donationChainWellFormedExcept`, `donationChainWellFormedExcept_of_frame`, `endpointReplyOnCore_donationChain_cases`, `endpointReplyCrossCoreDispatch_preserves_donationChainWellFormed`, `replyFrameHeadIsBound`, `replyFrameHeadIsBound_of_no_head`, `returnDonatedSchedContext_preserves_donationChainWellFormed_of_except`, `faultReplyOnCore_preserves_donationChainWellFormed`, `replyTransferOnCore_preserves_donationChainWellFormed` |
| **The reply path's donation pop is head-driven** (WS-HP HP4, `v0.35.38`): both reply spines and `.replyRecv`'s pop decide whether to return a donated scheduling context from whether the answered reply *frame* heads one, not from the recorded server's binding.  Behaviour was preserved on every state the tree then reached — the two triggers agree wherever the sever can put the state, and the golden trace is byte-identical — so what the flip bought is HP6: the orphan head a splice can leave is exactly what a binding-driven trigger cannot see, and since HP6.8 (`v0.35.45`) that state is **reachable**.  The frame is an **argument**, because the reply leg clears the answered caller's link to it before the pop runs; everything the pop *decides* is read at the pop's own state, which is what keeps `returnDonatedSchedContext`'s `boundThread` guard vacuous.  The pop also validates the thread it rebinds (`donationRecipientAcceptable`), which the binding-driven reading did not need and the head-driven one does. The two reply footprints kept their binding-driven resolvers with the gap closed by a theorem rather than by prose; HP6.2 (`v0.35.44`) repointed them, before the splice, because the splice is what makes the two resolvers disagree.  The **frozen** mirror flips in the same cut (HP4.7): `frozenEndpointReplyWithDonationReturn` reads the frame and `frozenEndpointReplyServerDonation?` is deleted, because `frozenBranchOperationChecked .endpointReplyToBlockedCaller = true` claims the two are run beside each other and a binding-driven mirror of a head-driven operation would be one question answered in two places.  `FO-042` fires the pop on both surfaces — no half of `FO-041` ever had — its second half is the state where the two triggers disagree, its third is HP4.6's recipient guard, which the frozen pop was missing under a docstring claiming it carried every live guard, and its fourth is the live step's ID promotion, which it was missing too | `SELE4N_SPEC.md` §8.12.9 items 1-7, GitBook 12 | `lake build SeLe4n.Kernel.IPC.CrossCore.EndpointReplyDispatch`; `lake build SeLe4n.Kernel.API`; `lake exe sele4n` (the golden trace, byte-identical); `lake exe smp_cross_core_reply_suite` (§3.3', the head-driven trigger named on a queued holder and declining on a frame that heads nothing); `lake exe frozen_ops_suite` (FO-042, the donation popped on both surfaces, the trigger-divergence state, and the two guards both sides now refuse on); `lake exe smp_ipc_suite` (§3.9b, the frame-keyed pop on a state whose reply leg has already consumed the caller's reply object); Tier 3 | `replyFrameHeadHolder?`, `replyFrameHeadHolder?_eq_some`, `replyFrameHeadHolder?_of_head`, `replyFrameHeadHolder?_congr`, `answeredFrameHeadContext?_eq`, `applyReplyDonation`, `applyReplyDonation_characterisation`, `applyReplyDonationOnCore`, `applyReplyDonationOnCore_characterisation`, `replyRecvPopDonation`, `replyFrameHeadHolderDonation`, `replyFrameHeadHolderDonation_of_no_head`, `donationRecipientAcceptable`, `returnDonatedSchedContext_rejects_bound_recipient`, `returnDonatedSchedContext_ok_recipient_unbound`, `replyDonationHolderHome`, `lockSet_endpointReplyOnCore_covers_donationPop`, `lockSet_endpointReplyRecvOnCore_covers_donationPop`, `lockSet_endpointReply_donatedHolder_tcb_write_mem`, `lockSet_endpointReply_donatedSc_write_mem`, `frozenReplyFrameHeadContext?`, `frozenReplyFrameHeadHolder?`, `frozenEndpointReplyWithDonationReturn`, `frozenDonationRecipientAcceptable` |
| **The cancellation reclaim is head-driven too** (WS-HP HP5, `v0.35.39`), and HP6 forced it rather than symmetry: after the splice — live since `v0.35.45` — a frame becomes the head whose recorded reply target is gone, so a binding-driven reclaim declines there and leaves `.donated scId victim` live across a cancellation that made the victim `.ready` — the `donationOwnerValid` break WS-RR RR7.22 was written to close, at the one state that reading cannot see.  `cancelledCallerDonation?` reads the victim's own frame's `.head` link through the same frame-keyed resolver both reply spines use; the victim's id is consulted by nothing and that is pinned, not inferred from an underscore.  RR7.22's `donationHolderIsReplyTarget` is **deleted** rather than left beside the new reading, its head-keyed successor `donatedContextIsOwnerFrameHead` runs binding → head (the opposite direction from the reply path's, because here the consumers quantify over bindings), and its builder measures the cost: all but two clauses come out of `donationOwnerValid`.  A resolved holder may resolve to no TCB — `SchedContext.boundThread` is tied to no stored TCB by any invariant — so what rules that out is the pop declining.  Two footprint claims became **theorems** the binding reading could not have stated: the head the pop clears **is** the victim's own reply object, and a reclaim excludes both removal members.  And the reclaim is **exercised for the first time**: before HP5.5 every `.blockedOnReply` fixture in the tree held a Reply whose `next` was unset, so the arm declined under both readings and the flip would have landed untested.  Two fixtures that *claimed* to exercise it were corrected rather than re-blessed — the golden trace's `SCO-020b/c/d`, which built a `.donated` binding with no Reply object at all, and `SmpIpcSuite`'s OD5.2 pair, whose two halves declined for the same reason — and `main_trace_smoke.expected` is byte-identical afterwards, which is the measurement that behaviour is restored exactly rather than re-baselined | `SELE4N_SPEC.md` §8.12.10 items 1-6, GitBook 12 | `lake build SeLe4n.Kernel.Lifecycle.Invariant.CancellationReplyShape`; `lake build SeLe4n.Kernel.IPC.CrossCore.Cancellation`; `lake exe smp_cancellation_suite` (§3.20: the reclaim fired on the agreeing shape and on the orphan head, with the retired reading computed beside the live one); `lake exe smp_ipc_suite` (§3.18 OD5.2, both halves now on the stack and the declining one on the live pushed state); `lake exe sele4n` (the golden trace, byte-identical, with `SCO-020b/c/d` restored by correcting the fixture rather than the expectation); Tier 3 | `cancelledCallerDonation?`, `cancelledCallerDonation?_independent_of_victim`, `cancelledCallerDonation?_eq_answeredFrameHeadContext?`, `cancelledCallerDonation?_some`, `cancelledCallerDonation?_some_of_frame_head`, `cancelledCallerDonation?_none_below_the_cut`, `donatedContextIsOwnerFrameHead`, `donatedContextIsOwnerFrameHead_of_no_owner`, `donatedContextIsOwnerFrameHead_of_no_donation`, `donatedContextIsOwnerFrameHead_of_donationOwnerValid`, `returnDonationToCancelledCaller_no_donation_to_victim`, `cancelIpcBlocking_reply_no_donation_to_victim`, `returnDonatedSchedContext_ok_of_boundAndRecipient`, `returnDonatedSchedContext_ok_server_not_reserved`, `abortHolderPendingIpc_eq_self_of_lookup_none`, `cancelReclaimHead?_eq_replyObject`, `cancelSplicedFrameAbove?_of_donation`, `cancelSplicedFrameBelow?_of_donation` |
| `.reply` and `.replyRecv` declare the frame above the cut, which the splice writes, resolved from the same expression the arm's existing reply member comes from — and no *reachable* footprint grew, because the new member and the donation-return members are mutually exclusive | `SELE4N_SPEC.md` §8.12.8, `CLAUDE.md` | `lake build SeLe4n.Kernel.Concurrency.Locks.LockSetTransitions`; `lake exe lock_set_suite`; `lake exe deadlock_freedom_suite`; Tier 3 | `answeredReplyFrameAbove?`, `answeredReplyFrameAbove?_of_no_reply`, `lockSet_endpointReply`, `lockSet_endpointReplyRecvOnCore`, `lockSet_endpointReplyRecvOnCore_size_le_eighteen`, `maxLockSetSize`, `lockSet_endpointReply_frameAbove_write_mem`, `lockSet_replyRecv_frameAbove_write_mem`, `lockSet_endpointReplyOnCore_covers_splicedFrameAbove`, `lockSet_endpointReplyRecvOnCore_covers_splicedFrameAbove`, `lockSet_cancelDonationOnCore_covers_victim`, `lockSet_cancelDonationOnCore_covers_bindingSchedContext`, `lockSet_cancelDonationOnCore_covers_stateLevel`, `lockSet_cancelDonationOnCore_covers_donatedOwner`, `lockSet_cancelDonationOnCore_covers_pop`, `lockSet_cancelDonationOnCore_covers_outerCaller_key` |
| `.replyRecv` pops the donation **between** its two legs — seL4-MCS's own `doReplyTransfer` → `reply_remove` → `receiveIPC` order — because the receive leg re-links the very Reply the reply leg just answered and `Reply.isFree` reads both stack links | `SELE4N_SPEC.md` §8.12.8, `CLAUDE.md` | `lake exe smp_ipc_suite` (§3.21 runs the passive server's `seL4_ReplyRecv` loop end to end, with the un-popped receive leg's `.replyCapInvalid` refusal as its paired negative); Tier 3 | `replyRecvPopDonation`, `replyRecvPostReceiveDonation`, `replyRecvBody`, `replyRecvPopDonation_preserves_ipcInvariantFull`, `replyRecvPostReceiveDonation_preserves_ipcInvariantFull`, `replyRecvPostPopState`, `replyRecvPoppedDonation`, `replyRecvBody_preserves_ipcInvariantFull` |
| The reachability census's **domain** sees a state transformer however its result type is spelled: a declaration whose result is an `abbrev` of a state-carrying type, and one whose declared **sort** is itself an `abbrev`, both enter it — the two halves of one question, normalised at **reducible** transparency so an `abbrev` unfolds while a dependent projection stays shut.  Both widenings admit nothing on the live tree, so each ships a planted transformer **and** a control over an alias holding no state, which is what makes the pair decide *the alias is normalised* rather than *anything spelled this way is a carrier* | `SELE4N_SPEC.md` §SM3.C, `CLAUDE.md` | `lake build SeLe4n.Testing.KernelTransitionReachabilityCensus` (Tier 1; the census prints its own three counts, and the reconciliation fails in both directions — an unrecorded transformer and a stale pin entry are each a build failure); Tier 3 (the raw spelling and the unrestricted `whnf` are refused) | `declaresNonPropSort` (the one owner of *is this a non-`Prop` sort*, asked of the telescoped body so the whole type and a `∀` cannot answer differently), `stateCarryingTypes`, `carrierFixpointBound`, `carrierFixpointRefusalViolations`; the plants `censusWitnessNestedAliasTransformer` / `censusWitnessNestedAliasCounter` (the result half, `v0.35.128`) and `censusWitnessAliasedSortTransformer` / `censusWitnessAliasedSortCounter` over `CensusWitnessCarrierSort` (the sort half, `v0.35.135`), each transformer in `nonExecutedTransitionsPrivate` and each counter provably out |
| Every definition that writes reply-stack data names a chain result, or is recorded as a half-step of the composite that does, or — on the frozen execution surface, which has no chain predicate of its own — as mirroring a live site that states one.  The set is derived from the elaborated environment of **both** roots and the frozen surface, and reconciled against the registry in both directions, so a new bare consume fails the build on any of the three | `SELE4N_SPEC.md` §8.12.8, `CLAUDE.md` | `lake build SeLe4n.Testing.ReplyStackWriteCensus` (Tier 1; witnesses on both directions of every predicate) | `chainWritePrimitives`, `directWriteCandidates`, `chainWriteRegistry`, `ChainDiscipline`, `halfStepResolves`, `resultViolations`, `disciplineViolations`, `reconciliationViolations` |
| A **donated** scheduling context carries budget, period and deadline and **not** priority or domain: a donee runs its client's work on the client's reservation at its own scheduling band and in its own partition, exactly as seL4-MCS does, and rises to the client's band only through priority inheritance | `SELE4N_SPEC.md` §8.13 | `lake build SeLe4n.Model.State`; `lake env lean --run tests/PriorityManagementSuite.lean` (WS-OD-PRIO-01, 02) | `SchedContextBinding.ownScId?` (the reservation a thread *owns*) against `SchedContextBinding.scId?` (the one it *runs on*), `SystemState.threadBasePriority`, `threadBasePriority_eq`, `FrozenSystemState.threadBasePriority_eq_live`, `resolveEffectivePrioDeadline_fst_eq_boostedPriority` (unconditional since `v0.35.133`: the resolver's base half is the thread's own bucket key at EVERY binding, which subsumes the retired `.donated`-specific pin), `effectiveBucketPriority_congr` (unconditional since `v0.35.134`, which retired that accessor's arm-specific family for the reason the resolver pin above gives), `getCurrentPriority_donated`, `getCurrentPriorityChecked_donated`, `effectiveSchedParams` (**every** thread's own domain since `v0.35.136`, pinned unconditionally by `effectiveSchedParams_domain_eq` — `v0.35.133` collapsed the band's readers and left the `.bound` arm reporting `sc.domain` on the stated ground that the domain mirror had no writer known to break it; PR #897's review found the writer); the classifier is a *narrowing* of `scId?` (`ownScId?_eq_scId?_of_isSome`, `ownScId?_eq_some_iff`), so the thread-owned and reservation-owned halves can never name different contexts |
| The two `bound*Consistent` predicates are facts about their **writers**, not invariants: `returnDonatedSchedContext`'s bottom arm installs a `.bound` binding and writes neither the recipient's `priority` / `domain` nor the reservation's, so a reservation whose band or domain moved while it was on loan comes back disagreeing.  Nothing is mis-scheduled — every band and domain read is the thread's own — so the residue is a verification gap, registered with its closure | `SELE4N_SPEC.md` §8.13, `CLAUDE.md`, `docs/REGISTERED_DEBT.md` table C | `lake env lean --run tests/PriorityManagementSuite.lean` (WS-RR-PRIO-09 drives the configure and the pop as **live operations** and asserts both pairs disagree; WS-RR-PRIO-10 is the unreconfigured control that isolates the reconfiguration from the pop); `lake build SeLe4n.Kernel.Scheduler.Operations.Selection`; Tier 3 | `donationReturnSchedContext_priority`, `donationReturnSchedContext_domain` (the pop writes neither reservation field, so a cut that reconciles there must change a theorem), `effectiveSchedParams_domain_eq` and `resolveEffectivePrioDeadline_fst_eq_boostedPriority` (both unconditional, which is what makes the residue invisible to the scheduler), `schedContextConfigurePropagates` (the gate that spares a donee and so leaves the reservation's fields moving alone); `boundThreadPriorityConsistent` has no consumer at all and `boundThreadDomainConsistent` is a conjunct of `schedulerInvariantBundleExtended`, which no IPC transition claims — so no theorem in the tree is false |
| One definition decides which object supplies a thread's base priority, and every reader in the tree is pinned to it by theorem rather than by resemblance — so a later cut cannot unpick the split one site at a time | `SELE4N_SPEC.md` §8.13 | `lake build SeLe4n.Kernel.Scheduler.Operations.Selection` | `resolveEffectivePrioDeadline_fst_eq_threadBasePriority` (unconditional), `getCurrentPriority_eq_threadBasePriority` (by `rfl`), `effectiveSchedParams_priority_deadline_eq_resolve`, `effectiveBucketPriority_eq_resolveEffective`, `threadBasePriority_congr`; the three scheduler resolvers also read the reservation's deadline and domain, so they split the arm and are tied back rather than defined through it |
| A priority update on a thread holding a donated context writes **that thread's own TCB** and leaves the donor's reservation untouched, and a reconfiguration of a donated reservation rewrites neither the donee's priority nor its domain — the authority crossing reported while closing WS-OD and its mirror, both closed at `v0.35.3` | `SELE4N_SPEC.md` §8.13, `REGISTERED_DEBT.md` WS-OD | `lake build SeLe4n.Kernel.SchedContext.PriorityManagement`; `lake env lean --run tests/PriorityManagementSuite.lean` (WS-OD-PRIO-03…06) | `updatePrioritySource`, `updatePrioritySource_donated` (which object moves, stated as an equation on the whole state), `updatePrioritySource_donated_preserves_donor_schedContext`, `updatePrioritySource_preserves_ipcInvariantFull`, `updatePrioritySource_preserves_projection` (its SchedContext-observability hypothesis narrowed to the priority source), `frozenSetPriority`, `schedContextConfigureBoundPropagate` with `schedContextConfigurePropagates` (one predicate both halves consult, so neither can be gated without the other); `.tcbSetPriority` and `.tcbSetMCPriority` both reach the write through one helper, so both arms are covered by the same fix |
| The bucket invariants and the priority-agreement carrier follow the read: a donee's recorded run-queue bucket is its own base priority, and the carrier ranges over `.bound` alone — where quantified over every binding the donation **falsified** it whenever the donor's and the donee's base priorities differed, since the reservation's `priority` had to equal one before the hand-off and the other after and the hand-off writes neither field | `SELE4N_SPEC.md` §8.13 | `lake build SeLe4n.Kernel.Scheduler.Invariant.PerCore` | `effectiveParamsMatchRunQueue`, `effectiveParamsMatchRunQueueOnCore`, `boundThreadPriorityConsistent`, `boundThreadPriorityConsistent_frame`, `resolveEffectivePrioDeadline_fst_eq_boostedPriority` (since `v0.35.133` the same conclusion with NO hypothesis: with `TCB.priority` the base's one home the carrier is load-bearing for no read, and is kept as the statement that `schedContextBind` and `schedContextConfigureBoundPropagate` propagated the configured band) |
| The reply path's donation return deschedules the now-passive recorded server at the core the state **places** it on, not at a core its caller resolved — so a *delegated* reply whose server was preempted rather than running no longer leaves that server queued, `.unbound`, and selectable against no reservation.  The core is not a parameter at all: the step resolves the thread it deschedules, so its placement is a fact to look up | `CHANGELOG.md` v0.35.37, `docs/REGISTERED_DEBT.md` table A | `lake build SeLe4n.Kernel.InformationFlow.NonInterferenceCrossCore`; `lake exe smp_cross_core_reply_suite` (the witness runs **both** spellings on one state — a server queued on core 2 and current nowhere — and asserts they disagree); Tier 3 | `descheduleAtPlacement`, `descheduleAtPlacementCores`, `placedCoreOf?`, `applyReplyDonationOnCore`, `applyReplyDonationOnCore_confinedToCores`, `applyReplyDonationOnCore_eq_single_of_placed_at_bootCore`, `placedCoreOf?_congr_of_scheduler_eq`, `descheduleAtPlacementCores_congr_of_runQueue_current_eq`, `descheduleAtPlacement_machine_eq`, `descheduleAtPlacement_passiveServerIdleFrame`; the bridge to the single-core spine is **conditional** on that placement, because an unconditional equation with the boot-pinned `removeRunnable` is exactly what was false |
| A **delegated** `.replyRecv` declares the five objects the *invoking* receiver's own pre-receive return touches — its receive leg is `.receive`'s transition, and the arm's donation return runs after it, so the invoker still carries the `.donated` binding it entered with.  Two threads cannot share a scheduling context, so the recorded server's members provably never alias these | `SELE4N_SPEC.md`, `CLAUDE.md`, GitBook 12 | `lake build SeLe4n.Kernel.IPC.CrossCore.EndpointReply`; `lake exe deadlock_freedom_suite`; `lake exe lock_set_suite` | `lockSet_endpointReplyRecvOnCore_covers_preReturn`, `lockSet_replyRecv_preReturn_sc_write_mem`, `lockSet_replyRecv_preReturn_owner_tcb_write_mem`, `lockSet_replyRecv_preReturn_head_write_mem`, `lockSet_replyRecv_preReturn_belowHead_write_mem`, `lockSet_replyRecv_preReturn_stateLevel_write_mem`; the suites execute the footprint and pin that dropping the five drops the size by exactly five |
| No **reachable** `.replyRecv` declares the ceiling's twenty-one: the re-donation members are live exactly when the endpoint has a queued sender and the invoker's pre-receive return exactly when it does not, so no state carries both groups | `SELE4N_SPEC.md`, `CLAUDE.md` | `lake exe lock_set_suite` | `lockSet_endpointReplyRecvOnCore_size_le_eighteen` (no hypothesis at all since WS-HP HP6.2 repointed the footprints onto the pop's own trigger, where it took two coherence facts; the sharper `…_size_le_seventeen` is retired in the same cut, because its owner merge occurs on no state the head reading reaches), `receivePreReturn?_of_sender`, `receiveRendezvousDonatedSc?_of_no_sender`; the suite exercises both reachable widths and carries the negative that neither reaches `maxLockSetSize` |
| **The push** costs no lock-footprint ceiling: the `.call` footprint already write-locks every object it writes — the old head included since `v0.35.4` — so nothing the push does moves `maxLockSetSize`.  (The pop and the suspend pipeline do; see the row below) | `SELE4N_SPEC.md` §8.12.7 | `lake build SeLe4n.Testing.LockFootprintBoundCensus` | `lockSet_endpointCallOnCore_covers_donationPush`, `lockSet_endpointCall_donatedSc_write_mem`, `lockSet_endpointCall_reply_write_mem`, `lockSet_endpointCall_receiver_tcb_write_mem`; the census reports 47 declared footprints, every one bounded at its full arity |
| **A thread's base priority has one home, and the readers and theorems that classified by binding are gone** (`v0.35.133`, completed `v0.35.134`).  PR #897's review found a **sixth** reader against the collapse itself — `threadSchedulingParams`, the Z1-N migration bridge in the root-imported model API, still taking a `.bound` thread's band from `sc.priority`.  Deleted rather than collapsed, on measurement: zero consumers anywhere in the tree.  Asking the same question of the rest of the tree found `effectiveBucketPriority`'s six theorems carrying hypotheses the body collapse had made dead — three binding-arm instances of the unconditional lemma, one about an expression shape the accessor no longer contains, and two frames demanding that SchedContext lookups agree of an accessor that reads no store — the identical sweep `v0.35.133` ran on `resolveEffectivePrioDeadline`'s family and not on this one | `CLAUDE.md`, `SELE4N_SPEC.md` §8.12 | `lake build` (515 jobs); `./scripts/ak7_cascade_check_monotonic.sh`; Tier 3 (one positive over the congruence and its citation, four negatives refusing each retired name tree-wide) | `effectiveBucketPriority_congr`, `effectiveSchedParams`, `SystemState.threadBasePriority` |
| **The frozen execute phase reads a thread's effective priority exactly as the live kernel does, and that is stated rather than assumed** (`v0.35.134`).  PR #897's review reported `frozenComputeMaxWaiterPriority` as diverging from the live `computeMaxWaiterPriority`; it did against `v0.35.132`, and `v0.35.133`'s one-home collapse closed it while leaving the frozen docstring asserting the divergence and nothing relating the two readings.  `effectiveSchedParams_fst_eq_boostedPriority` is the live half, **derived** from the existing pair bridge so the two resolvers cannot part; `frozenComputeMaxWaiterPriority_eq_live_reading` is the cross-surface half, stated at the level of the **fold** and quantified over every live state because the reading reads none of it, and mutation-verified (replacing its proof with `rfl` alone fails).  **Sweeping that question found `frozenResumeThread` reading the wrong priority in three places** — a four-field-short restore, no `pipBoost` recompute, and a preemption test on the two **bases** — none of them reachable by anything the tree runs, because all three existing frozen-resume scenarios leave `scheduler.current` unset and `.tcbResume` is outside `FrozenOpBranch.all`.  The live clear is `TCB.restoredToReady` now, called by both surfaces, because it had no name to call | `CLAUDE.md`, `SELE4N_SPEC.md` §R5.D, `docs/REGISTERED_DEBT.md` (the frozen differential-coverage row), GitBook 12 | `lake build SeLe4n.Kernel.FrozenOps.Agreement`; `lake exe suspend_resume_suite` (SR-032/033/034, each computing the retired reading beside the live one; all three mutation-verified); Tier 3 (five positives over `TCB.restoredToReady`'s field list, two negatives refusing the inlined clear and the base comparison, both mutation-tested with the tokens kept) | `TCB.restoredToReady`, `effectiveSchedParams_fst_eq_boostedPriority`, `frozenComputeMaxWaiterPriority_eq_live_reading`, `frozenResumePreemptionReading_eq_live`, `frozenResumeThread`, `restoreToReadyStaging` |
| **The reply-link teardown's bundle statement is honest about the state it runs on** (WS-RR RR8.7, `v0.35.80`).  Two production theorems asked for `ipcInvariantFull` of a state **together with** that state's answered caller not being `.blockedOnReply`, and `replyCallerLinkage`'s second direction refutes exactly that pairing — a Reply still naming a caller obliges that caller to be reply-blocked — so both had premises no state satisfies and asserted **nothing** while their names read as coverage.  Found by reading, proved in Lean, and closed by the *narrowest* relaxation that admits the state: `replyCallerLinkageExcept st woken` keeps the reciprocal pair required at the woken thread and drops the blocking clause as a **disjunct**, never by excusing the thread from the clause.  The **unit** is the pair, not either half: the restore opens the relaxation and the teardown closes it, so `restoredAndConsumed` is what carries the full bundle end to end.  The contradiction is pinned permanently, so the spelling cannot come back | `CLAUDE.md`, `SELE4N_SPEC.md` §8.12.16, `docs/REGISTERED_DEBT.md` (the retired splice statement), `docs/planning/REPLY_FRAME_REMOVAL_PLAN.md` RM1.5, GitBook 12 | `lake build SeLe4n.Kernel.Lifecycle.Invariant.CancellationReplyShape`; `lake build SeLe4n.Kernel.IPC.Invariant.Structural.DualQueueMembership`; `python3 scripts/check_ipc_invariant_dethreading.py` (the relaxed view registered as a pre-state predicate; 184 bundles, zero post-state conjunct bindings); Tier 3 (two negatives refusing the retired spellings tree-wide, each mutation-tested by reintroducing the name as **code**) | `replyCallerLinkage_refutes_woken_linked_caller` (the permanent pin — the two premises derive `False`), `replyCallerLinkageExcept`, `ipcInvariantFullExceptReplyLinkage`, `replyCallerLinkage_of_except_of_unreferenced`, `restoreToReadyStaging_establishes_ipcInvariantFullExceptReplyLinkage`, `consumeReplyLink_closes_exceptReplyLinkage`, `restoredAndConsumed`, `restoredAndConsumed_preserves_ipcInvariantFull`, `consumeCallerReply_establishes_ipcInvariantFull_of_exceptReplyLinkage`, `consumeCallerReply_closes_replyCallerLinkageExcept`, `notQueueBlocked_bounds_no_endpoint_queue` |
| **All three `cancelIpcBlocking` arms preserve `ipcInvariantFull`** (WS-RR RR8.7, `v0.35.82`) — the reply arm was the last, and the only one whose arm is a **four-step composition** rather than one whole-store sweep: reclaim, splice, restore, teardown, pinned to that composition by `rfl` so a step inserted or reordered fails to elaborate.  No two steps carry the bundle for the same reason — the reclaim composes the holder abort with `returnDonatedSchedContext`'s carriage at the *relaxed* donation-owner bundle, the splice is three `.reply` stack-link stores, and the restore and teardown carry it only **as a pair** (`v0.35.80`).  Its hypothesis set is larger than its siblings' and includes **both directions** of one local coherence fact: `replyFrameHeadHolderDonation` at the victim's reply object (head → binding, which the pop's carriage is stated over) and `donatedContextIsOwnerFrameHead` (binding → head, which the no-donation payoff quantifies over).  Neither entails the other — a frame head whose context is `.bound` to its holder satisfies the second and refutes the first, a binding with no frame satisfies the first vacuously — and `ipcInvariantFull` entails neither, which is WS-HP HP7's own reason for keeping the first stated.  The abort's own "not blocked on reply" premise is **derived inside the two arms that abort** and is *false* of a general holder, so a hypothesis of that shape would be unsatisfiable rather than redundant | `CLAUDE.md`, `SELE4N_SPEC.md` §8.12.16, `docs/REGISTERED_DEBT.md`, GitBook 12 | `lake build SeLe4n.Kernel.Lifecycle.Invariant.CancellationReplyShape`; `lake build tests.SmpCancellationSuite` (the keystone applied, and the arm equation by `rfl`); `python3 scripts/check_ipc_invariant_dethreading.py` (184 bundles, zero post-state conjunct bindings); Tier 3 (eleven anchors, each mutation-tested by a change that keeps every token and breaks the relation) | `cancelIpcBlocking_replyArm_preserves_ipcInvariantFull`, `cancelIpcBlocking_reply_arm_eq`, `returnDonationToCancelledCaller_preserves_ipcInvariantFull`, `returnDonationToCancelledCaller_preserves_allTimeoutBudgetsNone`, `returnDonationToCancelledCaller_victim_tcb_rewrite`, `abortHolderQueueCoherent`, `abortHolderPendingIpc_preserves_ipcInvariantFull`, `abortHolderPendingIpc_preserves_allTimeoutBudgetsNone`, `abortHolderPendingIpc_offQueue_tcb_eq`, `cancelledCallerDonation?_holder_holds_victim_donation`, `cancelledCallerDonation?_holder_ne_victim`, `spliceThreadReplyFrameOut_preserves_ipcInvariantFull`, `spliceThreadReplyFrameOut_timeoutBudgetFrame`, `restoredAndConsumed_congr`, `endpointQueueRemove_timeoutBudgetFrame`, `abortPendingIpcOnEndpoint_preserves_allTimeoutBudgetsNone` |

## 3. Fault handling

| Claim | Where it is made | Check it with | Artefact |
|-------|------------------|---------------|----------|
| A fault is **delivered**, never returned — no execution path returns a thread to its faulting instruction without handler action | `SELE4N_SPEC.md`, README | `lake exe fault_handling_suite` | `faultDeliverOnCore_not_dispatchable`, `faultDeliverOnCoreChecked_not_dispatchable` |
| Fault delivery is total: no handler, an unresolvable one, missing rights, a denied flow, or an unlinkable reply all converge on a fail-closed suspend | `SELE4N_SPEC.md` | `lake build SeLe4n.Kernel.IPC.CrossCore.Fault` | `IPC/CrossCore/Fault.lean` |
| The live fault entry is the **flow-checked** arm, so a deployment policy can refuse a fault delivery | `SELE4N_SPEC.md` | `./scripts/test_full.sh` (Tier 0/3 pair) | `faultDeliverOnCoreChecked` |
| A kernel-origin exception is never delivered to a user handler | `SELE4N_SPEC.md` | `lake build SeLe4n.Kernel.Architecture.ExceptionModel`; `./scripts/test_aarch64_cross_build.sh` | `.kernelAbort` classification; `halt_if_kernel_origin` in `trap.rs` |
| The fault wire format round-trips and fits the message-register budget | `SELE4N_SPEC.md` | `lake build SeLe4n.Kernel.Architecture.Fault` | `decodeFault_encodeFault`, the length theorem |

## 4. SMP

| Claim | Where it is made | Check it with | Artefact |
|-------|------------------|---------------|----------|
| Per-core scheduler state, selection, wake, timer tick, PIP, domains and CBS are verified per core, not boot-core pinned | `SELE4N_SPEC.md`, README | `./scripts/test_full.sh` | `Scheduler/Invariant/PerCore*.lean` |
| Cross-core IPC — call, reply, notification, cancellation — preserves the IPC bundle | `SELE4N_SPEC.md` | `lake exe smp_ipc_suite` | `IPC/CrossCore/` |
| Every cross-core SchedContext hand-off migrates the CBS replenish queue | `SELE4N_SPEC.md` | `lake exe smp_ipc_suite` | `migrateSchedContextReplenishment`, `replenishQueueAffinityConsistent_smp` |
| TLB shootdown is a verified protocol with bounded wait and generation-tagged descriptors | `SELE4N_SPEC.md` | `lake exe smp_tlb_shootdown_suite` | `Architecture/TlbShootdownProtocol.lean` |
| Non-interference holds per core, and accepted covert channels are enumerated rather than assumed away | `SELE4N_SPEC.md` | `lake exe smp_information_flow_suite` | `acceptedCovertChannel_perCoreCount` |
| Declassification is audited, with causal provenance and refusal recording | `SELE4N_SPEC.md` | `lake exe smp_information_flow_suite` | `InformationFlow/Declassification*.lean` |
| The WS-SM theorem total is measured, not hand-summed — **909 theorems** across 1119 registered entries (210 are `def`s) | `SELE4N_SPEC.md`, GitBook 12 | `python3 scripts/generate_smp_theorem_manifest.py --check`; `lake build SeLe4n.Kernel.Concurrency.PhaseTheoremManifest` | `docs/smp_theorem_manifest.json` |
| The deployed reader-writer lock is the one the Lean FIFO spec describes: `STATIC_RW_LOCK_POOL` is `[QueuedRwLock; 4]`, and its refinement was proved before the pool was repointed | `SELE4N_SPEC.md`, GitBook 12 | `lake build SeLe4n.Kernel.Concurrency.Locks.QueuedRwLockRefinement`; `./scripts/test_full.sh` | `queuedSim`, `queuedRwLock_refines_rwLockSpec`, `queuedRwLock_admits_in_spec_order` |
| Each lock refinement bridge derives its trace correspondence instead of assuming it: no `_honest` capstone takes `ListBlockBisim` as a premise, and no conjunct is a tautology | `SELE4N_SPEC.md` | `./scripts/test_full.sh` | `honestBlock`, `listHonestBlocks_listBlockBisim`, `rust_rwLock_refines_lean_honest`, `ticketTrace_preserves_ticketLockSim`, `ticketLockSim_not_universal` |
| The Tier-5 correspondence oracle drives both real Rust locks rather than modelling them, checking implementation agreement, the ticket interval and the state encoding after every operation, and both oracles print one **identity** line per state — the writer's core, the sorted reader cores, the queue in order with modes — read back out of the ticket lock's per-core words on the Rust side, so a wrong-waiter promotion, a reordered queue or a changed mode is a mismatch where a flag, a count and a length agreed | `SELE4N_SPEC.md`, `DEVELOPMENT.md` | `./scripts/test_tier5_cross_language.sh`; `cargo test -p sele4n-hal --features host_tools --bin rw_lock_oracle` (`every_step_is_rendered_with_identities`, `a_transient_divergence_is_visible_in_the_lines`) | `rust/sele4n-hal/src/bin/rw_lock_oracle.rs` (`Driver::render`), `tests/Tier5/RwLockOracle.lean` (`renderState`) |
| The deployed lock is exercised under a concurrency model checker and under miri | `DEVELOPMENT.md` | `./scripts/test_loom_queued_rw_lock.sh`; `./scripts/test_miri_queued_rw_lock.sh` | `queued_rw_lock::loom_model`, CI job `test-loom-concurrency-model` |
| The ticket-FIFO refinement covers traces containing **withdrawals**: the ledger tombstones, promotion retires tombstones rather than mistaking them for waiters, and the FIFO capstone is about queue position | `SELE4N_SPEC.md`, `CLAUDE.md` | `lake build SeLe4n.Kernel.Concurrency.Locks.QueuedRwLockRefinement`; `./scripts/test_full.sh` | `queuedHeadLive`, `skipDeadOps_spec`, `promoteFrom_preserves_queuedSim`, `queuedRwLock_admits_in_spec_order` |
| The **deployed** ticket-FIFO lock can withdraw a queued request, and the withdrawal reaches Lean: a split acquisition (`enqueue` / `is_served` / `complete_*` \| `cancel`), a per-core withdrawal slot published before the head check behind a `SeqCst` fence, and a compare-exchange arbitrating between the withdrawing core and the previous holder's skip loop | `SELE4N_SPEC.md`, `CLAUDE.md` | `./scripts/test_loom_queued_rw_lock.sh` (18 models); `./scripts/test_miri_queued_rw_lock.sh`; `./scripts/test_tier5_cross_language.sh`; `bash scripts/check_lock_ffi_symmetry.sh` | `QueuedRwLock::cancel`, `claim_withdrawal_of`, `pass_turn`, `ffi_rw_lock_cancel` |
| A core may not take a second ticket while its previous withdrawal is unclaimed — `enqueue` parks until the slot is empty, the non-blocking attempts are refused, and a stale withdrawal publishes nothing — so a double withdrawal cannot lose a publication and stall the lock; the Lean model enables the issue only for a core holding no ticket and proves the publish never overwrites | `SELE4N_SPEC.md`, `CLAUDE.md` | `./scripts/test_loom_queued_rw_lock.sh` (`double_withdrawal_by_one_core_does_not_strand_the_lock`, `pending_withdrawal_refuses_the_non_blocking_attempt`); `cargo test -p sele4n-hal --lib queued_rw_lock`; `./scripts/test_tier5_cross_language.sh` (`check_head_live`, `check_withdrawal_slots`, the pinned excluded shapes); `./scripts/test_tier3_invariant_surface.sh` | `QueuedRwLock::await_withdrawal_retired`, `QueuedTicketWf.ledgerCoresNodup`, `QueuedTicketWf.publish_slot_empty`, `queuedSim_not_holdsTicket` |
| A two-phase-locking bracket **fully unwinds**: its shrinking phase withdraws before it releases, so a refused or completed bracket leaves the core with no queued request at any member of its footprint — with no distinctness or resolvability condition on the footprint | `SELE4N_SPEC.md`, `CLAUDE.md`, GitBook 12 | `lake build SeLe4n.Kernel.Concurrency.Locks.LockSetHeld`; `lake exe with_lock_set_suite`; `./scripts/test_full.sh` | `unwindAll`, `unwindAll_leaves_no_queued_request`, `syscallEntryUnderRevalidatedLockSet_refused_leaves_no_queued_request` |
| A release by a non-holder, a re-acquisition by a holder and a withdrawal by a holder are the **deployed** lock's no-ops, decided by its per-core held word before anything is written, and a guard releases only what it acquired; the refinement represents the holders (`queuedHeldSim`) and derives the no-op blocks rather than assuming them, and the undeployed CAS-retry bridge claims no no-op at all | `SELE4N_SPEC.md`, `CLAUDE.md`, GitBook 12 | `lake build SeLe4n.Kernel.Concurrency.Locks.QueuedRwLockRefinement`; `cargo test -p sele4n-hal --features host_tools --bin rw_lock_oracle`; `./scripts/test_loom_queued_rw_lock.sh` (`unwind_by_a_non_holder_never_touches_the_holder`, `every_pair_of_units_is_safe`); `./scripts/test_tier3_invariant_surface.sh` | `QueuedRwLock::held`, `queuedHeldSim`, `queuedBlock.releaseRead_noop`, `check_holders`, `scan_queued_rw_lock_protocol_intact` |
| A queued core's second acquisition, and a withdrawal by a core with no request, are the **deployed** lock's no-ops too, decided by its per-core request word; every per-core entry point loads the core's own words before its first shared write, the per-core state matrix classifies every entry point in every state, and the refinement's per-core branch hypotheses are the words the implementation reads, with the spec's branch derived from `queuedHeldSim` / `queuedRequestsSim` | `SELE4N_SPEC.md`, `CLAUDE.md`, GitBook 12 | `lake build SeLe4n.Kernel.Concurrency.Locks.QueuedRwLockRefinement`; `cargo test -p sele4n-hal --features host_tools --lib -- queued_rw_lock::sequential_tests::per_core_state_matrix`; `cargo build -p sele4n-hal` (the `build.rs` entry-point and load-before-write pins); `cargo test -p sele4n-hal --features host_tools --bin rw_lock_oracle`; `./scripts/test_loom_queued_rw_lock.sh` (`every_pair_of_units_is_safe`, fourteen single-lifecycle units unbounded; `every_chained_unit_meets_every_unit`, the three chained units under a stated preemption bound); `./scripts/test_tier3_invariant_surface.sh` | `QueuedRwLock::request`, `QueuedRwLock::own_request`, `queuedRequestsSim`, `queuedBlock.acquireRead_queued`, `queuedSim_involved_of_request`, `check_requests`, `PER_CORE_ENTRY_POINTS` |
| A withdrawal of a request the spec has already admitted **realises** the admission on the deployed lock (`CancelOutcome::Holding`), decided on the core's recorded mode and the lock's words — a served writer with no reader, a reader with no live write request ahead — while the spec's own withdrawal hands a withdrawn head's turn on by promoting the reader run behind it; both bridges fold that promotion, the refinement pins each live request's mode word to the spec's queued mode (`queuedRequestModesSim`), the oracle holds every withdrawal's verdict to the spec's, and the loom models tally that both verdicts occur across the schedules | `SELE4N_SPEC.md`, `CLAUDE.md`, GitBook 12 | `lake build SeLe4n.Kernel.Concurrency.Locks.RwLock SeLe4n.Kernel.Concurrency.Locks.QueuedRwLockRefinement SeLe4n.Kernel.Concurrency.Locks.RwLockRefinement`; `cargo test -p sele4n-hal --features host_tools --lib -- queued_rw_lock`; `cargo test -p sele4n-hal --features host_tools --bin rw_lock_oracle` (`cancel_outcome_reports_a_planted_divergence`, `cancel_of_the_head_promotes_the_reader_run_behind_it`); `./scripts/test_loom_queued_rw_lock.sh` (`served_reader_withdrawal_is_an_admission` and its four siblings); `bash scripts/check_lock_ffi_symmetry.sh` (per-symbol signatures); `./scripts/test_tier3_invariant_surface.sh` | `RwLockState.cancelPromotes`, `rwLock_cancel_admits_only_the_head_reader_run`, `cancelPromoteFrom`, `readerRun_preserves_queuedSim`, `honestBlock.cancel_promoting`, `queuedRequestModesSim`, `QueuedRwLock::cancel`, `CancelOutcome`, `write_request_ahead`, `Driver::expect_outcome` |
| Every per-core sequence of up to four entry points of the deployed lock, from every state a single thread can drive a core into, does what one classification predicts — the loom gate's "op-sequences of length ≤ 4" sentence stated where it is decidable, and derived from the matrix's `cell` rather than written as a second list; the loom enumeration itself is every unordered pair of the lock's single-lifecycle units, one per thread, unbounded, plus every chained unit against every unit under a stated preemption bound, the unit lists held to the entry points by `build.rs` | `CLAUDE.md`, `DEVELOPMENT.md`, GitBook 07 | `cargo test -p sele4n-hal --features host_tools --lib -- queued_rw_lock::sequential_tests::per_core_census_to_depth_four`; `cargo build -p sele4n-hal` (`run_unit_covers_entry_points`, `verify_run_unit_scanner`); `./scripts/test_loom_queued_rw_lock.sh` (`every_pair_of_units_is_safe`, `every_chained_unit_meets_every_unit`) | `cell`, `check_step` (the per-op advance) via `Cell::Acts`, `per_core_census_to_depth_four`, `UNITS`, `CHAINED_UNITS`, `CHAINED_PREEMPTION_BOUND`, `run_unit_covers_entry_points` |
| The ticket-lock bridge covers exactly the calls `ticket_lock.rs` defines: no block for a re-acquisition by a queued or holding core or a release by a non-holder, and every admitted trace respects the caller contract | `SELE4N_SPEC.md`, `CLAUDE.md`, GitBook 12 | `lake build SeLe4n.Kernel.Concurrency.Locks.TicketLockRefinement`; `./scripts/test_tier3_invariant_surface.sh` | `TicketLockState.callerContract`, `ticketBlock_respects_contract`, `ListTicketBlocks_contractTrace` |
| The release budget in ticks bounds the ticks a timer counts across the delay from **any** start counter: the duration converts by the ceiling, not the absolute conversion's floor | `SELE4N_SPEC.md`, `CLAUDE.md` | `lake build SeLe4n.Kernel.Concurrency.Locks.ReleaseBudgetTiming SeLe4n.Kernel.Architecture.TimerModel` | `hardwareDurationToModelTicks`, `hardwareTimerToModelTick_sub_le_duration`, `elapsed_ticks_le_releaseBudgetTicks`, `releaseBudgetTicks_rpi5_phase_witness` |
| Every per-core entry point of the deployed lock decides the executing core's case in a branch that controls its first shared write — pinned at the level of statements, with a self-tested checker | `CLAUDE.md` | `cargo build -p sele4n-hal` (`core_entry_point_status`, `verify_core_entry_point_scanner`) | `CORE_ENTRY_POINTS`, `CORE_HELPERS`, `core_entry_point_status` |
| A lock-delay bound has a **time** denomination: the execution carries a per-step cost, and the admission and contention bounds have cycle-denominated forms that collapse back to the step bounds at unit cost.  The cycle form is conditional on a declared per-critical-section ceiling — an assumption about the deployment's code, stated as a hypothesis rather than derived | `SELE4N_SPEC.md`, `CLAUDE.md` | `lake build SeLe4n.Kernel.Concurrency.Locks.RwLock`; `./scripts/test_full.sh` | `RwLockExecution.stepCost`, `RwLockExecution.BoundedCriticalSection`, `rwLock_writer_admitted_within_cycle_budget`, `lockContention_elapsed_bounded`, `rwLock_writer_cycle_budget_at_unit_cost` |
| A queued core may withdraw its request in the **abstract** lock: the withdrawal preserves every INV-R conjunct, admits only the reader run behind a withdrawn head (nobody from anywhere else), never installs a writer, releases nothing, and lengthens nobody else's wait | `SELE4N_SPEC.md`, `CLAUDE.md` | `lake build SeLe4n.Kernel.Concurrency.Locks.RwLock`; `./scripts/test_full.sh` | `RwLockOp.cancel`, `rwLock_cancel_preserves_wf`, `rwLock_cancel_admits_only_the_head_reader_run`, `rwLock_cancel_nonhead_admits_no_one`, `rwLock_cancel_not_effective_release` |
| The liveness results that conclude "becomes the holder" say so under an explicit no-withdrawal window, and the ones that conclude "leaves the queue" need none | `CLAUDE.md` | `./scripts/test_full.sh` | `RwLockExecution.noCancelIn`, `rwLock_writer_liveness`, `rwLock_writer_admitted_within_release_budget` |

## 5. Data structures and performance

| Claim | Where it is made | Check it with | Artefact |
|-------|------------------|---------------|----------|
| The object store is a verified Robin Hood hash table with proven O(1) lookup, not a benchmarked one | README, `SELE4N_SPEC.md` | `lake exe robin_hood_suite` | `RHTable.invExt`, `allTablesInvExtK` |
| The CNode radix tree is verified flat-array, with the same treatment | `SELE4N_SPEC.md` | `lake exe radix_tree_suite` | `Kernel/RadixTree/` |
| No executable definition reads or writes an object table raw, on the live surface or the frozen one — both spellings of each, and the write patterns are derived from one classification rather than enumerated | `SELE4N_SPEC.md` §SM3.C, `CLAUDE.md` | `python3 scripts/lean_store_read_census.py` (`STORE_READ_CODE=0`, `STORE_WRITE_CODE=0`); `--self-test` | `_TABLE_OPS`, `table_op_violations`, `branch_symmetry_violations`, `ACCESSOR_BODIES`, `WRITE_PRIMITIVE_BODIES` |
| A frozen store changes `objects` and nothing else — stated once at the write, inherited by every operation built from it rather than re-derived per body | `CLAUDE.md`, `SELE4N_SPEC.md` §8.12 | `lake build SeLe4n.Kernel.FrozenOps.Commutativity` | `frozenWithObjectStored_ok`, `frozenWithObjectStored_only_modifies_objects`, `frozenOnlyObjects_rfl` / `_trans`, `frozenRewriteObject_only_modifies_objects` |

## 6. Hardware and build

| Claim | Where it is made | Check it with | Artefact |
|-------|------------------|---------------|----------|
| The HAL compiles **and generates code** for `aarch64-unknown-none` in both profiles, with the three `.S` sources assembled and clippy denied | README, `CI_POLICY.md` | `./scripts/test_aarch64_cross_build.sh` | CI job `aarch64 Cross Build` |
| The cross target cannot be silently dropped or weakened to a `check` | `CI_POLICY.md` | `python3 scripts/check_aarch64_cross_target.py` | 14-case self-test |
| Broadcast TLB maintenance is confined and the non-IS variants are gated | `SELE4N_SPEC.md` | `python3 scripts/check_tlbi_broadcast_discipline.py` | `scripts/tlbi_local_allowlist.txt`, `rust/sele4n-hal/src/tlb.rs` |
| No third-party code is linked into the runtime kernel binary | README, `THIRD_PARTY_LICENSES.md` | `./scripts/test_rust.sh` | `rust/` is `#![no_std]`, `core::*` only |

## 7. Process

| Claim | Where it is made | Check it with | Artefact |
|-------|------------------|---------------|----------|
| Every version-bearing site agrees with `lakefile.toml` | `DEVELOPMENT.md` §8 | `./scripts/check_version_sync.sh` | 36 sites, `scripts/version_locations.sh` |
| Every deferred item has an owner and a closure target | `DEVELOPMENT.md` §6 | `python3 scripts/check_deferral_registration.py` | *Registered debt index*, `REGISTERED_DEBT.md` |
| Every plan's numbering, counts and cross-references are consistent | `DEVELOPMENT.md` §11 | `python3 scripts/check_workstream_plan.py` | `docs/planning/` |
| No identifier or path encodes a workstream code | `DEVELOPMENT.md` §6 | `python3 scripts/check_identifier_naming.py` | `scripts/identifier_naming_baseline.json` |
| A gate that cannot run is reported NOT RUN, never PASS | `CI_POLICY.md` | `SELE4N_REQUIRE_GATES=1 ./scripts/test_tier4_smp_bootcheck.sh` | `scripts/test_gate_skip_accounting.sh` |
| Website-linked paths still exist | `DEVELOPMENT.md` §9 | `./scripts/check_website_links.sh` | `scripts/website_link_manifest.txt` |

## 8. What is **not** claimed

Stating these is part of the point of this index. Each is registered debt with
an owner, not an oversight.

| Not claimed | Why | Owner |
|-------------|-----|-------|
| That the kernel boots on hardware | No bootable image exists: no `[[bin]]`, no aarch64 Lean object code, no bare-metal runtime hosting. Every runtime seam behind the readiness gate is wired and dormant | SM10.1 |
| That per-object fine locks are deployed | SM3.C.9 is deferred: the `@[export]` bodies are, with one exception, not yet wrapped in `withLockSet`, so fine locks are a model-level discipline.  WS-RR RR7.11 (`v0.34.64`) makes `lockSetForSyscall` answer `some` for eight of the thirty-five arms — the suspend plus the seven IPC hot-path arms — each with its coverage (`.replyRecv` for a delegated reply too since WS-OD OD3.5, which declares the recorded server's own TCB lock unconditionally and retires PR #892 review round 6's `none`), and RR7.12 (`v0.34.65`) makes the **syscall seam acquire them**: `syscallDispatchCrossCoreEntry` brackets its atomic step, with the undeclared arms running exactly as before.  What still brackets nothing is the per-core scheduler path (timer tick, `.reschedule` SGI receiver, secondary bring-up), which commits under the SM5.I global entry lock only — so live WCRT is that lock's, not the fine-lock bound | WS-RR RR7 |
| That a syscall's caller always gets an answer | Nearly.  Every *returning* arm stages its frame (WS-RA, v0.33.37) and, since WS-RR RR7.14 (`v0.34.67`), so does every *forcibly unblocked* thread — `timeoutThread` stages `.ipcTimeout`, `cancelIpcBlocking`'s four blocked arms stage `.ipcCancelled`.  What is still owed is **delivery**: `contextRestoreSeamLive` is `false` until SM10.1, so a staged frame reaches no hardware register yet, and the interim trap layer poisons a blocked caller's registers with `blocked_resume_sentinel_regs()` rather than resuming it | WS-BP BP7 |
| Unconditional SMP starvation-freedom | The WCRT capstones take `hBandProgress` as an externalized deployment hypothesis, and the liveness trace model is still boot-core pinned | WS-SL |
| That live WCRT matches the fine-lock bound | Kernel entry is serialised by one global ticket lock, so the live bound is weaker than `PerCoreWcrt.lean`'s | SM10.1 |
| That Tier 4 acceptance gates have passed | They need the bootable image, so they have never executed. They report NOT RUN rather than PASS | SM10.1 |
| That a fault message's `MR4` onward reaches a handler on hardware | No receive path writes past `MR3` into the IPC buffer yet | WS-RR |

## Proof claim qualification

Not every theorem carries the same assurance.  A claim about proof coverage
must say which of these it means, because "N theorems" hides the difference.

| Category | Description | Assurance level |
|---|---|---|
| **Substantive preservation** | Proves that a *successful* operation preserves an invariant over *changed* state. | High |
| **Error-case preservation** | Proves that a *failed* operation preserves an invariant by returning unchanged state. Trivially true. | Low (technically correct but not security evidence) |
| **Compositional preservation** | Derives post-state invariant from pre-state through operation-specific transfer lemmas (`cspaceSlotUnique_of_storeObject_*`, `CNode.insert_slotsUnique`, etc.). (WS-E2 H-01 resolved: all preservation proofs refactored to this pattern.) | High |
| **Structural invariant** | Proves a genuine structural property requiring a witness — e.g. `endpointQueueNoDup_of_dualQueue_of_headBlocked`, which derives queue duplicate-freedom from the dual-queue invariant plus the head conjunct rather than assuming it, and `perCoreStatsPlausible_tick_implies_irq`, which needs the containment bound as evidence. (WS-E2 C-01 resolved: former tautological proofs reformulated. WS-G5 retired the original example here: `CNode.slotsUnique` became trivially true — `HashMap` key uniqueness is structural — so a theorem taking it as a witness no longer proves anything, and WS-RR RR7.34 replaced it with two that do.) | High |
| **End-to-end chain** | Proves a multi-step semantic property across subsystem boundaries (e.g., `badge_notification_routing_consistent` — badge propagation from mint through notification signal/wait). (WS-E2 H-03 resolved.) | High |
| **Non-interference** | Proves that a high-domain operation preserves low-equivalence for unrelated observers. | Critical for security assurance |

## Update policy

When a claim changes:

1. update the canonical root source first;
2. update the GitBook mirror(s) in the same PR;
3. refresh the row here;
4. run at least `./scripts/test_smoke.sh` (`./scripts/test_full.sh` when Tier-3
   anchors or policies changed).

A claim that loses its evidence command loses its row. A claim the tree no
longer supports moves to §8 with an owner — it is not quietly deleted.
