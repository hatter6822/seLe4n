# Registered Debt

**This file is the project's single debt register.** Every deferred item in
seLe4n has a row here, with an owner and a closure target. A row leaves only
when the work lands or the deferral is retracted.

`CLAUDE.md`'s deferral rule requires exactly this, and forbids in-source TODOs
that age out with the surrounding workstream. The rule is enforced rather than
asserted: `scripts/check_deferral_registration.py` (Tier 0) fails a source
comment that declares itself untracked and cites nothing, and fails a citation
naming a row number that does not exist here.

**There is one register and it is this one.**
`docs/audits/AUDIT_v0.30.11_DEFERRED.md`, which four authorities once named,
was never created and will not be: a second register competing with the
canonical one is how the first divergence happened.

This file is **not** a record of what was done. What each version changed is in
[`CHANGELOG.md`](../CHANGELOG.md), one entry per merged PR; what is in flight
is in `CLAUDE.md`'s *Active workstream context* and the phase plans under
[`planning/`](planning/); how to work on any of it is
[`DEVELOPMENT.md`](DEVELOPMENT.md).

## How to use it

Three tables, by who can close the item:

| Table | Owner |
|-------|-------|
| **A** | a live WS-RR phase — every row has a numbered sub-task in [`SMP_RELEASE_READINESS_PLAN.md`](planning/SMP_RELEASE_READINESS_PLAN.md) |
| **B** | SM10 — each needs the image or the runtime seam SM10.1 produces |
| **C** | no pre-v1.0.0 owner — each row states why it may wait |

A row in **C** constrains what v1.0.0 may claim; WS-RR's RR8.15 hand-off check
reads that table, and RR8.13 records closing versions.

To add a row: put it in the table whose owner can close it, name the file it
lives in, and give it a closure target. To cite it from source, reference this
file (and the row number, for the §C.1 enumeration) rather than declaring the
item untracked.

### A — owned by a live phase or workstream

Every row here has a numbered sub-task in a **live** plan that must close before
WS-RR RR8 does — [`docs/planning/SMP_RELEASE_READINESS_PLAN.md`](planning/SMP_RELEASE_READINESS_PLAN.md)
for the RR rows, and
[`docs/planning/SCHEDCONTEXT_DONATION_CHAIN_PLAN.md`](planning/SCHEDCONTEXT_DONATION_CHAIN_PLAN.md)
for WS-OD, which is not an RR phase but is owed on the same schedule (RR8 is the
closure phase and cannot close over open work).  The register exists so the item
is visible from this file even when the plan is archived.

| Debt | Where it lives | Closure target |
|------|----------------|----------------|
| **No gate derives which kernel transitions are on an executed path** — the *class* behind WS-RR RR8.12's finding, registered at `v0.35.90`.  Two verified behavioural steps (WS-OD OD1.7's holder wake, WS-RR RR7.22/RR8.11's replenishment migration) were added to `cancelIpcBlockingOnCore`, which no production path calls, while the live path re-composed that composite's parts — and nothing in the tree could say so.  The instance is fixed and the class is not: the same shape sits one level down at the suspend pipeline's G3, which re-spells `cancelDonationOnCore` (pinned by `suspendDonationArm_eq_cancelDonationOnCore`, a `rfl` that fires even when a dispatcher change updates all of the dispatcher's own frames), and in every `*OnCore` composite that exists as a theorem-level surface beside a live re-composition.  **The remedy is a derivation, not more pins**: `SeLe4n/Testing/ExportCommitDisciplineCensus.lean` already derives the state-committing `@[export]` set from the elaborated environment and reconciles it against a registry in both directions; the counterpart owed here is the *reachability* census — every kernel definition that writes `SystemState` is either reachable from a committing export, or registered as a model / reference / mirror surface with a reason.  **LANDED `v0.35.91`** as `SeLe4n/Testing/KernelTransitionReachabilityCensus.lean` (Tier 1): at that version 528 state transformers derived from the environment, 288 reachable from one of the 7 committing `@[export]`s, 240 pinned by name and reconciled in both directions — figures dated, because the census prints its own and `v0.35.114` moved them when the domain stopped excluding `opaque` declarations.  **And landing it corrected this row's own remedy claim, which was wrong.**  The sentence here said the census *"would have failed on the day OD1.7 put a behavioural step into a transition nothing runs"* — it would not: `cancelIpcBlockingOnCore` already existed and was already unreachable, so its entry in the pin would not have moved.  A reachability partition sees a **new** non-executed transition; it cannot see a **step added inside an already-registered one**.  What sees that is the census's `standsBesideLive` category, which names the non-executed surface, the live definition that re-composes it, and a **pin theorem** that RELATES them — so a step added to one side and not the other fails the build.  *Relates*, not *mentions* (PR #897 review, `v0.35.96`): as first landed the check asked only that the statement name both programs, which a conjunction of reflexive equations satisfies while relating nothing, so `pinRelatesPrograms` now requires an `Eq` or `Iff` conclusion with each program on exactly one side and the two on opposite sides, with three witness theorems carrying the accepted-but-empty shapes and asserted to be refused.  Measured rather than argued: adding a step to `cancelIpcBlockingOnCore`'s body and not to the live path breaks `cancelIpcBlockingOnCore_eq_reclaimed_deschedule` outright, and the six reconciliation and pin checks are each mutation-tested with a case that keeps every token and breaks only the relation.  **Residual, stated in the census's own docstring**: a registered surface with no pin makes no agreement claim, and only two rows are pinned today; extending the pinned set is what closes the class rather than the instance. | `SeLe4n/Testing/KernelTransitionReachabilityCensus.lean` | **CLOSED `v0.35.91`** (the pinned-set extension is the follow-on) |
| **`sameSchedContextBindings` is declared where the Operations layer cannot see it, and the receive leg has no frame at all** — found while narrowing the `.receive` replenish segment (PR #897 Codex review, `v0.35.112`).  A dequeued `Call` whose donation prerequisites fail — the receiver already holds a context, or the sender holds none — migrates nothing either, and the transition's own test for that is `callDonationSchedContext?`, whose pre-state answer has to be transported across the receive leg by the backward `sameSchedContextBindings` frame.  Two things stand in the way, both **measured on the import closure** rather than read off module paths.  (1) **No such frame exists for `endpointReceiveDual` or `endpointReceiveDualWithCaps`**: the two theorems that need one — `endpointReceiveDual_preserves_donationBudgetTransfer` and `endpointReceiveDual_preserves_donationOwnerUnique` (`IPC/Invariant/Structural/DualQueueMembership.lean`) — each **inline** the whole rendezvous composition, so the transport must be extracted from them before it can be cited; that extraction is a de-duplication, which is the evidence the frame was missing rather than merely unnamed.  (2) **The bridge to the resolver has no home beside the resolver**: `IPC/Operations/Donation.lean`'s import closure (64 modules) does not contain `IPC/Invariant/Defs.lean`, where `sameSchedContextBindings` is declared, and `Defs`' closure does not contain `Donation` — the two are siblings — so a `callDonationSchedContext?`-to-frame bridge could only sit in a module that sees both, which today means `IPC/CrossCore/EndpointReply.lean`: the wrong layer for a fact about the resolver.  That is this tree's own *a shared answer must be reachable from every asker* shape (`v0.35.59`), and the remedy is the same one — move the owner down: `sameSchedContextBindings` with its `refl`, `trans` and `of_objects_eq` goes below `Operations/Donation.lean`, the two invariant consumers (`donationBudgetTransfer_of_sameSchedContextBindings`, `donationOwnerUnique_of_sameSchedContextBindings`) stay in `Defs.lean`, and the `SeLe4n.Kernel` namespace is kept so that no call site is renamed.  **What is *not* the obstacle, because this row's first draft said it was**: `EndpointReply.lean` does reach `DualQueueMembership.lean`, through `EndpointCall` → `Scheduler.Operations.PerCoreWake` → `PerCoreSwitchToThread` → `PerCoreChooseThread` → `Scheduler.Invariant.PerCore` → `CrossSubsystem` → `Capability.Invariant.Defs` → `IPC.Invariant` → `IPC.Invariant.Structural`, every module production and the first edge already present at `v0.35.111` — and a `#check` of all four frames from a module importing `EndpointReply` confirms it outright, so they were reachable all along and this narrowing is *available at a placement cost* rather than blocked.  Until it lands the `.receive` footprint over-declares two replenish-queue write locks on such a `Call`: sound, because a footprint wider than its operation is; not free, because lock contention is SM8.D's CC-5 channel; and unobservable today, because the footprint is inert until Cut 9 brackets the seam. | `SeLe4n/Kernel/IPC/Invariant/Defs.lean`, `SeLe4n/Kernel/IPC/Operations/Donation.lean`, `SeLe4n/Kernel/IPC/Invariant/Structural/DualQueueMembership.lean`, `SeLe4n/Kernel/IPC/CrossCore/EndpointReply.lean` | **WS-RR RR8.12 follow-on**, before Cut 9 brackets the syscall seam |
| **The revocation family has no syscall arm** — found by WS-RR RR8.12's reachability census on its first run (`v0.35.91`).  `cspaceRevoke`, `cspaceRevokeCdt`, `cspaceRevokeCdtStrict`, `cspaceRevokeCdtStreaming`, `cspaceRevokeCdtTransactional`, `revokeCdtFoldBody`, `revokeCdtReportingStep`, `revokeCdtScaffold`, `processRevokeNode` and `streamingRevokeBFS` are all outside the live closure, and the reason is not that the dispatch reaches a different spelling: **`API.lean` has no revocation arm at all**, so no capability a thread can present revokes anything.  This is verified machinery with no ABI path, and it is the *opposite* shape from the retype family beside it, where the census found an unreachable sibling of a live seam.  Two things make it a decision rather than a deletion.  seL4 revokes through `seL4_CNode_Revoke`, so an absent arm is a **gap in the syscall surface** and this project's implement-the-improvement rule says wire it rather than document the absence; and the CDT machinery underneath is real, proved and consumed by the *delete* path, which is live.  **The remedy is the one `v0.35.78` used for the capability-reference table**: measure what reads it, decide wire-or-retire on the measurement rather than on the name, and record which.  Until it lands, **v1.0.0 must not claim that a capability derivation tree can be revoked by a thread holding the root**, because no syscall does that. | `SeLe4n/Kernel/API.lean`, `SeLe4n/Kernel/Capability/Operations.lean` | **WS-RR RR8.12 follow-on**, before SM10 |
| **Four state transformers are consumed by nothing at all** — the same first run (`v0.35.91`).  `cleanupActiveDonation`, `timerTickChecked`, `switchDomainChecked` and `endpointCallWithDonation` are outside the live closure, no theorem mentions them, no test suite drives them and no gate names them; each is cited only in its own definition and in documentation prose, which is the tautological-citation shape this project already retires for anchors.  Measured with the WS-HP HP7 discipline — over the code view, with test-suite and `scripts/` consumers subtracted separately — because "unused" counted naively would have taken sixteen more definitions that a suite or a gate does consume.  The project rule is unconditional for *superseded* code, and what is owed first is the judgement these four have not had: `timerTickChecked` and `switchDomainChecked` are the unused members of the flow-`Checked` wrapper family whose siblings *are* driven, so deleting two of a symmetric family may be worse than keeping them, and that is exactly the call to make explicitly rather than by omission. | `SeLe4n/Kernel/API.lean`, `SeLe4n/Kernel/IPC/Operations/{Endpoint,Donation}.lean` | **WS-RR RR8.12 follow-on**, before SM10 |
| ~~**OD1.7's payoff is not lifted to the live suspend pipeline**~~ — registered at `v0.35.90` (WS-RR RR8.12, second cut), in the cut that fixed the behaviour it is about, and **CLOSED `v0.35.92`** (fourth cut).  `wakeAbortedDonationHolder_holder_runnable` says the reclaim's wake leaves the aborted donation holder queued or running; what no theorem said is that it is **still** so after the six pipeline stages that follow G2 — the chain reversion, the donation arm, the placement deschedule, the pending-state clear, the `.Inactive` store and the G7 scheduling point.  `suspendThreadOnCore_holder_still_placed` is that lift, and it is the composition of frames the row predicted rather than a new argument.  **Two things landing it established that the row did not anticipate.**  The holder-is-not-the-victim fact — which is what licenses G4's removal to leave the holder alone — existed only as a sentence in `suspendThreadOnCore`'s G4 comment; it is `cancelAbortedHolderWake?_ne_victim` now, derived from the wake's own two guards, which read the same TCB and demand incompatible constructors of its `ipcState`.  And the holder's **resolvability** has to travel with its placement rather than be assumed at the end: a scheduling point strands a thread whose TCB does not resolve (`preemptCurrentOnCore` re-enqueues the outgoing thread only when it does), so every stage carries `(getTcb? holder).isSome` forward and `switchToThreadOnCore_preserves_threadPlacedOnSomeCore` states that side condition rather than hiding it.  The chain walk's run-queue and `current` frames moved from `IPC/Invariant/FaultProgress.lean` to `Scheduler/PriorityInheritance/Propagate.lean` for the same reason RR8.12's second cut named a prefix — *a shared answer must be reachable from every asker*: `FaultProgress` imports `IPC.CrossCore.Fault`, which imports `Cancellation`, so the payoff could not have reached an answer stated there.  Its three `_not_mem_of_not_mem` forms were **retired** with the move, the biconditional being the answer and `updatePipBoostOnCore_mem_runQueueOnCore` having been the step-level one since RR8.2's predecessor WS-RR RR2.6. | `SeLe4n/Kernel/IPC/CrossCore/Cancellation.lean` §15 | **CLOSED `v0.35.92`** |
| ~~**The single-core `suspendThread` reference path still strands the holder**~~ — registered at `v0.35.90` (WS-RR RR8.12, second cut) and **CLOSED `v0.35.93`** (fifth cut).  The boot-pinned single-core `Lifecycle.Suspend.suspendThread` — **no production caller** (`Platform.FFI.suspendThreadInner`'s `@[export]` was retired at RR5.17), exercised by `tests/SyscallDispatchSuite.lean` SD-020..SD-023 — reached for the bare teardown, and the wake is not core-count-dependent, so WS-OD OD1.7's strand was reachable on it.  **The remedy was the relocation the row prescribed, and the row's own reasoning is what made it the right one**: `cancelIpcBlockingReclaimed` lived in `IPC/CrossCore/Cancellation.lean`, which *imports* `Lifecycle/Suspend.lean`, so the second asker could not see the shared answer — and when a question has one owner and an asker that cannot see it, **the owner is in the wrong layer**.  It was: every one of the relocated definitions reads a `TCB`, a run queue or a replenish queue, and none reads anything cross-core.  They keep the `SeLe4n.Kernel` namespace they were declared in, so **the move renames nothing** and all 168 references in the tree are untouched; the one new import (`SchedContext.ReplenishAffinity`, which imports only `Scheduler.Operations.Core`) was measured cycle-free before the move rather than after.  `tests/SmpCancellationSuite.lean` §3.26 (vi) drives the single-core reference on the stranding state and computes the retired bare G2 beside it, so the witness is known to discriminate.  The structural divergence the row notes — `revertPriorityInheritance` vs `propagatePipChainCrossCore`, with no bridge theorem — is **unchanged** and is not what this row was about. | `SeLe4n/Kernel/Lifecycle/Suspend.lean` §D1-G | **CLOSED `v0.35.93`** |
| **The reply path does not take the answered frame off its reply stack** — reported at `v0.35.4` while auditing that cut's own remedy, and the remaining shape of the finding it closed.  `v0.35.4` made the reply stack doubly linked so a frame can be cut out of the middle in `O(1)`, and wired that detach into the **cancellation** path (`detachCancelledCallerFrame`, run between the reclaim and `consumeReplyLink`).  The **reply** path was left as it was: `endpointReplyOnCore` runs `SystemState.consumeCallerReply` with no detach, and `Reply.consumed` clears both stack links on any frame that is not a head — so when the answered frame has a frame above it, the consume falsifies `donationChainWellFormed.prevLinkReciprocal` at that frame.  **Reachable**: the reply seam's own docstring records that authority flows from *holding* the reply capability and that a copied or minted one held by a different server is legitimate delegated authority, so on a chain `C1 → C2 → S` a delegate can answer `C1` out of order while `S` has pushed a frame above `C1`'s.  **Fail-closed, not a corruption**: the later pop's reciprocity test (`replyStackOuterCaller?`) refuses the stale link and returns `.invalidArgument` having written nothing, and the same test refuses a *re-linked* Reply, so there is no confused deputy and no privilege escalation.  The cost is a **wedge** — that reply fails permanently, the intermediate caller stays blocked and the scheduling context stays with the server — against the threads in one call chain and one reservation, available to a party that already holds delegated reply authority over that chain.  Severity **Medium**, availability only; not exploitable today (nothing boots, the SVC seam halts before SM10.1).  Before `v0.35.4` the same input produced the object-pinning leak that cut was opened to fix, so this is not a new exposure class.  The remedy is seL4's `reply_remove` non-head branch on the reply path, which needs one more declared footprint member on `.reply` and `.replyRecv` and moves the declared ceiling to 22 (it was 17 when this row was written; PR #894's review took the `.replyRecv` arm to 21 for the five objects the *invoking* receiver's own pre-receive return touches).  Stated as a precondition in `Reply.consumed`'s own docstring.  **CLOSED at `v0.35.6`** by WS-RM, in twenty-six sub-tasks across six phases.  `removeCallerReplyFrame` is the one removal step -- the detach folded to the identity where nothing links down, then the consume -- and `endpointReplyOnCore`, `endpointReply` and `endpointReplyRecv` all run it; `.reply` and `.replyRecv` declare the frame the detach writes (`answeredReplyFrameAbove?`, resolved from the same expression the arm's existing reply member comes from), taking the declared ceiling to **22**, with no *reachable* footprint growing since the new member and the donation-return members are mutually exclusive.  The reply leg's head case is stated rather than hidden -- `donationChainWellFormedExcept` at the consumed head -- and discharged by the donation pop that follows it in the same transition (`endpointReplyCrossCoreDispatch_preserves_donationChainWellFormed`, composed by the fault reply and by seL4's `doReplyTransfer`).  **A second defect was found and fixed in the same cut**: `.replyRecv` ran its donation pop *after* the receive leg, and the receive leg re-links the very Reply the reply leg just answered -- so on the MCS passive-server steady state, where that Reply still headed the donated context, `linkCallerReply` and the server-first stash both refused `.replyCapInvalid` and **no passive server whose client had donated could complete a `seL4_ReplyRecv`**.  The pop now runs between the legs, which is seL4-MCS's own `doReplyTransfer` -> `reply_remove` -> `receiveIPC` order; the fused resolution is split into `replyRecvPopDonation` and `replyRecvPostReceiveDonation`, each with its own bundle theorem stated at the state its own step runs on, and `tests/SmpIpcSuite.lean` §3.21 exercises the loop end to end with a negative pinning the pre-fix refusal.  What keeps the surface closed is derived rather than listed: `SeLe4n/Testing/ReplyStackWriteCensus.lean` (Tier 1) collects every definition that writes reply-stack data from the elaborated environment and reconciles it against a registry in both directions, so a new site that consumes a caller's Reply bare is a build failure on the day it is written.  **Nothing owed** | [`docs/planning/REPLY_FRAME_REMOVAL_PLAN.md`](planning/REPLY_FRAME_REMOVAL_PLAN.md), `SeLe4n/Kernel/IPC/Operations/Endpoint.lean`, `SeLe4n/Kernel/IPC/CrossCore/EndpointReply.lean`, `SeLe4n/Kernel/IPC/DualQueue/Transport.lean`, `SeLe4n/Kernel/API.lean`, `SeLe4n/Model/Object/Reply.lean` | WS-RM |
| ~~**A delegated `.reply` does not deschedule a *preempted* recorded server**~~ — **CLOSED `v0.35.79`** (WS-RR RR8.6): the sweep the row asked for was run, and it found a **fifth** site the row had not named — `suspendThreadOnCore`'s own G4/G4b pair, a removal at the home and a removal at the running core, which between them miss a victim **queued off its home** (an unpinned thread preempted on a secondary core is re-enqueued there by `preemptCurrentOnCore`), so a `.tcbSuspend` on such a thread marked it `.Inactive` and left it dispatchable.  `descheduleThread`, `cancelIpcBlockingOnCore` and the suspend's G4 all read `placedCoreOf?` now — through `descheduleAt` / `descheduleAtPlacement`, one primitive — with `descheduleSgi?` the poke resolved on the same placement, the three scheduler-domain footprints re-keyed on it (`suspendThreadOnCoreSchedLockSet`'s run-queue segment is a pair over the placed and executing cores), `cancelIpcBlockingOnCoreSchedLockSet_covers_deschedule` relating the pre-state footprint to the post-wake removal, and `tests/SmpCancellationSuite.lean` §3.24 computing the retired reading beside the live one on the queued-off-home shape.  The original text follows.  Reported at `v0.35.36` while mapping WS-HP HP4's blast radius, and the **third and fourth** instances of the class PR #895 review rounds 10 and 11 closed at two sites.  `endpointReplyCrossCoreDispatch` computes `expectedCore := determineExecutingCore st expected` and hands it to `applyReplyDonationOnCore`, whose last step is `removeRunnableOnCore (…) replier executingCore`.  `determineExecutingCore` finds a core the thread is *current* on and otherwise answers `bootCoreId`; `removeRunnableOnCore st tid c` edits **only** core `c`'s run queue and current slot.  So when the recorded server is **queued rather than running** — preempted by its own core's timer tick, say — the deschedule is a complete no-op: it removes the server from core 0's queue (where it is not) and clears core 0's current slot only if it is the server (it is not).  The server's donated scheduling context has been returned, so it is `.unbound`, and `resolveEffectivePrioDeadline`'s `.unbound` arm (since `v0.35.3`) returns its legacy TCB priority — so it is selected on its own core and **runs charged to no reservation**, which is precisely the temporal-isolation failure `replyRecvServerDeschedule` exists to close.  **Reachable**: a delegated reply capability is legitimate authority in seL4-MCS and this kernel deliberately admits one (PR #822 review 6J-lYm removed the `replier == expected` gate), `endpointReplyOnCore` carries no guard requiring the recorded server to be current, and preemption requeues a running thread.  **No invariant fires**: `passiveServerIdle`'s antecedent is "not queued and not current", which is false here — the *conjunct whose antecedent is the property you want* shape PR #895 review round 8 recorded.  Severity **Medium**: not exploitable today (nothing boots; the SVC seam halts before SM10.1), availability and temporal isolation only, not memory safety or confidentiality; the cost is unbounded interference with every reservation on the affected core.  **The remedy already exists in the tree**: `descheduleAtPlacement` (`IPC/CrossCore/EndpointCall.lean`), whose own docstring says it is for *"a thread resolved from the state, whose placement is a fact to look up rather than a parameter to pass"*, with `applyReplyDonationOnCore`'s core parameter removed so no caller can pass a proxy — the shape round 10 chose for `replyRecvServerDeschedule`.  **And the sweep is not finished**: `descheduleThread` and `cancelIpcBlockingOnCore` (`IPC/CrossCore/Cancellation.lean`) deschedule a state-resolved victim at `determineTargetCore`, which round 10 records as *equally* a proxy (`affinityAdmitsCore` is `true` on every core for an unpinned thread, so `runQueueAffinityConsistentOnCore` does not pin one to that answer either).  Rounds 10 and 11 fixed two of at least four sites that ask the same question, which is this project's own sweep rule failing in the way it describes.  **The reported site is FIXED at `v0.35.37`**, on the maintainer's instruction after the finding was surfaced for triage: `applyReplyDonationOnCore`'s core parameter is gone and its last step is `descheduleAtPlacement`, so no caller can supply a proxy.  The bridge to the single-core spine becomes honestly conditional (`applyReplyDonationOnCore_eq_single_of_placed_at_bootCore`), because an unconditional equation with the boot-pinned `removeRunnable` is exactly what was false; the confinement theorem's core list is read off `descheduleAtPlacementCores` — the same resolver the step uses — and is stated on the **pre**-state, which is the only state a caller holds; and `tests/SmpCrossCoreReplySuite.lean` exhibits a recorded server queued on core 2 and current nowhere, runs **both** spellings on it, and asserts they disagree, so the witness discriminates without needing a mutation the frame lemmas no longer admit.  **The sweep was what remained**: `descheduleThread` and `cancelIpcBlockingOnCore` still descheduled a state-resolved victim at `determineTargetCore`, which round 10 records as equally a proxy — and so did the live suspend's G4/G4b pair, which the row had not named.  **CLOSED v0.35.79 (RR8.6)** | `SeLe4n/Kernel/IPC/CrossCore/EndpointReplyDispatch.lean`, `SeLe4n/Kernel/IPC/CrossCore/EndpointCall.lean`, `SeLe4n/Kernel/IPC/CrossCore/Cancellation.lean` | RR8 |
| ~~**Two spellings of "tear down the caller↔Reply link"**~~ — **CLOSED `v0.35.63`** (WS-RR RR8.5): `consumeReplyLink` is the reply path's own `SystemState.consumeCallerReply` now, read through its pure projection `SystemState.consumeCallerReplyLink` (the one state the infallible step leaves, with the error arm *eliminated* by `consumeCallerReply_isOk` rather than defaulted) under the victim's own `replyObject`, and `consumeCallerReply_eq_link` is the bridge every cancellation-side fact is derived through — thirty declarations went with the raw-insert twins — `clearTcbReplyObject` / `clearReplyObjectCaller` and the twenty-eight theorems stated over them — every surviving `consumeReplyLink_*` theorem keeps its statement (the projection lemma gains the index-completeness hypothesis the reply path's own projection theorem reads) and is a corollary of its `consumeCallerReply_*` twin, and `cancelIpcBlocking_lifecycle_eq` is deleted rather than given a third hypothesis, because with the teardown running through `storeObject` its definitional lifecycle frame holds on no reachable `.blockedOnReply` state.  The survivor was chosen for the reason the row below names: the reply path's step is the one the chain results are stated over, and it writes through `storeObject`'s bookkeeping where the twin wrote raw.  The reply-stack census registers the projection as a primitive and the teardown as a site of it, so a pure transition reaching for either bare fails Tier 1.  What the collapse *measured* is the wider drift it is one instance of — see the raw-store-write row in table C.  *Original row:* `SystemState.consumeCallerReply` (the reply paths) and `Lifecycle.Suspend.consumeReplyLink` (the cancellation path) perform the same two writes — clear `Reply.caller`, clear the caller TCB's `replyObject` — in opposite orders through different helpers.  One question with two answers, which this project has paid for repeatedly; **pre-existing**, and not a soundness defect: each has its own chain and bundle results, each is registered in `SeLe4n/Testing/ReplyStackWriteCensus.lean`, and the two agree on every state.  The cost is that a fact proved of one does not transfer to the other, so the reply and cancellation surfaces carry parallel lemma families — which is how WS-RM came to need `removeCallerReplyFrame` beside `spliceThreadReplyFrameOut` (then `detachFrameAboveThreadReply`; renamed at WS-HP HP6.1) rather than one step serving both.  Severity **none** as a live defect; the risk is the ordinary one for a duplicated answer: the next correction reaches one copy.  **Registered rather than absorbed** into WS-RM, whose §9 records the measurement: collapsing them is a rename across roughly 215 mention sites and two lemma families, which would have made an already-large cut unreviewable.  The remedy is to define one and derive the other, with the survivor's write order decided by which one the chain results are stated over.  ~~**Closure target: RR8.5** (before RR8 closes)~~ **Closed at RR8.5, `v0.35.63`.** | `SeLe4n/Model/State.lean`, `SeLe4n/Kernel/Lifecycle/Suspend.lean`, [`docs/planning/REPLY_FRAME_REMOVAL_PLAN.md`](planning/REPLY_FRAME_REMOVAL_PLAN.md) §9 | RR8 (closed) |
| ~~**A donated SchedContext is never returned when its owner's IPC is cancelled**~~ — **CLOSED `v0.35.86`** (WS-RR RR8.11).  The residue was the migration congruence, and building it found that the congruence could not be stated of the code as written: `cancelIpcBlockingMigrated` aimed the migration at `determineTargetCore st victim` — the home of the thread the reclaim is *about to* bind the context to — while the reclaim's guards are fail-closed, so on a refusal it moved `scId`'s replenishments to a core no thread bound to `scId` is homed on, which is `replenishQueueAffinityConsistentOnCore`'s own negation.  The reply path never had the defect because its migration sits in the `.ok` continuation of its return, so the asymmetry was between two spellings of one question and the pure spelling had it wrong.  The destination is now `replenishHomeOfSchedContext` of the **post-teardown** state — the home of the thread the context is bound to, which is what the invariant demands — so a refused reclaim is a self-migration that `migrateSchedContextReplenishment_noop` collapses to the identity, and `cancelIpcBlockingMigrated_establishes_replenishQueueAffinityConsistent_smp` and its cross-core lift take **no** hypothesis beyond `objects.invExt` and the pre-state invariant, because the destination obligation becomes `replenishHomeOfSchedContext_spec`.  Behaviour on every reachable state is unchanged (`cancelIpcBlockingMigrated_eq_victim_home_of_committed`; the golden trace is byte-identical), and `tests/SmpCancellationSuite.lean` §3.25 computes the retired destination beside the live one on a refused reclaim with the holder and the victim homed on different cores — the retired reading moves the replenishment, the live one does not.  What follows is the row as it stood. — reported v0.34.95 while proving the RR7.22 residual, and a *model/specification gap* rather than a broken proof.  This model's `.blockedOnReply` arm was `consumeReplyLink (restoreToReadyCancelled st tid) tid tcb` (`Lifecycle/Suspend.lean`), which clears `TCB.replyObject` and `Reply.caller` and touches **no** `schedContextBinding`; `suspendThread`'s G3 then dispatches on the *victim's own* binding, which for a donating caller is `.unbound`, so that arm is the identity.  The server therefore keeps `schedContextBinding = .donated scId victim` while the victim is moved to `.ready` and then `.Inactive`, and `donationOwnerValid`'s second clause — the owner is `.unbound` **and** `.blockedOnReply` — is false on a state a live `.tcbSuspend` reaches.  **No existing theorem is unsound**: nothing claims `donationOwnerValid` across suspend, which is exactly what makes this a false-assurance gap rather than a broken proof, and why the RR7.22 residual's reply arm cannot state the full bundle.  Severity **Medium**: not exploitable (nothing boots; the SVC seam halts before SM10.1) and it needs a TCB write right over the victim, but the effect crosses a domain boundary — suspending A transfers A's CBS reservation to server B permanently, and A is left `.unbound` with no SchedContext, so it can never be scheduled again after resume.  Availability, not memory safety or confidentiality.  **REMEDIATED at `v0.34.97`**, by returning the context rather than weakening the invariant.  The cut's own text said it was "implementing seL4's behaviour", and `v0.35.40` re-read upstream to establish *which* behaviour: `cancelIPC` runs `reply_remove_tcb` and donates nothing, while **revoking the reply capability** (`finaliseCap` → `reply_remove` → `reply_pop`) does return the context to its caller — the Reference Manual's documented behaviour, and the same function `doReplyTransfer` uses.  So this remediation is upstream's `reply_remove` semantics applied at the *cancellation* point, where upstream defers them to Reply-object finalisation; the difference is **when**, not whether, and this kernel's binding typing forces the earlier point (`.donated scId owner` names its owner, so `donationOwnerValid` fails the instant that owner stops being reply-blocked).  New code must not remove the reclaim in the name of parity.  (The first draft of this correction said upstream "permanently strands" the reservation, which described the `cancelIPC` path and attributed it to the whole kernel.)  `cancelIpcBlocking`'s reply arm is now `consumeReplyLink (restoreToReadyCancelled (returnDonationToCancelledCaller st tid tcb) tid) tid tcb` — the return runs **first**, while the caller is still `.blockedOnReply`, so every intermediate state satisfies the invariant.  The holder is found through `cancelledCallerDonation?`, which reads the caller's own recorded reply target; `donationHolderIsReplyTarget` (`Lifecycle/Invariant/CancellationReplyShape.lean`) states the fact that makes that lookup complete — the holder of a caller's donation is that caller's reply target, and a real thread — since `ipcInvariantFull` admits `.blockedOnReply epId rt` for any `rt` and relates `rt` to no donation.  The *behaviour* needs no hypothesis; the hypothesis scopes the payoff `cancelIpcBlocking_reply_no_donation_to_victim`: after the arm, **no** thread holds a SchedContext donated by the cancelled caller, so `donationOwnerValid`'s second clause has no witness to fail on.  The SM5.H replenishment migration sits in `cancelIpcBlockingMigrated` at the cross-core layer, where this tree resolves home cores for every other donation-carrying path.  Three things the cut established beyond the fix: the return is invisible to **every** observer, not merely a high one (`projectKernelObject` strips `schedContextBinding` and `boundThread`, so the obvious worry — a low server seeing a high caller's cancellation — does not arise); the declared footprint gained the SchedContext and the holder's TCB, reaching eight members and still inside `maxLockSetSize`; and three copies of the return's field-frame case analysis collapsed onto one derivation (`returnDonatedSchedContext_ok_storeChain`).  **And the remediation introduced one**, found at `v0.34.98` and owned by WS-OD OD1: the reclaim can leave the holder `.unbound` while it is still `.blockedOnCall`, which `passiveServerIdle` forbids.  Reachable at depth 1 with no chain — the server Calls an endpoint with no receiver waiting, so it blocks keeping the donation, and the reclaim then unbinds it in place.  No theorem is unsound (nothing claims `passiveServerIdle` across `cancelIpcBlocking`), but the conjunct is false of a state a live `.tcbSuspend` reaches.  **Behaviourally closed at `v0.34.104`** (WS-OD OD1.4): the reclaim is `returnDonatedSchedContext (abortHolderPendingIpc st holder) holder scId tid`, ending the holder's outstanding send or call *before* the hand-back — the same ordering argument the return-before-restore uses, one level down, since with the return first the intermediate state is the very violation being closed.  The abort is `abortPendingIpcOnEndpoint`, the timeout's **objects-only** prefix, so `cancelIpcBlocking_scheduler_eq` and its four cross-core consumers are untouched; it is all-or-nothing (a refused return discards the abort, which is what keeps `returnDonationToCancelledCaller_eq_self_of_getTcb?_none` true); and every fact the hand-back reads survives it, because the abort writes no `schedContextBinding` and no SchedContext.  **And proved at `v0.34.105`** (WS-OD OD1.5): `cancelIpcBlocking_preserves_passiveServerIdle` holds on every arm — the two queue arms and the `.ready` arm from the shape modules that own them, and the reply arm by composing the abort's frame with the hand-back's.  The load-bearing step is that every thread the cancellation rewrites ends in a state `passiveServerIdle` permits, so the conjunct's own filter discharges it and the pullback only fires on threads the transition left alone; the abort is what puts a `.blockedOnCall` holder into that half, and `ipcStateQueueMembershipConsistent` is what makes the abort *succeed* (a refused abort would leave the holder exactly where the defect left it).  The same cut closed the footprint: the abort *splices*, so it writes the holder's endpoint **and its two queue neighbours**, three members the declared set did not name — a footprint that is false is worse than a wide one.  Summed the footprint is eleven of nine and the bound holds by case analysis on `tcb.ipcState`; on the reply arm it was nine of nine, with **no headroom**, which WS-OD OD3.5 recovered at `v0.34.128` by making the victim's splice-neighbour members arm-selected like every other member of the family (the widest arm was then eight; WS-OD OD3.7's two below-head reads took it to ten, `lockSet_cancelIpcBlockingOnCore_size_le_ten`).  **Still owed** (owner RR8): that the migration *establishes* `replenishQueueAffinityConsistent_smp` on the composite — the reply chain's identical shape is proved, and the only gap is a congruence over the two TCB writes the teardown performs between the return and the migration; and `Reply.donatedSc` — the field seL4 uses for this link (`reply->replyNext`) — was **declared and never written to `some`** when this row was written, with wiring it named here as the follow-on that matches seL4's structure.  That half is **closed**: WS-OD OD4 (`v0.35.2`) made `donateSchedContext` a *push* onto `SchedContext.scReply`, and `v0.35.4` replaced `donatedSc` with `Reply.next : ReplyStackLink` so the stack is doubly linked and a middle frame comes off it in `O(1)`.  `donationHolderIsReplyTarget` survived that wiring rather than being removed by it, because it stated a fact about a *cancelled caller's own recorded reply target*, which no stack link determines — and **WS-HP HP5.3 (`v0.35.39`) retired it**, by making the reclaim read the stack instead: `cancelledCallerDonation?` resolves the context off the victim's own reply frame's `.head` link, so the fact the payoff needs is `donatedContextIsOwnerFrameHead` (the donation the victim owns is the one its own frame heads) and the recorded reply target is read by nothing.  The two readings agree on every state `severAtCut` can produce and differ at an orphan head, which is the state HP6's splice creates — **reachable since `v0.35.45`**; `tests/SmpCancellationSuite.lean` §3.20 exhibits both.  So the congruence above is the whole of what this row still owes.  ~~**Closure target: RR8.11**~~ **— closed at `v0.35.86`; see the closure note at the head of this row, which records the defect the congruence work found.** | `SeLe4n/Kernel/Lifecycle/Suspend.lean`, `SeLe4n/Kernel/IPC/CrossCore/Cancellation.lean`, `SeLe4n/Kernel/IPC/Operations/Donation/Primitives.lean` | RR8 |
| **A SchedContext is never donated onward, so a passive server at call depth ≥ 2 can never run** — reported at `v0.34.98` while proving the reply arm of the RR7.22 residual, and a *model/specification gap* of the same class as the row above.  `applyCallDonation` (`SeLe4n/Kernel/IPC/Operations/Donation/Primitives.lean`) donates only when the **caller's** binding is `.bound scId`; a caller already holding `.donated scId owner` falls through its `\| _ => .ok st` arm and donates nothing, and `donateSchedContext` is the only operational construction site of `.donated`, so nothing else can.  seL4-MCS's `receiveIPC` → `maybeDonateSchedContext` reads `sender->tcbSchedContext`, which is set for bound **and** donated holders, so seL4 passes the context down the call chain and this kernel stops it at the first server.  Two consequences.  **Functional**: at depth ≥ 2 the callee is `.unbound` and receives no budget, so it can never run — `docs/spec/SELE4N_SPEC.md` §8.12.7's claim that donation "enables **passive servers**" is false there, which the implement-the-improvement rule makes a code defect rather than a documentation one.  **Invariant**: a holder can reach `.donated sc A` ∧ `.blockedOnCall ep2` (it Calls an endpoint with no receiver waiting, so no rendezvous occurs and it keeps the context), and the `v0.34.97` reclaim above then leaves it `.unbound` ∧ `.blockedOnCall`, which `passiveServerIdle` forbids; onward donation does **not** close that half, because seL4 reaches the same state and permits it.  Severity **Medium**: not exploitable (nothing boots; the SVC seam halts before SM10.1), and the effect is availability plus a false-assurance gap rather than memory safety or confidentiality — but it needs no more authority than an ordinary two-level passive-server chain.  **Owned by WS-OD**, whose plan wires the two Reply fields the model already declares for this and never writes (`donatedSc`, `prev`) plus the `SchedContext.scReply` head that `Reply.wellFormed`'s docstring already names and the structure does not have, keeps the binding's `owner` at the immediate donor so all five donation conjuncts hold at depth `n` unchanged, lands the return before the donation so no depth-2 chain is ever serviced by the flat return, validates every chain link it follows so a re-linked Reply cannot redirect a context, and closes the `passiveServerIdle` half **first** with an objects-only abort rather than with `timeoutThread`, which writes the scheduler and would break the reclaim's objects-only frame.  **CLOSED at `v0.35.2`** (WS-OD OD4): `callDonationSchedContext?` and the footprint's `endpointCallDonatedSc?` both read the caller's *effective* context (`SchedContextBinding.scId?`), so a `.donated` caller donates exactly as a `.bound` one does — seL4-MCS's `maybeDonateSchedContext`; `donateSchedContext` became a four-store **push** that mounts the donor's own `replyObject` as the context's new stack head, fail-closed in three ways on that frame, and the `.call` footprint declares every key it writes already, so `maxLockSetSize` is unmoved.  `donateSchedContext_preserves_donationChainWellFormed` closes the loop OD2.4 opened, and `passiveServerHoldsDonatedContext_atCallDepthTwo` is the statement the row asked for: a passive server reached at call depth ≥ 2 holds a scheduling context **and** that context's `boundThread` names it back.  §8.12.7 of the specification is true at every depth now, which is what the implement-the-improvement rule required rather than a weakened claim.  **Nothing owed** | [`docs/planning/SCHEDCONTEXT_DONATION_CHAIN_PLAN.md`](planning/SCHEDCONTEXT_DONATION_CHAIN_PLAN.md), `SeLe4n/Kernel/IPC/Operations/Endpoint.lean`, `SeLe4n/Kernel/IPC/Operations/Donation/Primitives.lean`, `SeLe4n/Model/Object/Reply.lean` | WS-OD |
| **The cancellation reclaim's holder abort is projection-visible, so the reply arm's information-flow result gained an obligation** — introduced at `v0.34.104` by WS-OD OD1.4 and stated rather than assumed away.  Until that cut, `returnDonationToCancelledCaller` wrote only `TCB.schedContextBinding` and `SchedContext.boundThread`, both erased by `projectKernelObject`, so `returnDonationToCancelledCaller_preserves_projection` and the reply arm's `cancelIpcBlocking_blockedOnReply_preserves_projection` were unconditional in the labelling — two of the tree's four fully-substantive cancellation NI results.  OD1.4's `abortHolderPendingIpc` prefix splices the **holder** out of the holder's endpoint queue, which writes the endpoint object, the holder's queue neighbours' links and the holder's own `ipcState` / `threadState` / queue links — every one of which *survives* projection.  So a low observer that can see the holder's endpoint could observe a high victim's cancellation through it.  **This is the endpoint/notification queue-representation gap the three queue arms already carry** (`hTeardownProj`, registered under WS-RR RR3), reaching the reply arm through the holder instead of the victim; it is not a new class.  Severity **Medium**: a confidentiality gap in the *model's* NI surface rather than a live channel — nothing boots, the SVC seam halts before SM10.1, and the flow requires a deployment that labels a server's endpoint below its client.  **What landed instead of a weakened theorem**: the obligation is named (`abortHolderProjectionStable`, `SeLe4n/Kernel/IPC/CrossCore/CancellationNI.lean`), quantified over the resolution rather than over a supplied thread (the holder comes from `cancelledCallerDonation?`), and **discharged outright wherever the abort is inert** (`abortHolderProjectionStable_of_allowed`, from `abortHolderPendingIpc_eq_self_of_allowed`) — which is every state on which the `passiveServerIdle` hole did not arise, so no information-flow result that held before the remediation is weakened on the states it held for.  **The remedy this row was registered with is RETRACTED at `v0.35.83`** (RR8.8's first cut).  It read *the queue label-uniformity invariant*, and that invariant is **unestablishable**: the live send / call gate is `endpointFlowGate ctx ep (threadLabelOf sender) (endpointLabelOf ep)`, an **order** and not an equality, so a lower-labelled and a higher-labelled sender are both admitted onto one higher-labelled endpoint — `publicLabel → kernelTrusted` is the flow `securityFlowsTo_prevents_label_escalation` documents as intended, and `endpointAdmissionAdmitsMixedObservability` (`SeLe4n/Kernel/InformationFlow/Projection.lean`) is the admission as a `decide`-checked theorem, with an observer that sees exactly one of the two.  Establishing uniformity would mean narrowing the gate to label equality and refusing a lower-labelled client's send to a higher-labelled server.  **Two of the three write classes are DISCHARGED at `v0.35.84`, and the third cannot be.**  The *holder's own TCB* closes through `donationHolderHigh_of_donorHigh` over the new state invariant `donationOwnerFlowsToHolder` (the state form of `label victim ⊑ label endpoint ⊑ label holder`, which the donating `Call`'s gate and the server's receive gate license at the transition and the store records no trace of).  The *endpoint object* closes through `endpointObjectHigh_of_admittedThreadHigh` — **and needed a conjunct that did not exist**: `endpointFlowGate` compares `endpointLabelOf` while `objectObservable` decides visibility from `objectLabelOf`, two *independent* `LabelingContext` fields with nothing relating them, so the step `v0.35.83` asserted "from the admission gate" was not available.  `LabelingContextValid.endpointObjectCoherence` is the missing conjunct — an endpoint's flow label flows to its own object's label — discharged structurally for every constructed context from the new `DeploymentLabeling.hEndpointObjectCoherence`, which the one base constructor meets by reflexivity.  The third class, the holder's **queue neighbours**, closes from nothing: their labels are constrained only against the endpoint's.  `abortHolderSpliceHigh_of_victimHigh` is the reduction, stated over the shared `endpointSpliceHigh` (relocated to `InformationFlow/Invariant/Operations.lean` so this asker can reach the predicate the notification path's removal already used).  **Still owed**: the reduction stops at `endpointSpliceHigh`, because `endpointQueueRemove` — the *single* removal `abortPendingIpcOnEndpoint` runs — has no projection lemma yet, where `endpointQueueRemoveDual` does.  Writing it must bridge one mismatch found while reducing: `endpointSpliceHigh` names the predecessor through `queuePPrev` and the single removal reads `queuePrev`, which agree only under RR8.3's `queuePPrevAgreesWithPrev`.  Until that lands, `abortHolderProjectionStable` still carries its whole obligation as a hypothesis — the labelling layer under it is proved, the connection is not.  The row below this one carries the neighbour class and its measurement.  **Closure target: RR8.8** (before RR8 closes) | `SeLe4n/Kernel/IPC/CrossCore/CancellationNI.lean`, `SeLe4n/Kernel/Lifecycle/Invariant/SuspendPreservation.lean` | WS-OD (closed v0.35.2); re-homed to **RR8** at v0.35.56 per RR0.10 |
| **...and so is the holder *wake* the same reclaim performs, so the reply arm's information-flow result gained a second obligation** — introduced at `v0.34.108` by WS-OD OD1.7, the scheduler twin of the row above.  OD1.7 closed a **denial-of-service defect** OD1.4 had left behind: the abort ended the holder's send or call and left it `.ready` on **no** run queue, with `resumeThreadOnCore` refusing it (it demands `threadState = .Inactive`), `schedContextBind` re-bucketing only an already-queued thread, and `chooseThreadOnCore` never scanning ready TCBs — so an ordinary `.tcbSuspend` on a reply-blocked caller stranded the server it had called, permanently, denying service to every other client of that server.  Reported as a security finding rather than folded in silently; not exploitable in practice (unmerged at the time, and nothing boots before SM10.1).  The fix places the holder on its **home** core's run queue, and a run-queue insert is filtered by the inserted thread's *own* observability (`projectRunnable`), so the wake is invisible exactly when the holder is non-observable — which the victim's label does not determine.  Severity **Medium**, and the same class as the row above: a confidentiality gap in the model's NI surface, not a live channel.  **What landed instead of a weakened theorem**: `abortHolderWakeHigh` (`SeLe4n/Kernel/IPC/CrossCore/CancellationNI.lean`), quantified over the same resolution, **discharged outright wherever no donation is resolved** (`abortHolderWakeHigh_of_no_donation`) — so the remediation adds an obligation exactly where it adds a write and nowhere else.  **CLOSED `v0.35.84`** (RR8.9).  Re-scoped at `v0.35.83` and discharged one cut later: `abortHolderWakeHigh_of_donationOwnerFlowsToHolder` proves the obligation outright from `donationOwnerFlowsToHolder`, with `cancelAbortedHolderWake?_donation` relating the wake's resolved holder to the donation resolver's.  The row said the general discharge was the *same* fact the row above waits on, "so the two close together"; the two did **not** — this obligation is a single `threadObservable ctx observer holder = false`, so the state invariant is the whole of it, a run-queue insert being filtered by the inserted thread's own observability and asking nothing about any queue's representation.  It therefore inherited **none** of the row above's residue, and the docstring on `abortHolderWakeHigh` that said it waited on the (unestablishable) queue label-uniformity invariant was corrected at `v0.35.83` and records the discharge now.  **Closure target: RR8.9** (before RR8 closes) | `SeLe4n/Kernel/IPC/CrossCore/CancellationNI.lean`, `SeLe4n/Kernel/IPC/CrossCore/Cancellation.lean` | WS-OD (closed v0.35.2); re-homed to **RR8** at v0.35.56 per RR0.10 |
| ~~**A timed-out thread strands its queue successor**~~ — **CLOSED v0.34.100 (behaviour), v0.35.58 (the two-removals residual)**; reported at `v0.34.99` while settling which endpoint-queue removal the WS-OD OD1 abort should use, and a live defect rather than a design one.  The tree has **three** removals — this row said "two", which WS-OD OD3.9 (`v0.34.134`) refuted by finding the identical defect in the third (`spliceOutMidQueueNode`), and WS-RR RR8.4 refuted again by finding that the boundary question has **five** askers once the frozen mirror and the third removal's own boundary write are counted.  The two this row is about are:  `endpointQueueRemoveDual` (`SeLe4n/Kernel/IPC/DualQueue/Transport.lean`), which every path but one uses, patches the successor's `queuePPrev` to the removed thread's own `pprev` and *requires* that field to agree with `queuePrev` — its `pprevConsistent` guard returns `.error .illegalState` otherwise.  `endpointQueueRemove` (`SeLe4n/Kernel/IPC/DualQueue/Core.lean`), whose only kernel-side caller is `timeoutThread`, patches the successor's `queuePrev` and leaves its `queuePPrev` naming the removed thread.  So after the timer tick times out a budget-exhausted thread blocked on an endpoint, the thread behind it carries a `queuePPrev` that disagrees with its `queuePrev`, and **every later dual-queue removal on it fails** — a cancellation, a rendezvous pop, a `.tcbSuspend` — so it can never leave the queue.  The head case fails too, by the guard's other conjunct.  **No invariant catches it**: `queuePPrev` is read by zero `ipcInvariantFull` conjuncts (`grep -c queuePPrev SeLe4n/Kernel/IPC/Invariant/Defs.lean` is `0`), which is what makes this a false-assurance gap rather than a broken proof, and `intrusiveQueueWellFormed` constrains only head, tail, `queuePrev` and `queueNext`.  Severity **Medium**: availability — a thread stuck in an endpoint queue for the life of the system and an operation that fails `.illegalState` with no way to make progress — not memory safety or confidentiality, and not exploitable while nothing boots.  This is the one-question-two-answers shape at its plainest: two removal implementations that disagree about a field.  **CLOSED at `v0.34.100`** (WS-OD OD1.1), by making the two removals **agree** rather than by collapsing them: `endpointQueueRemove` now gives the successor `tcb.queuePPrev`, which is the right value in both cases by construction — a removed head carries `.endpointHead`, which the successor inherits as the new head, and a removed interior node carries `.tcbNext prev`, which is exactly the back-pointer the successor's new `queuePrev` names.  Collapsing to one removal was the first instinct and is **not** what landed: moving the timeout onto the dual removal would import its `pprevConsistent` precondition, which no invariant states — the Boolean checker in `SeLe4n/Testing/InvariantChecks.lean` checks it and the Prop-level `intrusiveQueueWellFormed` does not — and would falsify the existing argument that the timeout's error branch is dead.  Witnessed by an executed trace check rather than by a theorem, because no conjunct reads the field: `[SCO-020a]` reports `inherited=true dual_dequeue_ok=true`, and on the pre-fix code — the patch still present, one field missing, the token-preserving mutation the gate rule asks for — it reports `false`/`false`.  The whole trace changed by exactly that one line.  **CLOSED at `v0.35.58`** (WS-RR RR8.4) — and the sentence this row used to carry, that the two removals "now write the same fields to the same values", was **false**, which is the finding the collapse produced.  They agreed on the head and parted on the tail: `endpointQueueRemove` asked `q.tail = some tid` — the **fact** — while `endpointQueueRemoveDual` asked `removed.queueNext = none`, a **proxy** for it, and derived the new tail from `queuePPrev`.  Under a connected queue the two coincide, and the paragraph above already says why that is not enough: `intrusiveQueueWellFormed` constrains a queue's head and its tail and relates them nowhere.  So on a state the bundle admits — a queued thread with no successor that is not the tail — the inferring form cleared the tail and **stranded the queue's real tail**, a thread that could then never be dequeued.  That is this row's own defect one field over, which is the measure of how little "they agree today" was worth.  `queueRemoveBoundary` (`SeLe4n/Model/Object/Types.lean`, beside `queueUnlinkPredecessor` / `queueUnlinkSuccessor`) is the shared definition and asks the fact, with the four shapes a guarded removal writes named once; the dual now **checks** that the fact and the proxy agree (`queueTailPairAgrees`, the first factor of `dualQueueRemovalGuard`) and refuses that state rather than mishandling it — fail-closed where the inference was fail-open.  The payoff is that WS-OD OD1.3's `spliceRemovedIsTailWhenLast` is **deleted**: every consumer was conditioned on the dual succeeding, so the guard discharges what the hypothesis supplied, and three theorems each shed a hypothesis.  **The fact did not vanish and this closure is not its closure**: queue connectivity is still a missing invariant, now `dualQueueRemovalGuardHolds`'s `hTailLast` — the **position**-half obligation this row predicted, and it turned out to be two halves rather than one, discharged by whoever *calls* a removal from where the thread sits in the queue rather than by every theorem about one.  A cut reading this row as "connectivity is now entailed" has read it backwards; the row that still owns the gap is the connectivity row below.  **RR8.3 landed at `v0.35.57`** and removes the blocker this row named: `queuePPrevAgreesWithPrev` is `dualQueueSystemInvariant`'s fourth conjunct — so the sentence above, that `queuePPrev` is read by zero `ipcInvariantFull` conjuncts, describes the tree *before* that cut and not HEAD — the dual removal's `pprevConsistent` check is the named `dualQueueRemovalGuard` the transition itself reads, and `dualQueueRemovalGuardHolds` / `…_of_dualQueueSystemInvariant` discharge its pairing half from the invariant rather than assuming it.  What the collapse still has to supply is the **position** half, which no invariant entails and which RR8.3 measured rather than asserted: a detached thread carrying `(none, .endpointHead, none)` satisfies the pairing and fails the guard, so a caller must know where in the queue the thread sits — `timeoutThread`'s does, from `ipcStateQueueMembershipConsistent`, and supplying it is the collapse's own work.  **CLOSED v0.35.58 (RR8.4)** | `SeLe4n/Model/Object/Types.lean`, `SeLe4n/Kernel/IPC/DualQueue/{Core,Transport}.lean`, `SeLe4n/Kernel/IPC/Invariant/{Defs,QueueSplicePreservation,TimeoutAbortPreservation,Structural/PerOperation}.lean` | WS-OD (closed v0.35.2); re-homed to **RR8** at v0.35.56 per RR0.10 |
| **`.replyRecv` writes a SchedContext under no declared lock** — reported at `v0.34.128` while implementing WS-OD OD3.5's arm-selected split, and a *verification-soundness* defect rather than a documentation one.  `replyRecvBody`'s third stage, `replyRecvReturnDonation` (`SeLe4n/Kernel/API.lean`), returns the recorded server's donation and then runs `applyCallDonationOnCore nextThread tid` whenever the receive leg dequeues a queued `Call`; `donateSchedContext`'s first `storeObject` writes the **new** caller's SchedContext, which is provably not the returned one, since two threads cannot be bound to a single context.  `lockSet_replyRecv` named only the returned one.  So the declared 2PL footprint did not cover the arm's own writes, and a `.replyRecv` on one core and a `.tcbSuspend` of that queued caller on another — whose footprint *does* name that SchedContext, via `cancelBindingSc?` — had **provably disjoint** footprints while both writing it: the lost-update shape the declaration exists to exclude, with `donationOwnerUnique` and `scThreadIndexConsistent` the invariants at risk.  It is the passive-server steady state, not an edge case: the receiver is `.unbound` at that point precisely because the return just made it so.  `.call` has declared exactly this member since SM6.A.5 and says why (`lockSet_endpointCall_donation_extension`) — one question, two answers.  Severity **High** as a verification defect, **none** as a live one: kernel entry is serialised by the SM5.I global ticket lock, so no two cores execute kernel transitions concurrently and the fine-lock footprints are a model discipline until the migration lands.  **CLOSED at `v0.34.128`**, in the reporting cut: `receiveRendezvousDonatedSc?` resolves the member through `endpointCallDonatedSc?` — `.call`'s own resolver for the same question — applied to the send-queue head the `newSenderTid` member beside it already reads, and the recorded server's TCB joins it unconditionally, which is what lets a *delegated* reply declare a footprint at all (PR #892 review round 6 had made that case refuse for want of room; `lockSetForSyscall_replyRecv_delegated` is retired in favour of `_delegated_declares`).  `maxLockSetSize` moved 9 → 11 for the two members, the maintainer's decision against narrowing the declaration further on the hottest IPC path; `admissibleCriticalSection` for the 1 ms tick falls from 37 µs to 30 µs and the CC-5 bound widens in proportion.  The same cut declared `stateLevelLock` on every donation-carrying footprint, because `SystemState.scThreadIndex` is an `RHTable` whose insert may rehash the whole table and so does not decompose by object.  **Nothing owed**: the arm-selected narrowing is licensed in full.  `endpointQueueRemove_objects_ne`, `abortPendingIpcOnEndpoint_other_tcb_eq`, `abortHolderPendingIpc_other_tcb_eq` and `returnDonatedSchedContext_other_tcb_eq` state each step at keys *outside* its write set — the four the tree lacked — and `cancelIpcBlocking_replyArm_tcb_frame` composes them: with the reclaim live, the arm rewrites exactly the holder, its two queue neighbours and the cancelled caller, all four declared members.  The removal's two link patches are pinned to `queueNeighbourPatch` by `rfl` (`endpointQueueRemove_eq_patches`) rather than re-derived, so the third inlined copy of that shape is gone.  **CLOSED at `v0.34.128`** | `SeLe4n/Kernel/Concurrency/Locks/LockSetTransitions.lean`, `SeLe4n/Kernel/IPC/CrossCore/EndpointReply.lean`, `SeLe4n/Kernel/Lifecycle/Invariant/CancellationReplyShape.lean` | WS-OD |
| WS-DT slices D1, D6, D8 — two `ipcInvariantFull` conjuncts still threaded as post-state hypotheses; no dispatch payoff theorem — **closed v0.34.43** (the measured baseline was 103 bindings over six conjuncts, all de-threaded; the three payoff theorems landed with the per-arm bundle layer, RR3.15–RR3.26) | `SeLe4n/Kernel/IPC/Invariant/`, `SeLe4n/Kernel/API.lean` | RR3.1–RR3.26 |
| Cross-core SchedContext donation never migrates the CBS replenish queue, breaking the SM5.H affinity invariant on a live path — **closed v0.34.42** (all three live paths, RR2.20 included) | `SeLe4n/Kernel/IPC/Operations/Donation.lean`, `SeLe4n/Kernel/IPC/CrossCore/EndpointReplyDispatch.lean` | RR2.1–RR2.12, RR2.20 |
| The live `.send` arm carries no `ipcInvariantFull` preservation while SM6.D claims coverage — **closed v0.34.42** (and the `.receive` arm's WithCaps form, which the audit had mismeasured as covered, in the same cut) | `SeLe4n/Kernel/IPC/CrossCore/EndpointSend.lean` | RR2.14, RR2.15 |
| Cancellation NI rests on a `hTeardownProj` hypothesis whose closure form returns its own premise — **partially closed v0.34.42** (`.ready` and `.blockedOnReply` arms discharged; the three queue arms wait on the queue-link relocation, **RR8.8** — not on the label-uniformity invariant this cell named until `v0.35.83`, which is unestablishable: of their two write classes the endpoint object is **closed** at `v0.35.84` (`endpointObjectHigh_of_admittedThreadHigh`, over the admission gate composed with the `endpointObjectCoherence` conjunct that cut added) and the queue **neighbours** close from no labelling fact at all) | `SeLe4n/Kernel/IPC/CrossCore/CancellationNI.lean` | RR2.18, then RR3 |
| VM faults are unhandled: a data or instruction abort returns to the faulting instruction, wedging the core.  Unreachable only because nothing boots — **closed v0.34.44** (every non-`SVC` arm delivers; `faultDeliverOnCore_not_dispatchable` says no disposition leaves the thread runnable on the core it faulted on) | `SeLe4n/Kernel/Architecture/ExceptionModel.lean`, `rust/sele4n-hal/src/trap.rs` | RR4.1–RR4.27 |
| `TCB.faultHandler : Option CPtr` has no consumer — **closed v0.34.44** (`resolveFaultHandler` resolves it through the thread's own CSpace and gates it on seL4's `sendFaultIPC` predicate: send, and grant **or** grant-reply) | `SeLe4n/Model/Object/Structures.lean` | RR4.7 |
| `.replyRecv` — the idiomatic fault-handler loop — does not route through the RR4.14 reply seam, so a handler that answers a fault with `seL4_ReplyRecv` reaches the ordinary reply and wakes the faulted thread `.ready` at the instruction that faulted.  `.reply` is closed; the workaround is `.reply` + a separate `.receive`.  `replyRecvBody` composes the reply leg, a receive leg and a donation return in one transition, so the fault branch cannot be substituted for its reply leg without re-deriving what the other two legs are handed | `SeLe4n/Kernel/API.lean` (`replyRecvBody`), `SeLe4n/Kernel/IPC/CrossCore/Fault.lean` | RR7 |
| The staged dispatch payoff's `.reply` arm is confined to unfaulted callers (`syscallDispatchQuiescence.replyNoPendingFault`).  Composing `faultReplyOnCore_preserves_ipcInvariantFull` into it needs a lemma the reply chain does not carry — that `endpointReplyCrossCoreDispatch` leaves its target `.ready`, hence `passiveServerIdleAllowed`, which the fault reply's abandon arm consumes at the **post**-state; threading a post-state hypothesis instead is what the RR3 gate forbids.  The transition-level bundle exists and is proven; only its composition into the payoff waits | `SeLe4n/Kernel/IPC/Invariant/DispatchPayoff.lean`, `SeLe4n/Kernel/IPC/CrossCore/EndpointReplyInvariant.lean` | RR7 |
| A core that delivers a fault cannot switch away from the blocked thread: the SM10.1 context restore installs no successor, so `trap.rs::deliver_fault` halts the core instead of `eret`ing back onto the faulting instruction.  Unreachable at v0.34.44 (no core sets `lean_ready`) | `rust/sele4n-hal/src/trap.rs` | SM10.1 |
| A user send's `MessageInfo` label is discarded at delivery: `IpcMessage.label` is set by kernel-originated (fault) messages only, so a thread holding a send capability to a fault endpoint cannot mint a `seL4_Fault_tag` — deliberate, and the pass-through needs its own authority story before it is restored | `SeLe4n/Model/Object/Types.lean`, `SeLe4n/Kernel/API.lean` | RR7 |
| On hardware only `MR0`-`MR3` of a delivered fault message reach the handler's registers: the WS-RA return frame carries four message registers in `x2`-`x5` and no receive path writes `MR4` onward into the receiver's IPC buffer (seL4's `setMRs_fault` does — the buffer write is the WS-RA "4-register return window" residual, which RR4 gave its first consumer).  The model delivers every word (`decodeFault_encodeFault`); an `unknownSyscall` (13 words) or `userException` (5 words) handler on hardware sees its first four.  Closure: the receive-side IPC-buffer write (`Architecture.IpcBufferRead` gains a write twin through the receiver's VSpace), staged at RR7 so the fault path and the `.receive`/`.replyRecv` arms take it together | `SeLe4n/Kernel/Architecture/Fault.lean` (`encodeFault`), `SeLe4n/Kernel/Architecture/SyscallReturn.lean`, `SeLe4n/Kernel/Architecture/IpcBufferRead.lean` | RR7 |
| The RR4.18 row promised scheduler and capability invariant preservation on both fault paths; what landed is `ipcInvariantFull` (delivery, reply, suspend, abandon, restart) and `objects.invExt`.  The cross-core Call and reply chains the fault path composes carry no `schedulerInvariantBundle` / `capabilityInvariantBundle` preservation themselves (`endpointCall_preserves_schedulerInvariantBundle` and `endpointReply_preserves_{scheduler,capability}InvariantBundle` are the single-core forms), so the fault path cannot compose what its substrate lacks.  Closure: lift the two bundles over `endpointCallCrossCoreDispatch` / `endpointReplyCrossCoreDispatch`, then compose `faultDeliverOnCore` / `faultReplyOnCore` (one-TCB rewrites and `removeRunnableOnCore` on top) — an RR7 slice alongside the `.replyRecv` fault branch, which needs the same lift | `SeLe4n/Kernel/IPC/Invariant/FaultPreservation.lean`, `SeLe4n/Kernel/IPC/CrossCore/DispatchInvariant.lean` | RR7 |
| ~~No production `LabelingContext`: the hardware boot path leaves `testLabelingContext`, which maps every non-zero id to `publicLabel`, installed~~ — **CLOSED v0.34.48**: `deploymentLabelingContext` builds a context that is `LabelingContextValid` unconditionally, `confinedLabelingContext` is the two-domain production instance, the boot wrapper's context argument is mandatory and an inadmissible one fails the boot closed *before* any state is committed, and the pre-boot labeling reference is one the guard rejects; the platform binding stores the `DeploymentLabeling` source, so what a hardware boot installs is admitted *and* `LabelingContextValid` by theorem (`PlatformBinding.labeling_valid`, PR #889 review).  The guard itself became exact: a declared separation witness — two admissible threads, neither the sentinel nor an idle thread (`separationWitnessAdmissible`) — the kernel *evaluates*, replacing the three-id sample `testLabelingContext` evaded, so `isInsecureDefaultContext ctx = false` now entails `LabelingContextValid.labelNonTriviality`  **Review round 2 (PR #889)**: the source carries the four policy fields (`memoryOwnership`, `endpointPolicy`, `declassificationPolicy`, `auditMonitorClearance`) with their fail-closed defaults, so a binding configures them where it declares its labeling; before, every hardware boot was forced to the constructor's defaults with no declassification and no audit monitor; **review round 3**: the boot wrapper refuses a boot whose labeling's declared separation witnesses are not installed threads of the boot state (`declaredWitnessesInstalled`, `uninstalledSeparationWitnessBootError`) — the guard decides that two admissible ids are separated, only the boot state can say they are threads; **review round 5**: the family's lower witness is a parameter — thread `1`, the old fixed witness, is the boot VSpace root's object id on every binding, so a boot carrying its own root could never install it — the RPi5 binding declares `rpi5LowerWitnessIndex`, and `PlatformBinding.witnessesOffBootVSpaceRoot` holds every binding's witnesses apart from its root by evaluation | `SeLe4n/Kernel/InformationFlow/Policy.lean`, `SeLe4n/Platform/FFI.lean` | RR5.1–RR5.5 |
| ~~Two kernel seams (SVC dispatch, cross-core suspend) do not consult the per-core `lean_ready` gate, though `kernel_entry.rs` claims every seam does~~ — **CLOSED v0.34.48**: both consult it (`LEAN_UPCALLS_OUTSIDE_THE_GATE` is down to the boot install, which cannot sit behind the gate it establishes), the SVC seam halts — ahead of every prefilter and of the `x7` narrowing, pinned by `svc_arm_readiness_gate_status` (PR #889 review) — and the suspend seam returns `IllegalState`, and RR5.9 added the compile-time half — a Lean `extern` may be declared, defined or exported only under `feature = "hw_target"`, which `cfg(not(test))` did not achieve | `rust/sele4n-hal/src/svc_dispatch.rs`, `rust/sele4n-hal/src/ffi.rs` | RR5.6–RR5.9 |
| ~~Three `@[export]` runtime-seam modules are staged-only, outside the production import closure, so a linked image would not carry their symbols~~ — **CLOSED v0.34.48**: promoted (with the two closure modules they pull in, so the staged count fell by five), and `scripts/check_kernel_entry_exports.py` verifies every required symbol against the built static archive on each Tier-1 run, over a requirement derived from every HAL `extern "C"` declaration (archive, assembly global, or a reconciled `EXPECTED_UNRESOLVED` entry — the intersection form hid a rename on either side, PR #889 review); **review round 3**: an assembly provider is a *defined* symbol (directive and label) in a source `build.rs` assembles, not a `.global` directive, and the boot entry `lean_kernel_main`, once exported, must call `bootAndInitialisePlatform` in its own body (`boot_entry_binding_failures`) — vacuous until SM10.1 writes it, decisive after; **review round 4**: the provider scan blanks every preprocessor-conditional region, follows the builder chain that reaches `.compile()` in a function reachable from `main`, and intersects with the assembled archive's object symbols when a cross build is present; **review round 5**: the boot-entry gate reads the exporting declaration's statements over the string-free view — an executed top-level call and no other kernel-state installer, the installers derived by closure over the Lean tree (`kernel_state_writers`) — where round 3 accepted an identifier occurrence; **review round 6**: the three inventories read the shared code views with strings blanked, and an `EXPECTED_UNRESOLVED` entry expires the moment the Lean tree exports it (`stale_exported`), so an export outside the import closure is a failure rather than a pass; **review round 7**: the boot entry must execute `bootAndInitialiseRPi5`, the generic entry fixed at `RPi5Platform`; the inventory includes the library root `SeLe4n.lean`; the assembly providers are read off the compile's executed chain; **review round 8**: the archive parsers accept global text (`T`) only — a data object under a function extern's name no longer resolves it (`executable_definitions`) — and the assembled sources follow the compiled builder's binding instance (`rust_code_view.binding_statement_before`), a rebound or unbound receiver counting nothing before the compile; **review round 9**: a receiver rebound by *assignment* is a binding boundary too, the boot entry must branch on the checked boot's `Except` and halt on `.error` (`boot_entry_handles_failure` — executing the call is necessary and not sufficient, since a failed boot installs no kernel state), and `build.rs`'s independent readiness scanner reads the library root `SeLe4n.lean` as well; **review round 10**: the boot entry's failure check parses the match arms (the `.error` arm's own body must halt), refuses a diverging statement before the handling match, and resolves the match to the binding the boot produced rather than to a rebinding of its name; **review round 11**: the `.error` arm's halt must be its terminal action, not a token inside it; **review round 12**: every name the gate reads is *resolved* rather than matched — the export attribute is parsed from the list (`lean_code_view.attribute_arguments`, shared with `build.rs`, so `@[inline, export lean_kernel_main]` is the same export on both sides), the checked boot and the halt must denote the pinned declaration by Lean's suffix rule and are refused where the declaration binds the name locally, the halt set is derived from the `@[extern "ffi_fatal_halt…"]` primitives and their aliases, and an `extern` declaration's requirement is its effective linker name (`#[link_name]` included; `build.rs` refuses such an alias for a Lean symbol, which no readiness gate could be attributed to); **review round 13**: arms are scoped by column — a `match` nested inside an arm no longer donates its `.error` arm to the match that contains it, and an arm's terminal statement is the last line at its body's own top level, so a halt in the `else` branch of a multi-line conditional is not the arm's outcome; **review round 14**: the shadow check consults the shared binder scanner (`have`, a `do` reassignment and a `for` binder all shadow), a statement that *may* exit refuses (`LEAN_MAY_EXIT`, the mirror of `build.rs`'s `statement_may_exit`), and the halt-alias closure requires the unique-candidate resolution `reference_failure` already applied; **review round 15**: a constructor pattern binds too (the whole binder region is searched), both Lean code views parse `r#"…"#` raw strings, `statement_may_exit` guards the statements *inside* a tripwire's failure branch as well as those before it, and a value binding of `lean_ready` disables the bare spelling; **review round 16**: the boot entry must name the checked boot and the halt by their *fully-qualified* names and the accepted expression is the call and its arguments rather than a prefix (which ends the binder- and operator-parsing class outright), the readiness guard must be written `crate::lean_ready::lean_ready(..)`, and three parser defects are fixed: the halt seed locates its attribute on the string-free view, an `extern` block is split into items so a `#[link_name]` binds to the item it decorates, and an uninvoked `.macro` body is not an assembly provider; **review round 17**: the boot entry's contract left this gate for the elaborator — `SeLe4n/Testing/BootEntryContract.lean` finds the exporting declaration with `getExportNameFor?`, requires it to call `Platform.FFI.bootAndInitialiseRPi5OrHalt` (the checked boot with its failure handled, so the `.error` arm is a definition rather than syntax a scanner must read) and walks `Expr.getUsedConstants` for any path to a kernel-state installer around it, failing its own elaboration and carrying four witnesses so it is decisive before SM10.1 writes the entry; the Python gate keeps the link-level half, where an `extern <abi>? { … }` block is now resolved rather than spelled (`extern r"C"`, `extern { … }` and `extern "C-unwind"` declared nothing to it, at three sites including two in `build.rs`), a `#[link_name]` is located on the string-free view, and `.if 0` / `.rept` regions provide no assembly symbols; **review round 18**: the contract reads the production environment (both roots, pinned by `env.header.moduleNames`), requires the approved boot call to be an *unconditional* action rather than an occurrence (`unconditionalActions` — `getUsedConstants` says a constant occurs, not that it runs), and pins the entry's FFI type to `UInt64 → BaseIO Unit`, the type `boot.rs` calls the symbol at; and the kernel model gains `wellFormed`'s fifth conjunct `objectBudgetRespected`, with `bootFromPlatformCheckedWithIdleThreadsFor_objectIndexBounded` proving `objectIndexBounded` of a successful production boot — nothing bounded the object count before, so a config filled to `maxObjects` booted and the idle fold took it past the cap; **review round 19**: the action walk validates the `Bind` *instance* before treating an application as sequencing (a lawless `Bind` on a type defeq to `BaseIO Unit` discards both arguments), the reachability walk reads `opaque` bodies (`allowOpaque := true` — an `opaque` alias of an installer was a leaf), and the object budget gets its own boot diagnostic (`objectBudgetBootError`; round 18 added the conjunct without a cascade branch, so a size fault was reported as an embedded-identity mismatch — the defect round 2 fixed for the idle-slot reservation, repeated); **review round 20**: the action walk truncates at the first provably non-returning action (`neverReturns`, seeded from the `@[extern]` halt primitives by symbol and closed over aliases — an entry that halts *before* the approved boot reported it as executed), and project provenance is decided by *excluding* the dependency roots (`isProjectDeclaration`; a `SeLe4n` prefix is an enumeration of the project's roots and cannot see one that does not exist yet, so a bypass in a future top-level module would never have been walked), with the dependency list reconciled against the environment's own module roots so a new one fails the build; **review round 21**: the hand-written `Expr` analysis is *gone* — rounds 18-21 were four consecutive findings against `unconditionalActions`, a hand-rolled abstract interpreter round 17 wrote to decide whether an arbitrary `BaseIO` term boots, which is round 16's unbounded-input argument one level below text.  The entry is now **required to be** `bootAndInitialiseRPi5OrHalt` applied to a configuration (`isApprovedBootApplication`, one `Meta.isDefEq`): stronger than the walk it replaces, since that admitted any extra action which happened not to write kernel state, and it retires eleven analysis definitions and 253 lines.  Thirteen witnesses, three of them acceptances.  The corollary for scanners with no elaborator to ask is unchanged — a macro invocation inside a Rust `extern` block is refused rather than read past, since its expansion declares symbols no `fn`-shaped search can see; **review round 22**: three sites where one question had two implementations and only one was right — the generic boot wrapper passed `allCores` while the machine carried a narrower `declaredCoreCount` (`declaredCoresOfConfig` derives it; `declaredCoresOfConfig_allCores` shows the full-width identity, so existing callers are unchanged), the handoff refusal used the per-PE `fatal_halt` where the tree's system-wide barrier is `gic::halt_all()` (already-online secondaries kept servicing interrupts after a refused handoff), and the assembly *source* fallback accepted a `.data` label as a function provider — the section question the *archive* parser has asked since round 8 (`executable_label_names`, unrecognised sections treated as non-executable so it under-approximates); **review round 23**: two corollaries — *a proxy is not the fact* (the handoff compared PSCI `CPU_ON` acceptances against the declared count while `smp::CORE_IRQ_READY`, published by each core after `enable_irq`, is what says the PE will service work; `irq_ready_core_count_within` waits for it, bounded, so a PE that never publishes fails the boot rather than hanging it) and *a bound has two sides* (round 22 clamped `declaredCoreCount` from above and left zero, where the boot installs no idle thread on any core and returns `.ok` with nothing to run; `declaredCoreCountInRange` is `wellFormed`'s sixth conjunct, with its own diagnostic and six runtime witnesses); **review round 24**: round 23's wait was paced with `cpu::wfe_bounded`, whose `max_ticks` is documented *informational* ("does not bound the actual `wfe`"; the body opens `let _ = max_ticks;`), so a secondary that dies in init sends no event, the first sleep never returns and the topology refusal is unreachable — a wait that cannot time out cannot fail closed.  It is now the counted spin `shootdown::wait_all_acked_bounded_in` already chose for the same hazard, clocked by `crate::timer::read_counter` with the clock injected, and four host tests that the bound terminates; **review round 25**: three scanners whose unrecognised-input branch silently skipped, which is fail-open in each — `EXTERN_FN` now reads Rust's `r#` raw-identifier escape (which names the same linker symbol) and an `extern` item that is neither a readable `fn` nor a symbol-free `static`/`type`/`use` exits with a reason, `executable_label_names` models GAS's section *stack* (`.pushsection`/`.popsection`/`.previous`, an unresolvable change leaving the section unknown and so not executable, statements split on `;` for both halves of `asm_definitions_in`), and both `@[export]` collectors read `«…»` identifiers and **refuse** an argument in neither form — the Lean inventory is the readiness-gate seam set, so a guillemet-spelled non-`lean_` export was an upcall no gate was required for; the closed direction differs by output (a requirements scanner refuses unreadable input, a providers scanner drops it); the raw-identifier question is swept across all four sites that ask it (`build.rs`'s `extern_block_declarations`, which feeds the readiness seam set, and both `enclosing_fn` implementations, which key every name-based lookup), and the statement split's own edge is closed — a `#define` body is a cpp template, never split into assembler statements | `scripts/staged_module_allowlist.txt`, `SeLe4n.lean`, `SeLe4n/Testing/BootEntryContract.lean` | RR5.15, RR5.16 |
| ~~SM4.G residue: `bootFromPlatformWithIdleThreads` is proven correct and has no production caller, so `idleThreadEnqueuedOnCore` is never established at boot and `schedulerNoStall_smp`'s `hIdle` is discharged by hypothesis only~~ — **CLOSED v0.34.48**: the production wrapper runs `bootFromPlatformCheckedWithIdleThreads`, a thin composition over the checked boot that **enqueues** each core's idle thread (dispatching one, as `bootFromPlatformWithIdleThreads` does, would have left the predicate false and broken `queueCurrentConsistent`), and RR5.10 lifted `inferThreadState` off the boot core first so the resulting state is not re-classified `.Inactive`.  The boot queue is characterised exactly (`bootFromPlatformCheckedWithIdleThreads_runQueueOnCore_eq`), so the keystone `…_chooseThreadOnCore_succeeds` takes no hypothesis beyond the boot and `…_chooseThreadOnCore_idle` pins each core's first selection; the stored idle TCB is the queued `.Ready` form and the boot state is `threadStateConsistent` (`…_threadStateConsistent`), and `PlatformConfig.wellFormed` reserves the idle slots so the fold overwrites nothing (PR #889 review)  **Review round 2 (PR #889)**: the reservation also covers every object a config entry *references* (`bootObjectReferencesReservedIdleSlot`, total over `KernelObject`) and is refused with its own diagnostic rather than as a duplicate id, and the idle objects are unreachable by user authority at all — `syscallResolveCap`, the one resolution every invoked capability passes through, refuses a capability naming a reserved idle object (`capTargetsReservedIdleObject`, `syscallResolveCap_ok_not_reserved`), so a boot CNode or a transfer that carried one yields a slot that resolves like an empty one and no `.tcbSuspend` can remove a core's only guaranteed runnable thread; **review round 3**: the binding boot installs idle threads on the binding's declared cores (`PlatformBinding.declaredCores`, `bootFromPlatformCheckedWithIdleThreadsFor`), and the RPi5 binding declares every model core (`rpi5_cores_eq_allCores`), so the all-cores theorems are the hardware boot's; **review round 4**: the reference check covers a notification's `boundTCB` and every SchedContext reference; **review round 5**: `PlatformBinding.coreCountLe` bounds the declared count by the model, `declaredCores` is the prefix with exactly `coreCount` members, and the reservation is stated model-wide — an undeclared core's idle slot is absent after the boot (`bootFromPlatformCheckedWithIdleThreadsFor_undeclared_idle_absent`), never free; **review round 6**: the reference check reads an untyped's `children` and `parent`; **review round 7**: a boot TCB is stored under its own thread id (`tcbIdentitiesMatchSlots`, the fourth `wellFormed` conjunct) and the reference check reads its `tid` — a TCB at an ordinary id carrying an idle thread's id would have let a retype dequeue the idle thread; the platform entry boots the bound config (`bindPlatformConfig`), so the binding supplies the machine configuration and the boot VSpace root; **review round 8**: the reference check reads `queuePPrev` and is pinned by constructor arity (per-kind helpers destructure every kernel object, so a new field must be classified), the sweep adding a TCB's reply references and carried capabilities, a Reply's `replyId` and `prev` and a SchedContext's `scId`; `bootSafeObjectCheck` requires all three queue links empty; the identity relation covers SchedContexts and Replies (`embeddedIdentitiesMatchSlots`); and the raw suspend seam refuses idle ids before the transition (`suspendThreadCrossCoreStep_idle_refused`); **review round 11 (P1)**: a raw thread-id *operand* escaped the capability chokepoint — `.schedContextBind` resolves its capability to the SchedContext and takes the thread from `args.threadId`, so an ordinary SchedContext capability could bind and re-prioritise the idle TCB — and the refusal now sits at the raw-operand lift points `validateThreadIdArg` / `validateObjIdArg` (`validateThreadIdArg_ok_not_reserved`, `dispatchCapabilityOnly_schedContextBind_idle_operand_refused`); **review round 15**: a boot TCB's `cpuAffinity` must name a core the platform declares (`bootAffinitiesDeclared`, diagnostic `undeclaredAffinityBootError`), vacuous on `allCores`; **review round 20**: that boot check had no live counterpart — `decodeAffinity` accepts any `v < numCores`, so `.tcbSetAffinity` could migrate a thread onto an absent PE right after a successful boot, queued where nothing runs it with the reschedule SGI sent to a core that cannot take it.  The declared count now travels with the machine (`MachineConfig.declaredCoreCount` → `applyMachineConfig` → `MachineState.declaredCoreCount`, proved by `bootFromPlatformCheckedWithIdleThreadsFor_declaredCoreCount`) and `setThreadCpuAffinityWithMigration` refuses an out-of-range affinity fail-closed (`setThreadCpuAffinityWithMigration_rejects_undeclared_core`; unpinning is never caught, `…_none_passes_declared_check`), with `PlatformBinding.declaredCoreCountAgrees` holding the boot's count and the transition's count to one fact — `simSingleCoreMachineConfig` exists because the single-core binding was sharing the four-PE `simMachineConfig`, which is what the gap was | `SeLe4n/Platform/Boot.lean`, `SeLe4n/Kernel/API.lean`, `SeLe4n/Kernel/Scheduler/Operations/Core.lean`, `SeLe4n/Platform/Contract.lean` | RR5.10–RR5.14 |
| ~~`suspend_thread_inner` commits kernel state outside the kernel-entry lock~~ — **CLOSED v0.34.48** by retiring the `@[export]` (this row's own remediation offered that or a bracket; retiring removes the hazard rather than mitigating it, and matches WS-RA's treatment of the twin `syscall_dispatch_inner`).  The Lean definition stays as the single-core reference path the dispatch suite exercises; the C symbol is gone, pinned by a Tier-3 negative anchor | `SeLe4n/Platform/FFI.lean` | RR5.17 |
| ~~Two `debug_assert!` lock/vector tripwires vanish from the release image~~ — **CLOSED v0.34.48**: both are unconditional branches to `cpu::fatal_halt()`, and `build.rs`'s `release_surviving_tripwire_status` holds each to that relation on the statement-level view (a halt nested under another condition, or moved above the branch, is refused); the lock-order tripwire asks ownership (`round_lock_held_by(core)`) — a held/free flag halted every innocent core during a shootdown (PR #889 review); **review round 2**: the scanner compares the whole `if` predicate against the declared failure condition rather than looking for a token in it, so a reversed polarity (`if vbar.is_multiple_of(2048)`) is refused; **review round 6**: each tripwire is pinned with the operation it protects and `tripwire_dominates_protected_operation` requires it among the statements dominating every occurrence of that operation — a branch that halts but is deleted from its caller, nested under a dead condition or moved below the operation is refused; **review round 7**: the branch must end in `fatal_halt` (`statement_halts`), not in any divergence — a `return` hands control back to the acquire; **review round 8**: the branch must be a top-level statement of the helper or sit under a block that executes unconditionally on the image (`tripwire_branch_halts`, `unconditional_block_interior`: a bare or `unsafe` block, or one under exactly `#[cfg(target_arch = "aarch64")]`) — an exact-condition `if` nested under a further condition halted only when that condition held; **review round 9**: nothing may leave the helper before that branch (`statement_may_exit`) — an `if <the failure condition> { return; }` above it returns exactly when the condition holds — and an unqualified `lean_ready(..)` resolves to the gate only where the file imports it and defines no function of that name (`bare_ready_call_resolves`) | `rust/sele4n-hal/src/` | RR5.18 |
| The scheduler's dispatch and the IPC rendezvous do not write `TCB.threadState`: `scheduleEffectiveOnCore` / `switchToThreadOnCore` move the current slot and the run queue and leave the TCB object alone, and no rendezvous writes a `.Blocked*`, so the full classification `threadStateConsistent` is a **boot-state theorem** (`bootFromPlatformCheckedWithIdleThreads_threadStateConsistent`) and false after any core's first dispatch; the harness re-establishes it with `syncThreadStates` before checking (PR #889 review round 2).  The relation the live decisions read — `tcbSuspend` / `tcbResume` / the cross-core cancellation / the fault suspend test the field against `.Inactive` only — is `threadInactiveFlagConsistent`, proved of the boot state (`…_threadInactiveFlagConsistent`).  **Partly discharged at `v0.34.86` (WS-RR RR7.36)**: the per-core context switch preserves it (`switchToThreadOnCore_preserves_threadInactiveFlagConsistent`) together with the primitive it composes (`preemptCurrentOnCore_preserves_threadInactiveFlagConsistent`), under two side conditions that are the two ways the relation genuinely breaks — a displaced thread stranded off every queue, and a dispatch of a thread the state classifies `.Inactive`.  The reusable frame is `threadInactiveFlagConsistent_of_frame` / `…_of_frame_placing` over `threadPlacedOnSomeCore`, with `inferThreadState_eq_inactive_iff` the characterisation, so the remaining surfaces are per-transition applications rather than fresh arguments.  **Still owed**: the wake and idle-enqueue paths (which *change* the stored flag as well as the placement, so they need a third frame), the lifecycle suspend/resume pair, and the IPC block / wake / cancellation / fault-suspend writers.  No code may cite `threadStateConsistent` of a post-dispatch state | `SeLe4n/Kernel/Scheduler/Operations/Core.lean`, `SeLe4n/Kernel/Scheduler/Operations/Selection.lean` | RR7.36 |
| ~~SM2.C-defer (verified RwLock completion) had no durable registration or closure target outside its own plan~~ — **CLOSED v0.34.50**: WS-RR phase RR6 is that closure target, and it landed the plan's substance rather than its bookkeeping.  What the plan itself asserted falsely is corrected in place (RR6.25): the D-1.9 row claimed a theorem that does exist (`rwLock_fifo_admission_temporal`), §5.5 described the MCS queue the v0.32.148 cut deleted and is now boxed with that correction, and Appendix B's reproduction commands named a binary target and two gate scripts that did not exist.  The two residual SM2.C datatype extensions RR6 deliberately does **not** absorb are re-registered individually in table C below, per this row's own instruction | [`docs/planning/SMP_RWLOCK_DEFERRED_COMPLETION_PLAN.md`](planning/SMP_RWLOCK_DEFERRED_COMPLETION_PLAN.md) | RR6.25, RR6.26 |
| ~~The deployed RwLock is the CAS-retry `rw_lock.rs` while the Lean spec was tightened to strict FIFO; `QueuedRwLock`, the FIFO implementation, has zero consumers~~ — **CLOSED v0.34.50**: `STATIC_RW_LOCK_POOL` is `[QueuedRwLock; 4]`, so the lock the kernel deploys is the ticket-FIFO one the spec describes.  **The proof came first**: `SeLe4n/Kernel/Concurrency/Locks/QueuedRwLockRefinement.lean` establishes the refinement (`queuedSim`, whose ticket-protocol well-formedness `QueuedTicketWf` pins the ghost ledger to the machine words; `queuedBlock`'s state-indexed blocks — eleven at RR6, thirteen since WS-LC LC2 added the withdrawal shapes; `queuedTrace_preserves_queuedSim`; the capstones `queuedRwLock_refines_rwLockSpec` and `queuedRwLock_admits_in_spec_order`) *before* RR6.10 repointed the pool, so no shipped version carried an unrefined core lock.  `rw_lock.rs` is retained deliberately and its retention reasons are recorded in its own module docs — it is the Tier-5 oracle's second implementation, it owns the `WRITER_BIT` / `READER_MASK` layout `queued_rw_lock.rs` now imports rather than re-declares, and its own D-4 refinement was *completed* (`honestBlock`, `rust_rwLock_refines_lean_honest`) rather than deleted.  `build.rs` pins the pool's element type, so a revert to `[RwLock; …]` fails the build | `rust/sele4n-hal/src/lock_bridge.rs`, `rust/sele4n-hal/src/queued_rw_lock.rs`, `SeLe4n/Kernel/Concurrency/Locks/QueuedRwLockRefinement.lean` | RR6.4–RR6.11 |
| ~~The Tier-5 oracle models the lock instead of driving it, by its own docstring~~ — **CLOSED v0.34.50**: the oracle drives **both** real implementations — a `RwLock` and a `QueuedRwLock` — through every generated operation and checks three relations after each one: that the two implementations agree, that the queued lock's ticket interval matches the abstract waiter queue, and that the encoded state word is `encodeRwLock` of the abstract state.  Its rendered state is read back from the deployed lock's own word (`W=` / `R=`) rather than from the model it is compared against, which is what made the old harness a second model | `rust/sele4n-hal/src/bin/rw_lock_oracle.rs` | RR6.1–RR6.3 |
| ~~`loom` and `miri` gates the RwLock plan declares mandatory are unmet — neither tool reaches the deployed lock~~ — **CLOSED v0.34.50**: `loom` is a `cfg(loom)` target dependency with five models over the deployed `QueuedRwLock` (`scripts/test_loom_queued_rw_lock.sh`, run by the `test-loom-concurrency-model` CI job), and `miri` runs the lock's own suite under `-Zmiri-strict-provenance` (`scripts/test_miri_queued_rw_lock.sh`, wired into `test_nightly.sh` under `NIGHTLY_ENABLE_EXPERIMENTAL=1`).  Both gates were verified **decisive by a relation-breaking mutation** rather than by a deletion: removing `await_turn` from `acquire_read` — which leaves every token in place — fails two of the five loom models.  The stress thresholds the plan declares are met by named constants that scale under `cfg(miri)` rather than by a comment (`STRESS_ITER`, `FIFO_ACQUISITIONS`) | `rust/sele4n-hal/Cargo.toml`, `scripts/test_loom_queued_rw_lock.sh`, `scripts/test_miri_queued_rw_lock.sh`, `.github/workflows/lean_action_ci.yml` | RR6.20–RR6.22 |
| Fine-lock migration Tracks B and C: the capability-transfer footprint closure (**landed** — RR7.7 declared the transfer's destination and its CDT write at `v0.34.60`, RR7.8 resolved and proved it at `v0.34.61`, RR7.9 the four capability operations' at `v0.34.62`, and `UncoveredLockDomain.capTransferReceiverCnode` and `.cdtNodeAllocation` are deleted), the decoded-driven resolver (**landed** `v0.34.63`) and the IPC hot-path declarations (**landed** `v0.34.64` — eight of thirty-five arms declared with their coverage; the receive side's own CDT write closed in the same cut, which is what moved `maxLockSetSize` to 9), the **syscall seam's bracket** (**landed** `v0.34.65`, RR7.12 — `syscallDispatchCrossCoreEntry` resolves, acquires, re-resolves and refuses on change, with the undeclared arms bit-identical to before), the **export-commit census** (**landed** `v0.34.66`, RR7.13 — the state-committing `@[export]` set derived from the elaborated environment and reconciled against a registry both ways, so a new seam that commits without a bracket fails the build), the **scheduler domain's runtime and the three per-core scheduler entries' brackets** (**landed** `v0.34.89`, RR7.39 — `SystemState.schedulerLocks` gave `SchedLockId.runQueue` / `.replenishQueue` the state words they never had, `Concurrency.runBracketed` became the one revalidating bracket both domains run, and the timer tick's footprint was corrected from two run-queue locks to every core's after its target-aware wakes were found to escape it), and SM3.C.9's `withLockSet` wrapping of the remaining `@[export]` bodies — **seven seams commit and five bracket**; the two that do not are the fault-delivery entries, which have no `lockSetForSyscall` footprint because a fault is not a syscall — 7 of the plan's 12 PRs (the remaining 3 are Track D's, in section B below) | [`docs/planning/SMP_FINE_LOCK_MIGRATION_PLAN.md`](planning/SMP_FINE_LOCK_MIGRATION_PLAN.md) | RR7.7–RR7.13 and RR7.39–RR7.41; **two** lock domains remain registered (from seven): `syscallSeamSchedulerDomain` — RR7.39's residual, owner RR8, carried by **RR8.12** — and `taintTablePerKeyStore`, owner fine-lock Track D.  `dynamicPipChain` closed at `v0.34.90` (RR7.40: the PIP chain's footprint names each member's home-core run queue over the domain RR7.39 built, and the walk's writes are proved inside it) and `cspaceWalkInteriorCnodes` in the same cut (RR7.41: a read lock on every CNode a resolution passes through, with the conflict against a `cspaceDelete` on the path proved) |
| ~~Six production `native_decide` uses, against a release note claiming zero~~ — **CLOSED v0.34.47**: the theorem inventories store packed keys (`SeLe4n/PackedString.lean`) and every distinctness witness is the kernel's `decide` | `SeLe4n/Kernel/Concurrency/Locks/*Inventory.lean` and siblings | RR7.6 |
| ~~Cancellation/timeout error-frame staging, owed before the context-restore seam flips~~ — **CLOSED v0.34.67**: a thread taken out of a blocking IPC was handed nothing, so the SM10.1 context restore would have delivered its own argument spill back as a return value.  `timeoutThread` now stages `Architecture.timeoutFrame` (`.ipcTimeout`) and `cancelIpcBlocking`'s four blocked arms stage `Architecture.cancelledIpcFrame` (`.ipcCancelled`, a new `KernelError` at discriminant 57) — different errors because a caller may reissue a timed-out request but a cancelled one may name an endpoint that is gone.  The `.ready` arm and the *resume* spelling `restoreToReady` stage nothing, both pinned as negatives.  Closes the obligation [`SYSCALL_RETURN_ABI_PLAN.md`](planning/SYSCALL_RETURN_ABI_PLAN.md) §9 registered against SM10.1 | `SeLe4n/Kernel/IPC/Operations/Timeout.lean`, `SeLe4n/Kernel/Lifecycle/Suspend.lean`, `SeLe4n/Kernel/Architecture/SyscallReturn.lean` | RR7.14 |
| **The scrub's cleaned extent is the model's abstract convention, not the allocator's** (SM7.D item 5; AN4-G.3 / LIF-M03).  `scrubExtent` — which `scrubObjectMemory` zeroes and `retypeIcacheOp` cleans — is `(ObjId × objectTypeAllocSize, objectTypeAllocSize)`, while the real child extent is the untyped allocator's `regionBase + offset` (recorded as `UntypedChild.offset` / `.size`).  On hardware neither the zeroing stores nor the `DC CVAU` lands on the object's actual backing memory, so a scrub that misses real memory hands the previous owner's bytes to the new one — a **data-disclosure** gap once bootable, not merely stale instruction lines.  The fix belongs to the **scrub**, not the cache seam: it needs a reverse child→untyped resolver that does not exist, a fallback for objects with no parent record (boot-built objects, in-place re-types), and a change to `scrubObjectMemory` whose projection lemmas quantify over the abstract range.  Correcting the cache operand alone would be strictly *worse* — it would clean an extent the scrub does not zero.  Both consumers read `scrubExtent`, so the bridge rewrites one function and the operand follows (`retypeIcacheOp_cleans_scrub_extent` fails if they drift).  **Re-targeted at v0.34.71 (WS-RR RR7.20)** from `SM10.1`, which is not an owner — SM10.1's content became WS-BP, and this is kernel-model work no boot row covers.  **Owner: the untyped/retype surface. Closure target: post-v1.0.0.**  v1.0.0 must state the scrub's extent honestly rather than claim the allocator's | `SeLe4n/Kernel/Lifecycle/Operations/ScrubAndUntyped.lean`, [`docs/planning/SMP_TLB_SHOOTDOWN_PLAN.md`](planning/SMP_TLB_SHOOTDOWN_PLAN.md) §SM7.D | RR7.20; post-v1.0.0 |
| ~~WS-RA's application-IPC-label follow-on, registered only inside a review narrative~~ — **REGISTERED v0.34.68 (WS-RR RR7.17)**, and the gap itself is now the debt: seL4's `seL4_MessageInfo` label is the *sender's*, passed to the receiver untouched, and userspace protocol design rests on it (a method number, a request discriminant, a version tag).  This kernel does not pass it through — `IpcMessage.label` is set by **kernel-originated** messages only (RR4's fault deliveries carry their `seL4_Fault_tag` there) and a user send leaves it at `0`, so an application that wants a method number spends a message register on it.  **The naive fix is unsound**: carrying the sender's label verbatim would let any thread holding a send capability to a fault endpoint mint a message whose label *is* a `seL4_Fault_tag`, and the handler cannot distinguish that from a kernel-delivered fault — it would decode a forged `vmFault` and reply, restarting a thread that never faulted.  The v3 range separation protects the kernel-*status* half only; the four fault tags live below the base precisely so a handler's receive can read them.  Two candidate designs are stated in the plan's §9 (reserve a sender label range and clamp; or carry provenance out of band), neither chosen because both need an authority story WS-RA does not own.  **Closure target: WS-CB**, whose hierarchical-CBS work already reopens the message path for its admission protocol — deliberately not WS-BP, since the boot path needs no label and first boot must not wait on a userspace-protocol decision | [`docs/planning/SYSCALL_RETURN_ABI_PLAN.md`](planning/SYSCALL_RETURN_ABI_PLAN.md) §9, [`docs/planning/HIERARCHICAL_CBS_PLAN.md`](planning/HIERARCHICAL_CBS_PLAN.md) | RR7.17; closes in WS-CB |
| ~~SM7's ASID completeness gap — `asidAllocateWithShootdown` is complete and proven with no callers, and the ASIDControl/ASIDPool object family does not exist.  Registered against SM8; **SM8 closed without absorbing it**~~ — **RE-TARGETED v0.34.71 (WS-RR RR7.20)** to **WS-AP**, post-v1.0.0 (section below).  Two SM7 debts named SM8: this one, and SM7.F.4 (b)(iv), the initiator-atomic `requiresFlush` ASID-allocate seam.  They are one root — `asidAllocateWithShootdown` has no user-reachable caller while the ASID capability family does not exist — so they now have one owner instead of a closed phase's name | `SeLe4n/Kernel/Architecture/TlbShootdownProtocol.lean`, [`docs/planning/SMP_TLB_SHOOTDOWN_PLAN.md`](planning/SMP_TLB_SHOOTDOWN_PLAN.md) §SM7.F.4 | RR7.20; **WS-AP**, post-v1.0.0 |
| ~~SM7.D's four SM10.1-owned deferred cache-maintenance items appear in no SM10 sub-task~~ — **CLOSED v0.34.71 (WS-RR RR7.20)**: two of the four had already closed (item 1 at `v0.34.57`, item 3 at `v0.33.37`), and the remaining three — the finding counted four against the tree as it then stood — each have a named owner instead of a phase that no longer exists.  **Item 4** (the `.bootImageLoad` clean-to-PoU, never emitted) is scheduled as [`SMP_BOOT_PATH_PLAN.md`](planning/SMP_BOOT_PATH_PLAN.md) **`BP4.5`** with the acceptance criterion SM7 already stated.  **Item 2** (the post-state is published before the maintenance is emitted) is not boot-path work: its *design* belongs to the fine-lock plan's **Track D**, which is the row that changes the commit model, and its *validation* to **WS-BP BP8** — see its own row below.  **Item 5** (`scrubExtent` is the model's abstract convention, not the allocator's) is AN4-G.3 / LIF-M03 and needs kernel-model work no boot row covers — see its own row below | [`docs/planning/SMP_TLB_SHOOTDOWN_PLAN.md`](planning/SMP_TLB_SHOOTDOWN_PLAN.md) §SM7.D | RR7.20 |
| **The cache/TLB maintenance is emitted after the post-state is published** (SM7.D item 2).  `modifyGetKernelState` installs the committed state and clears the ledger atomically; `completeIcacheMaintenance` and `completeShootdownRounds` both run *outside* that step, so another core can observe a retyped frame, map it and execute from it while stale instruction lines are still resident.  The TLB side is saved by the blocking `SHOOTDOWN_ACK` handshake; `IC IALLUIS` is fire-and-forget, so there is nothing to wait on.  "Emit before publishing" is unavailable to a pure-transition kernel — the operand is only known *after* the transition computes it, which is why the ledger exists — leaving "hold serialization across the barrier sequence", which changes the syscall bracket's locking discipline and interacts with the SM3 hierarchy and the SM7.B round lock.  **Not a live hole**: the model applies the invalidation to `perCoreICache` atomically inside the transition, so the committed state is coherent; the gap is the model-versus-hardware refinement gap.  **Owner: fine-lock Track D** for the design (it is the row that changes the commit model), **WS-BP BP8** for the validation | [`docs/planning/SMP_FINE_LOCK_MIGRATION_PLAN.md`](planning/SMP_FINE_LOCK_MIGRATION_PLAN.md) Track D, [`docs/planning/SMP_TLB_SHOOTDOWN_PLAN.md`](planning/SMP_TLB_SHOOTDOWN_PLAN.md) §SM7.D | RR7.20 |
| SM6's tracked debt carries no explicit closure target, and its `schedContextConfigure` entry went stale after the debt closed | [`docs/planning/SMP_CROSS_CORE_IPC_PLAN.md`](planning/SMP_CROSS_CORE_IPC_PLAN.md) | RR7.22 |
| ~~**`cancelIpcBlockingOnCore` does not carry `ipcInvariantFull`**~~ — **CLOSED `v0.35.85`** (WS-RR RR8.10: `cancelIpcBlockingOnCore_preserves_ipcInvariantFull`, over the arm-complete `cancelIpcBlocking_preserves_ipcInvariantFull`, in the production `SeLe4n/Kernel/IPC/Invariant/CancellationBundle.lean`).  The three scheduler steps the composite adds over the teardown contribute **no** hypothesis: only `passiveServerIdle` of the twenty conjuncts reads the scheduler, so `ipcInvariantFull_of_descheduleFrame` transports the rest, the migration and the wake frame it unconditionally (the wake because an *insert* cannot break a conjunct whose antecedent is "not queued"), and the deschedule's one obligation is discharged from `cancelIpcBlocking_victim_ready` rather than assumed.  What follows is the row as it stood. — the second of RR7.22 finding 4's two consumers; the first (`notificationSignalBoundOnCore`) closed at `v0.34.92`.  The reason the two differ is structural: the bound delivery uses `endpointQueueRemoveDual`, whose twenty-conjunct carriage RR7.22 built, while cancellation's blocked-on-endpoint arm uses **`removeFromAllEndpointQueues`** — a `RobinHood.fold` over the entire object store preceded by `spliceOutMidQueueNode`.  **A specification gap surfaced while building the carriage (v0.34.94)**: `ipcInvariantFull` constrains a queue only at its *boundaries* and carries **no connectivity** — nothing says the `queueNext` chain from a head reaches the tail — while the sweep advances a head to the removed thread's `queueNext` unconditionally.  On a bundle-satisfying state where the removed thread heads a queue with `queueNext = none` and the tail is a *different* thread, the sweep yields `head = none, tail = some x`, violating `intrusiveQueueWellFormed`'s P1.  The witness satisfies all twenty conjuncts (a `.ready` thread whose `queueNext` names the tail supplies the tail's membership, and `queueNextBlockingMatch`'s catch-all admits a `.ready` source); it is very likely unreachable — no transition creates it — but **unreachability is proved nowhere**, and the bundle is what a preservation proof may assume.  Severity **Low**: not exploitable (nothing boots, the global entry lock serialises every commit); the consequence if reachable is a queue whose members are unreachable from an empty head, so blocked threads are never woken — liveness, not memory safety.  **Remediated by option (a)**, the route RR7.22 itself established for a fact the bundle does not entail: the missing fact is *stated* and a caller discharges it from a reachability witness.  Building the remaining conjuncts surfaced **two more instances of the same gap**, both consequences of the same missing connectivity: `endpointQueueTailBlockedConsistent` needs the swept thread's *predecessor* to be blocked on the endpoint it is promoted to tail of, and nothing propagates blockedness backwards along `queueNext`; and `ipcStateQueueMembershipConsistent`'s witness clause accepts *any* thread whose `queueNext` names the blocked thread — in the queue or not — so a `.ready` thread can be a blocked successor's sole witness and sweeping it leaves that successor unwitnessed.  All three are stated as the clauses of `sweptThreadQueueCoherent` (`boundary`, `predecessorBlocked`, `successorAnchored`).  Landed at `v0.34.94`: the dual-queue third (`sweptQueue_wellFormed`, `sweptAndRestored_tcbQueueLinkIntegrity` — which holds over the *composite* and not over the sweep alone, the field clear being what repairs the swept thread's own dangling links — `sweptAndRestored_tcbQueueChainAcyclic`, `sweptAndRestored_dualQueueSystemInvariant`).  Landed at `v0.34.95`: the sharp pointwise reading (`sweptAndRestored_tcb_value`, a closed record rather than the weak `tcbQueueLinkRewrite`), the remaining **nineteen** conjuncts, the keystone `sweptAndRestored_preserves_ipcInvariantFull`, and the live arm it covers — `cancelIpcBlocking_endpointArm_preserves_ipcInvariantFull`, reached by a compiler-checked equation (`cancelIpcBlocking_endpoint_arm_eq`) rather than a restatement.  Landed at `v0.34.96`: the **notification** arm — its own purge description (`notificationPurgeBody` with a `rfl` pin), its own coherence hypothesis `sweptThreadOffQueueChains` (a fourth instance of the same gap: the restore clears the swept thread's links and there is no splice to repair its neighbours, while `ipcStateQueueMembershipConsistent`'s `.blockedOnNotification` arm is `True`), all twenty conjuncts, the keystone `purgedAndRestored_preserves_ipcInvariantFull`, and `cancelIpcBlocking_notificationArm_preserves_ipcInvariantFull`.  Two facts that arm does **not** take as hypotheses, because they are derivable: the swept thread bounds no endpoint queue (both boundary conjuncts demand a blocking state it does not have) and it holds no Reply object.  Landed at `v0.35.82` (RR8.7's second cut): the **reply** arm, which needed a different shape from its two siblings because it is a *four-step composition* rather than one sweep — `cancelIpcBlocking_reply_arm_eq` pins it to reclaim, splice, restore, teardown by `rfl`, and no two of those carry the bundle for the same reason (the reclaim composes the holder abort with the pop's carriage at the relaxed donation-owner bundle; the splice is three `.reply` stack-link stores; and `v0.35.80` established that the restore and the teardown carry it only **as a pair**).  Its extra hypotheses are `donationChainWellFormed`, `cancelDonationStackValid`, `abortHolderQueueCoherent` — the endpoint arm's three clauses again but for the *holder* the reclaim aborts, gated on the resolver since a caller cannot name that thread — and **both directions** of one local coherence fact (`replyFrameHeadHolderDonation` at the victim's reply object, and `donatedContextIsOwnerFrameHead`), neither of which entails the other and neither of which `ipcInvariantFull` entails.  Two things that arm does **not** take: the abort's "not blocked on reply" premise, which is *false* of a general holder and is therefore derived inside each aborting branch rather than hypothesised (a Tier 3 negative refuses the hypothesis form); and a caller-supplied holder, since the reclaim resolves its own. **Still owed**: only the composite over all five arms lifted to `cancelIpcBlockingOnCore`.  Live consequence until then, stated at the confinement rather than flatly (corrected by RR8.1, which measured it): the `.tcbSuspend` cross-core arm **does** carry the full bundle — `suspendThreadOnCore_preserves_ipcInvariantFull`, RR3's production per-arm layer — but only under `threadIpcFieldsQuiescent`, whose `ready` clause admits exactly the victims these arms are *not* about.  A victim blocked in an IPC queue falsifies that pack, and across such a suspend a caller has `ipcInvariant` and `objects.invExt` only.  The earlier wording here said the arm carries those two *only*, full stop, which understates the tree and reads as though RR3's arm theorem did not exist.  ~~**Closure target: RR8.7**~~ **— the reply arm closed at `v0.35.82`; the composite is RR8.10** (before RR8 closes) | `SeLe4n/Kernel/Lifecycle/Invariant/CancellationQueueShape.lean`, `SeLe4n/Kernel/Lifecycle/Invariant/CancellationNotificationShape.lean`, `SeLe4n/Kernel/Lifecycle/Invariant/CancellationReplyShape.lean`, `SeLe4n/Kernel/IPC/CrossCore/Cancellation.lean`, `SeLe4n/Kernel/Lifecycle/Operations/Cleanup.lean` | RR7.22; RR8 |
| ~~The Tier-0 grep gate banning non-IS TLBI, which the Rust HAL plan §4.4 claims exists, does not exist~~ — **CLOSED v0.34.41** as `scripts/check_tlbi_broadcast_discipline.py`, which also confines the `tlbi` mnemonic to `tlb.rs` and demotes the four local wrappers to `pub(crate)`, making §4.4's "private helpers" true structurally | `scripts/test_tier0_hygiene.sh` | RR1.9 |
| ~~No aarch64 target is compiled anywhere: 67 cfg-gated blocks, 57 `asm!` sites and all three `.S` files have zero compile coverage~~ — **CLOSED v0.34.41**; the `aarch64 Cross Build` CI job builds both profiles, verifies the `.S` sources assembled and lints the cross target.  (The 60 this row carried was a transcription of the register's 59, which counted two docstring mentions of the token; 57 is the measured figure) | `rust/`, `.github/workflows/` | RR1.1–RR1.8 |
| `numCores` is a literal `4` rather than `PlatformBinding.coreCount`, so the Sim binding's `coreCount := 1` can never shape kernel state | `SeLe4n/Kernel/Concurrency/Types.lean` | RR7.30 |
| **The splice has no `ipcInvariantFull` result** — the residue of WS-RR RR8.7's vacuity finding (`v0.35.80`).  `removeCallerReplyFrame_preserves_ipcInvariantFull` was deleted rather than repaired, because its premises were **contradictory**: it asked for the full bundle of the state it runs on *together with* that state's answered caller not being `.blockedOnReply`, and `replyCallerLinkage`'s second direction refutes that pairing outright (`replyCallerLinkage_refutes_woken_linked_caller`).  It therefore held on no state and asserted nothing, so nothing is *lost* by the deletion — but `removeCallerReplyFrame` (the splice then the consume) now carries no bundle statement at all, and it should carry the **relaxed** one, `ipcInvariantFullExceptReplyLinkage` at the answered caller, which the transition's own donation pop would then close exactly as it closes the chain transient.  What that needs is a relaxed twin of `storeObject_reply_stackLinks_preserves_ipcInvariantFull`: the splice's three stores write Reply stack links, and the existing lemma concludes the *full* bundle, so it cannot be composed under a relaxed reciprocity clause.  Severity **none** as a live defect — the reply spine's bundle coverage runs through `endpointReplyCrossCoreDispatch`, which is proved, and the cancellation arm's runs through `restoredAndConsumed`, which this cut proves; the gap is that the *removal step* alone is unstated, so a future caller must compose the halves rather than cite one theorem.  **Registered rather than absorbed**: the relaxed store twin is its own argument over the twenty conjuncts and belongs in the cut that needs it, not bolted onto the cut that found the vacuity.  A tombstone at the deletion site names both the replacement and this row | `SeLe4n/Kernel/IPC/Invariant/Structural/DualQueueMembership.lean`, [`docs/planning/REPLY_FRAME_REMOVAL_PLAN.md`](planning/REPLY_FRAME_REMOVAL_PLAN.md) RM1.5 | RR8.10 |

### B — owned by SM10

These cannot close before SM10 opens, because each needs the image or the
runtime seam SM10.1 produces.  WS-RR's obligation is that they are visible
here rather than only inside a phase plan.

| Debt | Where it lives | Closure target |
|------|----------------|----------------|
| The boot path does not exist: no `[[bin]]`, no aarch64 Lean object code, no `libsele4n.a`, no bare-metal runtime hosting, no `@[export] lean_kernel_main` | `rust/`, `lakefile.toml`, `SeLe4n/Platform/FFI.lean` | **Scheduled at v0.34.59** as [`SMP_BOOT_PATH_PLAN.md`](planning/SMP_BOOT_PATH_PLAN.md) `BP1` (object code), `BP2` (runtime hosting), `BP4.1` (the entry) and `BP5` (the image, packaged as SM10.1.1) |
| `lean_kernel_main`'s `initialiseKernelState` install would run outside the kernel-entry lock and after the secondaries are released — the lost-commit shape | `rust/sele4n-hal/src/kernel_entry.rs`, `SeLe4n/Platform/FFI.lean` | **Scheduled at v0.34.59** as [`SMP_BOOT_PATH_PLAN.md`](planning/SMP_BOOT_PATH_PLAN.md) `BP4.2`, which takes the ordering option: the install precedes the release of any secondary, so the race is closed by construction rather than by a lock |
| WS-RA frame delivery: the staged return frame is not delivered until the context restore goes live | `SeLe4n/Kernel/Concurrency/ContextRestoreSeam.lean` | **Scheduled at v0.34.59** as [`SMP_BOOT_PATH_PLAN.md`](planning/SMP_BOOT_PATH_PLAN.md) `BP7.1`–`BP7.4` (the three prerequisites the flag's docstring names), `BP7.5` (the cancellation/timeout delivery) and `BP7.6` (the flip they gate) |
| The DTB pointer never reaches Lean: `bootAndInitialiseRPi5FromDtbOrHalt` takes the blob as a `ByteArray`, and turning `rust_boot_main`'s `dtb_ptr` into one is a Lean-runtime allocation the bare-metal port owns.  Until then the board-versus-binding check WS-RR RR7.27 built has no hardware caller, and `SeLe4n/Testing/BootEntryContract.lean`'s `approvedBootCall` stays on the config-taking entry (moving it to the DTB wrapper is the one-line change that file anticipates) | `SeLe4n/Platform/FFI.lean`, `SeLe4n/Testing/BootEntryContract.lean` | **Scheduled at v0.34.59** as [`SMP_BOOT_PATH_PLAN.md`](planning/SMP_BOOT_PATH_PLAN.md) `BP4.3` (the read, which is why it depends on `BP2`) and `BP4.4` (re-pointing the contract) |
| Fine-lock migration **Track D** — commit partitioning; the one part of the fine-lock work WS-RR cannot land in full | [`docs/planning/SMP_FINE_LOCK_MIGRATION_PLAN.md`](planning/SMP_FINE_LOCK_MIGRATION_PLAN.md) | **Restructured at v0.34.133**, and re-pointed off SM10.1.  Its two Lean rows (PR 10 `transition_footprint_local`, PR 11 the representation obligation derived from the write-set lists) are gated on **nothing** and are owed before the runtime that relies on them; only PRs 12–13 are seam-gated, to **BP6** with validation at **BP8**.  Registered at v0.34.50 as a named dependency in [`SMP_RELEASE_CLOSURE_PLAN.md`](planning/SMP_RELEASE_CLOSURE_PLAN.md) §2 by RR6.27, which now states **two** inherited obligations rather than three: the per-key realisation of the *set* of structures the write-set lists name (not `SystemState.objects` alone — `RHTable.insert` rebuilds the table at three-quarters load even at a resident key, and `lifecycle.capabilityRefs` is rebuilt on every store), and the measured WCRT claim while SM5.I's global entry lock stands.  The third, the `SM3.C.9.b` timer-tick bracket, is **closed** — RR7.39 landed it at `v0.34.89` |
| `UncoveredLockDomain.taintTablePerKeyStore` names SM10.1 as owner; the SM10 plan carries no sub-task for it | `SeLe4n/Kernel/InformationFlow/FineLockFlow.lean` | SM10.1 — RR0.11 routes the plan-side row |
| **Eight** WS-SM phases register zero theorems in `smpInventoriedTheoremCount` — six (SM1, SM6..SM10) have no machine-checked inventory at all, and **SM0 and SM4 carry assumption ledgers** (`smpLatentInventory`, `smpRetiredInventory`) which `smpPhaseTheoremCount` excludes by design, leaving their own theorem catalogues unmeasured.  Only SM2, SM3 and SM5 contribute to the 909 | `SeLe4n/Kernel/Concurrency/PhaseTheoremManifest.lean` | SM10.3.13 |
| Hardware-validation scripts SM10.3.14–SM10.3.20 and the SM10.1.1 image build, each a runnable procedure `docs/HARDWARE_TESTING.md` documents with no script | `scripts/` | SM10.3, SM10.1.1 |
| Tier-4 acceptance gates have never executed: they need the bootable image, so `SMP_RELEASE_CLOSURE_PLAN.md` §2's "acceptance gates for SM0..SM9 green" is **unmet**, not merely unverified | `scripts/test_tier4_smp_bootcheck.sh` | SM10.1, immediately after D1 |
| SM1.H's QEMU bring-up gate has never executed a line of SMP HAL code: `test_qemu_smp_bringup.sh` SKIPs on every run because the workspace has no `[[bin]]` kernel target to hand it.  WS-RR RR7.16 **unchecked** the two SM1.H acceptance boxes that claimed the four-core boot and the four banners rather than restating them as "script authored", which would turn a hardware-behaviour criterion into an artifact-existence one | `scripts/test_qemu_smp_bringup.sh`, [`docs/planning/SMP_RUST_HAL_PLAN.md`](planning/SMP_RUST_HAL_PLAN.md) §8 | SM10.1.1 — the image the script needs; the gate then runs unchanged |
| SM10.6.2 archives the WS-RC artefacts; its still-open items (R7, R14) migrate here rather than into the archive | `docs/audits/` | SM10.6.2 |
| **SM10's sub-phases were lettered, and the letters were not execution order** — SM10.3.7 and SM10.3.10 consume the bootable image, and the version bump and tag ran before it existed | Closed at **v0.34.36**: re-sequenced into numbered sub-phases SM10.1..SM10.6 with the image build first and the tag last, and the old boot-path phase split from the validation that consumes it.  Old letters are preserved in historical prose and mapped in the plan's §3 table | **CLOSED** — no longer RR7.5's; found by review on PR #882 (v0.34.27), fixed at v0.34.36 |

### C — deferrals with no pre-v1.0.0 owner

Each row states why it may wait.  "Registered" is not "ignored": a row here
constrains what v1.0.0 may claim, and RR8.15's hand-off check reads this table.

| Debt | Why it may wait | Owner / closure target |
|------|-----------------|------------------------|
| ~~**The frozen removal carries none of the live removal's guard, so the differential mirror succeeds where the kernel refuses**~~ — **CLOSED `v0.35.59`** (a standalone cut — see the owner cell); found at `v0.35.58` by running WS-RR RR8.4's own sweep for who computes a queue boundary, which turned up `frozenQueueRemove` as the **fifth** asker after the plan row had said "two".  `endpointQueueRemoveDual` refuses three things: a `queuePPrev` that does not pair with `queuePrev` (RR8.3's `dualQueueRemovalGuard` pairing factor), a back-pointer that disagrees with where the thread sits in the queue (its position factor, which predates RR8.3 as the unnamed `pprevConsistent`), and — since RR8.4 — a queue `tail` field that disagrees with the removed thread's `queueNext`.  `frozenQueueRemove` refuses only `tcb.queuePPrev.isNone`, so on a state violating any of the three it **succeeds** where the kernel returns `.illegalState`.  That is the direction that matters on a differential surface: `frozenRunAgrees` compares outcomes, and a mirror that is more permissive than its subject reports agreement on states the kernel never reaches and silence on the states that distinguish them.  Two things this row is **not**.  It is not a kernel defect: `SeLe4n/Kernel/FrozenOps/` is experimental, reached by neither library root, built only by `tests.FrozenOpsSuite`, so the weakening is of a *check*, not of the kernel.  And it was not RR8.4's to close: at registration the guard lived in `SeLe4n/Kernel/IPC/DualQueue/Core.lean` while `FrozenOps/Core.lean` imports only `Model.FrozenState` / `Model.FreezeProofs` — deliberately, the mirror being a thin layer over the model — so carrying it meant relocating `dualQueueRemovalGuard` (and `queueTailPairAgrees`, `queuePPrevHeadPositionAgrees`, `queueLinkPairAgrees`) into the model beside the fields they read, then re-pointing RR8.3's discharges and Tier 3 anchors, then paying whatever fixture cost the newly-refused states carry.  The relocation is the right shape — the predicate is over `(IntrusiveQueue, ThreadId, TCB, QueuePPrev)`, all model types, with no kernel dependency, so the model is its proper home — and it is a cut of its own rather than a widening of this one.  **What RR8.4 did do** is unify the *boundary* the two compute (`queueRemoveBoundary`, which the model does export), and it establishes that the mirror was **right** on the tail question where the live dual was wrong: `frozenQueueRemove` already asked `q.tail == some tid` rather than inferring tail-ness from `queueNext`, and nothing in the tree compared the two on the state where they part.  It waited one cut, and closing it corrected this row's own count.  **The guard was one of FOUR refusals, not one of three**: beside it `endpointQueueRemoveDual` carries an unnamed `q.head.isNone \|\| q.tail.isNone`, an unnamed `prevTcb.queueNext ≠ some tid` in its predecessor patch, and an arm answering `.endpointQueueEmpty` where the mirror answered `.illegalState` — and `frozenRunAgrees` compares codes, so that last one was visible to the differential all along and invisible only because nothing drove both sides to it.  Relocating the guard and having the mirror call it — which is what this row asked for, and what the first attempt shipped — left a mirror that **still** succeeded where the kernel refuses: an empty queue with `pprev = .tcbNext p` passes the guard outright (`q.head ≠ some tid` holds vacuously for `none`, the link pair agrees, and the tail pair agrees because both its sides are false), so the guard-carrying mirror would have unlinked a node from a queue that has none.  **A named condition beside unnamed ones is a subset of the refusal set, and reaching it through a shared definition reads like agreement** — the one that had a name had it because a *proof* needed it (`dualQueueRemovalGuardHolds`), not because it is the whole precondition.  So the closure names the **question**: `dualQueueRemovalEnabled` is the whole store-free precondition and both removals read it; `queuePredecessorNamesSuccessor` is the factor that needs a store lookup, named and read by both with each resolving its own `prevTcb`; and the no-back-pointer arm answers `.endpointQueueEmpty` on both sides.  **What the closure cost, measured rather than estimated**: 233 lines relocated with no proof repaired, eleven proofs across five modules needing `cases pprev` moved ahead of their `split` (unfolding the enabling condition exposes the guard's own `match pprev` inside the `if`), and the **fixture cost this row predicted was zero** — the newly-refused states are states the kernel already refused, so no scenario in the tree was on one, `frozenRunAgrees` is unmoved and the golden trace is byte-identical.  Its witnesses are `FO-045` (the tail factor, with the retired `isNone`-only reading computed beside the live guard so the refusal is attributable) and `FO-046` (one half per remaining refusal, each on a state that passes everything the previous half checks and is refused by exactly one more thing), all four mutation-tested by reverting the refusal each is about.  What the row got right is the reading of the direction: a `frozenRunAgrees` PASS was evidence about states both sides accept and silence on the ones the kernel refuses, and that silence is now fifteen assertions. | **CLOSED `v0.35.59`**, by a standalone cut and deliberately **not** as a new RR8 sub-task.  Numbering is execution order here, so a cut landing before RR8.5 would have to *be* RR8.5 and shift RR8.5..RR8.16 up by one — and six of those IDs (RR8.5, RR8.7, RR8.12, RR8.13, RR8.15, RR8.16) are already cited in `CHANGELOG.md`, which the project's own rule treats as freezing them: a renumber would leave those entries resolving to different work, which is the one drift `check_workstream_plan.py` says it cannot see.  So the plan's RR8 preamble records the cut instead, and RR8.15's hand-off check reads this row struck. | closed `v0.35.59` |
| **`check_claim_evidence_citations.py` cannot see a Lean `def` name, so a deleted `def` cited as evidence passes** — found at `v0.35.46` while sweeping WS-HP HP7's deletions, and *measured* rather than reasoned.  Its `CITATION` pattern is `` `<ident>_<ident>` `` — at least one underscore — with a stated reason (single words like `cell` collide with English and would make the gate noise).  But Lean's convention is snake_case for `theorem`s and **lowerCamelCase for `def`s**, so every definition name in the tree is outside the gate's domain while every theorem name is inside it: the recognised set is not the derived one, and the direction is fail-**open** — a row naming a deleted `def` as evidence reports PASS.  Not hypothetical: a camelCase-aware scan of the index found `donationHeadPush` cited as evidence on the transitive-donation row and declared nowhere (the push is `storeDonationFramePush`), invisible to the gate since WS-OD OD4.  **Fixed in that cut**; what is registered is the gate.  The measurement also says why the fix needs its own cut rather than a widened pattern here: of 14 camelCase citations the index carries that resolve to nothing, **13 are legitimate** — seven retired names that the *claim* prose correctly says are deleted, two upstream C functions (`doReplyTransfer`, `receiveIPC`), two hypothesis binders (`hBandProgress`, `hIdle`) and two record fields (`boundThread`, `scReply`) — so widening the pattern and exempting thirteen names by hand would be the enumeration-standing-in-for-a-derivation shape this project retires.  The derivation is a **column split**: a name must resolve in the *Artefact* column, where the row says what discharges the claim, and claim prose may name a retired symbol freely.  That is well defined and not free — the file holds **two** tables with different column counts (4-column main, 3-column *Not claimed*) and one row with an embedded pipe — so it needs its own mutation-tested self-test cases, in both directions, with the gate failing closed on a row it cannot split.  Until it lands, a cut that deletes a `def` must sweep the index by hand.  **And the gate's FILE domain is the same defect one axis over, measured at `v0.35.50`**: it scans `docs/CLAIM_EVIDENCE_INDEX.md` and nothing else, so a dead *theorem* name -- snake_case, squarely inside the CITATION pattern -- passes everywhere else, and this project's own deletion rule names `CLAUDE.md` / `AGENTS.md` as part of a deletion's blast radius.  A sweep of the `lockSet_*` citations in `CLAUDE.md` found **3 of 31 dead at 6 sites each in both files** (`lockSet_replyRecv_size_le_thirteen_of_owner_eq_target`, retired at `v0.35.4`; `lockSet_endpointReplyRecvOnCore_size_le_thirteen` and `lockSet_cancelIpcBlockingOnCore_size_le_ten`, both re-based since), every one of them a live claim citing a theorem as evidence rather than a retirement notice.  **Fixed in that cut** by recording each lifecycle; what is registered is that the file domain, like the column domain, is a recognised set rather than a derived one | **Low severity and no soundness content**: the gate certifies *documentation*, so its blind spot produces a stale citation rather than an unsound proof, and the tree's other citation gate (Tier 3 anchors) reads code directly.  It may wait because the remedy is a scanner-domain change whose whole value is its witnesses, and landing it inside a proof cut would give it none | **unowned** — a Tier 0 gate cut; **before v1.0.0**, since RR8.15's hand-off check reads this table |
| **WS-CB** — scheduling contexts are flat: a reservation cannot contain reservations, so a component's bandwidth is the sum of its threads' and nothing bounds a group jointly; the root scheduler is fixed-priority, so the CBS guarantee (an admitted server receives its budget every window) is not a statement the model can make; the live CBS engine's exhaustion arms schedule a refill of at most one tick (`consumedAmount := budgetRemaining` under `budgetRemaining ≤ 1`), so a bound thread receives about one tick per period after its first window and budget consumed without exhaustion is never returned; admission sums every SchedContext in the store against one 1000 ‰ ceiling rather than per core; and `schedContextConfigure` applies `priority`, `domain` and a caller-supplied `deadline` to the bound SchedContext and TCB under the SchedContext write right alone, with no caller-MCP check and no domain authority | The hierarchy and the EDF-first root are feature work, not soundness defects of the flat model — every flat theorem stands.  Two rows here are **not** deferrals: the configure authority gap is CB0.3, the first implementation cut, recommended as the next PR regardless of when the rest of the workstream opens, and the one-tick refill is the engine switch CB1.6 (with its inert preparation CB1.2), recommended as the cuts after it; the deadline argument's retirement is CB1.6 too, and per-core admission — with every reservation move re-admitted on its destination core — is CB5.2 | [`docs/planning/HIERARCHICAL_CBS_PLAN.md`](planning/HIERARCHICAL_CBS_PLAN.md); post-v1.0.0 unless the maintainer opens it beside RR6–RR8; CB0.3 next, CB1.2 and CB1.6 after it |
| **WS-SL** — the scheduler liveness trace step relation (`stepPrecondition` / `stepPost` / `ValidTrace`) is `bootCoreId`-pinned, so no `ValidTrace` exhibits a step on a secondary core; and `hBandProgress` is an externalized deployment hypothesis whose FIFO/bucket-rotation composition was never built | Model completeness, not soundness: the per-core liveness *predicates* were lifted by SM5.J, and the capstones state their hypothesis explicitly.  v1.0.0 must therefore not claim unconditional SMP starvation-freedom | **WS-SL**, post-v1.0.0 (section below) |
| **WS-XV** — every mechanical cross-implementation gate in this tree is **nominal**: `check_lock_ffi_symmetry.sh` reconciles symbols and their types, `check_kernel_entry_exports.py` an `extern` set against object code, `build.rs` the readiness seams against the Lean `@[export]` inventory, `check_physical_address_width.sh` a constant against `Board.lean`, and `ExportCommitDisciplineCensus` a derived set against a registry.  **None is behavioural** — nothing drives one input through both implementations of a question and requires the same answer.  The surface named "conformance" (`rust/sele4n-abi/tests/conformance.rs`, RUST-XVAL-001..019, whose header says it "validates that Rust encoding matches the Lean decode layer") does it with *hand-transcribed literals* and a docstring citing the Lean file; the shifts it encodes live in `SeLe4n/Model/Object/Types.lean`, so a Lean-side layout change leaves all 112 conformance tests green.  The evidence that this is the tree's recurring defect class rather than a hypothesis: **seven of the twenty findings** across PR #892's five review rounds were two implementations of one question that had drifted — the chain extension against the revalidating bracket, board validation against board installation, the Rust RAM fold against the Lean coverage walk, the Rust token walk against the Lean tree walk, the Rust `status` filter against the Lean selector that did not get it, the object lock domain's sorted acquisition against the scheduler domain's verbatim one, and `schedContextUnbind`'s already-fixed re-queue against the cancellation reclaim's missing one — **plus one the branch found itself**, the two endpoint-queue removals that disagreed about `queuePPrev` (row A.5 above).  Each was fixed at its own site and the class stayed open, because the remedy `CLAUDE.md` states — *when a fix names a relation, grep for every other place that asks it* — is manual and reactive, and is skipped exactly when its author is most confident.  The work is `WS-XV` below.  **The audit was then run by hand against the device-tree pair** (`v0.34.115`) rather than left to a sixth review round, and found **four more** live divergences: only the first `/memory` node was read where the Rust store folds every one (so a board reporting its low aperture and high bank as two nodes bound the variant its first node covers); the root's declared `#address-cells` / `#size-cells` were ignored in favour of a fixed 16-byte stride, with the cell-aware extractor sitting unwired in the same file; a partial `reg` pair was truncated where Rust refuses the query; and `#size-cells` defaulted to 2 against both the specification and `FDT_DEFAULT_SIZE_CELLS`.  All four are fixed, and the yield is the argument for the mechanism: a person reading two parsers side by side found in one sitting what five review rounds had not.  **Round 6 then confirmed it from the other direction**: of its four new findings, two were divergences that audit had already closed one cut earlier (the multi-node memory fold and the cell-aware extent), so the by-hand run and an independent reviewer reached the same defects — and the round's own class-A finding, `.replyRecv` resolving its donation from the delegate's binding while `.reply` has used the recorded server's since PR #822's review, brings the count to **eight of twenty-four** across six rounds; rounds 8 and 9 take it to **twenty of thirty-six** across nine, round 9's seven including four Devicetree Specification rules the Lean parser did not enforce and the Rust walker does, and a *claim about a gate* — `v0.34.117`'s assertion that the legacy-consumer gate would catch a reintroduction of the walker it had just removed, which the gate's unconditional host-file exemption made false.  **Round 8 adds two more of the same shape at the next site along** — the peripheral `reg`'s whole-tuple refusal (the third round to find that relation unswept) and `init_mmu` conflating "no device tree" with "a device tree this parser cannot read" — and both were closed by giving the question **one** answer rather than a third patch: `fdtWholeEntryCount` for the tuple relation at all four sites, `dtb_dereferenced_range` for what the boot will dereference.  **Round 7 is five for five**, all in the Lean FDT parser and all citing the Rust walker as the correct side: the declared struct-block extent, the declared strings-block extent, the single-root requirement, the whole-tuple `ranges` refusal, and the unvalidated result — **thirteen of twenty-nine**.  What that round did *not* do is patch five reads: the four bounds findings were one defect (every read bounded by `blob.size`), so the remedy is a contract — `FdtBlob`, a view built only by a smart constructor and read only through accessors that refuse an access outside the block they name — and the tree's *second*, unbounded structure-block walk (`findMemoryRegProperty`, dead since `v0.30.8`) was removed rather than hardened.  That is tier 1 of this row's own remedy applied inside one file, and it is what the pair-level remedy (XV1) does to the pair | The instances are closed; what is missing is the gate that catches the *next* drift.  **The device-tree pair is not forced duplication and must not be gated — it must be removed** (maintainer's correction, recorded here rather than argued around): parsing a firmware-supplied blob before translation is enabled is bad practice, and it is also unnecessary, because boot page tables do not need the RAM *size*.  They need the image, its stacks, an early heap, a bounded window at the firmware's DTB pointer and the device window — every one of them a linker symbol or a board constant.  The standard arm64 sequence builds the boot map from those, enables the MMU, and only then reaches the FDT through a fixed mapping and parses it with translation on.  Doing the same here puts the blob's only parser in Lean, behind the verified surface, and deletes the Rust walker from the boot path with the whole extent, clamp and refusal machinery around it — tier 1 of the remedy (make the second implementation impossible) rather than tier 3 (check that two agree).  It waits on the Lean runtime existing on bare metal, which is **WS-BP**'s subject, so the row's remedy for this pair is **BP2.6** — a scheduled row, not a prose commitment: the register named WS-BP as the owner from `v0.34.114` and the plan carried no row discharging it until the RR7 audit round added one.  The remaining pairs (ABI encode/decode, boot map against the Lean memory map) are genuinely two-sided and keep the harness remedy.  Nothing in v1.0.0's claim set is false without this; the cost is that a behavioural divergence between a verified model and its executable twin is invisible to CI until a reviewer reads both | Cross-implementation validation; **post-v1.0.0 unless the maintainer opens it beside RR8** |
| ~~The removal does not preserve the donation accounting, because the recipient is derived from stack reachability rather than from who owned the reservation~~ — **CLOSED v0.35.54 (WS-HP HP10)**; registered `v0.35.14` as a depth-≥ 3 defect, **re-scoped at `v0.35.42`** — see the depth-2 paragraph below, which the original heading missed; **the depth-≥ 3 half is CLOSED at `v0.35.45`**).  Up to `v0.35.44` `spliceReplyFrameOut` wrote `above.prev := none` (`cancelledMiddleCallerPolicy = .severAtCut`), so every frame *below* a cut left the context's reply stack; the later pop then read the frame above the cut as the bottom and bound that caller `.bound scId`.  On a three-frame stack the reservation therefore settled on a thread strictly *inside* the chain while its owner was left `.unbound` for good — so a callee that delegates its caller's reply capability to a confederate, or anyone holding a suspend right over a middle caller, could permanently capture that caller's CBS reservation.  **WS-HP HP6 closed that**: the removal splices (`cancelledMiddleCallerPolicy = .spliceOutTheCut`), the frame above takes the cut frame's own downward link and the frame below links back up at it, and `donationAccountingPreserved_atCallDepthThree` states the payoff — the reservation leaves the cut owed outward and the pop that answers the bottom frame delivers it home.  **What stays open is depth 2 alone** (HP10).  **Not a divergence from seL4-MCS — upstream has the same cost**, re-verified at `v0.35.40` against upstream source at master, 13.0.0, 12.1.0, 12.0.0 and 11.0.0 (every release that has the function): `reply_remove`'s non-head branch writes `REPLY_PTR(next_ptr)->replyPrev = call_stack_new(0, false)` under the comment *"not the head, remove from middle - break the chain"*, so the frames below a cut leave the stack there too.  `v0.35.14` registered this row asserting the reverse and cited `REPLY_PTR(call_stack_get_callStackPtr(reply->replyNext))->replyPrev = reply->replyPrev` as having been read from the source; **that line is in no release**, and the "correction" propagated it to nine prose sites and three docstrings that had been right.  So the row stays open on its own merits — the capture is real and worth closing — and what closes it is an **improvement on** upstream rather than parity with it.  Two upstream facts that check *did* establish, both already landed: the pop's trigger is `call_stack_get_isHead(reply->replyNext)` (WS-HP HP4, HP5) and `reply_pop` donates only `if (tcb->tcbSchedContext == NULL)` (HP4.6's `donationRecipientAcceptable`, with upstream's own reason).  **Measured, not described**: `tests/SmpIpcSuite.lean` §3.22 exhibits it on a live three-frame stack together with the in-order contrast, and §3.20's depth-two witness provably cannot show it — a two-frame stack's lower frame is its bottom, so `severAtCut` and the named alternative `spliceOutTheCut` write the same value into the frame above.  **And the loss is not confined to depth ≥ 3** — that was this row's own scoping error, found at `v0.35.42` while deciding what remains after the splice.  At depth **2** the delegate answers the client out of order, the removal takes the client's frame — the stack's *bottom* — off the stack, and the later in-order pop finds the remaining frame at the bottom and binds the reservation to the **intermediate** caller, leaving the owner `.unbound` for good.  Both policies write `none` there, so the splice **provably cannot** reach it: the same sentence that explains why §3.20 cannot *measure* the depth-≥ 3 defect is the reason the depth-2 defect survives the fix for it.  §3.20 exercises the depth-2 *structural* outcome and asserts nothing about where the reservation ends up, so this half was in the tree's reach and in neither its witnesses nor this row.  It is **WS-HP HP10**, whose first code row is the missing cost witness; the remedy is one `SchedContext` field recording the reservation's origin and one arm reading it, and it is an improvement on seL4-MCS at **every** depth, since upstream's `reply_pop` donates to the answered frame's own `replyTCB` and so has the depth-2 loss too.  **The remedy is not the splice alone**: splicing re-heads a frame whose recorded server is by then `.unbound`, so answering that frame runs no pop, `Reply.consumed` keeps a head's links, and the result is a consumed frame heading a context — what `replyStackOuterCaller?_of_consumed_frame` refuses and what `v0.35.4` closed.  Recovering the accounting means moving the pop's **trigger** to "the answered frame heads a context" and its **source** to `SchedContext.boundThread`, which also makes the three *stated* coherence hypotheses (`replyStackHeadIsAnsweredReply`, `replyDonationOwnerIsAnsweredCaller`, `answeredHeadContextIsServerDonation`) derivable rather than assumed — **done at HP7, `v0.35.46`: all three are deleted** — and costs a budget yank at the pop — the behaviour `cancelIpcBlocking`'s reclaim already performs.  It touches the trigger, the source, the six pop call sites, `donationOwnerValid`, `passiveServerIdle` and every footprint declaring recorded-server members.  **In flight**: HP1 (`v0.35.35`) built the resolvers inert, HP2 (`v0.35.36`) proved the two triggers agree on every state `severAtCut` can reach and disagree at an orphan head, HP3 (`v0.35.37`) declared the splice's footprint member and moved the ceiling 22 → 23, **HP4 (`v0.35.38`) flipped the reply path's trigger** — both spines and `.replyRecv`'s pop now read the answered frame, the pop validates the recipient it rewrites (`donationRecipientAcceptable`), and the chain composite has already lost `answeredHeadContextIsServerDonation` for the strictly weaker `replyFrameHeadIsBound`.  Behaviour is unchanged on every state this tree reaches (the golden trace is byte-identical), which is the point: the divergence the flip makes visible is unreachable until HP6 turns the sever into a splice.  It also flipped the **frozen** mirror in the same cut (HP4.7, a sub-task added while implementing, §3.8.8): `frozenEndpointReplyWithDonationReturn` mirrors the live `.reply` *operation* and `frozenBranchOperationChecked` claims the two are run beside each other, so leaving that mirror binding-driven would have made it a second answer to the question HP4 had just re-keyed — with the divergence already scheduled at HP6 — and registering that as debt would have been documenting an asymmetry the implement-the-improvement rule requires removing.  It also surfaced a gap in the witness that closed rounds 13, 14, 15 and 22 on the same composite: **`FO-041` had never given the recorded server a `.donated` binding**, so the donation return round 13 added to that surface was compared on neither side.  `FO-042` pops on both and its second half is the state where the two triggers disagree.  HP8 owned the splice half alone and landed it at `v0.35.47`, with `FO-043` — a three-frame stack whose middle frame the reply answers — as the witness, because every scenario that surface carried sits at depth ≤ 2, where both removal policies write the same value and the flip is invisible.  **HP5 (`v0.35.39`) flipped the cancellation path's**, which §4 forces before the splice: after HP6 a frame becomes the head whose recorded reply target is gone, and a binding-driven reclaim there declines and leaves a `.donated` binding naming a `.ready` owner.  `cancelledCallerDonation?` reads the victim's own frame, WS-RR RR7.22's `donationHolderIsReplyTarget` is **deleted** in favour of the head-keyed `donatedContextIsOwnerFrameHead`, and two sentences about reachable states became theorems (`cancelReclaimHead?_eq_replyObject`; a reclaim excludes both removal members).  Two things the cut measured: the wake and both below-head footprint members needed **no** re-resolution, because they are *derived from* the trigger — the derivation discipline paying off; and **nothing in the tree fired the reply-arm reclaim at all**, so the flip would have landed untested.  One thing it got wrong first, and the shape is the project's own: the plan named three fixture files to sweep, a sweep over a named list is *a recognised set standing in for a derived one*, and the three came back clean while the **golden trace** then failed — `MainTraceHarness.lean` was not among them.  Two live fixture defects, both fixed: `SCO-020b/c/d` built a `.donated` binding with no Reply object at all (so the reclaim became the identity and all three lines flipped to `false`; the stack a live `Call` builds restores `main_trace_smoke.expected` **byte-identical**), and `SmpIpcSuite`'s OD5.2 pair was passing **vacuously**, both halves declining for the same reason because the TCB they handed the resolver carried no `replyObject`.  *A suite's assertions can pass vacuously; an exact golden trace cannot.*  `tests/SmpCancellationSuite.lean` §3.20 (HP5.5) fires it on the agreeing shape and on the orphan head, computing both readings side by side so the assertions are known to discriminate.  Two things HP4 deliberately did **not** do, each with its reason recorded in the plan's §3.8.7: `replyDonationReturn?` keeps its binding-driven meaning (its argument is the *holder*, the trigger's is the answered *caller*), and the two reply footprints kept their binding-driven resolvers with `lockSet_endpointReplyOnCore_covers_headDrivenPop` holding the gap closed.  **HP6.1 (`v0.35.41`) and HP6.2 (`v0.35.44`) landed first.**  HP6.1 renamed the removal family for the splice it becomes (545 occurrences in 27 modules, no semantic change) and found a *live* claim citing a symbol WS-RM had retired at `v0.35.6`.  HP6.2 repointed both footprints onto `answeredFrameHeadContext?` — the expression the pop itself reads — which had to precede the splice, since the splice is what makes the two resolvers disagree; the stand-in is deleted for a hypothesis-free coverage pair, the parameter is `donatedScHolderTid` because the head reading's second component is the thread the pop *unbinds*, and **three write-membership lemmas had to be added** because the second donation member had none on either footprint and `.replyRecv`'s SchedContext member had none either.  Its trade is stated rather than absorbed: `…_size_le_eighteen` becomes **unconditional** where it took two coherence facts, and `…_size_le_seventeen` is **retired** because its owner merge occurs on no state the head reading reaches.  **HP6.3–HP6.9 landed as one cut at `v0.35.45`** and closed the depth-≥ 3 half.  Five things it established that its own rows did not predict.  (1) **The splice is THREE stores, not two**: `above.prev := some below`, `below.next := some (.frame above)` and `rid.prev := none`, the last being seL4's `reply_unlink` downward half — without it the cut frame keeps a `prev` nothing names back, `prevLinkReciprocal` is false at the cut frame, and the bare splice would owe a relaxed predicate.  With it `donationChainWellFormed` is preserved **outright**, and it is free: the cut frame's lock was already a declared write member on both paths, so `maxLockSetSize` stays at HP3.5's 23.  (2) **The below side degenerates rather than refusing**, so the removal's refusal set is exactly the sever's and `spliceReplyFrameOutOrSelf`'s fold soundness carries verbatim — which means a stale upward link is still *reachable* and the reciprocity checks stay load-bearing.  (3) The plan's claim that HP6 closes **both** surviving divergences from upstream was **wrong twice** and is retracted: the second divergence is about *heads*, which HP6 does not touch, and HP6.7 is a restatement rather than a strengthening for the theorem it names — measured on the pre-splice tree, `removeCallerReplyFrame_preserves_donationChainWellFormed` already held on non-heads.  What HP6.7 *adds* is `removeCallerReplyFrame_splices_reciprocally`, which the sever could not state.  (4) `severAtCut_pop_leaves_no_head` — HP2.3's pin on the ordering — is **deleted**, because its first conjunct was the policy constant at the old value; its negative twin `answeredHeadContextIsServerDonation_false_of_orphan_head` was kept in that cut, having changed from a prohibition into a fact about reachable states — the warrant **HP7 (`v0.35.46`) then spent**, deleting the three stated coherence predicates, that refutation, HP2.1's equivalence, the scaffolding that consumed them and the binding-driven resolver `endpointReplyServerDonation?` itself: nine declarations, with the retired reading surviving only as a `private def` in the witness that refutes it (`tests/SmpCrossCoreReplySuite.lean`).  HP7 also found a **fourth** stated fact LIVE — `replyFrameHeadHolderDonation`, the one binding fact the trigger does not witness, whose own docstring wrongly claimed HP7 retires it — and HP7.4 **vacuous**, the dispatch packs having been re-keyed at HP4 rather than here.  (5) `tests/SmpIpcSuite.lean` §3.22 inverted from COST to PAYOFF, gained two negatives spelling the retired sever's values so the assertions are known to discriminate, and now measures the **second** pop delivering the reservation home; §3.20's depth-two halves and the golden trace are byte-identical, which is the measurement that the change is confined to depth ≥ 3.  **HP9 (`v0.35.48`) is the closure tier, and it took the payoff one depth further than the plan asked.**  `tests/SmpIpcSuite.lean` §3.23 builds a **four**-frame stack and cuts its third frame, because depth 4 is the shallowest stack with **two** frames below a cut — at depth 3 “the frame above reconnects” and “the frames below survive” are the *same* statement, since the splice writes the frame below's `next` and leaves its own `prev` untouched, so nothing at that depth distinguishes a reconnection from a transitive one.  The witness then runs **three** successive `returnDonatedSchedContextResolved` pops and ends with the reservation `.bound` on the original owner, which is the transitive form of `donationAccountingPreserved_atCallDepthThree`.  One thing the cut measured that its own row did not predict, and it changes what §3.23 is evidence *for*: a token-preserving mutation of the splice's **store shape** — reverting to the sever, or clobbering the frame below's own `prev` — does not merely fail the suite, it **fails to elaborate**, because `spliceReplyFrameStores_cases` pins the three stores as a theorem.  So the store shape needs no witness and §3.23's subject is the *composition*: that three pops over a spliced four-frame stack reach the owner.  That is stated in the witness rather than left for a reader to infer a mutation-verification that is not available.  HP9.4 recorded the three upstream facts this workstream rests on at `donationRecipientAcceptable` itself — each with the revisions it was read at — rather than in a review narrative, which is what `v0.35.40`'s retraction-of-a-retraction cost.  And HP9.3's own premise was **false**: the plan's row said this register entry closes and its withdrawn closing paragraph is restored, which was written before `v0.35.42` found the depth-2 loss.  It is corrected rather than acted on — the row stays **open**, owner **HP10**, and what HP9 earns is the depth-≥ 3 half stated at its transitive strength.  **HP10's field half landed at `v0.35.49` and is INERT** (HP10.1–HP10.5): `SchedContext.donationOrigin` records the thread that owned the reservation when it first left, written on a **first** push only, cleared by all five loan-enders — the pop's bottom arm, `schedContextBind`, `schedContextUnbind`, and the suspend path's `cancelBoundDonation` / `cancelBoundDonationOnCore`, which the plan's row had not named — and scrubbed on the destroy path so a stale origin cannot outlive its thread (`clearDonationOriginReferences`, with `retypeTargetDetached.tcbNotDonationOrigin` the obligation it makes safe).  **Nothing reads the field yet** and the golden trace is byte-identical, which is what makes this half inert.  **HP10.6 landed at `v0.35.50`**, and its sense of inert is a different one worth naming: the member is **live** — HP10.4 records an origin on every first push, so a depth-1 donating reply resolves it to `some`, and there the recorded origin *is* the answered caller, so `insertOrMerge` collapses it and the declaration is unchanged.  Both reply footprints declare the TCB the redirect will write, resolved through `donationOriginRecipient?` — the expression the arm will read, whose `.ok none` arm *is* the pop's own bottom arm — with the write-membership and resolved-coverage relations beside the declaration, so `maxLockSetSize` is **24**, the RPi5 tick admits **13 µs** per lock and the uniform 60 µs envelope is **4320 µs**.  The *reachable* figures did not move and that is a theorem: the origin member is live only where the pop is at the bottom of its stack, and there the two below-head members are absent (`replyStackBelowHead?_of_originRecipient`), so a reachable footprint trades two members for one.  The same row deleted **seven** parametric sharp bounds HP6.2 had orphaned — the five `_of_owner_eq_target` family, whose merge HP6.2 established is *false* on every state the arm reaches, and two `_of_no_donation` corners — since a sharp figure resting on a refuted hypothesis is worse than no figure.  **HP10.7 (`v0.35.51`) flipped the live arm and HP10.8 (`v0.35.52`) the frozen one**, and **HP10.9 (`v0.35.53`) is the payoff**: `donationAccountingPreserved_atCallDepthTwo` — a depth-2 out-of-order removal now settles the reservation on the thread that **owned** it, so the donation accounting holds at *every* reply-stack depth.  What the splice could not reach is stated at the code that closes it: the frame a depth-2 removal takes off the stack **is** the bottom, so `spliceFrameBelow?` answers `none`, the splice degenerates to the sever (`removeCallerReplyFrame_clears_prev_of_bottom_frame`) and either policy writes the same value into the frame above.  The theorem **derives** the reachability answer that names the wrong thread and **hypothesises** the two guards, one of which cannot be derived at all: the owner is `.blockedOnReply` on exactly the reply being answered until the reply leg's wake, so no statement about the removal alone can supply it; Tier 3 negatives refuse hypothesising either derived fact.  §3.20's accounting halves inverted from COST to PAYOFF and now drive the live `.reply` **spine** — they drove `returnDonatedSchedContextResolved` directly, which was an accurate proxy for the pop while nothing redirected and is a proxy that *omits* the redirect since HP10.7.  Two things that cut established rather than predicted.  **No mutation of the production code is available**: the origin write, the resolver, the three reply-path pops and the dispatch's recipient are each pinned as theorems, so a mutation fails to *elaborate* rather than failing the suite — which is HP9's own situation with the splice's stores — so the decisive comparison is a differential *within* the suite, one function applied to a chain whose first push recorded an origin and to `pushStore`'s, which predates HP10.4 and records none: a single field apart, opposite outcomes.  And **HP10.4's production write had never been measured**, every fixture that carried an origin having set the field by hand — *a witness whose field is supplied by its fixture asserts nothing about the production write that is supposed to supply it* — so §3.20 now runs the live push twice from a state where the owner still holds the reservation and asserts both directions (a **first** push records the origin, an **onward** push leaves it alone, which is what distinguishes an origin from a duplicate of `.donated scId owner`).  §3.22, §3.23 and the golden trace are byte-identical, which is the measurement that the cut is confined to the reachability gap, and *structural* rather than lucky: at depth ≥ 3 the pop sits at a `some` arm where `replyDonationRecipient_eq_of_outer_some` makes the redirect the identity by theorem.  The field is **history the kernel validates, not an invariant**: no `donationChainWellFormed` clause can relate it to the store, because "the origin is the bottom frame's thread, **or** a thread whose frame was removed" has an unstateable second disjunct and a clause carrying only the first would be false on exactly the states it exists for.  **CLOSED at `v0.35.54` (HP10.10), and what closes it is both halves being earned rather than the deferral being retracted**: the depth-≥ 3 half at `v0.35.45` (HP6's splice) and the depth-2 half at `v0.35.53` (HP10.9's recorded origin), with the spec (§§8.12.11, 8.12.14, 8.12.15), the claim index, the GitBook mirror and `CLAUDE.md` each carrying the claim and the theorem that discharges it.  The interim contract this row used to carry (*at reply-stack depth 2, do not read a successful pop as evidence that the context reached its owner*) is **retired at `v0.35.53`**, and the claim-set constraint is lifted in full: **v1.0.0 may claim that completing a call chain returns a client's reservation at every depth**, which is a claim seL4-MCS cannot make — upstream severs at depth ≥ 3 (`reply_remove`'s non-head branch writes zero into the frame above) and donates to the answered frame's own `replyTCB` at depth 2.  **Two things this row leaves behind rather than carrying.**  The footprint/transition resolution asymmetry HP10.8 found is its own row below and stays open, owner WS-HP; and the `severAtCut` constructor is deliberately **kept**, because it names the behaviour upstream still has and this kernel's improvement on it is only statable against something | **Neither a soundness gap nor a strengthening.**  Every invariant is preserved — what is lost is a real-time *guarantee* that nothing in this tree states as an invariant.  It may wait because no core is marked ready, so no reservation is enforced on hardware yet; what it constrains is the claim set — the depth-≥ 3 half is **earned** at `v0.35.45` (HP6) and the depth-2 half at `v0.35.53` (HP10.9), so v1.0.0 **may** say that completing a call chain returns a client's reservation; what is left is this row's own retirement (HP10.10).  (The *upstream-parity* half of this constraint was withdrawn at `v0.35.40`: `severAtCut` **is** seL4-MCS's reply-stack removal, so that claim was never in doubt) | **WS-HP** — [`docs/planning/DONATION_POP_TRIGGER_PLAN.md`](planning/DONATION_POP_TRIGGER_PLAN.md), 55 sub-tasks across 10 phases, registered `v0.35.16`; **before v1.0.0** |
| **WS-HP — the reply pop's origin member is resolved at two different states, and only one of them is the transition's** (registered `v0.35.52`, found by FO-044 rather than by a review).  `lockSet_endpointReply{,Recv}OnCore` resolve `donationOriginRecipient?` on the syscall's **pre-state**, because a footprint is acquired before the transition runs; `applyReplyDonationOnCore` resolves `replyDonationRecipient` **after the reply leg**.  The two can disagree at exactly one thread: the answered caller, which is `.blockedOnReply` before the leg and `.ready` after it, so HP10.7's `donationOriginRebindable` guard refuses it at the first state and admits it at the second.  **Not a live hole** — where they disagree the transition redirects to the answered caller, whose TCB the footprint declares unconditionally as `replyTargetTid`, so no write is undeclared; and for any other origin the reply leg changes nothing the guards read, since it wakes only the answered caller.  What is registered is that this soundness rests on a case analysis stated in prose rather than on the structural agreement every other member of this family has: HP4.1 resolves the *trigger* on the pre-state and passes it in precisely so that the footprint and the transition cannot name different frames, and the recipient does not yet get that treatment.  The remedy is to resolve the recipient on the pre-state and pass it as an argument, which is a signature change through `applyReplyDonation{,OnCore}`, `replyRecvPopDonation` and their decomposition lemmas | **Low severity, no soundness content today**: the divergence is confined to a thread the footprint already declares, and the behavioural outcome is identical at both states.  It may wait because the fix is a mechanical re-threading whose value is structural rather than behavioural, and landing it inside the HP10 payoff cuts would obscure both | **WS-HP**; before v1.0.0, since RR8.15's hand-off check reads this table |
| ~~**Sixty executable definitions write the object table raw, beside the one primitive that maintains its bookkeeping**~~ — **CLOSED `v0.35.77`** (the raw-write migration, `v0.35.64` → `v0.35.77`): every executable object-table write goes through `storeObject` / `withObjectStored` / `rewriteObject` or a typed update over it (the migration's twelve cuts, below), `STORE_WRITE_CODE` holds that population at **zero** in Tier 0 with the bodies that write raw by design registered and reconciled (D1, `v0.35.76`) — **corrected `v0.35.97`**: that zero was one spelling wide, the WRITE pattern having named `set` in its qualified branch only, so `st.objects.set k v` (the frozen surface's ordinary store) walked around it and thirty-one executable raw writes sat behind it; both patterns are now built from one `_TABLE_OPS` classification reconciled against `RHTable`'s and `FrozenMap`'s own sources in both directions and asserted symmetric across the two spellings, the frozen writes went onto the single state-level `frozenWithObjectStored` (plus the total `frozenRewriteObject`, which is what the one transition that had spelled its write `st.objects.insert` — and so sat past the zero as a *registered primitive* — now writes through), and the measured population is zero again, and `storeObject`'s capability-reference maintenance is an **erase over the displaced CNode's populated slots** rather than a filter over the whole table (D2, `v0.35.77`: `capabilityRefs_eraseFold_preserves_invExtK` is its half of `storeObject_preserves_allTablesInvExtK`, and `storeObject_capabilityRefs_of_not_cnode` states the payoff — a store of a non-CNode object at a key holding no CNode leaves the table structurally unchanged, where every endpoint, notification, TCB and SchedContext store used to pay `O(\|capabilityRefs\|)`).  What D2 found while landing is the row after this one.  Original text: **Sixty executable definitions write the object table raw, beside the one primitive that maintains its bookkeeping** (registered `v0.35.63`, measured while closing RR8.5 — the two deleted teardown twins were two of them).  **IN PROGRESS `v0.35.64`**: the primitive landed — `SystemState.rewriteObject`, the proof-carrying in-place rewrite whose body is the bare insert and whose bookkeeping-neutrality is a theorem, with `updateTcb` / `updateSchedContext` the typed read-modify-writes over it and `withObjectStored` the pure store for a key that may hold nothing — and the first nine declarations moved onto it (`Lifecycle/Suspend.lean`'s seven, `cancelBoundDonationOnCore` because its `rfl` bridge to the single-core arm forced it, and `PriorityInheritance/PerCore.lean`'s private mid-state copy, deleted in favour of the public `restoreToReadyMidState` the operation is now *defined* through).  Re-measured: **65 sites in 52 executable declarations across 22 files**, five of them the primitives that should be raw (`storeObject`, `rewriteObject`, `Builder.createObject`, `updateObjectAt`, the frozen store) and seven witnesses.  **IN PROGRESS `v0.35.65`**: the typed rewrites match on the **witnessed lookup** (`getTcbWitnessed?` / `getSchedContextWitnessed?` — `getTcb?` carrying its own equation, matched on the store and erased to the value), which is what lets a migrated site's consumer proofs reduce it (`simp only [site, getTcbWitnessed?_eq_some h]`) where `v0.35.64`'s dependent match could not be rewritten at all; the scheduler's context-save family (`saveOutgoingContext`, `saveOutgoingContextChecked`, `saveOutgoingContextOnCore`, `preemptCurrentOnCore`) and `setThreadCpuAffinity` moved, and `resumeThread`'s arm was re-spelled on the witnessed lookup.  Re-measured: **60 sites in 47 executable declarations across 22 files**.  **IN PROGRESS `v0.35.66`**: `enqueueRunnableOnCore`, `timerTick` (both TCB writes), `refillSchedContext` (a plain `updateSchedContext`) and `handleYieldWithBudget` moved; the single-thread PIP boost update became **one body** — `updatePipBoost` is *defined* as `updatePipBoostOnCore` at the boot core, its `rfl` bridge kept as the equation lemma — so its raw write was one site to migrate rather than two.  Re-measured: **53 sites in 41 executable declarations across 20 files**.  **IN PROGRESS `v0.35.67`**: `timerTickBudget` and `timerTickBudgetOnCore` take the store's witness that the charged TCB is the stored one (`hTcb : st.getTcb? tid = some tcb`), every caller resolves it through the witnessed lookup and the two suites now store the thread they charge; the per-core tick's charge became the named step `timerTickChargeCurrentOnCore`, because a dependent match inline under the tick's `have`-bound state is opaque to definitional unification and broke the `rfl` equation `timerTickOnCore_eq_prepared`; `enqueueIdleThreadOnCore` is a `withObjectStored`, the index bookkeeping sitting under the `objStore` lock its footprint already names; and the trace model's replenishment fold is a typed update.  Re-measured: **43 sites in 37 executable declarations across 17 files**.  **`v0.35.68`** migrated no site and *derived* one: the boot's idle install is `enqueueIdleThreadOnCore` on the intermediate state (the row below, closed), so the boot performs no raw write of its own through it and the figure is unchanged at 43 / 37 / 17.  **`v0.35.69`**: the four register-context writers — `writeReturnFrameToTcb`, `writeRestartFrameToTcb`, `writeFfiRegistersToTcb`, `writeFaultRegistersToTcb` — are `updateTcb`, each having had exactly its shape; re-measured **39 sites in 33 executable declarations across 15 files**.  **`v0.35.70`**: the fault path's seven TCB writers — `recordPendingFault`, `applyFaultRestart`, the four fail-closed dispositions over their deschedule, and `installFaultHandler` taking the store's witness for the TCB the operation hands it — are the typed rewrite; re-measured **32 sites in 26 executable declarations across 13 files**.  **`v0.35.71`**: the SchedContext operations and the priority management — `updatePrioritySource` (both arms the typed read-modify-write), `setMCPriorityOp` / `setMCPriorityOnCore` (the ceiling written under the witnessed lookup), `schedContextConfigureBoundPropagate` (taking the store's witness for the TCB it is handed), `schedContextBind`, `schedContextUnbind` and `schedContextYieldTo` (the first object under its lookup's witness, the second through the typed read-modify-write over the rewritten state, reduced in the proofs by `updateTcb_after_rewriteObject_schedContext`) — thirteen inserts across seven writers; re-measured **19 sites in 19 executable declarations across 10 files**.  **`v0.35.72`**: the cancellation spine's `suspendThreadOnCore` (its G6 `.Inactive` write is `updateTcb`, the shape the single-core suspend took at `v0.35.64`, with `updateTcb_ipcInvariantStage` replacing — and deleting — the store-or-identity stage lemma the composite consumed), the capability revoke sweep's `revokePendingTransfersStep` (the blocked sender through the witnessed lookup, the filtered message rewritten under its witness) and the destroy path's `clearDonationOriginReferences` (`updateSchedContext` on the enumerated key, under its unchanged guard); re-measured **16 sites in 16 executable declarations across 8 files**.  **`v0.35.73`**: the seven inhabitation witnesses — the dispatch payoff's four and the reachability pack's three — are the pure store on each fresh key and `rewriteObject` under the stores' own lookups for the bind's two in-place writes, their per-key characterisations reading the table through `withObjectStored_objects` rather than exposing it by `show`; re-measured **9 sites in 9 executable declarations across 6 files**.  **`v0.35.74`**: the two queue sweeps — `removeFromAllEndpointQueues` and `removeFromAllNotificationWaitLists`, the last two kernel transitions writing raw — dispatch on the enumerated object's kind, read the record from the accumulator through `getEndpointWitnessed?` / `getNotificationWitnessed?` and rewrite it under that witness, with the write-set guard outside the rewrite; re-measured **5 sites in 5 executable declarations across 4 files**, all five the primitives that should be raw (`storeObject`, `rewriteObject`, `Builder.createObject`, `updateObjectAt`, the frozen store), so **the executable population outside the primitives is zero**.  **`v0.35.75`**: the trace harness's 61 fixture inserts in 16 runners are `withObjectStored` (golden trace byte-identical), so the whole `SeLe4n/` tree — the harness included, which is the write census's domain — holds six raw writes: the five primitives and the reply-stack census's planted witness `censusWitnessRawTableWrite`.  **`v0.35.76`**: the Tier 0 write census is live — `scripts/lean_store_read_census.py` classifies the raw write spellings (`objects.insert` / `objects.erase`, method or qualified) with the read census's own classifier and emits `STORE_WRITE_CODE` / `STORE_WRITE_SPEC`, the six bodies that write raw by design are `WRITE_PRIMITIVE_BODIES`, reconciled in both directions, and `STORE_WRITE_CODE` is a `ZERO_METRICS` entry of `scripts/ak7_cascade_check_monotonic.sh` from its first measurement (live `0`, with `298` write mentions in propositions as the diagnostic), so a raw write reappearing in an executable position anywhere under `SeLe4n/` fails Tier 0 on the day it is written and regenerating the baseline cannot clear it.  **`v0.35.77`**: D2 landed and this row closed (see the head of the row).  `SystemState.storeObject` (`Model/State.lean`) is the object-store write: one `objects.insert` that also maintains `objectIndex`, `objectIndexSet`, `lifecycle.objectTypes`, `capabilityRefs` and `asidTable`, always `.ok`, with `storeObjectChecked` its capacity-guarded form and `Concurrency.updateObjectAt` the generic read-modify-write over it.  Measured over the comment-free code view (`objects.insert` / `objects.erase` inside a `def` / `instance` / `abbrev` / `opaque` body, the same recogniser the RR8.5 cut used): **75 raw-write sites in 60 executable declarations across 24 files** outside `SeLe4n/Testing/`, of which `storeObject` itself, `Builder.createObject`, `updateObjectAt` and the frozen surface's own store are the four that *should* be raw, and seven are state-building witnesses inside `IPC/Invariant/{DispatchPayoff,Reachability}.lean`; the rest are kernel transitions — the scheduler (`Operations/Core.lean` 6, `Selection.lean` 5), the fault path (`IPC/Operations/Fault.lean` 6), the suspend/restore family (`Lifecycle/Suspend.lean` 7), the SchedContext operations (4), the cleanup sweeps, the PIP writers, the return-frame staging — every one a `{ st with objects := st.objects.insert k v }` that bypasses the bookkeeping `storeObject` exists to keep, so that whether the index, the type table and the ASID table agree with the store after a transition depends on which surface wrote it.  That is *one question, two answers* at the scale of a store discipline, and it is why `cancelIpcBlocking_lifecycle_eq` had to be conditional on the arm's `storeObject` writes before RR8.5 deleted it, and why `storeObject_modifiedFields` names `.asidTable` while a raw insert can never move it.  **Not a soundness defect today**: the raw writers rewrite keys that already hold an object of the same kind, so the bookkeeping they bypass is semantically unchanged (the same object type, the same index membership), and the IPC and capability surfaces — where objects are created and retyped — already write through `storeObject`.  **Why it may wait**: the remedy is a *migration*, not a fix — and `v0.35.64` corrected what it migrates onto.  Not the pure `storeObject` projection (that is `withObjectStored`, RR8.5's shape, and it exists for the keys that may hold nothing) but the proof-carrying in-place rewrite `rewriteObject`: `storeObject` filters every capability reference and re-inserts into two more tables on every write, and an `RHTable` re-insert of an existing key is not structurally the identity without a no-resize hypothesis, so a scheduler tick spelled through it would pay on every quantum and every definitional field frame would have become a conditional theorem.  Each site moves with its proofs re-read through `updateTcb_eq_of_some` / `updateTcb_eq_self_of_none` — about fifty repairs for the first nine declarations, and no fixture cost.  The figure is a **floor over the recognised spelling** (`objects.insert` / `objects.erase`), not a set: a writer spelling the update another way is not counted, and the migration should re-derive the population from the elaborator rather than from this regex.  The first cut (`v0.35.64`) was the primitive plus the `Lifecycle/Suspend.lean` seven and the two twins a pin forced with them; the second (`v0.35.65`) was the witnessed lookups and the scheduler's context-save family; the third (`v0.35.66`) was the wake, the legacy tick, the refill, the budgeted yield and the PIP update, collapsed onto one body; the fourth (`v0.35.67`) was the two budget ticks under the store's witness, the per-core tick's named charge step, the idle enqueue as a store and the trace model's fold. | `SeLe4n/Model/State.lean`, `SeLe4n/Kernel/Lifecycle/Suspend.lean`, `SeLe4n/Kernel/Scheduler/Operations/{Core,Selection}.lean`, `SeLe4n/Kernel/IPC/Operations/Fault.lean` | closed `v0.35.77` |
| **Twelve executable keyed accesses hold the object table through an INDIRECTION, so both enforced store zeros are floors over the direct spellings only** (registered `v0.35.117`, found by reading the write census's own claim against the tree rather than by a review). `READ`, `WRITE` and `SWEEP` all key on the receiver text `.objects`, so a declaration that obtains the table and then keys into *that* is invisible to every one of them. Two spellings, one question: a **binding** (`let objs := st.objects`, then `objs.insert k v` / `objs[k]?`) and a **parameter** (`(objs : RHTable ObjId KernelObject)`). Three executable declarations use them — `endpointQueueRemove` (four writes, two reads), `spliceOutMidQueueNode` (two and two) and `queueNeighbourPatch` (one and one) — which is why the raw-write migration (`v0.35.64` → `v0.35.78`) passed over all three and why `CLAUDE.md` claimed for forty cuts that the only raw reads left were accessor bodies and propositions, and the only raw writes the five primitives. Both claims were **false**, and both are corrected in the same cut rather than left to read as measurements. **Detection landed at `v0.35.117`**: one classifier for both spellings (`table_receivers` derives the table-denoting identifiers from three binder positions — a signature binder, a lambda binder and an unbracketed ascription, read over the whole declaration rather than its signature alone — and from a binding of the projection, closed transitively; the table type has one definition so a binder and an ascription cannot disagree about it; the access alternation is `_TABLE_OPS`', the same one `READ` and `WRITE` are built from, so a newly classified operation reaches the direct and indirect censuses by construction), driven through `classify` via a `collect` hook so the declaration boundary, the `Prop` verdict and the region split stay one answer, floored per `(file, declaration, shape, kind)` — keys **and** counts, since a set of keys cannot see a second access in a declaration that already has one and a count cannot see the first in one that had none — reconciled in both directions in every mode, with the table's own operations exempt by **derivation** from `_TABLE_SOURCES` and that exemption reconciled both ways. The unit is the *access*, not the binding, because a binding count cannot see a second `objs.insert` added to a declaration that already aliases. `STORE_INDIRECT_CODE` reports the number and `STORE_INDIRECT_SCOPE` the claim; twelve self-test fixtures and eight floor cases are mutation-verified, and the decisive tree-level mutation respells one live `storeObject` write through a binding and is refused by name | **Not a soundness defect**: all twelve accesses are correct — they rewrite keys already holding an object of the same kind, which is exactly `rewriteObject`'s admissible case, so the bookkeeping they bypass is semantically unchanged, and RR8.4/OD3.9 already unified the *values* they write (`queueUnlinkPredecessor` / `queueUnlinkSuccessor` / `queueRemoveBoundary` / `tcbWithQueueLinks`). What was wrong was their **invisibility**, and that is closed: a thirteenth access fails Tier 0 on the day it is written. It may wait because the remedy is a *migration* with real proof cost and no behavioural content, and shipping it with the census that found it would mean one commit that both adds a gate and refactors the dual-queue removal's write-set surface — so a proof failure in the second would cost the first. It constrains one v1.0.0 claim and the constraint is now printed rather than implied: **the two store zeros are floors over recognised spellings**, which both `_SCOPE` lines and the prose citing them say | **The architecture is named, not left to be rediscovered.** No new primitive: `queueNeighbourPatch` becomes state-level, which is `Option.elim` over the `SystemState.updateTcb` this tree has had since `v0.35.65` (a witnessed lookup around `rewriteObject`, with its lemma library already in place — `updateTcb_eq_of_some`, `updateTcb_eq_self_of_none`, `updateTcb_getTcb?_self`, `rewriteObject_preserves_*`); `spliceOutMidQueueNode` then composes it twice with no table in scope, and `endpointQueueRemove` composes it for its two neighbour patches and `rewriteObject` / `withObjectStored` for the endpoint boundary and the link clear. **Cost, measured rather than estimated**: nine theorems stated over the *table* in `CleanupPreservation.lean` (`queueNeighbourPatch_invExt`, `_nonTcb`, `_backward`, `_at_self`, `_at_self'`, `_at_other`, `_tcb_forward`, `_tcb_value`, `_preserves_queuePPrevAgreesWithPrev`) restated at the state level, twenty-five theorems over `spliceOutMidQueueNode` across fifteen files re-read through the new reduction, and the two `_eq_patches` pins (`spliceOutMidQueueNode_eq_patches`, `endpointQueueRemove_eq_patches`) whose right-hand sides compose at the table level — the second of which also **restates the inlined boundary and link-clear records RR8.4 replaced with `queueRemoveBoundary` / `tcbWithQueueLinks`**, so it is a live instance of the duplication RR8.4 removed and is repaired in the same cut. Two Lean constraints the attempt must respect, both measured: a term-mode `let` whose body's type does not depend on it elaborates as a `have`, whose body is erased and opaque to definitional unification, so a chain of `let`-bound states makes every `rfl` over it fail (distinct names do not help); and a witnessed lookup is a *dependent* match, which `rfl` cannot reduce when the scrutinee is not a variable — which is why `updateTcb_eq_of_some` / `_eq_self_of_none` exist and why a bridge theorem to the old raw-insert chain cannot be closed by `rfl`. `SeLe4n/Kernel/IPC/DualQueue/Core.lean`, `SeLe4n/Kernel/Lifecycle/Operations/{Cleanup,CleanupPreservation}.lean`; **before v1.0.0** |
| ~~**The capability-reference table is written by three writers and read by no executable code, and the invariant that names it never reads it**~~ — **CLOSED `v0.35.78`** (registered `v0.35.77`, found while landing D2): **retired, on measurement.**  The row's remedy said *wire or retire, and the rule says wire*, and the measurement chose retirement: `lookupCapabilityRefMeta` had been `(lookupSlotCap st ref).map Capability.target` since the repository's root, so nothing had ever read the table; every slot-target query in the tree holds or fetches the CNode already, and `lookupSlotCap` is `O(1)` and yields the whole capability; the one consumer the remedy proposed — a revocation sweep over a *target* — is a target→slots question a table keyed by slot cannot answer; the boot builder installed populated CNodes without ever populating it; the frozen mirror never maintained it; and every CNode store paid a fold over the CNode's slots for it.  `LifecycleMetadata.capabilityRefs`, `storeCapabilityRef`, `revokeAndClearRefsState`, `lookupCapabilityRefMeta`, `capabilityRefMetadataConsistent`, the bundle-of-one `lifecycleMetadataConsistent`, the lifecycle capability-reference and stale-reference families, `lifecycleCapabilityStaleAuthorityInvariant`, the policy surface's owner-authority implication and the builders' `withLifecycleCapabilityRef` are deleted; `lifecycleInvariantBundle` is the identity layer, `IntermediateState.hLifecycleConsistent` is `objectTypeMetadataConsistent`, `allTablesInvExtK` is sixteen conjuncts, `storeObject`'s lifecycle write is the object-type insert alone, the four capability operations store once, and Tier 3 refuses every retired name tree-wide.  The one substantive fact the layer restated — a reply cap is backed iff its Reply resolves — stays where it was owned, in the capability layer's step-preserved `replyCapPointsToValidReply`.  Not a soundness change: no kernel decision read the table, so no transition's result moved and the golden trace is byte-identical; what it removes is a per-store table update that bought no consumer and an invariant row that read as coverage of a relation nothing checked. | `SeLe4n/Model/State.lean`, `SeLe4n/Kernel/Lifecycle/Invariant.lean`, `SeLe4n/Kernel/Capability/Operations.lean`, `SeLe4n/Kernel/Service/Invariant/Policy.lean` | closed `v0.35.78` |
| ~~**The boot's idle install is a second body of `enqueueIdleThreadOnCore`, pinned by prose**~~ — **CLOSED `v0.35.68`**: `Platform/Boot.lean`'s `enqueueIdleThread` has `state := enqueueIdleThreadOnCore ist.state c` (`enqueueIdleThread_state`, by `rfl` — decisive, since a second body is not `rfl` to the kernel model's), with the four `IntermediateState` witnesses the operation's own theorems (`enqueueIdleThreadOnCore_preserves_allTablesInvExtK` / `_perObjectSlotsInvariant` / `_perObjectMappingsInvariant` / `_lifecycleMetadataConsistent`) and every boot-level frame an instance of the kernel model's.  The obstacle was taken as prescribed: the definition and its Model-level frames moved to the production module `Scheduler/Operations/IdleEnqueue.lean`, upstream of the boot; the idle TCB (`createIdleThread`, `queuedIdleThread`, the field lemmas) moved to `Scheduler/IdleThread.lean` beside its identities, with the eleven qualified references swept and the two `queuedIdleThread_threadState` copies made one; `PerCoreIdle` consumes both and keeps the per-core-invariant surface.  Registered at `v0.35.67` (from the maintainer's question while the raw-write migration's fourth cut landed): the boot built core `c`'s idle TCB through `Builder.createObject` over `IntermediateState`, the kernel model did the same over `SystemState`, and the only thing holding the two together was the RR5.11 docstring's *"mirrors … definitionally parallel"* — true of `objects` and the run queue, false of the bookkeeping the store maintains and the builder skips.  On a successful checked boot those fields are inert (`bootFromPlatform_capabilityRefs_eq` — retired with the capability-reference table at `v0.35.78` — and `bootFromPlatformChecked_ok_idleSlotsFreshAt`), so the derivation changed no boot state and the golden trace is byte-identical — the case in which a derivation is taken rather than a pin. | `SeLe4n/Platform/Boot.lean`, `SeLe4n/Kernel/Scheduler/Operations/IdleEnqueue.lean`, `SeLe4n/Kernel/Scheduler/IdleThread.lean` | closed `v0.35.68` |
| **WS-HP — two stated coherence hypotheses survive the head-driven trigger, and no invariant entails either** (registered `v0.35.61`, the post-landing audit).  HP7 (`v0.35.46`) deleted the three *binding-driven* coherence facts and left the two the trigger does not witness: `replyFrameHeadIsBound` — a scheduling context that heads a reply stack is bound to a thread — carried by `endpointReplyCrossCoreDispatch_preserves_donationChainWellFormed`, `faultReplyOnCore_preserves_donationChainWellFormed` and `replyTransferOnCore_preserves_donationChainWellFormed`; and `replyFrameHeadHolderDonation` — the holder's binding *is* a donation of the context its frame heads — with consumers across the reply-path bundles and the dispatch packs' reply-stage fields.  The first was *predicted* to become a chain clause (its own docstring and the chain composite's said "HP7 is where it becomes a clause of the chain invariant and retires", and no HP7 row scheduled it), which is the forward-looking-prose shape `CLAUDE.md`'s retired-code section warns about; the audit corrected the sentence and registered the fact rather than predicting it a second time.  The remedy is a `headBound` clause of `donationChainWellFormed` (`scReply = some _ → boundThread = some _`) and a binding clause tying the head context's `boundThread` to a `.donated` binding of that context, with `donationChainFrame` extended over `boundThread` — a change to every transition that writes a binding (the push, the pop, bind, unbind, both `cancelBoundDonation` spellings, the retype cleanup), which is why it is a cut of its own rather than an audit correction | **Low severity, no soundness content**: both facts hold on every reachable state by the arguments their docstrings carry (the push and the pop write `boundThread` and `scReply` together, `schedContextBind` refuses a context that heads a stack, `schedContextUnbind` refuses a `.donated` holder), and every consumer states them as pre-state hypotheses the caller discharges rather than assuming them of a post-state | **WS-HP**; before v1.0.0, since RR8.15's hand-off check reads this table |
| **`Lifecycle/Suspend.lean` has a per-file namespace its siblings do not, so one subsystem's Operations/Invariant pair is qualified and the rest are not** (registered `v0.35.81`, found while writing WS-RR RR8.7's second cut).  `SeLe4n/Kernel/Lifecycle/Suspend.lean` opens `namespace SeLe4n.Kernel.Lifecycle.Suspend`, and exactly one other module joins it — `Lifecycle/Invariant/SuspendPreservation.lean`.  Every other module in the same subsystem is in plain `SeLe4n.Kernel`: `Operations/Cleanup.lean`, `Operations/CleanupPreservation.lean`, `Operations/RetypeWrappers.lean`, `Operations/ScrubAndUntyped.lean` and all three `Invariant/Cancellation{Reply,Queue,Notification}Shape.lean`.  So the Operations/Invariant *pair* for suspension is namespaced and the pair for cleanup is not, in one directory, with no stated criterion — which is *one question answered in two places* at the level of module layout.  **Three measurements.**  (1) **It is not load-bearing**: all 59 top-level declarations in `Suspend.lean` were searched against every other tracked `.lean` file and there are **zero** name collisions anywhere in the tree, so nothing is being disambiguated — and nothing local shadows an outer name either, which is the hazard a per-file namespace under its parent's own `open SeLe4n.Kernel` would otherwise carry.  (2) **It is undocumented**: the module header states the suspension and resumption sequences and gives no reason for the namespace, so a new module has no criterion to apply.  (3) **It costs 457 `Lifecycle.Suspend.` qualifications** on 453 lines across **27** tracked `.lean` files outside the two that share it — `CancellationReplyShape.lean` 157, `DispatchArmPreservation.lean` 53, `IPC/CrossCore/Cancellation.lean` 52, `tests/SmpCancellationSuite.lean` 38, `IPC/CrossCore/CancellationNI.lean` 34, `tests/LivenessSuite.lean` 33 — and reaches outside Lean: three live documents (`SMP_RELEASE_READINESS_PLAN.md`, `UNFINISHED_SMP_WORK.md`, `docs/spec/SELE4N_SPEC.md`) spell the prefix.  **No gate depends on it**, which was checked rather than assumed: the single occurrence in `scripts/test_tier3_invariant_surface.sh` is a *comment* — on `Lifecycle.Suspend.runningCoreOf?` — and not an anchor, so the code view strips it and the rename's sweep is three documents and the Lean tree.  What it buys is **one** thing, and only for the second file: `SuspendPreservation.lean` names the operations unqualified because it joined the namespace rather than importing into `SeLe4n.Kernel`.  **The live cost was measured rather than reasoned**: RR8.7's second cut wrote `abortHolderPendingIpc_other_tcb_eq` with a `Lifecycle.Suspend.` prefix and it failed to elaborate — that theorem is *about* Suspend's operations but lives in `CancellationReplyShape.lean`, hence in `SeLe4n.Kernel` — in the same proof as a `Lifecycle.Suspend.abortHolderPendingIpc_eq_self_of_lookup_none` that **requires** the prefix.  The prefix is mandatory for the operation and refused for the theorem about it, from the same call site.  Note the *name* is not the defect: under this project's internal-first naming rule `Lifecycle.Suspend` describes its subject and carries no workstream ID; the **partition** is what this row is about | **No soundness or correctness content**: every reference in the tree resolves today, `lake build` is the check, and the comment and three documents that spell the prefix are correct as written.  The cost is reviewer friction and the per-call-site ambiguity above, both of which a rename pays down without changing a single proposition | post-v1.0.0.  Collapsing it to `SeLe4n.Kernel` is a 457-occurrence mechanical rename over 27 files plus one Tier 3 comment and three documents, with `SuspendPreservation.lean` moved in the same cut; the other direction (namespace the remaining Lifecycle modules to match) is larger and would have to answer the same question for every subsystem.  Either is its own cut and has no business inside a proof cut.  Whichever is chosen, the criterion must be **stated in the module header**, or the next module decides it by coin flip again |
| **The Tier 0 convention gates re-implement three language front-ends by hand, at a tier that cannot ask the real one** (registered `v0.35.27`).  `scripts/check_unsafe_block_justifications.py` and its shared view `scripts/rust_code_view.py` are 3,591 lines hand-implementing *Rust lexing*, *Rust item parsing* and *CommonMark*; `scripts/lean_store_read_census.py` hand-implements *Lean declaration parsing*.  The evidence that this is the shape rather than a run of unrelated defects: **every review round of PR #895 from round 9 to round 19 found at least one construct in one of them** — a raw identifier, a 2024 attribute form, a scope opener, a raw doc literal, a fenced block, a token-separating comment, a UAX#31 name, an HTML block, a shift operator read as nesting, a Setext heading, an item-position macro carrying `fn` tokens, a store-read spelling quoted in a string, and a backtick inside a backtick fence's info string.  Each fix was correct and the next round supplied a spelling nobody had enumerated, because the set of valid spellings that defeats a hand-written front-end is unbounded while the set it has seen is finite.  Six successive rules were written into `CLAUDE.md` to close it (*enumerate the space*, *take the axes from the grammar*, *build the artefact*, …) and the rounds continued, which is the measurement that the remedy is not another rule.  **`CLAUDE.md` already names the exit and it was taken once**: round 17 of PR #889 says a Lean question goes to the Lean elaborator, and round 5 of PR #895 applied it — `SeLe4n/Testing/StoreReadClassificationCensus.lean` (Tier 1) reconciles the Tier 0 Lean classifier against `findDeclarationRanges?`, and round 6 then confirmed the mechanism by finding that it *would* have caught a defect it never saw.  What was never generalised is that Rust's front-end is `rustc` and that the `# Safety` question's front-end is **rustdoc** — the tool whose behaviour the property is literally about.  Round 12 recorded "when the property is about the whole artefact, build the artefact" and then hand-rolled a markdown renderer instead of asking the renderer | Every instance found so far is closed and each carries a mutation-verified witness, so the gates are correct on everything they have been shown; what is missing is the reconciliation that makes the next spelling a build failure instead of a review finding.  It may wait because the enforcement direction is sound — an unrecognised form fails the gate rather than being skipped (PR #889 round 25), so the gap is a *floor*, which the gates' own output and the prose citing them already say.  It constrains no v1.0.0 claim: the counts are reported as floors over recognised forms | **Largely closed at `v0.35.28`, by handing the questions to the real front-ends rather than by reconciling against them.**  `sele4n-hal` and `sele4n-abi` deny `clippy::undocumented_unsafe_blocks` and `clippy::missing_safety_doc` at their crate roots: the first is rustc's own parse of the block and the comment run above it, the second renders the item's documentation with the parser rustdoc uses and looks for the published section.  Both run with `-D warnings` on the host lane and on `aarch64-unknown-none`, which is where they have teeth — most of the HAL is `#[cfg(target_arch = "aarch64")]`, so deleting a real `// SAFETY:` comment produces **0** findings on the host and **2** on the cross target.  Two things were learned by mutation rather than assumed: `cargo clippy -- -W <lint>` reaches only the final compilation unit and is **silent** for every workspace member (indistinguishable from a clean tree), and the host lane alone cannot see the hardware half.  **What remains here**: the lints do not reach a non-`pub` `unsafe fn`, an `unsafe fn` declared inside an `extern` block — this crate has ten Lean upcalls needing a caller-facing contract, and no lint requires one of a foreign declaration — or the ARM ARM citation census.  The scanner keeps those, and its output now names its authority and its residue instead of claiming the whole surface.  **Round 16 is the measurement of how much the lints closed**: all three of its findings were this shape, and none was in a question a lint answers — one in the item split (which linker symbols a foreign block requires, which no lint reports), one in the Lean classifier at Tier 0 (where no elaborator is available, which is why the Tier 1 reconciliation exists), and one in the CommonMark residue the lints structurally cannot reach.  So the narrowing was real and the row stays open at its stated width.  **Round 17 sharpens what "the real front-end" means here**: its Python half was closed by handing scope resolution to `symtable`, which is the exit working; its Rust half found that `clippy::missing_safety_doc` and `rustdoc` *disagree* on a multi-line Setext heading — the lint accepts what the renderer does not publish — so naming a tool is not the end of the question, and the gate follows the rendering.  A reconciliation built against the lint alone would have inherited that leniency.  **Round 18 narrows the row again, and in the direction this entry did not anticipate.**  Its three findings were all this shape, and *two of them had an exact oracle in the standard library the whole time*: Python's identifier grammar is UAX#31, the same one Rust uses, so `str.isidentifier()` answers the `XID_Start` question this tree had been approximating with `[^\\W\\d]` (measured against rustc 1.94.1 over 28 codepoints — 27 agree, the sole divergence being a lone `_`), and Python's binding semantics are `ast`'s.  So this row's own framing — that the remedy is heavyweight reconciliation against an external tool — was itself part of why it stayed open: **before registering a front-end approximation as debt, check whether the exact answer is a standard-library call away.**  What is left after round 18 is genuinely heavyweight and unchanged: Rust *item* parsing and the CommonMark residue, for which the exits named above (a `rustc`-reconciled foreign-item split, `rustdoc --output-format json` for doc attachment) still stand.  **Round 19 landed inside that named residue one cut later**, on inline markup in a `# Safety` heading, which is the narrowing working as a measurement and not as a remedy: predicting where the next finding will be is not preventing it.  It also sharpens what the exit must deliver — the two authorities *disagree in both directions* on that axis (clippy refuses a code span, an entity and a split title that rustdoc renders; rustdoc renders `[Safety]` for a shortcut link clippy accepts), so a reconciliation against either tool alone would inherit the other's leniency.  The gate takes their intersection and says so; the residue where Tier 0 is the sole authority is unchanged and stated — a non-`pub` `unsafe fn` and the ten `extern`-block declarations, which no lint covers  Reconcile the remaining foreign-item split against `rustc`, and take the `# Safety` residue's *assembly* half — which doc sources attach to which item — from `rustdoc --output-format json` rather than re-deriving it, leaving one CommonMark question instead of a renderer; **before v1.0.0** |
| **The frozen surface is a hand-written second implementation whose differential coverage is a hand-written scenario list** (registered `v0.35.27`).  `SeLe4n/Kernel/FrozenOps/` mirrors live kernel transitions, and `frozenRunAgrees` is the relation that would catch a divergence — but which pairs are actually driven through both sides is a small set of hand-written scenarios (`FO-031` and its siblings), and the frozen↔live pairing itself is a Markdown table in `Operations.lean`'s module header.  So a mirror may omit a step its live twin performs and nothing reports it.  Measured on PR #895: **rounds 10, 11, 13, 14 and 15 are all that one shape** — a proxy core instead of placement, a deschedule wired on one arm of two, `Reply.consumed` adopted without the pop its contract defers to, and `returnDonatedSchedContext` mirrored without the `removeRunnable` its live caller pairs with it.  Rounds 13 and 14 are the same defect twice, the second inside the first's fix, and round 15 found two more on the same composite — a missing priority-inheritance revert and a missing recorded-server refusal.  This is `CLAUDE.md`'s own strongest rule — *one question answered in two places will diverge; derive both answers from one, or make the second impossible* — and the frozen surface is by construction the artefact that rule forbids — **but it is not a test double** (the maintainer's correction, `v0.35.102`).  It is the *execute* phase of this project's build → freeze → execute architecture: `Model.freeze` takes the **builder**'s `IntermediateState` to a `FrozenSystemState`, and `Platform/Boot.lean`'s `bootToRuntime_invariantBridge_empty` — *boot to runtime* — proves `proofLayerInvariantBundle ist.state ∧ apiInvariantBundle_frozen (freeze ist)`, which is a bridge worth proving only if the runtime is meant to run on the frozen representation.  What is missing is the dispatch, and that is C.1 row 14.  So the two implementations are an **interim**, and the differential is not a test of a model — it is the evidence that would license ending the duplication | Not a soundness gap in the shipped kernel **while C.1 row 14 stays open**, which is the whole of the mitigation and is worth stating as the measurement rather than the impression: `API.lean` contains no occurrence of `FrozenOps` or `frozen` at all, `kernelStateRef` is an `IO.Ref SystemState`, `bootAndInitialiseFromPlatformOn` installs `ist.state` rather than `freeze ist`, and `Model.freeze` has **no executable caller anywhere under `SeLe4n/`** — its occurrences in `Platform/Boot.lean` are all inside theorem statements.  The freeze is proved and not run.  (The `v0.35.27` reading *“linked into no image and built only by its own `lean_exe`”* was retired at `v0.35.60`, which put the subsystem in the library root, so it compiles into the static archive like any module; the clause survived five cuts past its own retraction, in the column a reader takes as evidence.)  **A mitigation resting on another registered row's non-closure is a deferral, not a defence**: under the intended architecture every divergence this row records is a *live kernel* defect that has been deferred rather than a test-double defect — the frozen pop missing `donationRecipientAcceptable`, `frozenSchedContextConfigure` propagating neither thread-owned parameter, the frozen bind carrying one of four refusals.  Those are operations the execute phase would run.  Every instance found is closed, and `v0.35.27` narrowed the recurrence at its site by mirroring the live function that **completes** a step (`frozenApplyReplyDonation`) rather than the one nested inside it, so the pairing is structural rather than remembered.  What it constrains is the value of the surface itself: a model-fidelity surface whose fidelity is checked by a hand-written list reports agreement it has not measured | **Partly mechanised at `v0.35.28`**, at the cause rather than at the instances: the coverage table's claim was leg-level (`frozenEndpointReply` against the bare `endpointReply`) and was read as operation-level, so "reply: checked" stood through four rounds of the composite missing a step.  `frozenBranchOperationChecked` / `frozenBranchOperationUncheckedReason` state the operation claim separately, three `decide` interlocks keep the table honest, and `operationDifferentialScenarios` backs it with executions reconciled in both directions.  It found a fourth divergence on its first run (`frozenUpdatePipBoost` re-bucketing from the old priority's bucket rather than from wherever the thread was).  **Round 22 closed the next layer down, and the finding is why the row is worth keeping open**: the operation claim named its live counterpart in a *comment*, and the name was the superseded single-core `endpointReplyWithDonation` — no production caller, and a reply leg that keeps the `replier == expected` gate the live cross-core spelling dropped, so the claim was false on every delegated reply-cap holder while the fixtures (replier = recorded server) could not see it.  The leg claim one table above had the identical defect, unswept.  Both counterparts are **data** now (`frozenBranchLiveOperation`, `frozenBranchLiveLeg`, each with a both-directions interlock), pinned by the theorem pair `endpointReplyCrossCoreDispatch_independent_of_replier` / `endpointReplyWithDonation_refuses_delegated_replier` rather than by a name, with a delegated scenario on each claim whose control asserts the superseded composite refuses it; and the live steps outside the operation differential — `replyTransferOnCore`'s fault branch and the WS-RA delivered-message staging — are stated (`frozenBranchOperationFrontier`) instead of implied.  **And `v0.35.38` (WS-HP HP4.7) found the next layer down, which is why the row stays open**: the one operation-checked branch's scenario had **never fired the pop**.  Through rounds 13, 14, 15 and 22 `FO-041` grew halves for delegation, a stale boost and a missing guard, and not one of them gave the recorded server a `.donated` binding — so the donation return round 13 *added to this surface* was compared on neither side, and round 15's operation-level claim, built to stop exactly that substitution, was resting on it.  A scenario registered against a claim is not a scenario that exercises the step the claim is about.  `FO-042` pops on both sides with the post-state asserted on the live one, its second half is the state where the two candidate triggers disagree — the mutation that decides the flip — and its third is a guard the frozen pop was simply **missing**: HP4.6's `donationRecipientAcceptable`, absent from `frozenReturnDonatedSchedContext` under a docstring stating in terms that *"both of the live guards come with it"*.  That sentence was true when written and false from HP4.6 onward, and the failure direction is the one this row is about — the mirror **succeeds** where the kernel refuses.  Asking it once more found the live step's **ID promotion** missing as well — `applyReplyDonation` refuses a holder `ThreadId.toValid?` will not promote — so a SchedContext bound to the sentinel thread took a different arm on each surface.  Both were found by asking the question the trigger flip raises, not reported; which is the row's own point, since nothing in the coverage machinery would have surfaced either.  **And `v0.35.101` found the next layer down, on the SCHEDULER rather than the object store** (PR #897, two reported and a third swept).  The frozen run queue is keyed by `TCB.boostedPriority`, and the mechanics of moving a thread between buckets were spelled **inline** in `frozenUpdatePipBoost` — so the boost half of that key had an answer and the *base* half had none, and three frozen writers of a base priority moved no bucket at all: `frozenSchedContextBind`, `frozenWriteBasePriority` (the shared writer `v0.35.99` had created one cut earlier), and `frozenSchedContextConfigure`, which propagated neither thread-owned parameter and so falsified `boundThreadPriorityConsistent` **and** `boundThreadDomainConsistent` on every post-configure state with a bound owner.  Two things this instance adds to the row's case.  The scenario that *built* the defective state already existed and was blind to it — `FO-047`'s eight per-object assertions passed while the bound thread sat in the band the bind had just left — so the decisive assertion is a whole-state `frozenRunAgrees` over the operation rather than another field comparison.  And the two `v0.35.99` priority witnesses are *structurally* blind, because their two fixtures do not correspond: `mkState`'s `runnable` defaults to `[]` while `frozenStateOf` queues every `.ready` TCB, so the frozen side queues threads the live side does not and the live re-bucket is the identity on them.  **A differential whose two fixtures differ in the field being compared cannot measure that field**, which is a fixture-level form of this row and worth checking whenever a new comparison is added.    **What remains**: twelve branches carry stated reasons rather than operation-level differentials, two of them findings in their own right — the live `.call` and `.receive` rendezvous each donate a scheduling context (`applyCallDonation`, `applyReceiveRendezvousDonation`) and this surface mirrors neither.  Mirror those two donations and give each remaining branch an operation-level scenario, so every mirror is exercised against the whole live operation.  **And that remedy is C.1 row 14's own precondition, which row 14 does not name**: it gates the dispatch switch on RPi5 freeze→operate→thaw *benchmarks* and states no correctness gate at all, so as registered the switch could be taken on performance evidence while this row is still open — a precondition living in neither row, which is the shape `CLAUDE.md` warns about.  The two are one piece of work read from opposite ends and the cross-reference is now in both; **before v1.0.0** |
| **Two live `.reply` composites answer the authority question differently** (registered `v0.35.35`).  `endpointReplyOnCore` — the leg the live `.reply` arm dispatches — dropped the `replier == expected` gate at PR #822 review 6J-lYm, on the stated ground that authority is the presented reply capability and seL4-MCS reply caps are delegatable; the **bare** single-core `endpointReply` (and `endpointReplyRecv` beside it) still carries it, and `endpointReplyWithDonation` inherits it by composition.  So a delegated reply-cap holder is admitted by the spelling the kernel runs and refused by the spelling `endpointReplyOnCore`'s own docstring calls the function it mirrors.  Found at PR #895 review round 22, by way of a frozen-surface coverage claim that named the refusing spelling as its counterpart.  Pinned in both directions by `endpointReplyCrossCoreDispatch_independent_of_replier` and `endpointReplyWithDonation_refuses_delegated_replier`, so the divergence is a machine-checked fact rather than a comment | **Not a soundness gap and not user-reachable.**  The direction is fail-*closed* — legitimate delegated authority declined, never illegitimate authority admitted — and `endpointReplyWithDonation` has no production caller: the live arm routes through `replyTransferOnCoreChecked` → `endpointReplyCrossCoreDispatch`.  What it constrains is what new code may assume: a caller reaching for the single-core composite gets delegation-refusing behaviour the kernel does not have, and the docstrings claiming a mirror relation between the two are now explicit about the divergence | Give the question one answer by retiring the gate from the bare spellings (it moves the trace-harness authority fixture and the `unfold endpointReply` proofs); post-v1.0.0 hardening unless a caller of the single-core composite appears first |
| WS-RC **R7** — CDT `descendantsOf` fuel-sufficiency proofs; `descendantsOf_fuel_sufficient` proves only `edges.length ≥ 0` | Proof hygiene: CDT operations are sound under the fuel-bound discipline; the sufficiency theorem is defence in depth | post-v1.0.0 hardening |
| WS-RC **R14** — the v1.x backlog the WS-RC plan deferred | Explicitly scoped out of v1.0 closure by that plan | post-v1.0.0 |
| WS-RC R4's two deliberate follow-on type-level promotions, registered only inside [`docs/planning/WS_RC_R4_TYPE_LEVEL_PROMOTION_PLAN.md`](planning/WS_RC_R4_TYPE_LEVEL_PROMOTION_PLAN.md), a plan marked COMPLETE | Both are strengthenings of an already-sound surface | post-v1.0.0 hardening; SM10.6.2 archives the plan, so the items must not travel with it |
| `REPLY_OBJECTS_COMPLETION_PLAN.md`'s deferred follow-up item 4 (reply-object lock stress) named a tracking home that carries no entry for it | The plan it named is a live plan; the item simply never arrived there | post-v1.0.0; the reply-object plan's status header now points here |
| The v0.32.148 queued-lock closure — [`docs/planning/SMP_PANIC_HANG_REMEDIATION_PLAN.md`](planning/SMP_PANIC_HANG_REMEDIATION_PLAN.md)'s SM2.E remediation, whose header and two-thirds of its body still describe the MCS queue that cut deleted — was recorded in an unrelated plan and never reached this file | Bookkeeping, not behaviour: the closure itself is real and in the tree, and the shipped artefact is a ticket lock | recorded here by RR0.9; the plan-side prose is SM10.2's work-list (register §7 row 33) |
| Idle TCBs carry `ObjId.sentinel` cspace/vspace roots | The idle thread never faults a user mapping; it becomes live work when RR5.14 repoints the production boot wrapper at the idle-installing entry, and that row must not close over it | RR5.14 must either give idle TCBs real roots or re-register this row |
| SM9.D.11 — taint propagation at capability transfer shipped as a scope reduction, leaving `capabilityBadgeChannel_out_of_scope`: a registered false-negative channel in the causal detector | A false *negative* in a detector, not a policy bypass | post-v1.0.0; RR0.11 routes the plan-side row |
| The SM8 class-C follow-on: the CC-1 capacity figure is stated in two places rather than single-sourced | Cosmetic duplication of a bound both sites agree on | post-v1.0.0 |
| ARM CCA + MPAM hardware partition isolation | Targets a successor SoC; not RPi5 | [`docs/planning/HARDWARE_PARTITION_ISOLATION_PLAN.md`](planning/HARDWARE_PARTITION_ISOLATION_PLAN.md), unscheduled |
| **R-ABI-L6** — cross-crate duplication of `MAX_METHOD_COUNT`, `MAX_PRIORITY`, `MAX_DOMAIN` and `MAX_SERVICE_MESSAGE_SIZE` between `sele4n-abi` (limits) and `sele4n-types` (identifiers + error enums) | Duplication, not divergence — the values agree and the ABI conformance tests compare them; the risk is that a future edit changes one copy | post-v1.0.0 hardening.  Recorded in [`docs/AUDIT_NOTES.md`](AUDIT_NOTES.md) §R-ABI-L6, which declared itself untracked and is the one v0.29.0 R-ABI item still open — L3, L4, L5, L7 and L8 record settled decisions, not deferrals.  Found during the RR0 review round (v0.34.31), not by the pre-SM10 audit |
| `AsidPool.allocate`'s rollover scan is quadratic: `List.range (maxAsidValue - 1)` filtered by `List` membership in `activeAsids`, so a rollover against a nearly full ASID space walks up to 65,535 × 65,535 comparisons — the compiled fail-closed regression (`tests/AsidPoolSuite.lean` T05) takes ~38 s, which is also what a saturated rollover would cost the live kernel (test-performance audit, v0.34.47) | Correct and proven (`allocate_result_fresh`, `allocate_preserves_wellFormed`); rollover against a saturated space is not on any live path before SM10.1, and the fix is a representation change under existing proofs (a sorted or bitmap `activeAsids`, or the `activeCount` early exit) rather than a semantic one | post-v1.0.0 performance pass; the test keeps exercising the real scan until then |
| The 32 in-source post-1.0 hardening candidates enumerated below | Mostly strengthenings of surfaces that are already correct — but **"none is a soundness gap" was wrong of rows 14–17**, and the correction is the useful part.  Those four deferred putting `FrozenOps` in the production import chain, which is what places a module inside the *derived* domain of the Tier 1 censuses and the production/staging partition gate.  Outside it the subsystem was exempt from five of the six censuses and from the partition gate entirely, and five real defects shipped through the hole (`v0.35.12`, `v0.35.38`, `v0.35.47`, `v0.35.52`, `v0.35.58`) on a surface carrying the **live** `TCB`, `Reply`, `SchedContext` and `IntrusiveQueue` records.  **A deferral that removes gate coverage is not a deferred strengthening** — it is a standing exemption from the checks, and it reads like the former because the item is phrased as work not done rather than as protection withdrawn.  Rows 15–17 closed at `v0.35.60`; row 14 is re-scoped to the architectural dispatch switch, which the RPi5 benchmark genuinely gates.  A reader triaging the remaining 28 should ask of each whether it withdraws a check or merely postpones a strengthening | post-v1.0.0 hardening, listed individually so none ages out with its comment |
| `crossSubsystemFieldSets` lists 11 field-sets while `crossSubsystemInvariant` has **12** conjuncts: `untypedRegionsDisjoint` was appended without a matching `_fields` entry, so the pairwise disjointness analysis and every frame lemma derived from it cover 11 of the 12 | Incompleteness, not unsoundness — the uncovered predicate simply gets no frame lemma, so proofs needing it establish it directly; nothing false is proved | post-v1.0.0; closing it means adding `untypedRegionsDisjoint_fields` and redoing the analysis over C(12,2) = 66 pairs.  Found during the RR0 review round (v0.34.27), not by the pre-SM10 audit |
| The IPC and capability write-sets (`ipcEndpointOp_modifiedFields`, `capabilityOp_modifiedFields` in `SeLe4n/Kernel/CrossSubsystem.lean`) carry `storeObject`'s three index/ASID fields (`objectIndex`, `objectIndexSet`, `asidTable`), which those operations write only vacuously — an in-place store of a key already present, and never of a `.vspaceRoot`.  The v0.34.70 docstring called tightening them back out "registered rather than assumed" and registered it nowhere; the RR7 audit round (v0.34.109) registers it here | Over-declaring costs disjointness, never soundness (`preservesFieldsOutside_mono`): the only loss is that a frame lemma needing those three fields disjoint from an IPC or capability write is unavailable until the two conditional facts are proved — that `RHSet.insert` is a no-op on a present key, and that neither family ever stores a `.vspaceRoot`.  No consumer needs the narrower lists today | post-v1.0.0 hardening; the two conditional facts, then the lists narrowed with their `_preservesFieldsOutside` theorems re-proved at the narrower lists |
| **A gate's registry is indexed by string keys, and `check_anchor_symbol_liveness.py` only resolves Python *symbols*, so a Tier 3 anchor naming a retired registry KEY reads as coverage** — found at `v0.35.97` as the **fourth** artefact watching one change, and found by Tier 3 rather than by the sweep: after the third failure this project's *sweep what was pinning the thing you deleted* rule was run, and run against the retired **pattern** the red gate had named, so the anchor pinning the retired `WRITE_PRIMITIVE_BODIES` key for `frozenUpdatePipBoost` stayed invisible.  The anchor itself is repointed and both directions are mutation-tested, so nothing is open in the tree; what is open is the **mechanism**.  `check_anchor_symbol_liveness.py` refuses a Tier 3 anchor naming a Python symbol nothing reads, resolving the reference through `symtable`; the registries these gates are reconciled against (`WRITE_PRIMITIVE_BODIES`, `ACCESSOR_BODIES`, `OBJECTS_FIELD_OWNERS`, `NON_RUST_KEYWORD_SOURCES`, …) are keyed by **string literals**, which that resolution cannot see at all — so an anchor pinning a key the dict no longer holds fails only when a Tier 3 run reaches it, and one pinning a key that was *never* there would fail at once while one pinning a key deleted later fails silently in the other direction if the anchor is a negative.  Severity **none** as a live defect (Tier 3 is a required gate, so the failure is loud, merely late and un-swept); the cost is that the cut's author cannot derive the set of artefacts a deletion invalidates, which is precisely the rule this file now states three times.  The remedy is to extend the liveness gate's domain from bound names to **dict-literal string keys**: parse each registry's keys with `ast`, collect the string literals every Tier 3 anchor names, and fail on an anchor naming a key no reconciled registry holds.  Deferred because it is a gate cut of its own and this one was already a review fix. | `scripts/check_anchor_symbol_liveness.py`, `scripts/test_tier3_invariant_surface.sh` | none pre-v1.0.0 |
| ~~**`returnDonatedSchedContext`'s bottom arm rebinds a recipient `.bound` without refreshing the reservation's recorded `priority`**~~ — **CLOSED `v0.35.133`**, by the row below rather than by anything narrower, exactly as this row predicted.  With `TCB.priority` the only home there is nothing for the pop to refresh, no field is copied between objects, and the projection objection that refuted the instance fix at `v0.35.100` has no subject: the pop writes no band at all.  `donationReturnSchedContext_priority` and its Tier 3 negative stay, because *why* the pop leaves the configured band alone is still worth stating at the record.  **v1.0.0 may now claim that a completed call chain restores the origin's scheduling band.** | `SeLe4n/Kernel/IPC/Operations/Endpoint.lean`, `SeLe4n/Model/State.lean` | CLOSED |
| ~~**A `.bound` thread's base priority has two homes rather than one**~~ — **CLOSED `v0.35.133`**.  `TCB.priority` is the only home; five readers (`SystemState.threadBasePriority`, `resolveEffectivePrioDeadline`, `effectiveSchedParams`, `getCurrentPriorityChecked`, `FrozenSystemState.threadBasePriority`) now read it at every binding, `effectiveBucketPriority` — the THIRD copy of the resolver, kept only for an import cycle — collapsed to `TCB.boostedPriority` with its pin becoming `rfl`, and both run-queue invariants' `.bound` arms moved off the reservation's field, which was over-strong the moment the base had one home — and then **collapsed entirely**, since all three arms had become one statement and the `.bound` arm's `| _ => True` fallback was silently excusing a bound thread whose reservation did not resolve.  Three theorems whose hypotheses the cut killed (`boostedPriority_eq_resolve_unbound`, `resolveEffectivePrioDeadline_fst_eq_boostedPriority_of_agree`, `resolveEffectivePrioDeadline_fst_of_donated`) retire into the one unconditional `resolveEffectivePrioDeadline_fst_eq_boostedPriority`, because a name like `_of_agree` kept past its hypothesis teaches a false dependency — that weakening the bind/configure propagation would cost the selector its priority ordering, which it does not.  `SchedContext.priority` survives on the write side alone as the band a reservation configures its bound thread to; `boundThreadPriorityConsistent` is no longer load-bearing for any read and remains as the statement that the propagation happened.  **Behaviour-preserving on every reachable state, measured**: `schedContextConfigurePropagates` is the very condition the retired resolver classified on, the library builds 518/518 and the golden trace is byte-identical at 239/239.  No proof was weakened: eleven collapsed to `rfl`, two shed a case analysis, nine were deleted with the readings they were about, and one is new. | `SeLe4n/Model/State.lean`, `SeLe4n/Model/FrozenState.lean`, `SeLe4n/Kernel/Scheduler/Invariant.lean`, `SeLe4n/Kernel/Scheduler/Operations/Selection.lean` | CLOSED |

| **The `ConstantInfo`-asker inventory reads Lean probes that are Python strings, so it must model Python's string construction** — registered `v0.35.132` after the sixth consecutive PR #897 review round found a defect in that model (`.format`/`%`/f-string reconstruction, splice completion, subject keys, named templates, a raw-text prefilter, an alias hop).  The gate's question — *which `ConstantInfo` constructors does each asker match* — is textual, and the text is **built at runtime**, so a static reader is an approximation whose defeating spellings are unbounded while the ones it has seen are finite: this file's own rule, at the level of a whole artefact rather than one pattern.  **Not closable by a canonical spelling**, and that was measured rather than assumed: all 41 probe *assignments* are plain literals, but three `@SENTINEL@` `.replace` sites substitute *computed* values (`", ".join(...)`), so a "substitute with literals or refuse" rule would refuse every real probe in the repository.  The structural exit is to stop approximating a string the program can simply **print**: each probe-bearing gate grows an `--emit-probe` mode that writes the exact source it hands to `lake env lean`, and the asker inventory reads that instead of reconstructing it — which deletes `_reconstruct`, `_reconstruct_holed`, `_HOLE`, `_constructor_completing_holes`, `_unreadable_assemblies`, `_module_string_bindings`, `_probe_alias_bindings` and every future round of this class, and is *exact* where the reader is a floor.  Severity **none** as a live defect (the gate is a floor over recognised forms and says so); the cost is a review round per cut that touches it.  **Deliberately not taken in `v0.35.132`**, which fixed the two reported instances and measured the exit rather than performing it: it touches five gates' probe plumbing and belongs in a cut of its own. | `scripts/check_declaration_kind_askers.py`, `scripts/check_content_flow_coverage.py`, `scripts/check_live_arm_per_core_routing.py`, `scripts/check_module_axioms.py`, `scripts/check_tlbi_broadcast_discipline.py`, `scripts/check_ipc_invariant_dethreading.py` | post-RR8 |
| **`scThreadIndexConsistent` is prose, not a predicate** — `SystemState.scThreadIndex`'s docstring (`SeLe4n/Model/State.lean`) states the index's consistency property in as many words, and no Lean definition says it: the donation push and pop maintain the index correctly at every depth (the donor is cleared either way, and `donationReturnBinding`'s two arms name the same context, so `donationReturnBinding_scId?` makes the index insensitive to stack depth), but nothing *states* that they do.  Registered at `v0.35.2` as WS-OD closes, rather than left in the plan that named it | A documented-but-unstated invariant on an index every donation-carrying footprint already serialises through `stateLevelLock`; the property is true and maintained, so this strengthens a correct surface rather than closing a gap | post-v1.0.0 — the implement-the-improvement candidate is a `scThreadIndexConsistent` definition plus preservation at the four writers |

| **An endpoint queue's membership lives in its members' TCBs, whose labels differ from each other's — so a removal writes an observable thread's link field, and an observable thread's projection already names a non-observable one** — registered at `v0.35.83` by WS-RR RR8.8, which set out to close the three cancellation-NI obligations against the remedy the register had scheduled and found that remedy refuted.  `TCB.queuePrev` / `queuePPrev` / `queueNext` **survive** `projectKernelObject`, and the live admission gate is `endpointFlowGate ctx ep (threadLabelOf sender) (endpointLabelOf ep)` — an **order**, not an equality — so a lower-labelled and a higher-labelled sender are both admitted onto one higher-labelled endpoint (`endpointAdmissionAdmitsMixedObservability`, `decide`-checked, `SeLe4n/Kernel/InformationFlow/Projection.lean`).  Two consequences.  **(1) Standing**: an observer cleared for the lower sender sees that thread's TCB with `queueNext` naming the higher one, so the low projection contains a high thread's identity with no operation having run — the same class as the `replyObject` erasure SM6.D landed for exactly this reason, on a field that names a thread instead of a Reply.  **(2) On removal**: splicing the higher thread out rewrites the lower one's links, which is the one write class of the cancellation teardown that no labelling fact can cover, and it is why `abortHolderProjectionStable` and the three queue arms' `hTeardownProj` cannot be discharged today.  At `v0.35.84` the *other* two classes were discharged and this one isolated: `abortHolderSpliceHigh_of_victimHigh` derives the endpoint object's and the holder's own non-observability and takes the neighbour clause as its one parameter, so what this row owes is now a single named clause of `endpointSpliceHigh` rather than a whole projection obligation.  **Severity Medium.**  Not exploitable and not a false theorem: the tree's NI results are true, and what the leak costs is their *strength* — low-equivalence is finer than it should be, so "a low observer cannot distinguish" claims less than the prose suggests.  Nothing boots before SM10.1 and the SVC seam halts; and the shipped RPi5 binding is immune, `confinedLabelingContext` passing `lowTrusted` / `highUntrusted`, which are **mutually** non-flowing (`lowTrusted_highUntrusted_mutually_isolated`), so it admits no cross-label endpoint flow at all and its queues are uniform by construction.  What is exposed is a first-class member of the same production family: `indexPartitionedLabelingContext` obliges only `lowerLabel ≠ upperLabel`, so applying it to `publicLabel` / `kernelTrusted` — the one-way flow `securityFlowsTo_prevents_label_escalation` documents as intended — exhibits it.  **Why it may wait, and what the remedy is.**  A *labelling* remedy does not exist: uniformity is unestablishable, and narrowing the gate to equality would refuse a lower-labelled client's send to a higher-labelled server.  Stripping the links at projection time is **unsound** rather than merely coarse — the projection would no longer determine the next dequeue, so two low-equivalent states would step to states differing in the endpoint's own visible `head`, and the step-level NI theorems would become false.  The remedy is therefore representational and is forced: **a queue's content must live in an object whose label dominates every member's**, which the endpoint is (`endpointFlowGate_implies_securityFlowsTo`) and a member's own TCB is not — i.e. make the endpoint and notification queues **non-intrusive**, the list in the endpoint object.  Then a removal writes only that object, which is non-observable whenever any member is, no neighbour TCB is written at all, and an observable endpoint provably has only observable members, so nothing leaks in either direction.  Measured before scheduling: **2272 whole-word occurrences of `queueNext` / `queuePrev` / `queuePPrev` across 67 tracked `.lean` files** (`rg -o -w 'queueNext|queuePrev|queuePPrev'` over `git ls-files '*.lean'`), plus the dual-queue invariant surface stated over the links — far past RR8, whose remaining rows are bookkeeping, and past WS-BP and WS-CB, neither of which may open until RR8 closes.  So it is registered here rather than absorbed, and **v1.0.0 must not claim that a low observer cannot learn a high thread's identity from endpoint queue state** | **no pre-v1.0.0 owner**; opens as its own workstream after WS-BP.  RR8.8 closes having reduced `abortHolderProjectionStable` to this one class; RR8.9 closes outright, needing none of it |
| **`test_lib.sh`'s code-view classifier refuses a check whose TEXT contains `source `, not one that RUNS it** — `_scans_lean_source` reads the whole concatenated argv and answers "not a Lean scan" for any `bash` command containing `*lake*`, `*scripts/*` or `*"source "*`, so that `bash -lc 'source ~/.elan/env && lake build …'` cannot run inside the code-view overlay; `_run_with_view`'s fail-closed branch then **refuses** such a command outright when it also mentions `.lean`.  That is a presence check standing in for "this script invokes the shell builtin `source`", and an anchor whose *pattern* merely quotes the word is refused with it — `v0.35.121` met it on a Lean binder named `source`, with a refusal message naming a shape the anchor does not have. | **Fail-closed, so a usability defect rather than a hole**: it refuses rather than reading raw text, which is the direction this project chooses everywhere else, so nothing is silently unchecked.  The tighter test is command position (`-c source `, `&& source `, `; source `), still an approximation because the argv is concatenated before the guard sees it and an exact answer needs the shell lexer `check_identifier_naming.py` already carries.  Owner: the harness, in its own cut with witnesses in `scripts/test_code_view_wiring.sh`, since `test_lib.sh` is what every tier routes through. |
| **The satisfiability gate cannot compare two BOUNDED-GAP anchors, which is now the tree's dominant anchor form** — `check_anchor_consistency.py`'s containment reasoning rests on `_literal_runs`, which refuses a pattern carrying a quantifier or a class, so a bounded gap (`[^\n]*(\n([ \t][^\n]*)?)*`) is undecomposable and that family is reached only by the exact-key comparison.  `v0.35.121` exhibits a live pair the gate passed: a retired `v0.35.86` positive and a new negative sharing their anchored prefix AND their gap, differing only in the tail, with the negative's tail a literal prefix of the positive's — so every match of the positive contains a match of the negative and no tree satisfies both. | **The inference is sound and narrow**: same anchored prefix, same gap regex, one tail containing the other as literal text.  It may wait because the *dynamic* answer is stronger and is arriving — `scripts/check_changed_file_anchors.sh` runs the anchors of every file a cut changes, which decides a positive's satisfaction rather than its consistency, and found this pair in seconds.  Owner: the anchor-consistency gate, after the sweep lands, so the two can be measured against each other rather than assumed complementary. |
| **The two information-flow gate facts are established at the WRITE and inhabited by the BOOT, and not yet lifted to the two checked dispatches that perform the write** — registered at `v0.35.126` by WS-RR RR8.16, the remainder of the PR #897 review finding that `blockedSenderFlowsToEndpoint` and `donationOwnerFlowsToHolder` (`SeLe4n/Kernel/InformationFlow/Invariant/Composition.lean`, `v0.35.83`/`v0.35.84`) were *only* consumed as hypotheses.  **What landed.**  Transport: `blockedSenderShrinks` with `.refl`, `.trans` and `blockedSenderShrinks_of_ipcStateFrame` — weaker than `ipcStateFrame` on purpose, since a send rendezvous writes the receiver `.ready` and a wake writes a runnable state, so neither frames every `ipcState` while both leave the blocked-sender set no larger — plus `donationOwnerFlowsToHolder_of_sameSchedContextBindings` for the sibling.  Derivation: `donationFlowFromBlockedDonor` makes the donation fact a **consequence** of its sibling and the receiving gate (`label owner ⊑ label ep ⊑ label holder`), so only one of the two needs an independent story.  Establishment: `storeTcbIpcStateAndMessage_preserves_blockedSenderFlowsToEndpoint`, stated at the one production write of a blocking `ipcState` with the endpoint gate as an argument.  Inhabitation: `bootFromPlatformCheckedWithIdleThreads_flowGateFacts` proves **both** facts of the production boot state for **every** labelling context, from `bootFromPlatformCheckedWithIdleThreadsFor_ok_tcb_quiescent` (every stored TCB `.ready` and `.unbound`) — so neither is an assumption nothing exhibits.  **What is owed**: the per-transition lift from that store to `endpointSendCrossCoreDispatchChecked` and `endpointCallCrossCoreDispatchChecked`, whose refusal branches are `blockedSenderShrinks.refl` and whose admitted branch needs a per-TCB dichotomy for `endpointSendDualWithCapsOnCore` / `endpointCallDualWithCapsOnCore` — the shape `storeTcbIpcStateAndMessage_tcb_backward_fields` has for the primitive and no composite in the tree has for itself.  Measured rather than estimated: the send composite is a cap transfer, a queue splice, a store, a wake and an SGI computation, and `grep` finds no `_tcb_backward`, `_dichotom` or per-TCB pullback for either; writing one is a cut of its own, not a rider.  **Also owed, and a design decision rather than a proof**: neither fact can join `ipcReachable`, which is not parametrised by a `LabelingContext`, so a `ctx`-parametrised reachable-state pack would have to be created — the reviewer's third clause.  **Severity Low**: no theorem is false and no state is mis-described; what the gap costs is that a consumer of the cancellation-NI reductions must still carry the fact by hand across a send or a call rather than reading it off the pack.  Nothing boots before SM10.1.  **Closure target: before v1.0.0** | `SeLe4n/Kernel/InformationFlow/Invariant/Composition.lean`, `SeLe4n/Kernel/IPC/CrossCore/EndpointSend.lean`, `SeLe4n/Kernel/IPC/CrossCore/EndpointCall.lean`, `SeLe4n/Kernel/IPC/Invariant/Reachability.lean` | WS-RR **RR8.16** (partially closed `v0.35.126`) |

#### C.1 — The 32 in-source post-1.0 hardening candidates

Each of these stated in its own docstring that "no currently-active plan file
tracks it".  That sentence made the deferral self-describing and unfindable at
once: a reader could only meet it by opening the file it lived in.  They are
enumerated here, and the source comments now point at this table instead of
declaring themselves untracked.  Line numbers are the `v0.34.26` positions and
will drift; the identifier beside each is stable.

| # | Site | Deferred item |
|---|------|---------------|
| 1 | `SeLe4n/Platform/Boot.lean` (`applyGicTimerSetup`) | TLB/ASID maintenance for HAL-parity boot |
| 2 | `SeLe4n/Platform/Boot.lean` (`bootFromPlatform`) | Minimum-configuration validation (≥ 1 initial thread, valid scheduler state) |
| 3 | `SeLe4n/Platform/Boot.lean` (bundle bridge) | Builder operations preserve only 4 of the 12 `proofLayerInvariantBundle` components for general configs |
| 4 | `SeLe4n/Platform/RPi5/MmioAdapter.lean` (P-L2) | `readCString` fuel-exhaustion return-type upgrade |
| 5 | `SeLe4n/Platform/RPi5/MmioAdapter.lean` (P-L4) | `extractPeripherals` beyond 2-level DTB nesting |
| 6 | `SeLe4n/Platform/RPi5/MmioAdapter.lean` (P-L5) | Multi-core extension of the MMIO write-sequence atomicity argument |
| 7 | `SeLe4n/Platform/RPi5/MmioAdapter.lean` (P-L11) | FFI `opaque BaseIO` contract bridging |
| 8 | `SeLe4n/Model/Builder.lean` (AF5-F) | Anonymous invariant tuples → named structures (100+ destructuring proof sites) |
| 9 | `SeLe4n/Model/Object/Structures.lean` (`descendantsOf_fuel_sufficient`) | Substantive CDT fuel-sufficiency proof; today it proves `edges.length ≥ 0` |
| 10 | `SeLe4n/Model/Object/Structures.lean` (`CdtChildReachable`) | Transitive-closure bridge from reachability depth to BFS fuel bounds |
| 11 | `SeLe4n/Kernel/Service/Invariant/Acyclicity.lean` | Section-scope split of the acyclicity proof idiom |
| 12 | `SeLe4n/Kernel/Service/Operations.lean` (`serviceBfsFuel`) | Rename to match the DFS-equivalent search it implements (~77 call sites) |
| 13 | `SeLe4n/Kernel/IPC/Invariant/Defs.lean` (`donationChainAcyclic`) | Formal bridge from donation edges to `blockingAcyclic` for cycles of length > 2 |
| 14 | `SeLe4n/Kernel/FrozenOps/Core.lean` | **Re-scoped `v0.35.60`, still open**: `API.lean` dispatching syscall processing *through* the frozen monad — the architectural two-phase switch, which AG8-D rationale point 4 gates on RPi5 freeze→operate→thaw benchmarks that do not exist. The *import-chain* half is closed (rows 15–17): being in the chain is not being the dispatch path.  **Re-scoped again `v0.35.102`: the performance gate was the only one stated, and it is not the binding one.**  The switch also requires the frozen surface to *agree* with the live one on every operation it would dispatch — which is table C's open frozen-fidelity row, unnamed here until now, so this row as written admitted taking the switch on benchmark evidence alone.  Correctness gate first, then benchmarks |
| 15 | ~~`SeLe4n/Kernel/FrozenOps/Commutativity.lean`~~ | **CLOSED `v0.35.60`** — in the production import chain via `SeLe4n.lean` → `FrozenOps.Invariant` → `Commutativity` |
| 16 | ~~`SeLe4n/Kernel/FrozenOps/Invariant.lean`~~ | **CLOSED `v0.35.60`** — imported by `SeLe4n.lean` directly |
| 17 | ~~`SeLe4n/Kernel/FrozenOps/Operations.lean`~~ | **CLOSED `v0.35.60`** — in the chain via both root imports |
| 18 | `SeLe4n/Kernel/RobinHood/Bridge.lean` (DS-L2) | `Except`-returning `insertNoResize` (~50 call sites) |
| 19 | `SeLe4n/Kernel/Lifecycle/Operations/ScrubAndUntyped.lean` (`retypeFromUntyped_atomicity_under_sequential_semantics`) | Re-establishing retype atomicity under SMP/preemption on real hardware |
| 20 | `SeLe4n/Kernel/CrossSubsystem.lean` (`collectQueueMembers_fuel_sufficiency_documented`) | `QueueNextPath` → `queueNext` traversal bridge (the IPC subsystem's sole remaining TPI-DOC item) |
| 21 | `SeLe4n/Kernel/CrossSubsystem.lean` (`crossSubsystemInvariant`) | Compositional proof that all **12** predicates hold under arbitrary interleaving of all 34 operations |
| 22 | `SeLe4n/Kernel/Capability/Invariant/Defs.lean` (AF5-F) | Right-associative `∧` chains → named structure |
| 23 | `SeLe4n/Kernel/API.lean` (`resolveExtraCapsDetailed_empty`) | Fold-level induction generalising swap-invariance beyond the empty-input base case |
| 24 | `SeLe4n/Kernel/Architecture/Invariant.lean` (`retypeFromUntyped` disjointness, ~L1800) | Full-coverage proof for transitive multi-level untyped nesting — a richer invariant (root-restricted disjointness or transitive-ancestor exclusion) |
| 25 | `SeLe4n/Kernel/Architecture/Invariant.lean` (`retypeFromUntyped_objectOfKernelType_preserves_untypedRegionsDisjoint`, ~L1884) | The `.untyped` → `.untyped` retype target, the one case the six allowed object types do not exhaust |
| 26 | `SeLe4n/Kernel/Architecture/Invariant.lean` (zero-`regionBase` child, ~L1952) | Full-coverage proof for the zero-`regionBase` child case |
| 27 | `SeLe4n/Kernel/CrossSubsystem.lean` (untyped ancestry, ~L466) | Transitive ancestor/descendant tracking via a CDT-style closure — the standalone model-refinement effort rows 24–26 all reduce to |
| 28 | `SeLe4n/Kernel/Scheduler/PriorityInheritance/BlockingGraph.lean` (`blockingChain`) | Formal blocking-cycle detection and removal; today PIP propagation stops at the cycle boundary, leaving stale boosts (conservative — over-promotion, never inversion) |
| 29 | `SeLe4n/Kernel/RobinHood/Bridge.lean` (DS-L5) | Restructuring the 400K–800K-heartbeat `Lookup.lean` / `Preservation.lean` proofs into smaller lemma units |
| 30 | `SeLe4n/Kernel/RobinHood/Bridge.lean` (DS-M04) | Entry-wise correctness proof yielding `LawfulBEq (RHTable α β)`; consumers must supply `[LawfulBEq β]` at the call site until it exists |
| 31 | `SeLe4n/Kernel/Capability/Operations.lean` (`revokeCdtTransactionalTraversal`) | The monotonicity lemma witnessing that the fold never sets `firstFailure` to `some`; the completion test is what keeps the defensive branch honest meanwhile |
| 32 | `SeLe4n/Kernel/Capability/Operations.lean` (C-L3, `ipcTransferSingleCap`) | A sender-rights field on `CdtEdgeKind`, so a transferred capability's CDT edge records the rights the sender held; closing it moves the 14 CDT-edge composition proofs |

**None is a soundness gap**, and all but one strengthen a surface that is already
correct — a tautological witness replaced by a substantive proof, a fuel bound
made structural, a tuple given a name, a validated-elsewhere precondition
internalised.  That is why they may wait; it is not why they may be forgotten.

The exception was the donation-accounting row (`v0.35.14`), which was neither: it
preserved every invariant and still lost a real-time guarantee, and unlike its
neighbours it constrained v1.0.0's claim set rather than only its polish.  It is
**closed at `v0.35.54`** — the depth-≥ 3 half at `v0.35.45` (WS-HP HP6's
chain-preserving removal) and the depth-2 half at `v0.35.53` (HP10.9's recorded
origin), both **earned** rather than retracted — so what a reader should take from
it now is the shape rather than the debt: it sat here with no owner until
`v0.35.16` registered a plan for it, and a row whose subject is a claim the project
makes needs an owner before it needs a remedy.  Its one surviving fragment, the
footprint/transition resolution asymmetry HP10.8 found, has its own row above.


## WS-SL — scheduler liveness completion

**Owner**: Scheduler subsystem. **Closure target**: post-v1.0.0. Registered as
a row in table C above; the work list is here because it has no plan file.

SM5.J lifted the per-core liveness *predicates* at v0.31.64 —
`eventuallyExitsOnCore`, `higherBandExhaustedOnCore`,
`CanonicalDeploymentProgressOnCore`, `WCRTHypothesesOnCore`, `selectedAtOnCore`
and siblings all read `currentOnCore c` / `runQueueOnCore c`. The trace model
underneath them was not lifted. What remains, in execution order:

| # | Item | Where |
|---|------|-------|
| SL1 | Lift the trace step relation: `stepPrecondition`, `stepPost` and `ValidTrace` read `bootCoreId`, so no `ValidTrace` exhibits a step taken on a secondary core | `SeLe4n/Kernel/Scheduler/Liveness/TraceModel.lean` |
| SL2 | Construct `hBandProgress` rather than externalising it — the FIFO/bucket-rotation composition that would discharge the band-progress obligation | `SeLe4n/Kernel/Scheduler/Liveness/Yield.lean` |
| SL3 | Restate the liveness capstones over the lifted traces, so the SMP starvation-freedom result is a statement about multi-core executions | `SeLe4n/Kernel/Scheduler/Operations/PerCoreWcrt.lean` |

None of the three is a soundness defect: the per-core forms are correct and the
capstones state `hBandProgress` explicitly rather than hiding it. The cost is
that v1.0.0's liveness claim is **conditional** and must be stated that way.
**SM10 may not claim unconditional SMP starvation-freedom.**

## WS-XV — cross-implementation behavioural agreement (**absorbed into WS-BP**)

**Owner**: **WS-BP**, as its phase **BP0**
([`SMP_BOOT_PATH_PLAN.md`](planning/SMP_BOOT_PATH_PLAN.md) §4.1, §5). **Closure
target**: with WS-BP.

This section is retained as the *finding* — the evidence that nominal gates do
not catch behavioural drift — and no longer as a work list. The work is four
rows in that plan, and the merge is not tidying: XV1 was always a WS-BP
obligation and became **BP2.6** at `v0.34.120`; XV2 and XV3 are interim *by
their own text* and are retired by BP2.6; XV4 and XV5 sit on surfaces WS-BP
modifies. Half of WS-XV is deleted by WS-BP's own work and the other half is a
harness over what WS-BP changes, so held apart each document had to describe
the other's schedule to be readable.

The row-by-row mapping is at the end of this section, beside the pairs it
names; stating it twice is how a register drifts from itself.

The tree maintains a verified Lean model beside an executable Rust HAL, and in
several places the same question is answered on both sides.

**For the device-tree pair that duplication is not forced, and the fix is to
remove it rather than to check it.** The justification first written here — that
`init_mmu` needs the device tree's RAM extent before translation is enabled —
is wrong twice over. Parsing a firmware-supplied blob with the MMU off is bad
practice: it runs an attacker-influenced parser in the window with no memory
protection and no recovery but a halt. And it is unnecessary, because boot page
tables do not need the RAM *size*: they need the image `[_start, __bss_end)`,
the primary and secondary stacks, an early heap, a bounded window at the
firmware's DTB pointer, and the device window — every one a linker symbol or a
board constant. The standard arm64 sequence builds the boot map from exactly
those, enables the MMU, and only then maps the FDT and parses it with
translation on. `init_mmu`'s round-4 refusal already enumerates that list; it is
the map, not a check on one.

So the device-tree remedy is **BP2.6**: build the boot map from constants, enable
translation, bring the Lean runtime up, and let the verified Lean parser be the
blob's only reader — which deletes `find_ram_top_in_dtb` and the extent, clamp
and contiguity machinery from the boot path along with the pair. The pairs that
remain genuinely two-sided keep the harness remedy below.

**Round 6 measured the audit's yield.** Two of its four new findings were
divergences `v0.34.115`'s by-hand run had already closed — the multi-node memory
fold and the cell-aware extent — so an independent reviewer and a person reading
the two parsers side by side reached the same defects, one cut apart. The round's
own class-A finding is the same shape one subsystem over: `lockSet_endpointReplyRecvOnCore`
resolved its donation from the delegate's binding while `lockSet_endpointReplyOnCore`
has resolved it from the recorded server since PR #822's review — one question,
two answers, the second never updated. That is eight of twenty-four findings
across six rounds, plus one the branch found itself.

**Round 9 is seven for seven, and one of them is a claim in this branch's own
record.** Four were Devicetree Specification rules the Lean parser did not
enforce and the Rust walker does (§5.1 header compatibility, §5.4.2 node
internal order, §2.2.4 unique property names, §2.3.4 inherited availability),
one was both reservation sources never subtracted from discovered RAM, one was
`rpi5RuntimeContract.memoryAccessAllowed` still reading the canonical 4 GiB map
after `bindMachineConfig` began installing variants — and one was
`check_devicetree_legacy_consumers.sh`, which `v0.34.117`'s changelog and review
reply both said would catch a reintroduction of the walker it removed. It would
not: the host-file exemption was unconditional, so the walker could return to
exactly the place it was deleted from. That is **twenty of thirty-six** across
nine rounds, and the last of them is the sharpest evidence this row can carry —
a *claim about a gate* is as capable of being nominal as the gate itself, and
this one was written by the same hand that removed the symbol.

**Round 8 is where the response changed.** Two of its five findings were
relations this branch had already fixed *at another site*: the whole-tuple
refusal, found for the third time (memory `reg` at `v0.34.115`, bus `ranges` at
`v0.34.117`, peripheral `reg` here), and the boot's treatment of a device tree
it cannot read. Patching a third and fourth site would have been the same
mistake in a third costume, so each relation was given **one** answer that every
reader calls — `fdtWholeEntryCount`, `dtb_dereferenced_range` — which is this
row's own tier-1 remedy applied within a module. Its other three findings are
the *nominal-gate* thesis in miniature: an MMIO coverage check comparing extents
while the device identity sat parsed and discarded beside it, and two Tier 0
gates reading raw text where the code view is the tree's rule.

**Round 7 was five for five**, every one in the Lean FDT parser and every one
naming the Rust walker as the side that is right: the declared struct-block
extent, the declared strings-block extent, the single-root requirement, the
whole-tuple `ranges` refusal, and a result returned without the validation its
own type documents — **thirteen of twenty-nine** across seven rounds.  Two
things about how it was closed matter to this row.  First, the four bounds
findings were **one** defect wearing four costumes (every read bounded by
`blob.size` rather than by the extents the header declares), so the remedy is a
*contract* rather than four patches: `FdtBlob`, built only by `FdtBlob.of?` and
read only through accessors that refuse an access outside the block they name.
Second, the tree's *second* structure-block walk — `findMemoryRegProperty`,
deprecated since `v0.30.8`, no consumers, unbounded in the same way — was
**removed**, not hardened.  Both are tier 1 of this row's own remedy (*make the
second implementation impossible*) applied inside one file; XV1 applies it to
the pair.

The pairs, and what ties them today:

| Pair | The shared question | Tie today |
|------|---------------------|-----------|
| `cmdline::find_ram_top_in_dtb` / `DeviceTree.memoryRegionsFromNodes` | which memory does this blob declare as available? | **none** — and to be *removed* rather than tied: the Rust reader leaves the boot path when BP builds the map from constants |
| `cmdline`'s FDT token walk / `DeviceTree.parseFdtNodes` | is this structure block readable at all? | **none** — same disposition.  Round 7 closed five behavioural gaps here by hand, and the Lean side now enforces the header's declared extents as the Rust side always did; the *pair* is untouched |
| `sele4n-abi::encode_syscall` / `Architecture.RegisterDecode` | which register carries which field? | hand-transcribed literals in a Rust test |
| `mmu::boot_mapping_for` / `RPi5.rpi5MemoryMapForConfig` | what does the boot map at this address? | partial — `check_physical_address_width.sh` derives the *device window* from `Board.lean`, and one Rust test mirrors the boundaries |
| `objectLockBracketDomain` / `schedulerLockBracketDomain` | in what order does a bracket acquire a footprint? | **closed at v0.34.113** — one definition answers it |

What remains — **now scheduled as WS-BP's BP0 phase**, so this is a pointer
rather than a work list.  Each row's design, files and estimate live in
[`SMP_BOOT_PATH_PLAN.md`](planning/SMP_BOOT_PATH_PLAN.md) §5.

| Was | Now | Kind |
|-----|-----|------|
| XV1 | **BP2.6** — the device-tree pair removed rather than tied: the boot map built from linker symbols and board constants, the verified Lean parser left as the blob's only reader | scheduled since `v0.34.120` |
| XV2 | **BP0.1** — the shared device-tree fixture corpus, read by both suites | interim; BP2.6 retires it |
| XV3 | **BP0.2** — a Tier 0 check that both sides consume every fixture | interim; BP2.6 retires it |
| XV4 | **BP0.3** — the `MessageInfo` layout emitted from Lean and asserted by the Rust conformance suite, replacing hand-transcribed shifts across 112 tests | permanent |
| XV5 | **BP0.4** — one address set through `mmu::boot_mapping_for` and `rpi5MemoryMapForConfig`, replacing the single mirrored-boundaries test | permanent |

None of the five is a soundness defect, and none of the drifts they would have
caught is open: every instance is fixed, including the four the `v0.34.115`
hand audit of the device-tree pair found after this workstream was registered. The cost of leaving them is that the
**next** divergence between the model and its executable twin is invisible to
CI, which is how seven of PR #892's twenty review findings arose.

## WS-IN — internal-first naming, the grandfathered remainder

**Owner**: whole-tree hygiene. **Closure target**: the PR after v0.34.48
(maintainer's decision, PR #889 review round 17). Registered here because it
has no plan file.

`scripts/check_identifier_naming.py` holds Rust at **zero** workstream/audit
IDs in identifiers and paths, and ratchets every other surface against
`scripts/identifier_naming_baseline.json`: a legacy name's per-file occurrence
count may fall but never rise. At v0.34.48 the baseline carries **962
occurrences of 261 identifiers over 292 (identifier, file) pairs** — 489 in
`tests/`, 314 in `scripts/`, 147 in `SeLe4n/`, 12 in build and workflow files.
It is falling opportunistically (1017 → 975 → 972 → 963 → 962 over the last
four touches), because the naming rule retires a historical name only when a
workstream that can rename it in the same commit touches the code.

What the remainder is, largest first:

| Occurrences | Where | What |
|---|---|---|
| 292 | `scripts/test_tier3_invariant_surface.sh` | anchor comments and `rg` patterns citing the workstream that commissioned each anchor |
| 104 | `tests/NegativeStateSuite.lean` | per-area check names carrying audit IDs |
| 94 | `tests/An10CascadeSuite.lean` | a suite *named after* its audit |
| 88 | `tests/Ak9PlatformSuite.lean` | likewise |
| 78 | `SeLe4n/Kernel/Architecture/Invariant.lean` | theorem names carrying phase codes |
| 63 | `tests/fixtures/scenario_registry.yaml` | scenario keys |

By family: SM5 (134), AN10 (99), H16 (94), AK9 (75), SM3 (58), SM2 (47),
SM4 (39), H12 (34).

The 147 in production `SeLe4n/` sources are the ones that matter: the rule
exists because a workstream ID stops meaning anything the moment the workstream
closes, and a reader of `Kernel/Architecture/Invariant.lean` cannot tell from
`ak7_*` what the theorem says. Closing this is a rename sweep plus every Tier-3
anchor, fixture key and `lakefile.toml` target that cites the old names, which
is why it is its own PR rather than a rider on a review round.

## WS-AP — the ASID capability surface

**Owner**: Architecture subsystem (VSpace / ASID). **Closure target**:
post-v1.0.0. Registered as a row in table C above; the work list is here
because it has no plan file.

Two SM7 debts named **SM8** as their closure target, and SM8 closed without
absorbing either. WS-RR RR7.20 re-targets both, because they are one root: the
kernel has a complete, proven `asidAllocateWithShootdown` and **no way for
userspace to reach it**, since the ASIDControl / ASIDPool object family does not
exist. Building the initiator-atomic seam for an unreachable primitive would
violate the wire-it-into-the-consumer rule — the consumer is what is missing.

| # | Item | Where |
|---|------|-------|
| AP1 | The `ASIDControl` and `ASIDPool` object kinds, their capabilities and their rights — the family seL4 uses to delegate ASID allocation, and the reason `asidAllocateWithShootdown` currently has no caller | `SeLe4n/Model/Object/`, `SeLe4n/Kernel/Capability/` |
| AP2 | The syscall arms that invoke them, with their footprints and their `ipcInvariantFull` obligations, so allocation is reachable through the ordinary capability discipline rather than as an internal primitive.  Consumes AP1 | `SeLe4n/Kernel/API.lean`, `SeLe4n/Kernel/Concurrency/Locks/LockSetTransitions.lean` |
| AP3 | SM7.F.4 (b)(iv): the initiator-atomic per-core wrapper for the `requiresFlush` ASID-allocate, so the allocating core's own `perCoreTlb` view is retired atomically with the transition rather than only in the deferred catch-up.  Consumes AP2 — it is the seam that has no caller until then | `SeLe4n/Kernel/Architecture/TlbShootdownProtocol.lean` |

**None of the three is a live defect.** `asidAllocateWithShootdown` is proven
and unreachable, so nothing calls it wrongly; the catch-up seam
(`shootdownCatchUpPerCore` → `drainInitiatorPerCoreView`) drains the initiator
for every posted round, so there is no permanent hole even when AP3 lands late
— only the transient commit→catch-up window, and it is vacuous while fills are
unwired here. The cost is that **v1.0.0 does not ship a userspace-reachable
ASID surface**, and the release note must not imply one.

## WS-OD — SchedContext donation chains

**The priority-authority crossing found while closing this workstream is fixed
at `v0.35.3`** and is no longer registered debt.  `updatePrioritySource` had
classified `.bound scId` and `.donated scId owner` identically, so
`.tcbSetPriority` / `.tcbSetMCPriority` on a thread *holding* a donated context
wrote the **donor's** `SchedContext.priority` — an authority crossing, since
both arms are gated on a TCB-write right over the *target* and the caller's MCP
ceiling, and neither says anything about the donor; the rewritten field then
travelled back with `returnDonatedSchedContext`, so the client resumed in a band
it never asked for.  Medium–High **integrity**, live at call depth 1 since
donation landed and widened by OD4 to reach a thread several hops up a chain.
The remedy is seL4-MCS's own split rather than a refusal: a donee runs on the
donor's budget, deadline and domain at **its own** priority, so the write lands
in the donee's TCB and the read prefers it.  One classifier decides
(`SchedContextBinding.ownScId?`), one resolver answers
(`SystemState.threadBasePriority`), and the five readers are pinned to it by
theorem.  The **mirror** crossing found while verifying that fix is closed in
the same cut, and it is the *domain* as much as the priority:
`schedContextConfigure` propagated **both** thread-owned parameters into
`sc.boundThread`'s TCB, which after a donation is the *donee*, so a capability
on the client's reservation could rewrite the server's own base priority and
**migrate its scheduling domain** — permanently, since the donee keeps both
fields.  Both propagations are now gated on the bound thread *owning* that
reservation, through one predicate both halves consult, and `effectiveSchedParams` reports a
donee's own domain, since every live domain filter reads `tcb.domain`.  What
remains with **WS-CB** (CB0.3, CB1.6) is the other authority question on that
path: `schedContextConfigure` answers to a SchedContext write right with no
caller-MCP check at all.  See `CHANGELOG.md` at `v0.35.3`.

Registered at `v0.34.98`. **OD1 is closed** — OD1.1–OD1.7 landed at
`v0.34.100`, `v0.34.101`, `v0.34.103`, `v0.34.104`, `v0.34.105`, `v0.34.106` and
`v0.34.108` (OD1.7, the aborted holder placed rather than merely unblocked, found
by review and reported as a security finding). **OD2 is closed** at `v0.34.125` —
`SchedContext.scReply`, an implemented `Reply.wellFormed`, `donationChainFrom`
and `donationChainWellFormed` with its frame family, and the conjunct in
`ipcReachable` with all three inhabitation witnesses re-discharged; additive, so
no transition changed, and the predicate is vacuously true of every reachable
state. **OD3.1–OD3.3 landed at `v0.34.126`** — the donation return became a
four-write reply-stack pop with a fail-closed head validation, sixteen copies of
its case analysis collapsed onto `returnDonatedSchedContext_ok_storeChain`
(retiring the duplicate `returnDonatedSchedContext_walk`), three statements
changed where three claims stopped being true (the Reply frame, the widened
binding trichotomy, and two reusable frames dropped to the `caller` projection
they are about), and `returnDonatedSchedContext_eq_legacy_of_none` witnesses that
the tree's behaviour is unchanged.  The depth-≥ 2 conjunct obligations are stated
now (`donationReturnOuterValid`) rather than deferred, so OD4.3 does not reopen
them; the one composite that is still `newOwner? = none`-conditioned says so and
names OD4.3 as the row that removes it, before OD4.1 makes the arm reachable.
**OD3.4 landed at `v0.34.127`** — `replyStackOuterCaller?`, the one-link
lookahead resolver, with the confused-deputy validation §3.4 assigns to OD5.1
built in rather than deferred, and `outerCallerAcceptable`, the guard that makes
the pop validate the donee it mints a binding for.  **OD3.5 landed at
`v0.34.128`** — the arm-selected cancellation footprint, plus a *finding* the row
did not anticipate: `.replyRecv` performs two SchedContext hand-offs and declared
one, so the passive-server steady state wrote a kernel object under no declared
lock (see section A).  **OD3.6 landed at `v0.34.129`** — a second unanticipated
finding, and the sharper one: `.receive` performed *no* SchedContext donation at
all, so seL4-MCS's `receiveIPC` → `maybeDonateSchedContext` had no counterpart
here and a passive server taking its **first** request with `seL4_Recv` ran the
client's work charged to no reservation.  Reported as a Medium
budget-enforcement bypass before being fixed; the remedy is one shared step both
receiving arms and `replyRecvReturnDonation` call.  **OD3.7 landed at
`v0.34.130`–`v0.34.131`** — the pop reads **two** objects below the reply-stack
head, not the one the row named (`replyStackOuterCaller?` reads the frame below,
and `outerCallerAcceptable` then reads that frame's caller's TCB to validate it
before the pop binds a context to it, so an unlocked read is a
time-of-check/time-of-use window).  The row's own escalation clause fired and was
answered in favour of the raise: `maxLockSetSize` 11 → **13**,
`admissibleCriticalSection` for the 1 ms tick 30 → 25 µs (and 23 µs since
WS-OD OD3.13 took the ceiling to 14), the uniform envelope 1980 → 2340 → 2520 µs — only `.replyRecv` needed it, and the *reachable* `.replyRecv`
declares twelve (`lockSet_endpointReplyRecvOnCore_size_le_thirteen`), stated rather
than left for a reader to re-derive.  **OD3.8 landed at `v0.34.132`, closing
OD3** — the pop is the one transition in the tree that writes `Reply.donatedSc`,
`Reply.prev` and `SchedContext.scReply`, so it is the one that cannot reach
`donationChainWellFormed_of_frame`;
`returnDonatedSchedContext_preserves_donationChainWellFormed` closes that, and
with it the predicate is preserved by **every** kernel transition.  Acyclicity is
*derived* from the invariant's own termination clause
(`donationChainFrom_head_not_mem_tail`) rather than added as a conjunct, the
congruence the proof consumes is chain-scoped rather than whole-store, and the
`some`-head arm is exercised on OD2.4's depth-2 witness — a theorem discharged
only where the context heads no stack would be indistinguishable from one whose
writing arm is wrong.  Two of the row's five deliverables were already
discharged: the projection re-derivation landed with the fourth store at OD3.1,
and the de-threading family size does not move, since the new theorem is not an
`ipcInvariantFull` bundle.

**OD3.9–OD3.13 landed at `v0.34.134` → `v0.34.137`** — the sweep OD3.8 did not
anticipate.  OD3.9 fixed a **live** defect (`spliceOutMidQueueNode` left its
successor's `queuePPrev` naming the removed thread, so that thread could never
again be dequeued and every later bound-notification delivery to it returned
`.illegalState` — an authority-crossing denial of service, reported before being
fixed), and OD3.10–OD3.13 declared the queue-structure TCBs four more arms write
and none had named: `.notificationSignal`'s two splice neighbours, and the one
neighbour each of `.send`, `.call`, `.receive` and `.replyRecv` relinks.  The
last took `maxLockSetSize` 13 → **14** — the escalation clause firing and being
answered — with `admissibleCriticalSection` for the 1 ms tick 25 → 23 µs and the
uniform envelope 2340 → 2520 µs.

**OD3.14 landed at `v0.34.141`, closing OD3** — the *priority* half of the
rendezvous hand-off, reported by review on the OD3 cut and pre-existing rather
than introduced by it.  OD3.6's donation moves a queued caller's scheduling
context, hence its base priority, and `resolveEffectivePrioDeadline` is
`max basePrio pipBoost`, so the inherited **boost** travelled by no route at all
on the `.receive` arm: a chain blocked behind the dequeued caller stopped dead at
it — unbounded priority inversion, on the arm a passive server takes its first
request with, and biting with no donation at all since `applyCallDonation` is the
identity for an already-`.bound` receiver that still gains the waiter.
`applyReceiveRendezvousHandoff` is the donation and the walk under **one** reading
of the guard; the same sweep found the defect at a sibling (`.replyRecv` walks
from the *recorded server*, which is the receiver only on a non-delegated reply)
and closed it with `applyReceiveLegPipHandoff`, gated on the equality that makes
the first walk be the second.  `maxLockSetSize` does not move: a chain is
state-discovered and unbounded, so its locks are declared through the
`pipChainStart_<τ>` markers rather than through `lockSet_<τ>`, and the marker
family grows with the walks — two new ones, plus the reply leg's own corrected to
name the thread it actually walks from.

**OD3.15 landed at `v0.34.142`** — the ceiling's derived figures stop being
hand-maintained.  `scripts/check_lock_ceiling_figures.py` (Tier 0) holds every
prose copy of `maxLockSetSize`, the RPi5 tick's admissible per-lock cost and the
uniform-cost envelope to the Lean sources they are derived from, on RR7.28's
shape: both axes derived, a canonical spelling for the live claim so narrative
naming a superseded value stays free, a near-miss reported as a gate defect
rather than skipped, and five documents pinned to carry the statement.  Four
consecutive review rounds of this phase each found a stale copy the previous
round's sweep had missed, which is what made it a mechanism rather than a
correction.

**OD3.16 and OD3.17 landed at `v0.34.143` → `v0.34.144`** — review rounds 4 and
5, seven findings, four of them inside gates written one and two cuts earlier to
stop exactly the class they belong to: an unanchored formula pin that a longer
body satisfied, a gate reading Lean raw on the day the *gates read code, prose
reads prose* rule was cited to justify it, a subtraction whose two operands
counted different things, and a contract still denying a hand-off its own
footprint declares.

**OD3.18 landed at `v0.34.145`, closing OD3** — found by running the sweep rule
on the previous cut rather than by a review.  `LockSetTransitions.lean`'s module
header answers *which footprints need a SchedContext member* a second time, and
answered it wrongly three ways: `.receive` sat under "syscalls that do NOT need
donation extension" for twelve cuts after OD3.6 gave it a `donatedScId`, on the
very reasoning OD3.6 disproved; `lockSet_replyRecv`'s entry repeated the sentence
OD3.17 had just retired at the declaration site; and `tcbSetPriority`,
`tcbSetMCPriority` and `tcbSetAffinity` — each writing the target's **bound**
SchedContext, because priority and home core live there — were called "TCB-only
config ops", with `tcbSetAffinity` in neither list.  **The remedy is deletion of
the duplicate.**  The tree already derives this fact and proves it:
`permittedKinds` is the declared kind inventory, and the
`lockSet_consistent_<arm>` family states `∀ p ∈ (lockSet_<arm> …).pairs,
p.fst.kind ∈ permittedKinds <arm>` at each footprint's full arity, so a member
added without the kind being permitted fails to elaborate.  The two prose lists
were a third copy of it, checked by nothing.  A Tier 1 census over the
footprints' elaborated bodies was written for this row and **deleted before it
shipped**: it took six corrections in a row and was `unconditionalActions` again,
which is the recorded reason not to answer a proven question a second way.
**Nothing owed.**

**OD4, OD5 and OD6 landed at `v0.35.2`, closing the workstream.**  OD4 makes the
donation transitive: `applyCallDonation` reads the caller's *effective* context
(bound or itself donated), `donateSchedContext` becomes a four-store **push**
whose frame is the donor's own `replyObject`, and the push is fail-closed on that
frame in three distinguishable ways — a donation with no stack frame is what lets
the next pop clear an outer caller's frame.  The `.call` footprint does not grow
(`lockSet_endpointCallOnCore_covers_donationPush`) and `maxLockSetSize` is
unmoved.  The push preserves the chain
(`donateSchedContext_preserves_donationChainWellFormed`), so with OD3.8 the
predicate is preserved by *every* transition; the freshness that argument
consumes is the push's own refusal rather than an assumption.  All six pop sites
resolve their new owner through `returnDonatedSchedContextResolved`, and OD3.2's
`hBottom : newOwner? = none` is gone from the `ipcInvariantFull` composite.

OD5 makes the teardown paths chain-aware, and its load-bearing decision is that
each one **refuses rather than clears**: `linkReply` refuses to freshen a Reply
that still donates, and `lifecyclePreRetypeCleanup` refuses to retype either a
live frame or a context that still heads a stack.  Clearing would take a frame
off a stack the context still heads, so the walk would stop mid-chain and the
store would satisfy no chain at all — the same reason `consumeReply` deliberately
leaves the two stack fields alone (the reply leg consumes the link *before* the
pop reads them).  OD5.2 makes the cancelled-**middle**-caller decision the plan
forbids inheriting: both candidates are named, `severAtCut` is chosen, and the
choice is proved by a theorem whose second clause — no thread below the cut is
touched — is what distinguishes the policies.  Its cost is measured at stack
depth three (`tests/SmpIpcSuite.lean` §3.22): the original owner's reservation
ends up with the caller immediately above the cut.  OD5.3 found the consequence for the suspend footprint:
the teardown can rebind the victim `.donated`, the arm selector re-reads the
*post*-teardown binding, so the pipeline pops twice at depth ≥ 2 and
`suspendThreadOnCoreSchedLockSet`'s replenish segment is a **triple**.

OD6 states the payoff (`passiveServerHoldsDonatedContext_atCallDepthTwo`: a
passive server reached at call depth ≥ 2 holds a scheduling context whose
`boundThread` names it back), executes the depth-2 push and its resolved pop in
`tests/SmpIpcSuite.lean` and in the trace harness
(`SCN-DONATION-PUSH-DEPTH-TWO`, `SCN-DONATION-RETURN-RESOLVED-OUTER`), and
inverts the two OD2-era assertions that said a live donating call writes none of
the three reply-stack fields — their own comment named them as the checks that
fail the day the push lands.

Plan:
[`docs/planning/SCHEDCONTEXT_DONATION_CHAIN_PLAN.md`](planning/SCHEDCONTEXT_DONATION_CHAIN_PLAN.md)
(53 sub-tasks across OD1..OD6, all closed). Both section-A rows it owns are
closed: the onward-donation gap above, and the `passiveServerIdle` break the
`v0.34.97` reclaim introduced.

The point in one line: **`donateSchedContext` is the only operational
construction site of a `.donated` binding, and `applyCallDonation` reaches it
only from a `.bound` caller** — so a scheduling context stops at the first
passive server, and seL4's passive-server pattern does not work at call depth
≥ 2. Six decisions shape the fix, and each is a decision rather than a default:

* **The `passiveServerIdle` hole was fixed first, not last.** It was live on
  HEAD at registration, at depth 1 with no chain involved, so scheduling it
  late would have meant every intervening phase doing bundle work over a
  surface carrying a known-false conjunct. It was OD1, and it is closed: the
  reclaim ends the holder's outstanding send or call first (`v0.34.104`), the
  preservation theorem holds on every arm (`v0.34.105`), and the unblocked
  holder is placed on its home run queue (`v0.34.108`).
* **The pop lands before the push, and lands inert.** If the push landed first, a
  depth-2 chain would be reachable while the old return still writes `.bound` at
  the intermediate thread — permanently moving a scheduling context across a
  domain boundary in a state that **breaks no conjunct**, which is exactly the
  live-transition-ahead-of-its-proofs failure the numbering rule names.
* **`SchedContext.scReply` is built, not designed around** — done at `v0.34.125`.
  `Reply.wellFormed`'s docstring already said the strengthened form requires
  "`donatedSc.scReply` agrees with this reply", of a field that did not exist —
  the implement-the-improvement case in its plainest form. It also keeps the call
  footprint at eight of nine: without it the push must read the owner's TCB and
  the outer reply, for ten, and raising the ceiling would widen the published
  covert-channel bound. What OD2 shipped with it: the projection erasure in the
  **same cut** as the field (or the OD4 push would be observable in the interval);
  a boot-safety refusal of a config SchedContext that heads a stack; an
  implemented `Reply.wellFormed` whose two store-level clauses live in
  `donationChainWellFormed`, which carries the local one as its first conjunct;
  and the chain invariant as a conjunct of `ipcReachable` rather than of
  `ipcInvariantFull` — preserved through `donationChainFrame`, which is stated
  over the two projections the walk actually reads, so it *is* the read set.
* **The pop takes its new owner as an argument.** The reply leg consumes the
  target's reply link *before* the donation return runs, so the link is not there
  to read; the new owner is resolved from the pre-state and passed in, on the same
  discipline as the two resolvers already beside it.
* **The pop validates the link it follows.** `Reply` has `prev` and no `next`, so
  a cancelled middle caller cannot be spliced out by a backward scan, and Reply
  objects are re-linked to new callers. A stale `prev` over a reused Reply would
  hand a thread's SchedContext to an unrelated thread in another domain, so the
  pop requires the previous reply's own `donatedSc` to name the same context, and
  freshening clears both fields. That also gives `donatedSc` an operational
  reader — otherwise it would be written and never read.
* **The binding's `owner` stays the immediate donor**, which is what
  `donateSchedContext` already writes. That is what keeps all five donation
  conjuncts true at depth `n` with their current definitions, and it leaves the
  *binding* graph without chains — the chain lives entirely in the reply stack, so
  `donationOwnerValid_implies_donationChainAcyclic` stays true.

**What it unblocks.** The open RR7.22 residual — the cancellation reply arm's
`ipcInvariantFull` and the five-arm composite over `cancelIpcBlockingOnCore` — is
blocked on the `passiveServerIdle` half alone: without it the keystone would have
to take the holder's state as a named hypothesis, which is the false-assurance
shape that residual exists to remove. **OD1 unblocks it**, so the residual lands
immediately after OD1 rather than after the whole workstream.

## Workstream registry

The workstream families this project has run, with the versions each spans.
**This table is machine-read**: `scripts/check_identifier_naming.py` derives
its family grammar from the bold workstream name in each row of this table —
the rows, not this paragraph or any other prose in the file — rather than
from a hand-kept list, because a hand-kept list was the single largest source
of holes in that gate. A workstream added as a row here is covered by the
naming gate without anyone remembering to update it; a workstream that is only
mentioned in prose is not.

Scope, findings and evidence for any of these are in
[`CHANGELOG.md`](../CHANGELOG.md) at the versions named.

| Workstream | Versions |
|------------|----------|
| **WS-AP** | v0.34.71– (closure post-v1.0.0 — the ASID capability surface; two SM7 debts re-targeted from the closed SM8) |
| **WS-OD** | v0.34.98–v0.35.2 (complete; OD1 at v0.34.108, OD2 at v0.34.125, OD3 at v0.35.1, OD4–OD6 at v0.35.2 — SchedContext donation chains, [`SCHEDCONTEXT_DONATION_CHAIN_PLAN.md`](planning/SCHEDCONTEXT_DONATION_CHAIN_PLAN.md)) |
| **WS-RM** | v0.35.4–v0.35.6 (complete; RM1–RM6 all at `v0.35.6` — seL4's `reply_remove` on the reply path, and the `seL4_ReplyRecv` leg-ordering defect found while closing it, [`REPLY_FRAME_REMOVAL_PLAN.md`](planning/REPLY_FRAME_REMOVAL_PLAN.md)) |
| **WS-HP** | v0.35.16–v0.35.54 (HP1 at v0.35.35, HP2 at v0.35.36, HP3 at v0.35.37, HP4 at v0.35.38, HP5 at v0.35.39, the upstream-attribution retraction at v0.35.40, HP6 at v0.35.41 → v0.35.45, HP7 at v0.35.46, HP8 at v0.35.47, HP9 at v0.35.48, HP10 at v0.35.49 → v0.35.54, post-landing audit at v0.35.61 → v0.35.62 — the head-driven donation pop, the chain-preserving removal and the reservation's recorded origin, so a completed call chain returns a client's reservation at **every** reply-stack depth, 55 sub-tasks across 10 phases, [`DONATION_POP_TRIGGER_PLAN.md`](planning/DONATION_POP_TRIGGER_PLAN.md)) |
| **WS-XV** | v0.34.114–v0.34.124 (registered, then **absorbed into WS-BP as its BP0 phase**; the finding is retained in this file, the work is [`SMP_BOOT_PATH_PLAN.md`](planning/SMP_BOOT_PATH_PLAN.md) §5 BP0) |
| **WS-BP** | v0.34.59– (planned; opens after WS-RR RR8 closes — the bare-metal boot path **and the cross-implementation agreement it ends**, absorbing WS-XV as BP0 at `v0.34.124`, [`SMP_BOOT_PATH_PLAN.md`](planning/SMP_BOOT_PATH_PLAN.md)) |
| **WS-LC** | v0.34.51–v0.34.56 |
| **WS-CB** | v0.34.49– (planned; opens after WS-RR, or beside RR6–RR8 under the file partition in its plan's §2.3) |
| **WS-RR** | v0.34.26– |
| **WS-SL** | v0.34.26– (closure post-v1.0.0) |
| **WS-IN** | v0.34.48– (closure: the PR after v0.34.48) |
| **WS-DT** | v0.31.157–v0.34.43 |
| **WS-SM** | v0.31.2– |
| **WS-RA** | v0.33.37–v0.33.38 |
| **WS-RC** | v0.30.11–v0.31.2 |
| **WS-AN** | v0.30.6–v0.30.11 |
| **WS-AM** | v0.30.0 |
| **WS-AK** | v0.29.1–v0.30.6 |
| **WS-AL** | v0.29.13–v0.29.14 |
| **WS-AJ** | v0.28.1–v0.29.0 |
| **WS-AI** | v0.27.7–v0.28.0 |
| **WS-AH** | v0.27.2–v0.27.6 |
| **WS-AG** | v0.26.0–v0.27.1 |
| **WS-AF** | v0.25.22–v0.25.27 |
| **WS-AE** | v0.25.15–v0.25.21 |
| **WS-AD** | v0.25.11–v0.25.14 |
| **WS-AC** | v0.25.3–v0.25.10 |
| **WS-AB** | v0.24.0–v0.25.5 |
| **WS-AA** | v0.23.21–v0.23.x |
| **WS-Z** | v0.23.0–v0.23.21 |
| **WS-Y** | v0.22.22–v0.22.x |
| **WS-X** | v0.22.17–v0.22.21 |
| **WS-W** | v0.22.10–v0.22.16 |
| **WS-V** | v0.21.8–v0.22.9 |
| **WS-U** | v0.21.0–v0.21.7 |
| **WS-T** | v0.20.0–v0.20.7 |
| **WS-S** | v0.19.0–v0.19.6 |
| **WS-R** | v0.18.0–v0.18.7 |
| **WS-Q** | v0.17.7–v0.17.14 |
| **WS-N** | v0.17.0–v0.17.5 |
| **WS-M** | v0.16.14–v0.17.0 |
| **WS-M2** | v0.16.15 |
| **WS-L** | v0.16.9–v0.16.13 |
| **WS-K-H** | v0.16.8 |
| **WS-K-G** | v0.16.7 |
| **WS-K-F** | v0.16.5 |
| **WS-K-E** | v0.16.4 |
| **WS-K-D** | v0.16.3 |
| **WS-K-C** | v0.16.2 |
| **WS-K-B** | v0.16.1 |
| **WS-K-A** | v0.16.0 |
| **WS-J1-F** | v0.15.10 |
| **WS-J1-E** | v0.15.9 |
| **WS-J1-D** | v0.15.8 |
| **WS-J1-C** | v0.15.6; refinements v0.15.7 |
| **WS-J1-B** | v0.15.5 |
| **WS-J1-A** | v0.15.4 |
| **WS-I1** | v0.15.0 |
| **WS-F8** | v0.14.9 |
| **WS-F5** | v0.14.9 |
| **WS-H16** | v0.14.8 |
| **WS-H15** | v0.14.7 |
| **WS-H14** | v0.14.6 |
| **WS-H13** | v0.14.4 |
| **WS-H11** | v0.13.7 |
| **WS-H10** | v0.13.6 |
| **WS-H9** | v0.13.4 |
| **WS-H8** | v0.13.2 |
| **WS-H7** | v0.12.21 |
| **WS-H6** | v0.13.1 |
| **WS-H5** | v0.12.19 |
| **WS-H4** | v0.12.18 |
| **WS-H3** | v0.12.17 |
| **WS-H2** | v0.12.16 |
| **WS-H1** | v0.12.16 |
| **WS-G** | v0.12.6-v0.12.15 |
| **WS-E** | v0.11.0-v0.11.6 |
| **WS-D** | v0.11.0 |
| **WS-C** | v0.9.32 |
| **WS-B** | v0.9.0 |

Prior audits (v0.8.0–v0.9.32), milestone closeouts, completed workstream plans
and legacy GitBook chapters are archived in
[`dev_history/`](dev_history/README.md), including the audit plans that scoped
WS-F through WS-N.
