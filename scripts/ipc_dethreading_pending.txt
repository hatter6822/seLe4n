# WS-RR RR3 -- registered residuals of the `ipcInvariantFull` de-threading gate.
#
# The gate's `payoff_theorems` check requires each name in its PAYOFF_THEOREMS
# tuple to be declared somewhere in the tree.  A name listed here is a residual
# the project has looked at, sized, and deferred with a closure target; the
# gate reports it instead of failing on it.
#
# The registration is checked in BOTH directions, so it cannot rot into a
# silent exemption:
#
#   * a name listed here whose theorem HAS landed fails the gate (stale
#     registration -- delete the line);
#   * a name listed here that is not one of the gate's payoff theorems fails
#     the gate (dangling registration);
#   * a payoff theorem that is absent AND unlisted fails the gate, as before.
#
# Format: <theorem name> | <closure target> | <reason>
# Lines starting with `#` and blank lines are ignored.

dispatchWithCap_preserves_ipcInvariantFull | WS-RR RR3.15 | The bundle family covers the IPC and donation transitions only. `dispatchWithCap` routes twenty-five syscalls across the capability, VSpace, service, sched-context, lifecycle and TCB subsystems, and no `_preserves_ipcInvariantFull` theorem exists for any of them -- six of roughly thirty arms have any coverage at all. The payoff is a per-arm bundle workstream, not a composition of theorems already in the tree; see docs/planning/SMP_RELEASE_READINESS_PLAN.md RR3.15 for the sequence it was re-scoped into.
dispatchSyscall_preserves_ipcInvariantFull | WS-RR RR3.16 | Consumes RR3.15's per-arm bundles; blocked on the same gap. Named for the function it is about (`dispatchSyscall`), not the plan's original `syscallDispatch`, which names nothing in the tree.
