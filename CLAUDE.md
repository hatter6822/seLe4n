# CLAUDE.md — seLe4n project guidance

## What this project is

seLe4n is a production-oriented microkernel written in Lean 4 with machine-checked
proofs, improving on seL4 architecture. Every kernel transition is an executable
pure function with zero `sorry`/`axiom`. First hardware target: Raspberry Pi 5.
Lean 4.28.0 toolchain, Lake build system, version 0.30.10.

## Build and run

```bash
# Environment setup (runs automatically via SessionStart hook — no build)
./scripts/setup_lean_env.sh --skip-test-deps

# Full setup including test dependencies (shellcheck, ripgrep)
./scripts/setup_lean_env.sh

# Manual build (run separately after setup)
source ~/.elan/env && lake build

# Run executable trace harness
lake exe sele4n
```

## Validation commands (tiered)

```bash
./scripts/test_fast.sh      # Tier 0+1: hygiene + build
./scripts/test_smoke.sh     # Tier 0-2: + trace + negative-state
./scripts/test_full.sh      # Tier 0-3: + invariant surface anchors
NIGHTLY_ENABLE_EXPERIMENTAL=1 ./scripts/test_nightly.sh  # Tier 0-4
```

Run at least `test_smoke.sh` before any PR. Run `test_full.sh` when changing
theorems, invariants, or documentation anchors.

## Module build verification (mandatory)

**Before committing any `.lean` file**, you MUST verify that the specific
module compiles:

```bash
source ~/.elan/env && lake build <Module.Path>
```

For example, if you modified `SeLe4n/Kernel/RobinHood/Bridge.lean`:
```bash
lake build SeLe4n.Kernel.RobinHood.Bridge
```

**`lake build` (default target) is NOT sufficient.** The default target only
builds modules reachable from `Main.lean` and test executables. Modules that
are not yet imported by the main kernel (e.g., `RobinHood` before N4
integration) will silently pass `lake build` even with broken proofs.

A pre-commit hook enforces this automatically. Install it by running
`./scripts/install_git_hooks.sh` (invoked automatically by `setup_lean_env.sh`
and by the Lean Action CI workflow, so fresh clones and CI-cloned checkouts
are guarded without manual action). For CI contexts, `--check` verifies the
installer has run:

```bash
./scripts/install_git_hooks.sh          # install (idempotent no-op if present)
./scripts/install_git_hooks.sh --check  # verify installation (non-zero if absent/diverging)
./scripts/install_git_hooks.sh --force  # overwrite; backs up any diverging hook first
```

The hook:
1. Detects staged `.lean` files
2. Builds each modified module via `lake build <Module.Path>`
3. Checks for `sorry` in staged content
4. **Blocks the commit** if any build fails or sorry is found

Do NOT bypass the hook with `--no-verify`.

## Source layout

```
SeLe4n/Prelude.lean              Typed identifiers, monad foundations
SeLe4n/Machine.lean              Machine state primitives
SeLe4n/Model/Object.lean         Kernel objects (re-export hub)
  Object/Types.lean              Core data types, TCB, Endpoint, Notification
  Object/Structures.lean         VSpaceRoot, CNode, KernelObject, CDT helpers
SeLe4n/Model/State.lean          Kernel/system state representation
SeLe4n/Model/IntermediateState.lean  Q3-A: Builder-phase state with invariant witnesses
SeLe4n/Model/Builder.lean        Q3-B: Builder operations (invariant-preserving state construction)
SeLe4n/Model/FrozenState.lean    Q5: FrozenMap, FrozenSet, FrozenSystemState, freeze function
SeLe4n/Model/FreezeProofs.lean   Q6: Freeze correctness proofs (lookup equiv, radix equiv, invariant transfer)
SeLe4n/Kernel/Scheduler/*        Scheduler transitions + invariants
  Operations.lean                Re-export hub
    Operations/Selection.lean    EDF predicates, thread selection
    Operations/Core.lean         Core transitions (schedule, handleYield, timerTick)
    Operations/Preservation.lean Scheduler invariant preservation theorems
  PriorityInheritance.lean       Re-export hub (D4)
    PriorityInheritance/BlockingGraph.lean   Blocking relation, chain walk, acyclicity, depth bound
    PriorityInheritance/Compute.lean         computeMaxWaiterPriority
    PriorityInheritance/Propagate.lean       updatePipBoost, propagate/revert priority inheritance
    PriorityInheritance/Preservation.lean    Frame lemmas (scheduler, IPC, cross-subsystem)
    PriorityInheritance/BoundedInversion.lean Parametric WCRT bound, determinism
  Liveness.lean                  Re-export hub (D5)
    Liveness/TraceModel.lean     Trace model types, query predicates, counting functions
    Liveness/TimerTick.lean      Timer-tick budget monotonicity, preemption bounds
    Liveness/Replenishment.lean  CBS replenishment timing bounds
    Liveness/Yield.lean          Yield/rotation semantics, FIFO progress bounds
    Liveness/BandExhaustion.lean Priority-band exhaustion analysis
    Liveness/DomainRotation.lean Domain rotation bounds
    Liveness/WCRT.lean           WCRT hypotheses, main theorem, PIP enhancement
SeLe4n/Kernel/Capability/*       CSpace/capability ops + invariants
  Invariant.lean                 Re-export hub
    Invariant/Defs.lean          Core invariant definitions, transfer theorems
    Invariant/Authority.lean     Authority reduction, badge routing
    Invariant/Preservation.lean  Operation preservation, lifecycle integration
SeLe4n/Kernel/IPC/*              IPC subsystem
  Operations.lean                Re-export hub
    Operations/Endpoint.lean     Core endpoint/notification ops
    Operations/CapTransfer.lean  IPC capability transfer (WS-M3)
    Operations/Timeout.lean      Z6 timeout-driven IPC unblocking
    Operations/Donation.lean     Z7: SchedContext donation wrappers + preservation proofs
    Operations/SchedulerLemmas.lean Scheduler preservation lemmas
  DualQueue.lean                 Re-export hub
    DualQueue/Core.lean          Dual-queue operations
    DualQueue/Transport.lean     Transport lemmas
    DualQueue/WithCaps.lean      DualQueue with capability transfer
  Invariant.lean                 Re-export hub
    Invariant/Defs.lean          Core IPC invariant definitions
    Invariant/EndpointPreservation.lean Endpoint preservation proofs
    Invariant/CallReplyRecv.lean Call/ReplyRecv preservation proofs
    Invariant/WaitingThreadHelpers.lean Primitive waitingThreadsPendingMessageNone helpers
    Invariant/NotificationPreservation.lean Notification preservation proofs
    Invariant/QueueNoDup.lean    V3-K: No self-loops, send/receive head disjointness
    Invariant/QueueMembership.lean V3-J: Queue membership consistency proofs
    Invariant/QueueNextBlocking.lean V3-J-cross: queueNext blocking consistency proofs
    Invariant/Structural.lean    Structural invariants, composition theorems
SeLe4n/Kernel/Lifecycle/*        Lifecycle/retype transitions + invariants
  Suspend.lean                   D1: Thread suspension/resumption operations
  Invariant/SuspendPreservation.lean  D1: Transport lemmas for suspend/resume
SeLe4n/Kernel/Service/*          Service orchestration + policy
  Interface.lean                 Service interface definitions
  Operations.lean                Service operations
  Registry.lean                  Service registry
  Registry/Invariant.lean        Registry invariant proofs
  Invariant.lean                 Re-export hub
    Invariant/Policy.lean        Policy surface, bridge theorems
    Invariant/Acyclicity.lean    Dependency acyclicity proofs
SeLe4n/Kernel/Architecture/*     Architecture assumptions + VSpace + VSpaceBackend + RegisterDecode
  VSpace.lean                    VSpace HashMap map/unmap/lookup, W^X enforcement
  VSpaceBackend.lean             VSpace backend operations + HashMap instance (AG3-H)
  VSpaceInvariant.lean           VSpace invariant proofs
  TlbModel.lean                  TLB flush model + hardware adapter integration (AG6-F/G; AN9-B substantive `tlbBarrierComplete`)
  CacheModel.lean                AG8-B: Cache coherency model (D-cache/I-cache states, maintenance ops)
  BarrierComposition.lean        AN9-C: BarrierKind algebra + page-table / MMIO ordering theorems (DEF-A-M08/M09)
  TlbCacheComposition.lean       AN9-A: TLB+Cache composition theorem `pageTableUpdate_full_coherency` (DEF-A-M04)
  PageTable.lean                 AG6-A/B: ARMv8 4-level page table types, walk, W^X bridge
  VSpaceARMv8.lean               AG6-C/D: VSpaceBackend ARMv8 instance, shadow-based refinement
  AsidManager.lean               AG6-H: ASID pool allocator, rollover, uniqueness proof
  Adapter.lean                   Architecture adapter
  Assumptions.lean               Architecture assumptions
  Invariant.lean                 Architecture invariant re-export hub
  RegisterDecode.lean            Total deterministic decode: raw registers → typed kernel IDs
  SyscallArgDecode.lean          Per-syscall typed argument decode (msgRegs → typed structs)
  IpcBufferRead.lean             AK4-A: IPC-buffer overflow read helper (R-ABI-C01)
  IpcBufferValidation.lean       D3: IPC buffer address validation and setIPCBufferOp
  ExceptionModel.lean            AG3-C/F: ARM64 exception types, ESR classification, dispatch, EL0/EL1
  InterruptDispatch.lean         AG3-D: GIC-400 interrupt dispatch (acknowledge→handle→EOI)
  TimerModel.lean                AG3-E: Hardware timer binding (54 MHz RPi5, monotonicity proof)
SeLe4n/Kernel/InformationFlow/*  Security labels, projection, non-interference
  Enforcement.lean               Re-export hub
    Enforcement/Wrappers.lean    Policy-gated operation wrappers
    Enforcement/Soundness.lean   Correctness theorems, declassification
  Invariant.lean                 Re-export hub
    Invariant/Helpers.lean       Shared NI proof infrastructure
    Invariant/Operations.lean    Per-operation NI proofs
    Invariant/Composition.lean   Trace composition, declassification
SeLe4n/Kernel/RobinHood/*        Robin Hood hash table verified implementation
  Core.lean                      Types, operations, proofs (N1 complete)
  Set.lean                       RHSet type (hash-set wrapper over RHTable)
  Bridge.lean                    Kernel API bridge: instances, bridge lemmas, filter (N3)
  Invariant.lean                 Re-export hub (N2)
    Invariant/Defs.lean          Invariant definitions, empty table proofs, probeChainDominant
    Invariant/Preservation.lean  WF, distCorrect, noDupKeys, PCD preservation (all ops), helpers
    Invariant/Lookup.lean        Functional correctness (get after insert/erase), key absence
SeLe4n/Kernel/SchedContext/*      Scheduling context types, CBS budget engine, replenishment queue, operations (Z1–Z5)
  Types.lean                     Budget, Period, SchedContext, SchedContextBinding, BEq instances
  Budget.lean                    CBS budget operations: consume, replenish, admission control
  ReplenishQueue.lean            System-wide replenishment queue: sorted insert, popDue, remove, invariants (Z3)
  Operations.lean                Capability-controlled SchedContext operations: configure, bind, unbind, yieldTo (Z5)
  PriorityManagement.lean        D2: setPriorityOp, setMCPriorityOp, MCP authority validation, run queue migration
  Invariant.lean                 Re-export hub (Z2)
    Invariant/Defs.lean          Invariant definitions, preservation proofs, bandwidth theorems
    Invariant/Preservation.lean  Per-operation preservation theorems for SchedContext operations (Z5)
    Invariant/PriorityPreservation.lean  D2: Transport lemmas, authority non-escalation proofs for priority ops
SeLe4n/Kernel/SchedContext.lean  Re-export hub
SeLe4n/Kernel/RadixTree/*        CNode radix tree verified flat array (Q4)
  Core.lean                      CNodeRadix type, extractBits, O(1) lookup/insert/erase/fold/toList
  Invariant.lean                 24 correctness proofs (lookup, WF, size, toList, fold)
  Bridge.lean                    buildCNodeRadix (RHTable → CNodeRadix), freezeCNodeSlots, bridge lemmas
SeLe4n/Kernel/FrozenOps/*        Frozen kernel operations (Q7, experimental — not in production chain)
  Core.lean                      FrozenKernel monad, lookup/store primitives
  Operations.lean                15 per-subsystem frozen operations
  Commutativity.lean             FrozenMap set/get? roundtrip proofs, frame lemmas
  Invariant.lean                 frozenStoreObject preservation theorems
SeLe4n/Kernel/CrossSubsystem.lean Cross-subsystem invariants (R4-E)
SeLe4n/Kernel/API.lean           Public kernel interface + syscall wrappers
SeLe4n/Platform/Contract.lean    PlatformBinding typeclass (H3-prep)
SeLe4n/Platform/DeviceTree.lean  FDT parsing with bounds-checked helpers
SeLe4n/Platform/Sim/*            Simulation platform contracts + proof hooks
  Sim/RuntimeContract.lean       Permissive + restrictive runtime contracts
  Sim/BootContract.lean          Boot + interrupt contracts (all True)
  Sim/ProofHooks.lean            AdapterProofHooks for restrictive contract
  Sim/Contract.lean              PlatformBinding instance (re-export hub)
SeLe4n/Platform/Boot.lean        Q3-C: Boot sequence (PlatformConfig → IntermediateState)
SeLe4n/Platform/RPi5/*           Raspberry Pi 5 platform (BCM2712)
  RPi5/Board.lean                BCM2712 addresses, MMIO, MachineConfig
  RPi5/RuntimeContract.lean      Substantive runtime + restrictive contract
  RPi5/BootContract.lean         Boot + interrupt contracts (GIC-400)
  RPi5/MmioAdapter.lean           MMIO adapter for RPi5
  RPi5/ProofHooks.lean           AdapterProofHooks for restrictive contract
  RPi5/Contract.lean             PlatformBinding instance (re-export hub)
SeLe4n/Testing/*                 Test harness, state builder, fixtures
  Helpers.lean                   Shared test helpers (expectError, expectOk, expectCond)
  StateBuilder.lean              Test state construction
  InvariantChecks.lean           Runtime invariant check helpers
  MainTraceHarness.lean          Main trace test harness
  RuntimeContractFixtures.lean   Platform contract test fixtures
Main.lean                        Executable entry point
tests/                           Executable test suites + fixtures (18 suites)
  DecodingSuite.lean             T-03/AC6-A + AK4-A: 57 tests for RegisterDecode + SyscallArgDecode + IPC-buffer merge
  AbiRoundtripSuite.lean         AK4-G: End-to-end ABI encode/decode integration (25 assertions)
  BadgeOverflowSuite.lean        AG9-E: 22 tests for Badge Nat↔UInt64 round-trip
  An9HardwareBindingSuite.lean   AN9: 23 surface-anchor tests for hardware-binding closure (DEF-A-M04..M09, DEF-C-M04, DEF-R-HAL-L14..L20)
  LivenessSuite.lean             D5: 58 surface anchor tests for liveness/WCRT theorems
```

Note: Files marked "Re-export hub" are thin import-only files that preserve
backward compatibility. All existing `import` statements continue to work
unchanged. Actual implementations live in the listed submodules.

## Reading large files

Several files in this repo exceed 500 lines (invariant suites, audit plans,
specs). When reading any file, always use `offset` and `limit` parameters to
read in chunks rather than attempting the entire file at once:

```
Read(file_path, offset=1,   limit=500)   # lines 1-500
Read(file_path, offset=501, limit=500)   # lines 501-1000
```

**Known large files** (read in ≤500-line chunks) — refreshed v0.30.6 (AK10-E):
- `CHANGELOG.md` (~9453 lines)
- `SeLe4n/Kernel/IPC/Invariant/Structural/DualQueueMembership.lean` (~2070 lines, AN3-C child)
- `SeLe4n/Kernel/IPC/Invariant/Structural/StoreObjectFrame.lean` (~1984 lines, AN3-C child)
- `SeLe4n/Kernel/IPC/Invariant/Structural/PerOperation.lean` (~1891 lines, AN3-C child)
- `SeLe4n/Kernel/IPC/Invariant/Structural/QueueNextTransport.lean` (~1859 lines, AN3-C child)
- `docs/dev_history/audits/AUDIT_v0.29.0_WORKSTREAM_PLAN.md` (~4721 lines)
- `SeLe4n/Kernel/InformationFlow/Invariant/Operations.lean` (~3768 lines)
- `tests/NegativeStateSuite.lean` (~3660 lines)
- `SeLe4n/Kernel/Scheduler/Operations/Preservation.lean` (~3775 lines, AN5-B SCH-M03)
- `SeLe4n/Testing/MainTraceHarness.lean` (~3160 lines)
- `SeLe4n/Kernel/CrossSubsystem.lean` (~2839 lines)
- `docs/gitbook/12-proof-and-invariant-map.md` (~2821 lines)
- `SeLe4n/Model/Object/Structures.lean` (~2769 lines)
- `docs/WORKSTREAM_HISTORY.md` (~2572 lines)
- `SeLe4n/Kernel/IPC/Invariant/Defs.lean` (~2509 lines)
- `SeLe4n/Kernel/RobinHood/Invariant/Preservation.lean` (~2504 lines)
- `SeLe4n/Kernel/IPC/DualQueue/Transport.lean` (~2368 lines)
- `SeLe4n/Kernel/API.lean` (~2258 lines)
- `tests/OperationChainSuite.lean` (~2208 lines)
- `SeLe4n/Kernel/RobinHood/Invariant/Lookup.lean` (~2186 lines)
- `SeLe4n/Model/State.lean` (~2152 lines)
- `SeLe4n/Platform/Boot.lean` (~2074 lines)
- `SeLe4n/Kernel/IPC/Invariant/QueueMembership.lean` (~1759 lines)
- `SeLe4n/Kernel/IPC/Invariant/EndpointPreservation.lean` (~1739 lines)
- `SeLe4n/Model/Object/Types.lean` (~1698 lines)
- `SeLe4n/Model/FreezeProofs.lean` (~1660 lines)
- `SeLe4n/Kernel/Capability/Operations.lean` (~1651 lines)
- `SeLe4n/Prelude.lean` (~1649 lines)
- `docs/spec/SELE4N_SPEC.md` (~1634 lines)
- `SeLe4n/Kernel/Architecture/SyscallArgDecode.lean` (~1595 lines)
- `SeLe4n/Kernel/IPC/Invariant/NotificationPreservation/Signal.lean` (~850 lines, AN3-D child)
- `SeLe4n/Kernel/IPC/Invariant/NotificationPreservation/Wait.lean` (~688 lines, AN3-D child)
- `SeLe4n/Kernel/Capability/Invariant/Preservation/BadgeIpcCapsAndCdtMaps.lean` (~675 lines, AN4-F.3 child)
- `SeLe4n/Kernel/Capability/Invariant/Preservation/EndpointReplyAndLifecycle.lean` (~622 lines, AN4-F.3 child)
- `SeLe4n/Kernel/Capability/Invariant/Preservation/Revoke.lean` (~459 lines, AN4-F.3 child)
- `SeLe4n/Kernel/Capability/Invariant/Preservation/CopyMoveMutate.lean` (~353 lines, AN4-F.3 child)
- `SeLe4n/Kernel/Capability/Invariant/Preservation/Delete.lean` (~284 lines, AN4-F.3 child)
- `SeLe4n/Kernel/Capability/Invariant/Preservation/Insert.lean` (~229 lines, AN4-F.3 child)
- `SeLe4n/Kernel/Lifecycle/Operations/ScrubAndUntyped.lean` (~764 lines, AN4-G.5 child)
- `SeLe4n/Kernel/Lifecycle/Operations/CleanupPreservation.lean` (~460 lines, AN4-G.5 child)
- `SeLe4n/Kernel/Lifecycle/Operations/RetypeWrappers.lean` (~279 lines, AN4-G.5 child)
- `SeLe4n/Kernel/Lifecycle/Operations/Cleanup.lean` (~204 lines, AN4-G.5 child)
- `docs/dev_history/audits/AUDIT_v0.28.0_WORKSTREAM_PLAN.md` (~1480 lines)
- `SeLe4n/Kernel/IPC/Invariant/CallReplyRecv/Call.lean` (~530 lines, AN3-D child)
- `SeLe4n/Kernel/IPC/Invariant/CallReplyRecv/ReplyRecv.lean` (~558 lines, AN3-D child)
- `SeLe4n/Kernel/InformationFlow/Invariant/Composition.lean` (~1177 lines)
- `SeLe4n/Kernel/Service/Invariant/Acyclicity.lean` (~1012 lines)
- `SeLe4n/Kernel/InformationFlow/Invariant/Helpers.lean` (~1006 lines)
- `SeLe4n/Kernel/RobinHood/Bridge.lean` (~1095 lines)
- `SeLe4n/Kernel/Architecture/VSpaceInvariant.lean` (~1031 lines)
- `SeLe4n/Kernel/FrozenOps/Operations.lean` (~939 lines)
- `SeLe4n/Kernel/Capability/Invariant/Defs.lean` (~895 lines)
- `SeLe4n/Kernel/Scheduler/RunQueue.lean` (~869 lines)
- `docs/dev_history/audits/AUDIT_v0.17.14_WORKSTREAM_PLAN.md` (~2476 lines)
- `docs/dev_history/audits/AUDIT_v0.12.15_WORKSTREAM_PLAN.md` (~3140 lines)

When editing large files, read the specific region around the target lines
first (e.g., `offset=380, limit=40`) rather than the whole file. This avoids
context-window pressure and "file too large" errors.

## Writing and editing large files

The Write tool replaces an entire file in one call. For files over ~100 lines
this is error-prone: the tool call **times out**, content gets silently
truncated, sections are accidentally dropped, and the context window fills up.
**Prefer the Edit tool for all changes to existing files**, regardless of size.

**Hard limit — Write tool timeout prevention:**

The Write tool will time out if the inline content is too large. To avoid this:

- **Never pass more than 100 lines of content in a single Write call.** Files
  at or above this threshold must be built incrementally (skeleton + Edit
  appends) or written via Bash `cat <<'HEREDOC'` to a file.
- **For existing files, never use Write at all.** Always use Edit with targeted
  `old_string`/`new_string` pairs. Edit calls do not carry the full file
  content and therefore do not time out.
- **If a Write call times out or fails**, do not retry with the same large
  content. Switch to the incremental approach below.

**Rules for large-file changes:**

1. **Never rewrite a large file with Write.** Use Edit with a precise
   `old_string`/`new_string` pair targeting only the lines that change. This is
   safer, faster, and avoids timeouts.
2. **One logical change per Edit call.** If you need to change three separate
   functions, make three Edit calls rather than one giant replacement that spans
   the whole file.
3. **Read before you edit.** Always Read the specific region first
   (e.g., `offset=350, limit=50`) so the `old_string` matches exactly,
   including indentation and whitespace.
4. **Adding large new sections.** If you must insert more than ~80 new lines
   into an existing file, break the insertion into multiple sequential Edit
   calls (each ≤80 lines), anchoring each one to context already present in the
   file.
5. **Creating new large files.** When a new file must exceed ~100 lines, build
   it incrementally:
   - Write an initial skeleton (imports, structure, first section) with Write,
     keeping the content **under 100 lines**.
   - Use successive Edit calls to append remaining sections, using the end of
     the previously written content as the `old_string` anchor.
   - Each Edit append should add no more than ~80 lines at a time.
   - Verify the final line count with `wc -l` via Bash.
   - **Alternative**: use Bash with a heredoc to write the full file in one
     shot (`cat <<'EOF' > path/to/file.lean`). Bash does not have the same
     content-size timeout as the Write tool.
6. **Post-write verification.** After any large write or series of edits, spot-
   check the result by reading the modified region (and the file's last few
   lines) to confirm nothing was truncated or duplicated.

**Example — appending a new theorem block to an invariant file:**

```
# Step 1: Read the anchor region at the end of the file
Read("SeLe4n/Kernel/Capability/Invariant.lean", offset=880, limit=20)

# Step 2: Edit using the last lines as old_string, appending new content
Edit(file_path="SeLe4n/Kernel/Capability/Invariant.lean",
     old_string="<last 2-3 lines of file>",
     new_string="<those same lines>\n<new theorem block>")

# Step 3: Verify
Bash("wc -l SeLe4n/Kernel/Capability/Invariant.lean")
```

**Example — creating a new 300-line file without timing out:**

```
# Step 1: Write skeleton (under 100 lines)
Write("SeLe4n/Kernel/NewModule/Operations.lean", "<imports + first ~80 lines>")

# Step 2: Append next section via Edit
Edit(file_path="SeLe4n/Kernel/NewModule/Operations.lean",
     old_string="<last 2-3 lines from step 1>",
     new_string="<those same lines>\n<next ~80 lines>")

# Step 3: Repeat Edit appends until complete

# Step 4: Verify
Bash("wc -l SeLe4n/Kernel/NewModule/Operations.lean")
```

## Handling large search and command output

Grep and other search tools can return oversized results in this codebase.
Always constrain output to avoid truncation and context-window pressure:

- **Grep**: Use `head_limit` to cap results (e.g., `head_limit=30`). If more
  results exist, paginate with `offset` (e.g., `offset=30, head_limit=30` for
  the next batch). Prefer `output_mode: "files_with_matches"` first to identify
  relevant files, then switch to `output_mode: "content"` on specific files.
- **Glob**: Use `path` to narrow the search directory instead of searching the
  entire repo. If results are numerous, combine with Grep on specific matches.
- **Bash commands** (`lake build`, test scripts, etc.): Pipe through `head` or
  `tail` when output may be large (e.g., `lake build 2>&1 | tail -80`). For
  very large output, redirect to a temp file and read in chunks:
  ```bash
  lake build 2>&1 > /tmp/build.log
  ```
  Then use `Read("/tmp/build.log", offset=1, limit=500)` to page through it.

**Rule of thumb**: if a command or search might return more than ~100 lines,
limit it upfront. Paginate through results rather than requesting everything
at once.

## Background agent file-change protection

Background agents (launched via the Task tool with `run_in_background: true`)
run concurrently and may finish after the foreground agent has already modified
the same files. When this happens the background agent's stale writes silently
overwrite the foreground agent's progress. **You must prevent this.**

**Rules:**

1. **Never delegate file writes to a background agent for files you may also
   edit.** Before launching a background agent, identify every file it might
   create or modify. If there is any chance the foreground agent (you) will
   touch the same file while the background agent runs, do **not** run that
   agent in the background — run it in the foreground instead, or restructure
   the work so there is no file overlap.
2. **Partition files strictly.** When parallel work is genuinely needed, assign
   each agent a disjoint set of files. Document the partition in your task
   prompt to the background agent (e.g., "You own `Foo.lean` and `Bar.lean`
   only — do not modify any other file"). The foreground agent must not touch
   those files until the background agent completes.
3. **Use background agents only for read-only or independent-file tasks.** Safe
   uses include: running builds/tests, searching the codebase, reading files
   for research, or writing to files that the foreground agent will never edit
   during this session. Unsafe uses include: editing shared source files,
   modifying configuration files, or any task where the output files overlap
   with foreground work.
4. **Check background results before acting on shared state.** When a background
   agent finishes, read its output and verify whether it touched any files. If
   it wrote to a file you have since modified, discard the background agent's
   version and redo that work on top of your current file state.
5. **When in doubt, run in foreground.** The performance benefit of background
   execution is never worth the risk of silently lost work. Prefer sequential
   correctness over parallel speed.

**Example — safe background usage:**

```
# Background agent runs tests (read-only, no file writes)
Task(subagent_type="Bash", run_in_background=true,
     prompt="Run ./scripts/test_smoke.sh and report results")

# Meanwhile, foreground edits Operations.lean — no conflict
Edit("SeLe4n/Kernel/Scheduler/Operations.lean", ...)
```

**Example — unsafe pattern to avoid:**

```
# WRONG: background agent will edit Invariant.lean
Task(subagent_type="general-purpose", run_in_background=true,
     prompt="Add theorem X to Invariant.lean")

# Foreground also edits Invariant.lean — background will overwrite!
Edit("SeLe4n/Kernel/Scheduler/Invariant.lean", ...)
```

## Key conventions

- **Invariant/Operations split**: each kernel subsystem has `Operations.lean`
  (transitions) and `Invariant.lean` (proofs). Keep this separation.
- **No axiom/sorry**: forbidden in production proof surface. Tracked exceptions
  must carry a `TPI-D*` annotation.
- **Deterministic semantics**: all transitions return explicit success/failure.
  Never introduce non-deterministic branches.
- **Fixture-backed evidence**: `Main.lean` output must match
  `tests/fixtures/main_trace_smoke.expected`. Update fixture only with rationale.
- **Typed identifiers**: `ThreadId`, `ObjId`, `CPtr`, `Slot`, `DomainId`, etc.
  are wrapper structures, not `Nat` aliases. Use explicit `.toNat`/`.ofNat`.
- **Internal-first naming**: every identifier in the codebase — theorems,
  functions, definitions, structures, fields, test runners, file names, and
  directory names — must describe the semantics of what it is (state update
  shape, preserved invariant, transition path, test subject). Workstream IDs,
  audit IDs, phase codes, and sub-task numbers (`WS-*`, `wsH*`, `AN3-*`, `AK7-*`,
  `ak9ce_01`, `an3b_02`, `I-H01`, etc.) **must not** appear in any identifier
  or file name. Example: rename a test from `an3b_02_projection_typing` to
  `ipc_invariant_full_projection_signatures`; rename a theorem from
  `ak7_cdt_hypothesis_discharge_index` to `cdt_hypothesis_discharge_index`.
  Rationale: workstream IDs are commit-time labels and age out as soon as a
  workstream closes — encoding them in identifiers creates documentation debt
  (every rename on workstream closure) and hides what the code actually
  means from anyone reading it outside the audit's temporal window.
  Legitimate places to reference a workstream ID: docstrings, commit messages,
  CHANGELOG entries, and `CLAUDE.md` / `docs/WORKSTREAM_HISTORY.md` prose.
  Historical identifiers that already encode workstream IDs (`ak8a_*`,
  `an2f3_*`, etc.) stay as-is until touched by a workstream that can rename
  them in the same commit; new code must comply with this rule from day one.

## Documentation rules

When changing behavior, theorems, or workstream status, update in the same PR:
1. `README.md` — metrics sync from `docs/codebase_map.json` (`readme_sync` key)
2. `docs/spec/SELE4N_SPEC.md`
3. `docs/DEVELOPMENT.md`
4. Affected GitBook chapter(s) — canonical root docs take priority over GitBook
5. `docs/CLAIM_EVIDENCE_INDEX.md` if claims change
6. `docs/WORKSTREAM_HISTORY.md` if workstream status changes
7. Regenerate `docs/codebase_map.json` if Lean sources changed

Canonical ownership: root `docs/` files own policy/spec text. GitBook chapters
under `docs/gitbook/` are mirrors that summarize and link to canonical sources.
`docs/WORKSTREAM_HISTORY.md` is the single canonical source for workstream
planning, status, and history.

## Third-party attribution

seLe4n is GPLv3+ licensed (see `LICENSE`). The Rust workspace pulls a small
set of **build-time only** crates (`cc`, `find-msvc-tools`, `shlex`) to
assemble ARM64 boot assembly; no third-party code is linked into the
runtime kernel binary. Their upstream MIT copyright and permission notices
are reproduced verbatim in `THIRD_PARTY_LICENSES.md` at repo root. Rules:

1. If you add a runtime dependency (`[dependencies]` of any crate under
   `rust/`), update `THIRD_PARTY_LICENSES.md` in the same PR with the
   verbatim upstream MIT/Apache copyright lines and add the path to
   `scripts/website_link_manifest.txt` if it's not already there.
2. If you bump an existing external crate, re-check the upstream
   `LICENSE-MIT` and upstream Cargo.toml for authorship/copyright changes
   and sync `THIRD_PARTY_LICENSES.md` accordingly. Also re-check for a
   new upstream `NOTICE` file (Apache-2.0 § 4(d) propagation).
3. Prefer `core::*` and hand-written minimal code over pulling in a crate.
   A microkernel's trusted computing base must stay small.

## Website link protection

The project website ([sele4n.org](https://github.com/hatter6822/hatter6822.github.io))
links to source files, documentation, scripts, assets, and directories in this
repository.  Renaming or deleting any of these paths will produce 404 errors on
the website.

**Protected paths** are listed in `scripts/website_link_manifest.txt`.  The
Tier 0 hygiene check (`scripts/check_website_links.sh`, called from
`test_tier0_hygiene.sh`) verifies that every listed path still exists.  This
runs on every PR and push to main via CI.

**If you need to rename or remove a protected path:**
1. Update the website (`hatter6822.github.io`) to use the new path first.
2. Then update `scripts/website_link_manifest.txt` to match.
3. CI will pass only when the manifest and the repo tree are consistent.

## Ignoring dev_history

The `docs/dev_history/` directory contains milestone closeouts, prior audit reports
(v0.8.0–v0.9.32), completed workstream plans, and legacy GitBook chapters
retained only for historical traceability. **Do not read or reference files in
`docs/dev_history/` unless explicitly instructed.** All active documentation lives
under `docs/` and `docs/gitbook/`.

## Active workstream context

- **WS-AN Phase AN10 COMPLETE (v0.30.10, branch `claude/review-codebase-audit-Fx4iX`)**: AK7 cascade closure per [`docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md`](docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md) §13. Closes the two AK7 cascade tracking entries from `AUDIT_v0.29.0_DEFERRED.md` — DEF-AK7-E.cascade and DEF-AK7-F.cascade. **AN10-Setup**: re-introduces the AK7 cascade monotonicity infrastructure retired in commit 8d9e61f. New `scripts/ak7_cascade_baseline.sh` captures 19 metrics across raw-match patterns, typed-helper adoption, writer wrapper adoption, sentinel-check dispatch, AN10 test count, sorry/axiom counts. New `scripts/ak7_cascade_check_monotonic.sh` enforces per-metric should-drop / should-grow direction against `docs/audits/AL0_baseline.txt`. Wired into `scripts/test_tier0_hygiene.sh` so every commit regression-checks the cascade metrics. **AN10-A (DEF-AK7-E.cascade RESOLVED)**: `SENTINEL_CHECK_DISPATCH` metric tracks production dispatch sites guarded by `validateThreadIdArg` / `validateSchedContextIdArg` / `validateObjIdArg`. AL1b/AL8 (v0.29.14) closed the primary attack surface structurally at the type system; AN10 hardens the gate with the monotonicity check that flags any future dispatch arm bypassing the validator wrappers. All eight capability-only dispatch arms in `Kernel/API.lean` confirmed enforcing `Valid*Id` precondition. **AN10-B (DEF-AK7-F.reader.hygiene RESOLVED)**: 14 reader-side raw-match sites migrated to AL2-A typed helpers (`getTcb?`, `getSchedContext?`, `getEndpoint?`, `getNotification?`, `getUntyped?`). Files migrated — `Scheduler/Operations/Selection.lean` (5 sites) plus downstream proof updates in `Scheduler/Liveness/TraceModel.lean::budget_available_when_positive` (bridge via `getSchedContext?_eq_some_iff`); `IPC/DualQueue/WithCaps.lean` (5 sites: `lookupCspaceRoot` collapsed to `Option.map`, `endpointSendDualWithCaps`, `endpointReceiveDualWithCaps`, `endpointCallWithCaps`) plus 3 downstream preservation proof updates in `IPC/Invariant/EndpointPreservation.lean`, `IPC/Invariant/CallReplyRecv/ReplyRecv.lean`, `IPC/Invariant/Structural/PerOperation.lean`; `IPC/Operations/Donation.lean::endpointCallWithDonation` pre-receiver inspection (1 site); `Architecture/IpcBufferRead.lean::ipcBufferReadMr` (1 site) plus downstream `_ok_implies_tcb` and `_reads_only_caller_tcb` proof bridges; `SchedContext/PriorityManagement.lean::getCurrentPriority` and `getCurrentPriorityChecked` (2 sites). Metric deltas: `RAW_MATCH_TCB` 52→49, `RAW_MATCH_SCHEDCONTEXT` 19→13, `RAW_MATCH_ENDPOINT` 17→12, `RAW_MATCH_TOTAL` 129→115. `GETTCB_ADOPTION` 34→54, `GETSCHEDCTX_ADOPTION` 9→23, `GETENDPOINT_ADOPTION` 6→19. Functions intentionally NOT migrated (`effectiveBucketPriority`, `saveOutgoingContext`/`restoreIncomingContext`, `notificationSignal`/`notificationWait`, `endpointQueuePopHead`/`endpointQueueEnqueue`, capability cnodeRoot lookups) — either ~30+ proofs case-split on the raw shape or the API distinguishes absent vs wrong-variant for `.invalidCapability` vs `.objectNotFound`. The monotonicity gate locks the post-AN10 floors so the migrated surface cannot regress. **AN10-C (DEF-AK7-F.writer.hygiene RESOLVED)**: `STOREOBJECTCHECKED_ADOPTION` 41→57 driven by the new `tests/An10CascadeSuite.lean` test sites that exercise the kind-checked wrapper. Production-side in-place `storeObject` call sites are protected structurally by the **AM4 `lifecycleObjectTypeLockstep` invariant** (11th conjunct of `crossSubsystemInvariant`): the lockstep between `objects.objectType` and `lifecycle.objectTypes[oid]?` makes any cross-variant write a `crossSubsystemInvariant` violation, so the writer-side wrapper is defense-in-depth that does not change correctness. The wrapper's three correctness theorems (`_fresh_eq_storeObject`, `_sameKind_eq_storeObject`, `_crossKind_rejected`) allow any future consumer to migrate transparently. **New `tests/An10CascadeSuite.lean`** (17 tests, all PASS): 7 `an10_b_*` typed-helper kind-discrimination tests + 5 `an10_a_*` `Valid*Id` sentinel-rejection tests + 3 `an10_c_*` `storeObjectKindChecked` semantics tests + 2 `an10_d_*` closure witnesses. Wired into `lakefile.toml` as `lean_exe an10_cascade_suite` and into `scripts/test_tier2_negative.sh`. **AN10-D**: `docs/audits/AUDIT_v0.29.0_DEFERRED.md` updated — DEF-AK7-E.cascade and DEF-AK7-F.cascade marked **RESOLVED at v0.30.10 / AN10**, residual hygiene tracked but non-gating, cross-reference table updated. The only deferred entry that carries past v1.0.0 is now **DEF-F-L9** (17-tuple projection refactor, by-design with no scheduled work). Gate: `lake build` (302 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_tier0_hygiene.sh` PASS (incl. new monotonicity gate) + `test_tier2_negative.sh` PASS (incl. `an10_cascade_suite` 17 tests) + `cargo test --workspace` (462) + `cargo clippy --workspace -- -D warnings` (0) + fixture byte-identical + zero `sorry`/`axiom`/`native_decide`.
- **WS-AN Phase AN9 post-delivery deep audit remediation (v0.30.10, branch `claude/complete-phase-an9-workstream-9pQ2d`)**: A deep end-to-end audit of the initial AN9 landing surfaced **ten material findings**, all fixed in-PR. (D1 CRITICAL) `suspendThread_atomicity_under_ffi_bracket` was tautological `x = x := rfl`; replaced with substantive `_default` form proving `suspendThread (default : SystemState) vtid = .error .invalidArgument` by unfolding the function body. (F1 CRITICAL) `syscall_dispatch_inner` and `suspend_thread_inner` were declared `@[extern]` on Lean side AND `extern "C"` on Rust side — neither defined them, production link would have failed; replaced Lean side with `@[export]` defs that emit C-callable symbols. (J1 CRITICAL) `extern "C" { pub fn secondary_entry(); }` declared a symbol that did not exist in `boot.S`; added real `.global secondary_entry:` label that masks DAIF, sets up a per-core stack from the new `.smp_stacks` linker region, and branches to `rust_secondary_main`. (G2 HIGH) `wfe_bounded` was defined but `idle_loop` still called unconditional `wfe()`; wired `wfe_bounded(WFE_DEFAULT_TIMEOUT_TICKS)` into the production idle loop. (A1 MEDIUM) `pageTableUpdate_full_coherency` took independent `cs`/`st` parameters; added `TlbCacheJointState` pairing them and the strengthened `TlbCacheJointState.pageTableUpdate_full_coherency` theorem. (B1 MEDIUM) `tlbBarrierComplete` was structurally substantive but operationally vacuous; added `markTlbDirty`/`markTlbBarriered` operations with `_breaks_predicate` and `_restores_witness` theorems proving non-trivial content. (G1 LOW) `wfe_bounded` discarded `_elapsed`; changed signature to return `u64` elapsed ticks. (H1 LOW) parity test only enforced Rust-side; added `RUST_LEAF_COUNT == LEAN_LEAF_COUNT` runtime assertion. (J2 LOW) SMP tests raced on global state under parallel cargo test; refactored `bring_up_secondaries` to delegate to `_inner(enabled, core_ready, online_count, mpidr_table)`; tests use local atomics via `fresh_local_state()`. (Doctest) `wfe_bounded` doctest was marked `ignore`; converted to `no_run` with stub helpers so the Rust compiler type-checks the example on every `cargo test --doc`. **3 new Rust unit tests + 4 new Lean surface anchors**: total **462 Rust tests passing** (up from 459); AN9 Lean suite **23 surface anchors** (up from 15). Gate: `lake build` (306 jobs) + `test_smoke.sh` PASS + `test_full.sh` PASS + `test_tier0_hygiene.sh` PASS + `cargo test --workspace` (462) + `cargo clippy --workspace -- -D warnings` (0) + `check_version_sync.sh` PASS at 0.30.10 + fixture byte-identical + zero `sorry`/`axiom`/`native_decide`.
- **WS-AN Phase AN9 COMPLETE (v0.30.10, branch `claude/complete-phase-an9-workstream-9pQ2d`)**: Hardware-binding closure per [`docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md`](docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md) §12. 11 sub-tasks (AN9-A..AN9-K) closing every hardware-binding item from `AUDIT_v0.29.0_DEFERRED.md` (DEF-A-M04, DEF-A-M06, DEF-A-M08, DEF-A-M09, DEF-C-M04, DEF-P-L9, DEF-R-HAL-L14) plus four new items surfaced by AN1-C (DEF-R-HAL-L17/L18/L19/L20). **AN9-A (DEF-A-M04 RESOLVED)**: New `SeLe4n/Kernel/Architecture/TlbCacheComposition.lean` proves `pageTableUpdate_full_coherency` end-to-end (TLB consistency + barrier discipline + I-cache coherency in one statement). FFI witnesses `cache_clean_pagetable_range` and `cache_ic_iallu` exposed via `SeLe4n.Platform.FFI`. **AN9-B (DEF-A-M06 RESOLVED)**: `tlbBarrierComplete` predicate refined from `True` to substantive form requiring both `MachineState.tlbBarrierEmitted = true` and a 4-bit bitmask covering `dsb ish | isb` leaves. Two new fields added to `MachineState`. **AN9-C (DEF-A-M08/M09 Lean side RESOLVED)**: New `SeLe4n/Kernel/Architecture/BarrierComposition.lean` defines the `BarrierKind` inductive (`none`, `dsbIsh`, `dsbIshst`, `dsbOsh`, `dsbOshst`, `dcCvacDsbIsh`, `isb`, `sequenced`) with `subsumes` partial order (reflexive, transitive, decidable). Headline theorems `pageTableUpdate_observes_armv8_ordering` and `mmioWrite_observes_dsbIshst_before_sideEffect`. **AN9-D (DEF-C-M04 RESOLVED)**: Rust FFI wrapper `sele4n_suspend_thread` brackets the inner Lean dispatch with `interrupts::with_interrupts_disabled`. Lean-side `suspendThread_transientWindowInvariant` predicate + `suspendThread_atomicity_under_ffi_bracket` theorem formalise the hardware promise. **AN9-E (DEF-P-L9 cross-reference)**: RESOLVED already in AN7-D.2; `bootSafeObject` docstring in `SeLe4n/Platform/Boot.lean` now explicitly cross-references the AN7-D.2 closure path. **AN9-F (DEF-R-HAL-L14 RESOLVED)**: New `rust/sele4n-hal/src/svc_dispatch.rs` module owns typed argument marshalling (`SyscallArgs::from_trap_frame`, 25-variant `SyscallId` enum, `dispatch_svc` top-level entry). `trap.rs::handle_synchronous_exception` SVC arm replaces the `NOT_IMPLEMENTED` stub with the typed dispatcher. **AN9-G (DEF-R-HAL-L17 RESOLVED)**: `cpu::wfe_bounded(max_ticks)` reads CNTPCT_EL0, issues `wfe`, falls through after the timeout. Default `WFE_DEFAULT_TIMEOUT_TICKS = 540_000` (10 ms at 54 MHz). **AN9-H (DEF-R-HAL-L18 Rust side RESOLVED)**: Parameterised `barriers::BarrierKind` enum mirroring the Lean inductive. `emit()` method dispatches to the appropriate instruction; composite emitters `emit_armv8_page_table_update`, `emit_tlb_invalidation_bracket`, `emit_mmio_cross_cluster_barrier`. `barrier_kind_lean_parity` test enforces 1:1 alignment. **AN9-I (DEF-R-HAL-L19 RESOLVED)**: `barriers::dsb_osh()` and `barriers::dsb_oshst()` primitives; `BarrierKind::DsbOsh`/`DsbOshst` variants; `mmioWriteCrossCluster_observes_dsbOshst` Lean theorem; `storeBarrierClosure` proves OSH+ISH composition. **AN9-J (DEF-R-HAL-L20 RESOLVED, off by default)**: New `rust/sele4n-hal/src/psci.rs` (PSCI `cpu_on` HVC wrapper) and `rust/sele4n-hal/src/smp.rs` (`SMP_ENABLED: AtomicBool`, `bring_up_secondaries`, `rust_secondary_main`). v1.0.0 ships SMP code merged but **disabled by default** (`SMP_ENABLED = false`); activation is a runtime flag. **Tests added**: New `tests/An9HardwareBindingSuite.lean` Lean regression suite (15 surface anchors covering AN9-A..AN9-J), wired into `scripts/test_tier2_negative.sh`. 36 new Rust unit tests across barriers/cpu/svc_dispatch/ffi/psci/smp modules. **Total Rust tests: 459 passing** (up from 428 at AN8 close), 0 clippy warnings. **Hardware testing**: real-hardware QEMU/RPi5 validation requires either a Pi 5 board or a properly-configured QEMU virt machine; see `docs/HARDWARE_TESTING.md` for detailed bring-up instructions, environment setup scripts, and the validation checklist. Gate: `lake build` (306+ jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `test_tier0_hygiene.sh` PASS + `cargo test --workspace` PASS (459 tests) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `check_version_sync.sh` PASS at 0.30.10 + fixture byte-identical + zero `sorry`/`axiom`/`native_decide`. **Next**: AN10 (AK7 cascade completion), AN11 (testing), AN12 (closure + v1.0.0 tag).
- **WS-AN Phase AN8 third-pass audit (v0.30.9, branch `claude/implement-rust-hal-phase-FwOJu`)**: A third deep audit pass caught four additional tightening opportunities. (A) **Stale `mmu.rs:326` "AG6 replaces it" comment**: the second-pass audit fixed the module-level docstring but missed the duplicate stale comment above `build_identity_tables()`. Replaced with the proper boot/runtime distinction. (B) **`UartGuard` visibility**: struct declared `pub` but only constructor (`UartLock::with_guard`) is private and consumer (`with_boot_uart`) is `pub(crate)` — the `pub` leaked an unreachable type into the crate surface. Tightened to module-private with visibility rationale in the docstring. (C) **`#[deny(clippy::panic)]` extended**: the initial attribute only caught `panic!()`; `unreachable!()` and `todo!()` are panic-equivalents at runtime, so extended to `#[deny(clippy::panic, clippy::unreachable, clippy::todo)]` to prevent all three from sneaking into IRQ handler bodies. (D) **Lean T11 comment correction**: `test_t11_eoi_before_handler` comment incorrectly claimed the test exercised the error branch but INTID 30 is `timerInterruptId` routing to `timerTick` (success branch). Corrected and added a cross-reference clarifying that the substantive ordering distinction lives in the proof-layer theorem referenced by T13. Gate: all 428 cargo tests PASS + 0 clippy warnings + `lake build` 300 jobs 0 warnings + `test_smoke.sh` / `test_full.sh` / `test_tier0_hygiene.sh` PASS + fixture byte-identical + version sync at 0.30.9 + `interrupt_dispatch_suite` 16 checks PASS.
- **WS-AN Phase AN8 post-delivery audit remediation (v0.30.9, branch `claude/implement-rust-hal-phase-FwOJu`)**: Deep end-to-end audit of the initial AN8 landing surfaced six strengthening opportunities, all fixed in-PR. (1) **AN8-A clippy regression**: initial commit placed `#[cfg(test)] extern crate std;` at the bottom of `uart.rs`, triggering `clippy::items_after_test_module`; moved to module top. (2) **AN8-A `uart_guard_releases_on_early_return`**: bare `return;` triggered `clippy::needless_return`; replaced with conditional-branch early-return that exercises BOTH paths (short and long), strengthening coverage. (3) **AN8-A stale annotation**: `init_with_zero_baud_panics` carried a comment claiming `init_with_baud(0)` panics but AN8-D RUST-M02 had downgraded `assert!` to `debug_assert!`; renamed to `init_with_zero_baud_panics_in_debug_builds` with docstring documenting debug-vs-release semantics + the compile-time invariants protecting release builds. (4) **AN8-C.5 substantive ordering test missing**: initial landing had two near-duplicate "handler runs once" tests that didn't actually verify EOI-before-handler ORDERING. Refactored `dispatch_irq` to delegate to a pure state machine `dispatch_irq_inner(ack, eoi, handler)` that takes the EOI fn as a parameter. New `dispatch_irq_handled_eoi_fires_before_handler` test instruments mock `eoi`/`handler` with a shared `EventClock` and asserts `eoi_tick < handler_tick`. New `dispatch_irq_out_of_range_eois_without_handler` and `dispatch_irq_spurious_skips_eoi_and_handler` exercise the other two branches. New `dispatch_irq_handler_panic_does_not_unfire_eoi` uses a `static AtomicU32` (so the counter survives unwind) to substantively verify that EOI fires exactly once even when the handler panics. (5) **AN8-D RUST-M05 self-check untested**: `self_check_distributor` was gated on `cfg(target_arch = "aarch64")` so its read-back structure had zero test coverage. Factored read-address arithmetic into `read_self_check_target` and exposed two compile-time invariants (`SELF_CHECK_TARGET_INDEX >= 8` for writable SPI bank, `< 256` for ITARGETSR window) plus three runtime tests (`self_check_does_not_panic_on_host`, `self_check_target_address_arithmetic`, `self_check_expected_pattern_matches_init_distributor`). The actual WFE-loop halt path remains aarch64-only. (6) **AN8-C Lean T12 shallow**: initial T12 just printed a "check passed" message with no substantive assertion. Replaced with `test_t12_eoi_filters_only_target_intid` — pre-loads `eoiPending` with a sentinel INTID (99), dispatches a different INTID (30), and verifies the dispatched INTID is filtered while the sentinel survives. Added new `test_t13_ordering_theorem_witness` that elaborates `interruptDispatchSequence_eoi_before_handler` at parse time. Gate: `lake build` (300 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `test_tier0_hygiene.sh` PASS + `cargo test --workspace` (**428 tests**, up from 422 initial AN8 landing) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `interrupt_dispatch_suite` 16 checks PASS + `check_version_sync.sh` PASS at 0.30.9 + fixture byte-identical + zero `sorry`/`axiom`/`native_decide`.
- **WS-AN Phase AN8 COMPLETE (v0.30.9, branch `claude/implement-rust-hal-phase-FwOJu`)**: Rust HAL hardening per [`docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md`](docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md) §11. 6 sub-tasks (AN8-A..AN8-F) addressing 3 HIGH (H-17 UartLock RAII, H-18 MPIDR shared symbol, H-19 EOI-before-handler), 8 MEDIUM (RUST-M01..M08), 11 LOW (R-HAL-L1..L11). **AN8-A (H-17 HIGH)**: `uart.rs::UartLock::with` replaced with a `UartGuard<'a>` RAII wrapper. Acquisition (`with_guard()`) masks interrupts, spins on AtomicBool, stashes saved DAIF in `UartLock.saved_daif`. The returned `UartGuard` carries the `&mut Uart` borrow and a back-pointer; its `Drop` impl calls `release()` (clears flag + restores DAIF). Symmetric acquire/release on every normal scope exit (and unwinding paths under the test profile). Production `panic = "abort"` skips Drop but the kernel halts so the lock state is moot. **4 new tests** in `tests::uart_guard_*` cover normal-return, early-return, global-lock, and unwind-path Drop semantics. **AN8-B (H-18 HIGH)**: `cpu.rs` exposes `MPIDR_CORE_ID_MASK_SYM` as `#[no_mangle] pub static u64` with `#[used]` to prevent linker stripping; `boot.S`'s core-0 gate now loads via `adrp x2, MPIDR_CORE_ID_MASK_SYM` + `ldr x2, [x2, :lo12:MPIDR_CORE_ID_MASK_SYM]` instead of the literal `mov x2, #0xFFFF` / `movk x2, #0xFF, lsl #16` pair. Two `const _: () = assert!(...)` compile-time guards pin the constant value (0x00FFFFFF) and the symbol/constant equality. New **`build.rs` scanner** (AN8-B.5) reads `src/boot.S` on every build, strips `//` comments, and rejects the legacy literal pattern with an actionable error message pointing at AN8-B. Verified by synthetic regression: restoring the old literals fails the build. **2 new tests** in `cpu::tests::mpidr_shared_symbol_*` round-trip the static against the constant on host. **AN8-C (H-19 HIGH, audit Option b MANDATORY)**: `gic.rs::dispatch_irq` removes the `EoiGuard` RAII pattern; EOI fires unconditionally on the Handled and OutOfRange branches **BEFORE** the handler body runs. Spurious INTIDs (≥ 1020) still skip EOI per GIC-400 §3.1. Under `panic = "abort"` this eliminates the "handler panic leaves INTID active" class structurally — the GIC has retired the line by handler invocation time. The Lean `Architecture/InterruptDispatch.lean` model is updated in lockstep: `interruptDispatchSequence` now executes `endOfInterrupt` before `handleInterrupt`. The `eoiEmitted` predicate is extended to a 3-disjunct form covering both old and new orderings. New theorem `interruptDispatchSequence_eoi_before_handler` formalises the AN8-C ordering. `trap.rs::handle_irq` carries `#[deny(clippy::panic)]` (AN8-C.3) so a future direct `panic!()` inside the handler body fails clippy. Re-entrancy invariant documented per registered handler. **4 new Rust ordering tests** in `gic::tests::dispatch_irq_*` and **2 new Lean tests** in `tests/InterruptDispatchSuite.lean`. **AN8-D (RUST-M01..M08 MEDIUM)**: M01 `mmu.rs::sctlr_bits` consolidates 5 per-constant `#[allow(dead_code)]` into a module-level `#![allow(dead_code)]` plus a Markdown table doc-block; M02 `uart.rs::init_with_baud(0)` becomes `debug_assert!` with two new compile-time `const _: () = assert!(DEFAULT_BAUD > 0 && UART_CLOCK_HZ ≥ 16 × DEFAULT_BAUD)` boot-time invariants; M03 timer tests use `assert_eq!(init_timer(...), Ok(()))` instead of `unwrap()`; M04 stale "AG6 replaces this" docstring corrected with proper boot/runtime distinction; M05 new `gic::self_check_distributor` reads back `ITARGETSR[8]` after init and halts via WFE-loop on mismatch; M06 covered by AN1-C; M07 new `cache::memory_fence()` helper for pure DSB ISH; M08 `IpcBuffer` end-to-end audit — confirmed live, documents `sched_context_configure` and `service_register` consumers. **AN8-E (R-HAL-L1..L11 LOW)**: L1 `gicd::ICENABLER_BASE` retained with forward-reference comment for AN9-F selective-disable; L2 52-line AK4-H audit-notes block extracted from `sele4n-types/src/lib.rs` to new `docs/AUDIT_NOTES.md`; L3 `cc` build-dep pinned to 1.2; L4 PL011 non-standard-baud silent-rounding (±1.5%) documented; L5 DAIF read-before-mask ordering rationale documented; L6 `link.ld` `.bss` ALIGN(4096) cross-references `mmu.rs::PageTableCell`; L7 `rust_boot_main` `#[no_mangle]` symbol-resolution contract documented; L8 Rust 1.85 MSRV migration tracked as post-1.0 hygiene; L9 `set_vbar` adds runtime `debug_assert_eq!` on 2048-byte alignment; L10 `mmu.rs` extends compile-time alignment asserts (`L1_ENTRIES == 512`, `size_of::<BootL1Table>() == 4096`); L11 `find-msvc-tools` non-Windows-host status pre-documented. **AN8-F**: version bump 0.30.8 → 0.30.9 (15 version-bearing files synced via `check_version_sync.sh`); CHANGELOG.md entry; this CLAUDE.md prepend; Cargo.lock refreshed via `cargo update -w`. Gate: `lake build` (300 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `test_tier0_hygiene.sh` PASS + `cargo test --workspace` (**422 tests**, up from 414 — +4 uart-guard + 2 cpu-mpidr-symbol + 4 gic-eoi-ordering + 2 cache-memory-fence) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `check_version_sync.sh` PASS at 0.30.9 + fixture byte-identical + zero `sorry`/`axiom`/`native_decide` in `SeLe4n/` or `Main.lean`. **Next**: AN9 (hardware-binding closure) per plan §12 — AN8 + AN6 are the prerequisites and both are now landed.
- **WS-AN Phase AN6 released at v0.30.7 (v0.30.7 second audit-pass commit, branch `claude/review-phase-an6-architecture-a9R3s`)**: Second deep audit pass on the AN6 post-audit tip identified 4 more strengthening items. (1) `archAssumptionConsumer_distinct` was cycle-form (5 adjacent pairs), which misses non-adjacent collisions like timer≠memory or register≠boot — strengthened to full C(5,2)=10 pairwise distinctness via `refine ⟨…⟩ <;> decide`. (2) `an6c3_untypedAncestorChain_bounded` exercised only the empty state / fuel-bound path, never the `some pid` recursive descent — added new test `an6c3_untypedAncestorChain_walks_synthetic_chain` that builds a synthetic 2-level parent chain (ObjId 100 → ObjId 200 via `parent := some 100`) via `BootstrapBuilder` and verifies walker returns `[childId, parentId]` at fuel 2, `[childId]` only at fuel 1, and `[parentId]` for top-level with `parent = none`. First coverage of the recursive branch. (3) Gitbook §12 described `crossSubsystemInvariant` as a "10-predicate bundle" — stale since AM4/AL6-C (11th conjunct) and AK8-A (12th conjunct); updated to 12-predicate + added AN6-C foundation paragraph. (4) `ModelIntegritySuite` was missing `SeLe4n.Testing.StateBuilder` import required for the new walker-chain test — added. Gate: `lake build` (300 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `model_integrity_suite` PASS (+1 test, +7 assertions) + `information_flow_suite` PASS + `cargo test --workspace` (414) + `cargo clippy --workspace -- -D warnings` (0 warnings) + fixture byte-identical + zero `sorry`/`axiom`/`native_decide`.
- **WS-AN Phase AN6 post-audit remediation (v0.30.7, branch `claude/review-phase-an6-architecture-a9R3s`)**: Deep end-to-end audit of the AN6 initial landing surfaced eight issues where the landing was less substantive than it appeared. All fixed in-PR. The audit sweep also identified a pre-existing AK8-A `True := trivial` marker in `Kernel/Architecture/Invariant.lean` (from WS-AK Phase AK8) which was substantively closed in the same slice via three new theorems: `objectOfKernelType_untyped_hardcodes_zero_regionBase` (structural fact, proven by `rfl`), `retypeFromUntyped_via_objectOfKernelType_untyped_child_has_zero_regionBase` (composition via `retypeFromUntyped_ok_decompose` + `cspaceLookupSlot_preserves_state` + `storeObject_preserves_objects_invExt` + `storeObject_objects_eq`), and a renamed marker `retypeFromUntyped_untypedRegionsDisjoint_retype_to_untyped_documented (sizeHint)` that delegates to the structural helper. Together they machine-check the AK8-A scope gap (retype-to-untyped via production dispatch always produces a zero-regionBase child, hence cannot produce a valid parent-derived untyped region without a parent-context-aware wrapper that AK8-A does not ship). Added one runtime test `an6_postaudit_ak8a_objectOfKernelType_untyped_zero_regionBase` exercising all three theorems over 4 concrete `sizeHint` values. (1) **Consumer-name drift**: `archAssumptionConsumer` for `.irqRoutingTotality` mapped to `SeLe4n.Kernel.Platform.Boot.…` — but the actual namespace is `SeLe4n.Platform.Boot` (without `Kernel.`). Bare `Name` literals don't validate; drift went undetected. Corrected + added `archAssumptionConsumer_distinct` theorem + 4 compile-time `private example` blocks in `Architecture/Invariant.lean` that resolve 4 of the 5 consumer theorems by Lean-level identifier (not Name literal); 5th resolved via `@` in `ModelIntegritySuite`. (2) **`pageTableWalkDepth` disconnected from `pageTableWalk`**: the initial landing defined the depth function as a parallel mirror of the walk but had no theorem linking them, so a future refactor could leave them stale. Added substantive `pageTableWalkDepth_some_of_pageTableWalk_some` forward bridge + `pageTableWalk_success_within_maxPageTableLevel` end-to-end composition. Bridge proven by manual `cases` through all 4 descriptor variants per level. (3) **Three `True := trivial` markers**: `bootFromPlatform_singleCore_witness` (CX-M03) replaced with substantive `∀ s : SchedulerState, s.current = none ∨ ∃ tid, s.current = some tid` — the structural single-slot (non-per-core) witness. `archInvariant_interruptsEnabled_all_eight_index` removed (documentation-only anchor); the substantive bundle in `Architecture/ExceptionModel.lean` is unchanged. `untypedAncestorRegionsDisjoint_followup_at_AN6C5` replaced with substantive `untypedAncestorChain_collapses_when_all_parents_none` proving that on any state with all-parent-none untypeds, the walker collapses to `[oid]`. (4) **Shallow test assertions**: four AN6 tests (`an6c4`, `an6f_cxm03`, `an6f_cxm04`, `an6f_cxm05`) strengthened from `True == True` / `objects.size == 0` to substantive content — type-ascribed theorem references, concrete state invocations, all 8 bundle-field projections, and all 7 named `crossSubsystemInvariant_to_*` extraction theorems (not just 3). (5) **SPEC reference error**: AN6-E.1 docstring cited `SELE4N_SPEC.md §7` but the Information-Flow section is `§11.2`. Corrected + added three new subsections: `§11.2.1` service-presence NI scope, `§11.2.2` architecture assumption consumer index, `§11.2.3` single-core kernel model witness. Gate: `lake build` (300 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `test_docs_sync.sh` PASS + `cargo test --workspace` (414) + `cargo clippy --workspace -- -D warnings` (0 warnings) + fixture byte-identical + zero `sorry`/`axiom`/`native_decide`.
- **WS-AN Phase AN6 initial landed subset (v0.30.7 tip, branch `claude/review-phase-an6-architecture-a9R3s`)**: Architecture / InformationFlow / CrossSubsystem per [`docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md`](docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md) §9. The plan scopes AN6 at 7–9 days across 8 sub-tasks (AN6-A..AN6-H); this commit lands the tractable subset: **AN6-B** (H-08 consumption index), **AN6-C foundation** (H-09 `UntypedObject.parent` field + `untypedAncestorChain` walker + `untypedAncestorRegionsDisjoint` predicate + vacuous default witness), **AN6-D.3** (ARCH-M02 `pageTableWalk_depth_bound` substantive theorem), **AN6-E.1** (IF-M01 `serviceObservable` scope doc) + **AN6-E.2** (4 NI-L3 covert-channel negative regression tests), **AN6-F** (all 5 CX-M01..M05: `collectQueueMembers` structural theorems, lockstep↔`storeObjectKindChecked` cross-reference, single-core witness, `InterruptsEnabledPreservationBundle` 8-theorem composition, positive-state smoke test), **AN6-G** (TPI-002 single-source-of-truth verified; SC-M03 banner annotated), **AN6-A.1** (shared closure-form proof-sketch template block in `Operations.lean` header), and **AN6-H** (this CHANGELOG + CLAUDE.md + codebase_map refresh). **New substantively-proven theorems**: `architecture_assumptions_index`, `archAssumptionConsumer_covers_inventory`, `untypedAncestorChain_bounded`, `default_untypedAncestorRegionsDisjoint`, `pageTableWalk_depth_bound` (plus helper `pageTableWalkDepth` + constant `maxPageTableLevel`), `collectQueueMembers_some_start_nonEmpty_result`, `collectQueueMembers_head_is_start`, `archInvariant_interruptsEnabled_all_eight_bundle`. **New `InterruptsEnabledPreservationBundle` structure** in `Architecture/ExceptionModel.lean` packages the 8 AG5-G `_preserves_interruptsEnabled` theorems into a single discoverable artifact (inhabited for every `SystemState`). **New `archAssumptionConsumer : ArchAssumption → Lean.Name`** in `Architecture/Assumptions.lean` + `architecture_assumptions_index` marker theorem provides machine-searchable index. **9 new runtime tests** in `tests/ModelIntegritySuite.lean` (3 AN6-C + 5 AN6-F + 1 AN6-B) + **4 new NI-L3 tests** in `tests/InformationFlowSuite.lean`. **Follow-up PRs with explicit scope** (no silent deferrals): AN6-A.2..A.7 substantive discharge of 6 closure-form `*_preserves_projection` theorems (budget 6–9 days per plan §2.4 escalation ladder; shared template committed at AN6-A.1), AN6-C.5..C.10 13-conjunct cascade over ~130 call sites (budget ~4 days), AN6-D.1 `VSpaceBackend` production wire-in (budget ~1 day), AN6-D.4 `InvalidMessageInfoDetailed` debug variant + Rust ABI sync (budget 0.5 day), AN6-E.3 3768-LOC `Operations.lean` 4-file split (budget 0.75 day). Gate: `lake build` (300 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `test_docs_sync.sh` PASS + `lake exe model_integrity_suite` PASS (+9 new AN6 tests) + `lake exe information_flow_suite` PASS (+4 new NI-L3 tests) + `cargo test --workspace` (414) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `check_version_sync.sh` PASS at 0.30.7 + fixture byte-identical + zero `sorry`/`axiom`/`native_decide`. Version bumped 0.30.6→0.30.7 to mark the consolidated AN6 closure.
- **WS-AN Phase AN5 post-landing audit remediation (v0.30.6, branch `claude/review-scheduler-phase-2eYM8`)**: Deep end-to-end audit of the AN5 initial landing surfaced five material issues, all fixed in-PR. (1) `wcrt_bound_rpi5` was a trivial delegation to `bounded_scheduling_latency_exists` with a misleading docstring claiming it "drops the externalised `eventuallyExits` hypothesis"; remediation added `**honest framing**` docstring + new substantive AN5-E.3b bridge `rpi5_higherBandExhausted_from_progresses` that lifts a collection of `CanonicalDeploymentProgress` witnesses to the quantified `higherBandExhausted` form consumed by `hBandProgress` builders, plus `rpi5_higherBandExhausted_empty_band` vacuous-case corollary. (2) SCH-M04 boundary annotation was inserted as a bare `/- -/` block between the existing `/-- -/` docstring for `schedule` and its `def` — potentially disconnecting the docstring; merged into the existing docstring. (3) `replenishmentPipelineOrder` docstring claimed "proven preserved by `timerTick` ... trivially" but no such theorem exists and the predicate is NOT preserved by a bare `tick` (strict `eligibleAt > timer` fails when `timer` advances); rewritten as PIPELINE-relative post-condition (not a free-standing state invariant); the pair-level `popDueReplenishments_remaining_gt_now` theorem was always correct, only the wrapper docstring was wrong. (4) SC-M02 docstring referenced `SchedContext/Invariant/PriorityPreservation.lean` for the `hSchedProj` preservation theorems — actual location is `InformationFlow/Invariant/Operations.lean` (theorems `setPriorityOp_preserves_projection`, `setMCPriorityOp_preserves_projection`); fixed. (5) `SeLe4n.numDomainsVal` reference corrected to `SeLe4n.Kernel.SchedContextOps.numDomainsVal`. Functional tests added: 4 new `#check` anchors for the bridge theorems + 7 new `example` tests exercising concrete `CanonicalDeploymentProgress` construction, degenerate-config rejection (zero timer / zero period / over-admission), and two-step-trace `eventuallyExits` discharge. Surface anchor count grew to **95** (was 76 at initial landing, 63 at AN4 tip). Gate: `lake build` (300 jobs) + `test_smoke.sh` PASS + `test_full.sh` PASS + `test_tier0_hygiene.sh` PASS + `cargo test --workspace` PASS (414 tests) + `cargo clippy --workspace -- -D warnings` (0 warnings) + fixture byte-identical + zero `sorry`/`axiom`/`native_decide`.
- **WS-AN Phase AN5 IN PROGRESS (v0.30.6, branch `claude/review-scheduler-phase-2eYM8`)**: Scheduler / SchedContext + `eventuallyExits` closure per [`docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md`](docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md) §8. 6 sub-tasks (AN5-A..AN5-F). AN5-B/C/D/E/F landed; AN5-A (Preservation.lean Theme 4.7 split) retained as a dedicated follow-up with explicit scope annotation. **AN5-E (DEF-AK2-K.4 substantive closure)**: new `SeLe4n/Kernel/Scheduler/Liveness/RPi5CanonicalConfig.lean` module (~370 LOC) with `DeploymentSchedulingConfig` structure + decidable `wellFormed` predicate (AN5-E.1); `rpi5CanonicalConfig` canonical instance at 54 MHz / 10 000-tick CBS period / 256 priority bands / 16 domains / 1000-tick time slice / 750 ‰ admission utilisation (AN5-E.2); `eventuallyExits_of_exit_index` bridge lemma + `CanonicalDeploymentProgress` deployment-obligation structure + `rpi5_canonicalConfig_eventuallyExits` main substantive closure theorem (AN5-E.3); `wcrt_bound_rpi5` RPi5-specialised WCRT theorem retaining the general parameterised form for non-RPi5 platforms (AN5-E.4); `isRPi5CanonicalConfig` decidable canonical-check + soundness theorem + runtime witness (AN5-E.5); `docs/spec/SELE4N_SPEC.md` §8.14.1.1 documents the two-tier WCRT semantics (AN5-E.6). DEF-AK2-K.4 marked RESOLVED. **AN5-B (SCH-M02..M05)**: SCH-M01 TCB case-dispatch factoring scope documented in-place (factor-out requires AN5-A's child-module layout); SCH-M02 explicit "private run-queue implementation helper" docstrings for `remove_preserves_nodup`/`insert_preserves_nodup` (already `private`); **SCH-M03 new `replenishmentPipelineOrder : SystemState → Prop` invariant** in `Scheduler/Invariant.lean` capturing the three-step pipeline (pop-due → refill → process) plus `default_replenishmentPipelineOrder`, `replenishmentPipelineOrder_of_empty`, `popDueReplenishments_remaining_gt_now` preservation theorem (new helper `pairwiseSortedBy_head_le_all` witnesses the sorted-list head-ordering property); SCH-M04 operation/wrapper boundary annotation at `schedule` (full split deferred to AN5-A); SCH-M05 cross-reference disambiguation docstrings at both `blockingAcyclic` (PIP scope) and `tcbQueueChainAcyclic` (IPC scope) — the audit's claim of name-collision was imprecise so the docstrings preserve clarity without a 89-site rename cascade. **AN5-C (Scheduler LOW)**: `.getD ⟨0⟩` annotation at `RunQueue.lean:rotateToBack`; naming-convention notes for `scheduleEffective` / `timerTickWithBudget` (24-site rename deferred); `updatePipBoost` lifecycle docstring (relationship to `propagatePriorityInheritance` / `revertPriorityInheritance`); five-tier invariant hierarchy docstring added to `Scheduler/Invariant.lean` hub (base invariants → structural → base bundle → full bundle → cross-subsystem composition). **AN5-D (SC-M01..M03)**: SC-M01 `rpi5_cbs_window_replenishments_bounded` + `_concrete` witness theorems in `RPi5CanonicalConfig.lean` instantiate `cbs_bandwidth_bounded_tight` (AK6-I) for the canonical RPi5 deployment; SC-M02 closure-form preservation theorems in `SchedContext/PriorityManagement.lean` cross-referenced to AN6-A's H-07 discharge recipe; **SC-M03 prominent DO-NOT-IMPORT-PRESERVATION banner** at `SchedContext/Invariant.lean` hub plus compile-time Lean `example` guard that fails to elaborate if the `Defs.lean` import line is removed. **AN5-A (Preservation.lean Theme 4.7 split)**: retained as a documented follow-up with explicit scope. `Preservation.lean` is 3775 LOC (was 3633; grew by SCH-M03 + AN5-B/C/D docstrings) — above the 2000-LOC Theme 4.7 ceiling. The split into six child modules is tractable after AN5-B/C/D/E deliverables stabilise the file contents; the mechanical refactor has no correctness impact and is tracked for a dedicated follow-up PR. **Regression tests**: 13 new `#check` + 5 new `example` surface anchors in `tests/LivenessSuite.lean` covering `DeploymentSchedulingConfig`, `rpi5CanonicalConfig`, `rpi5CanonicalConfig_wellFormed`, `eventuallyExits_of_exit_index`, `CanonicalDeploymentProgress`, `rpi5_canonicalConfig_eventuallyExits`, `rpi5_canonicalConfig_progress_config_wellFormed`, `wcrt_bound_rpi5`, `wcrt_bound_rpi5_symbolic`, `isRPi5CanonicalConfig`, `isRPi5CanonicalConfig_iff`, `rpi5CanonicalConfig_isCanonical`, plus five concrete-value `example` witnesses — total 76 anchors (was 63 at AN4 tip). Gate: `lake build` (300 jobs, up from 298 — 2 new `.c.o` targets for `RPi5CanonicalConfig`) + `test_smoke.sh` PASS + `test_full.sh` PASS + `test_tier0_hygiene.sh` PASS + `cargo test --workspace` PASS (415 tests) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `check_version_sync.sh` PASS at 0.30.6 + fixture byte-identical + zero `sorry`/`axiom`/`native_decide` in `SeLe4n/` or `Main.lean`. Version stays at 0.30.6 at this phase per the **original** no-per-phase-bump convention in effect at the time (convention retired at v0.30.7 with WS-AN Phase AN6; from AN7 onward each phase bumps its own patch version). **Next**: AN6 (Architecture / InformationFlow / CrossSubsystem) per plan §9 can proceed in parallel with AN7 (Platform/API) per plan §2. AN5-A's mechanical file split can land as a dedicated follow-up PR at any time before AN12 portfolio closure.
- **WS-AN Phase AN4 COMPLETE (v0.30.6, branch `claude/complete-phase-an4-workstream-xBQoA`)**: Capability / Lifecycle / Service per [`docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md`](docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md) §7. 10 sub-tasks (AN4-A..AN4-J) addressing 4 HIGH (H-02..H-06), 15 MEDIUM (CAP-M01..M05, LIF-M01..M06, SVC-M01..M04), 5 LOW, plus two major Theme 4.7 file splits. **AN4-A (H-02)**: `lifecycleRetypeObject` moved into `SeLe4n.Kernel.Internal` so production dispatch can no longer reach the cleanup-bypass primitive by its bare name. 6 proof-chain files re-enable the bare name via `open SeLe4n.Kernel.Internal`; 5 test-harness files use fully-qualified `SeLe4n.Kernel.Internal.lifecycleRetypeObject`. New `scripts/lifecycle_internal_allowlist.txt` + `scripts/check_lifecycle_internal_allowlist.sh` wired into Tier 0 hygiene enforce the visibility contract — a synthetic unauthorized consumer triggers the expected CI failure. New regression function `runAN4A5LifecycleVisibilityChecks` in `tests/NegativeStateSuite.lean` demonstrates the cleanup-bypass difference (`Internal.lifecycleRetypeObject` leaves a dangling run-queue entry; `lifecycleRetypeWithCleanup` scrubs it). **AN4-B (H-03)**: `lifecycleIdentityNoTypeAliasConflict` (tautologically derivable from `lifecycleIdentityTypeExact`) removed; `lifecycleIdentityAliasingInvariant` collapsed to a bare `abbrev` for the exactness witness. Downstream destructures and one Service/Invariant/Policy.lean site migrated; new `lifecycleCapabilityRefNoTypeAliasConflict_of_exact` replaces the deleted `_of_identity` helper. **AN4-C (H-04 / Theme 4.1)**: CDT discharge index marker def + six `_cdt_hypothesis_discharged_at` companion theorems in `CrossSubsystem.lean` bridging `capabilityInvariantBundle st'` to `(cdtCompleteness, cdtAcyclicity) st'` for each CDT-modifying capability operation. Anchors the Theme 4.1 broader index (AN12-A extension). **AN4-D (H-05 / Theme 4.4)**: new `resolvedCnodeStillValid` precondition predicate on `cspaceLookupMultiLevel`; `_singleCore` witness discharges it unconditionally from `resolveCapAddress_result_valid_cnode`; `cspaceLookupMultiLevel_fails_closed_on_missing_cnode` anchors graceful-degradation semantics for the SMP case (tracked for AN9-D / AN9-J). **AN4-E (H-06)**: `mintDerivedCap` gains an explicit `isNull` guard on the constructed child candidate (rejects reserved-target + empty-rights corner case); `mintDerivedCap_preserves_non_null` theorem is unconditional; tightened-return-type wrapper `mintDerivedCapNonNull : Except KernelError NonNullCap` composes the base mint with the witness. Four downstream proofs walk the new two-`if` structure. **AN4-F (CAP-M01..M05)**: (1) AN4-F.1 new `@[documented_obligation]` tag attribute (`Lean.TagAttribute` registered in `SeLe4n/Prelude.lean`); `resolveCapAddress_caller_rights_obligation` retagged as `def : Unit := ()` marker. (2) AN4-F.2 `cspaceRevokeCdtTransactional` Phase-3 dead-branch gets detailed monotonicity rationale + happy-path theorem `_no_failure_no_cdt_node`. (3) **AN4-F.3 `Preservation.lean` 2464-LOC split into 6 children**: `Insert.lean` (229), `Delete.lean` (284), `CopyMoveMutate.lean` (353), `Revoke.lean` (459), `EndpointReplyAndLifecycle.lean` (622), `BadgeIpcCapsAndCdtMaps.lean` (675). Hub 42 LOC thin re-export. Every theorem byte-identical; 17 private helpers promoted to file-boundary scope. (4) AN4-F.4 new `RetypeTarget` subtype + `cleanupHookDischarged` predicate in `Capability/Invariant/Defs.lean`. (5) AN4-F.5 new `structure CapabilityInvariantBundle` with 7 named fields, bidirectional bridge `_iff_`, 7 `@[simp]` projection abbrevs (`.slotUnique`..`.objectsInvExt`); tuple form remains primary def at v0.30.6 with consumer migration tracked for a follow-up. **AN4-G (LIF-M01..M06)**: (1) AN4-G.1 `removeThreadFromQueue_tcb_present` witness + annotation. (2) AN4-G.2 new `lifecycleCleanupPipeline` named wrapper. (3) AN4-G.3 scrubObjectMemory H3-binding cross-reference. (4) AN4-G.4 new `retypeFromUntyped_atomicity_under_sequential_semantics` theorem. (5) **AN4-G.5 `Lifecycle/Operations.lean` 1600-LOC split into 4 children**: `Cleanup.lean` (204), `CleanupPreservation.lean` (460), `ScrubAndUntyped.lean` (764), `RetypeWrappers.lean` (279). Hub 54 LOC thin re-export. One proof (`removeFromAllEndpointQueues_preserves`) rewritten to apply `RHTable.fold_preserves` directly, eliminating a cross-file elaboration fragility where the former `epFold` helper's `let ep' : Endpoint := ...` pattern reshaped to `have` through the import boundary. 3 new children added to the AN4-A allowlist. (6) AN4-G.6 `lifecycleRevokeDeleteRetype` gets explicit non-transactional partial-failure contract docstring. **AN4-H (SVC-M01..M04)**: (1) SVC-M01 new `bootFromPlatform_service_fuel_sufficient` theorem. (2) SVC-M02 `Bfs` suffix historical-retention annotation (77-site atomic rename deferred as post-1.0 hygiene). (3) SVC-M03 idempotent-collision semantics documentation. (4) SVC-M04 `Acyclicity.lean` split deferred with documented rationale (same elaboration fragility AN4-G.5 required rewriting; file at 1012 LOC well under 2000-LOC ceiling). **AN4-I (LOW batch)**: stale `cspaceRevokeCdt_lenient` docstring scrubbed with explanatory note; SMP-assumption cross-reference at `lifecycleCapabilityRefObjectTargetTypeAligned`; `bfs_boundary_lemma` family-taxonomy annotation; `cspaceMutate` direct-overwrite annotation gains CDT-preservation theorem cross-reference. **AN4-J**: `CHANGELOG.md` entry, this `CLAUDE.md` prepend, `docs/WORKSTREAM_HISTORY.md` entry, large-files-list refresh (6 new Preservation children + 4 new Operations children replace the 2 retired parents). Gate: `lake build` (298 jobs, up from 278 — 20 new child modules + `.c.o` targets) + `test_smoke.sh` PASS (all META checks incl. Rust 415-test gate) + `test_tier0_hygiene.sh` PASS (AN4-A allowlist check wired in) + fixture byte-identical + zero `sorry`/`axiom`/`native_decide`. Version bumped 0.30.6→0.30.7 to mark the consolidated AN6 closure. **Next**: AN5 (Scheduler / SchedContext + `eventuallyExits` closure per plan §8), or parallel AN7/AN8 per plan §2.
- **WS-AN Phase AN3 COMPLETE (v0.30.6, branch `claude/phase-an3-ipc-subsystem-6bLHZ`)**: IPC subsystem per [`docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md`](docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md) §6. 7 sub-tasks (AN3-A..AN3-G) addressing 1 HIGH (H-01), 9 MEDIUM (IPC-M01..M09), 5 LOW (IPC LOW batch), plus Theme 4.2 (named projections) and Theme 4.7 (file splits). **AN3-A (H-01)**: `Donation.lean` partitioned into `Donation/Primitives.lean` (transport-independent, 559 LOC — `applyCallDonation`, `applyReplyDonation`, full preservation stack) and `Donation.lean` (transport-dependent wrappers, 188 LOC — `endpointCallWithDonation`, `endpointReplyWithDonation`, `endpointReplyRecvWithDonation`). Hub `SeLe4n.Kernel.IPC.Operations` re-exports `Donation.Primitives`, restoring symmetry with `Timeout`/`WithCaps` without reintroducing the `Operations -> Donation -> Transport -> Core -> Operations` cycle that AI4-A closed. Consumers needing the transport wrappers still import `SeLe4n.Kernel.IPC.Operations.Donation` directly — documented as prescriptive policy in `DualQueue.lean`. **AN3-B (IPC-M01 / Theme 4.2)**: named-projection refactor for `ipcInvariantFull`. New `structure IpcInvariantFull` with 16 fields; bidirectional bridge `ipcInvariantFull_iff_IpcInvariantFull`; 16 `@[simp]` projection theorems in the `ipcInvariantFull` namespace (`hInv.donationOwnerValid` replaces the fragile `hInv.2.2.2.2.2.2.2.2.2.2.2.1` 11-deep chain). 2 deep consumers migrated (Defs.lean:2484 + Architecture/Invariant.lean:1024). Tuple form retained as primary def; AN3-B.3 (swap primary) and AN3-B.6 (delete tuple) tracked as follow-ups. 3 new runtime tests (`an3b_01..03`) in `tests/ModelIntegritySuite.lean`. **AN3-C (IPC-M02 / Theme 4.7)**: `Structural.lean` 7626 → 33 LOC hub split into 4 children — `QueueNextTransport.lean` (1835 LOC), `StoreObjectFrame.lean` (1979 LOC), `DualQueueMembership.lean` (2065 LOC), `PerOperation.lean` (1887 LOC). All 39 `private` helpers promoted to public (file-boundary scope), zero theorem renamed/reordered/reproven. `scripts/test_tier3_invariant_surface.sh` updated (37 references retargeted `Structural.lean` → `Structural/`). **AN3-D (IPC-M03 + IPC-M04)**: `NotificationPreservation.lean` 1490 → 22 LOC hub + `Signal.lean` (850 LOC) + `Wait.lean` (688 LOC); `CallReplyRecv.lean` 1069 → 20 LOC hub + `Call.lean` (530 LOC) + `ReplyRecv.lean` (558 LOC). Tier-3 references updated (10 sites). **AN3-E (IPC-M05..M09)**: IPC-M05 `transferAux` kept as proof-local let-binding with annotation (Option B); IPC-M06 `storeObject_scheduler_eq_z7` kept `private` with rationale (Option B); IPC-M07 `ipcStateQueueConsistent` stays separate from `objectIndexConsistent` with scope note; IPC-M08 `allPendingMessagesBounded` endpoint-liveness-transitivity note; IPC-M09 `cleanupPreReceiveDonation` co-location banner + 2 compile-time `example` guards. **AN3-F (IPC LOW)**: `WaitingThreadHelpers.lean` narrowing docstring; `DualQueue.lean` re-export policy note; `CapTransfer.lean` `fillRemainingNoSlot` contract clarification; `EndpointPreservation.lean` field-preservation lemma-set rationale; `Structural/QueueNextTransport.lean` `QueueNextPath` helpers noted as already-factored. **AN3-G**: `CHANGELOG.md` entry, this `CLAUDE.md` prepend, `docs/WORKSTREAM_HISTORY.md` entry, large-files-list refreshed (8 new child entries replace the 3 retired parents). Gate: `lake build` (278 jobs, up from 262 — 8 new child modules + their `.c.o` targets) + `test_smoke.sh` PASS + `test_full.sh` PASS + `test_tier3_invariant_surface.sh` PASS + `test_rust.sh` PASS (415 tests) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `check_version_sync.sh` PASS at 0.30.6 + fixture byte-identical + zero `sorry`/`axiom`/`native_decide` in `SeLe4n/` or `Main.lean`. Version stays at 0.30.6 at this phase per the **original** no-per-phase-bump convention in effect at the time (convention retired at v0.30.7 with WS-AN Phase AN6; from AN7 onward each phase bumps its own patch version). **Next**: AN2-continuation (per AN2 entry) or AN4 (Capability/Lifecycle/Service) which can land in parallel with AN5/AN7/AN8 per plan §2.
- **WS-AN Phase AN2 IN PROGRESS — landed subset (v0.30.6, branch `claude/review-codebase-foundation-LgYTl`)**: Foundation hardening per [`docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md`](docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md) §5. The plan scopes AN2 as 4–5 days across 8 sub-tasks; this commit lands the safe-composition subset covering **all five identifier/address types** (`Badge`, `CPtr`, `Slot`, `VAddr`, `PAddr`) under Theme 4.3, plus the Badge H-12 closure, the UntypedObjectValid refinement subtype (AN2-F.3 partial), and the FND-M01/M04/M08 batch items. **AN2-A (H-13)**: `Badge` gains `private mk ::` + new smart constructors (`ofNat`, `zero`) + `private Badge.mkUnsafeForProof` helper; cross-module `Badge.mk n` and `⟨n⟩` anonymous-constructor calls are now elaboration errors. Manual `Inhabited Badge := ⟨⟨0⟩⟩` replaces `deriving Inhabited`. 17 production/test sites migrated. `BadgeOverflowSuite` gains tests `bov023` (`zero`), `bov024` (`ofNat`), `bov029`/`bov030` (theorem witnesses). Supporting theorems: `Badge.zero_valid`, `Badge.zero_toNat`, `Badge.ofNat_toNat`, `Badge.ofNat_valid`. **AN2-B.1/B.2/B.3/B.4 (Theme 4.3 follow-on)**: same `private mk ::` pattern applied to `CPtr`, `Slot`, `VAddr`, and `PAddr`; cascade reached ~250+ call sites across `SyscallArgDecode`, `Platform/DeviceTree`, `MainTraceHarness`, and every affected test suite — migration automated via a Lean-error-driven Python patcher that iteratively runs `lake build` on each suite target, extracts `Constructor for SeLe4n.{Slot,CPtr,VAddr,PAddr} is marked as private` errors, and rewrites the `⟨N⟩` at the offending column to `SeLe4n.{type}.ofNat N`. A handful of multi-line `(⟨N⟩, { ... })` patterns needed explicit `(... : Capability)` ascriptions. `PAddr.ofNat` keeps its bare `Nat → PAddr` signature in this commit — production decode paths (AK3-E's `decodeVSpaceMapArgsChecked`, AJ4-C's `validateIpcBufferAddress`) already gate against `2^physicalAddressWidth` before construction; a constructor-level width gate is tracked for AN2-continuation. **AN2-E (H-12)**: new `Badge.ofUInt64Pair (a b : UInt64) : Badge` lifts the bitwise-OR composition into the `UInt64` domain so the intermediate value never escapes `2^64`; `ofUInt64Pair_valid` witness proves `.valid` by construction. Tests (`bov025`-`bov028`) including commutativity + zero-right identity; supporting theorems `ofUInt64Pair_comm`, `ofUInt64Pair_zero_right`. **AN2-F.1 (FND-M01)**: `machineWordBits`, `machineWordMax`, `isWord64`, `isWord64Dec` hoisted before `CPtr`/`Slot` so both structurally delegate their hardware-width predicate (`isWord64Bounded := isWord64Dec ·.val`, now by `rfl`). Backwards-compat `_eq_isWord64Dec` theorems retained as `rfl` aliases. **AN2-F.3 (FND-M03, partial)**: new `UntypedObjectValid := { ut : UntypedObject // ut.wellFormed }` subtype in `Model/Object/Types.lean` with `toUntyped`/`wf`/`empty` helpers and `CoeHead UntypedObjectValid UntypedObject` for implicit coercion. Subtype is landed; tightening `allocate`/`retypeFromUntyped` entry signatures to accept it is tracked for AN2-continuation. **AN2-F.4 (FND-M04)**: new `abbrev minPracticalRHCapacity : Nat := 16` in `Kernel/RobinHood/Bridge.lean` replaces the magic `16` in the `Inhabited (RHTable α β)` instance. **AN2-F.8 (FND-M08)**: Markdown decision table added to the `ThreadId.toObjId` / `toObjIdChecked` / `toObjIdVerified` cluster clarifying sentinel/store-check discipline. **Deferred to AN2-continuation**: AN2-B.3/B.4 width-parameter follow-ups (FND-M02); AN2-C (`RegisterFile.gpr : Fin 32` + `LawfulBEq (RHTable κ β)`, H-11); AN2-D (`typedIdDisjointness` as a new conjunct of `crossSubsystemInvariant`, H-10 — 34-bridge cascade per AM4 playbook); AN2-F.3 cascade (`allocate`/`retypeFromUntyped` signature tightening); AN2-F.5 (DS-L5 heartbeat decomposition); AN2-F.6 (unchecked FrozenOps `private` gate — risky because Commutativity/Invariant proofs unfold the unchecked form across modules); AN2-F.7 (Phase-2 unreachable-branch proof); AN2-G (DEF-F-L9 17-tuple → `AllTablesInvExtK` structure refactor, ~80 sites). Gate: `lake build` (260 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `cargo test --workspace` (415) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `lake exe badge_overflow_suite` **30 PASS** (was 22 pre-AN2) + `check_version_sync.sh` PASS at 0.30.6 + `check_website_links.sh` PASS + fixture byte-identical to `tests/fixtures/main_trace_smoke.expected` (227 lines; private-mk changes alter only construction-site syntax, not emitted trace values) + zero `sorry`/`axiom`/`native_decide` in `SeLe4n/` or `Main.lean`. Version stays at `0.30.6` per the **original** no-per-phase-bump convention in effect at the time (retired at v0.30.7 with WS-AN Phase AN6; from AN7 onward each phase bumps its own patch version). **Next**: AN2-continuation (listed above), then AN3 (IPC subsystem).
- **WS-AN Phase AN1 IN PROGRESS (v0.30.6, branch `claude/phase-an1-blockers-8ETO8`)**: Critical-path blockers for the WS-AN v0.30.6 audit remediation portfolio per [`docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md`](docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md) §4. 4 sub-tasks (AN1-A README pointer, AN1-B pre-commit hook auto-installer, AN1-C stale WS-V/AG10 TODO retarget, AN1-D closure). AN1 touches no Lean proof surface; all changes are to infrastructure, documentation, and source-comment pointers. **AN1-A (C-01 / DOC-M01)**: `README.md` and 10 i18n READMEs replace the stale `AUDIT_COMPREHENSIVE_v0.23.21` pointer with a two-row metric-table entry — a `Canonical audit` row pointing at `AUDIT_v0.29.0_COMPREHENSIVE.md` (202 findings, remediated by WS-AK AK1–AK10) and a `Latest audit` row pointing at `AUDIT_v0.30.6_COMPREHENSIVE.md` (3 CRIT / 24 HIGH / 71 MED / 58 LOW / 40 INFO — initial scoring per §0.4). `scripts/website_link_manifest.txt` now protects the two audit files plus `docs/audits/` so `scripts/check_website_links.sh` catches future renames. **AN1-B (C-03)**: new `scripts/install_git_hooks.sh` — idempotent, shellcheck-clean installer for `scripts/pre-commit-lean-build.sh`. Default mode installs the hook if absent, no-ops if already identical to the canonical source, refuses with actionable message on divergence. `--check` returns 0 iff the installed hook matches the canonical source byte-for-byte (CI guard). `--force` backs up the diverging hook to `.git/hooks/pre-commit.backup-<timestamp>` and installs the canonical source. Prefers a symlink over copy so future edits to `pre-commit-lean-build.sh` propagate automatically. Wired into `scripts/setup_lean_env.sh` on both the fast-path and main-install paths so every fresh checkout gets the hook; wired into `.github/workflows/lean_action_ci.yml` as a `--check` step after setup so CI fails when the installer regresses. `CLAUDE.md` replaces the manual `cp` recipe with the installer convention. **AN1-C (H-24 / RUST-M06)**: the 6 primary `WS-V/AG10` TODOs called out explicitly in the audit (`trap.rs:186` SVC FFI; `lib.rs` HAL batch-doc block R-HAL-L6/L8/L12/L14/L16) are retargeted to live AN9-F/G/H/I/J sub-task IDs and cite their corresponding `DEF-R-HAL-L14/L17/L18/L19/L20` tracking entries. Repo-wide straggler sweep (per plan §4 step 3) covered the additional 46 sites found by `grep -rn "WS-V\|AG10" rust/ SeLe4n/` (total pre-AN1-C: 55 lines = 6 primary + 46 stragglers + 3 historical-kept). Each retarget uses one of three forms: (1) existing-`DEF-*` cite when a concrete tracking entry pre-exists; (2) named AN sub-task cite (AN9-D/F/A/B/H/I/J as appropriate) for items tracked live in the v0.30.6 plan; (3) "recorded as a post-1.0 hardening candidate; no currently-active plan file tracks it" prose (AK8-second-pass / AK10-J convention) for items without a natural DEF-* bucket. Remaining WS-V / AG10 tokens (3 sites — `conformance.rs:769` V1 test block header + `Assumptions.lean:135,138` AG10-C section headers) are purely historical completed-work labels and are preserved per the AN1-C acceptance criterion. Files: 6 rust + 25 SeLe4n = 31 files. **AN1-D**: `CHANGELOG.md` [in-progress] AN1 entry + `docs/WORKSTREAM_HISTORY.md` AN1 sub-entry + this `CLAUDE.md` active-workstream prepend. Gate: `lake build` (260 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `test_tier0_hygiene.sh` PASS (shellcheck covers `install_git_hooks.sh` + modified `setup_lean_env.sh`) + `cargo test --workspace` (414) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `check_version_sync.sh` PASS at 0.30.6 + `check_website_links.sh` PASS + `install_git_hooks.sh --check` PASS + fixture byte-identical + zero `sorry`/`axiom`/`native_decide`. Version stays at `0.30.6` per the **original** no-per-phase-bump convention in effect at the time (retired at v0.30.7 with WS-AN Phase AN6; from AN7 onward each phase bumps its own patch version). **Next**: AN2 (foundation hardening — H-10..H-13 subtype-gate cascade + FND-M01..M08 batch + DEF-F-L9 17-tuple refactor).
- **WS-AN Phase AN0 COMPLETE (v0.30.6, branch `claude/implement-phase-an0-3D5pR`)**: Pre-flight for the WS-AN v0.30.6 audit remediation portfolio per [`docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md`](docs/audits/AUDIT_v0.30.6_WORKSTREAM_PLAN.md) §3. 3 sub-tasks (AN0-A baseline capture, AN0-B sub-task inventory commit, AN0-C branch policy confirmation). **AN0-A**: new `docs/audits/AUDIT_v0.30.6_WS_AN_BASELINE.txt` records the WS-AN start state so AN12 can diff the gate numbers — `lake build` (260 jobs, 0 warnings); `cargo test --workspace` (414 passing / 0 failed / 0 ignored — 407 unit + 7 doctests); `cargo clippy --workspace -- -D warnings` (0 warnings — standard gate; `--all-targets` test-surface warnings tracked separately for AN11-C/D); `test_smoke.sh` + `test_full.sh` + `test_tier0_hygiene.sh` + `check_version_sync.sh` (0.30.6) + `check_website_links.sh` all PASS; fixture byte-identical to `tests/fixtures/main_trace_smoke.expected` (227 lines); zero `sorry`/`axiom`/`native_decide` in `SeLe4n/` or `Main.lean`; Theme 4.7 monolithic file LOC baseline (Structural 7626 / NI-Operations 3768 / Sched-Preservation 3633 / Cap-Preservation 2461 / Lifecycle-Operations 1473 = 18961 total); 24 Lean test suites / 25 `lake exe` targets / ~1614 aggregate assertions; audit-finding disposition (196 findings = 2 actionable CRITICAL + 23 actionable HIGH + 71 MEDIUM + 59 LOW + 40 INFO after C-02 resolved and H-22 downgraded); 11 deferred items absorbed from `AUDIT_v0.29.0_DEFERRED.md` (7 hardware-binding targeted for AN9, 2 cascade rollouts for AN10, 1 `eventuallyExits` closure for AN5-E, 1 `allTablesInvExtK` tuple→struct for AN2-G). Monotonicity-guard status: historical `scripts/ak7_cascade_baseline.sh` + `check_monotonic.sh` + `docs/audits/AL0_baseline.txt` were retired in commit 8d9e61f because the three tracked attack surfaces are now closed STRUCTURALLY at the type system (`NonNullCap` / `storeObjectKindChecked` / `Valid{Thread,Obj,SchedContext}Id`) — the baseline documents this as the current canonical enforcement layer rather than re-creating the numeric script. **AN0-B**: the plan file (`AUDIT_v0.30.6_WORKSTREAM_PLAN.md`, 4110 lines) is already landed; this commit adds the WS-AN AN0 entry to `CLAUDE.md`, `docs/WORKSTREAM_HISTORY.md`, and `CHANGELOG.md`. **AN0-C**: branch policy confirmed — AN1..AN12 all land on the designated WS-AN branch per task description's `Git Development Branch Requirements`. No production code or test-surface changes at AN0; version stays at 0.30.6. Gate: `lake build` (260 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `cargo test --workspace` (414) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `check_version_sync.sh` PASS at 0.30.6 + `check_website_links.sh` PASS + fixture byte-identical + zero `sorry`/`axiom`/`native_decide`. **Next**: AN1 (critical-path blockers — C-01 README pointer, C-03 pre-commit hook install, H-24 stale TODO retargeting).
- **WS-AK Phase AK10 COMPLETE — WS-AK PORTFOLIO CLOSED (v0.30.6, branch `claude/complete-phase-ak10-WQcyt`)**: Phase AK10 closes the 10-phase WS-AK v0.29.0 audit remediation portfolio (202 findings addressed: 2 CRITICAL + 23 HIGH + 76 MEDIUM + 101 LOW). **AK10-A** — fixture verification: `lake exe sele4n` byte-identical to `tests/fixtures/main_trace_smoke.expected` (227 lines); AK1..AK9 semantic changes (AK1-D atomic `ipcState := .ready`, AK2-A priority re-enqueue, AK6-G projection strips) land at invariant layers the trace harness does not exercise. **AK10-B** — doc sync across `README.md`, 10 i18n README variants, `docs/spec/SELE4N_SPEC.md` (§2 snapshot + "Active workstream" entry), `docs/WORKSTREAM_HISTORY.md` (WS-AK portfolio summary with 10-phase breakdown), `docs/gitbook/` affected chapters, `CHANGELOG.md` v0.30.6 entry. **AK10-C** — **patch-only** version bump v0.30.5 → **v0.30.6** per maintainer direction (v1.0.0 release tag deferred to separate manual maintainer action); 15 version-bearing files synchronized; `check_version_sync.sh` PASS. **AK10-D** — new `docs/audits/AUDIT_v0.29.0_ERRATA.md` formalises six audit-text clarifications (E-1 S-H03 verification, E-2 R-HAL-M12 dead-code removal — supersedes AK5-K's "defensive fall-through" annotation and completes the original audit remediation per `b .`-after-`bl` guidance, E-3 A-H01 three-layer extent, E-4 R-HAL-H02 partial DSB/ISB + missing TLB flush/D-cache clean, E-5 NI-H02 composition scope, E-6 finding-count arithmetic 202 not 201); **dead-code deletion**: both `__el0_serror_entry` and `__el1_serror_entry` in `rust/sele4n-hal/src/trap.S` replace the unreachable `restore_context` macro with `b .` (branch-to-self infinite loop), which is stronger defence-in-depth than the prior annotated fall-through (halts in place if the `-> !` divergence contract is ever violated, rather than attempting `ERET` with SError-corrupted context). **AK10-E** — CLAUDE.md large-file list refreshed; notable deltas: `InformationFlow/Invariant/Operations.lean` 2671→3768 (AK6-F/G/H), `CrossSubsystem.lean` 2211→2839 (AK8-A+AM4), `Platform/Boot.lean` 1270→2074 (AK3/AK9), `API.lean` 1895→2258 (AK6-F+AK7-E/F/I), `CHANGELOG.md` 5285→9453. **AK10-F** — residual LOW-tier verification: all LOW findings batched per-phase (AK1-J..AK9-H); zero `sorry`/`axiom`/`native_decide` in production proof surface. **AK10-G** — `scripts/check_website_links.sh` PASS; no protected paths renamed or removed. **AK10-H** — `docs/WORKSTREAM_HISTORY.md` WS-AK portfolio summary with gate-state table and cross-references to errata + deferred tracking. **AK10-J** — new `docs/audits/AUDIT_v0.29.0_DEFERRED.md` formalises 11 deferred items (7 hardware-binding: DEF-A-M04 TLB+cache composition, DEF-A-M06/AK3-I `tlbBarrierComplete`, DEF-A-M08/M09/AK3-K MMU/Device BarrierKind, DEF-C-M04 `suspendThread` atomicity Rust proof, DEF-P-L9 VSpaceRoot boot exclusion, DEF-R-HAL-L14 SVC FFI wiring; 4 proof-hygiene: DEF-F-L9 17-deep tuple refactor, DEF-AK2-K.4 `eventuallyExits` by-design, DEF-AK7-E.cascade `ValidObjId` rollout, DEF-AK7-F.cascade `ObjKind` migration). Every deferred row is recorded as a **post-1.0 hardening candidate; no currently-active plan file tracks it**, matching the convention from the AK8 second-pass audit (WS-V and AG10 are both closed workstreams — using them as deferral buckets is misleading). **Gate at v0.30.6 tip**: `lake build` (260 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `cargo test --workspace` PASS + `cargo clippy --workspace -- -D warnings` (0 warnings) + `check_version_sync.sh` PASS at 0.30.6 + `check_website_links.sh` PASS + `ak7_cascade_check_monotonic.sh` PASS + fixture byte-identical + zero `sorry`/`axiom`/`native_decide`. **Portfolio status**: COMPLETE. **Next workstream**: none currently active; deferred items are tracked per-ID in `AUDIT_v0.29.0_DEFERRED.md`.
- **WS-AK Phase AK9 audit remediation COMPLETE (v0.30.5, branch `claude/complete-phase-ak9-workstream-2npyb`)**: Deep end-to-end audit of the v0.30.4 Phase AK9 delivery surfaced five gaps where the plan's intent was not fully honored at the production-path level. (1) **AK9-A (P-H01) alias → rename**: v0.30.4 added `mmioReadByte` as a thin `@[inline]` alias over `mmioRead`; the plan specified RENAME. Inverted so `mmioReadByte` is now the primary definition and `mmioRead` is a `@[deprecated]` alias preserving backward compat. Three theorems renamed (`mmioReadByte_rejects_non_device`, `mmioReadByte_preserves_state`, `mmioReadByte_nonMmio_safe`) with backward-compat aliases. Two new positive correctness theorems `mmioRead{32,64}_alignedAndBounded_within_region` complete the four-theorem-per-width contract. (2) **AK9-F (P-M05) validation not wired**: v0.30.4 defined `applyMachineConfigChecked` but `bootFromPlatform` still called unchecked `applyMachineConfig` — the production path silently accepted malformed configs. Remediation wires `machineConfig.wellFormed` + `physicalAddressWidth ≤ 52` gates directly into `bootFromPlatformChecked` BEFORE constructing the IntermediateState; two soundness theorems (`bootFromPlatformChecked_ok_implies_machineConfigWellFormed`, `bootFromPlatformChecked_ok_implies_physicalAddressWidth_bound`) expose post-conditions. (3) **AK9-G (P-M06) interrupts-enable not wired**: v0.30.4 added `bootFromPlatformWithInterrupts` but the plan specified "Invoke at end of `bootFromPlatform` checked path." Remediation wires `bootEnableInterruptsOp` into `bootFromPlatformChecked`'s ok-branch so successful checked boots emit a post-HAL-Phase-3 state with interrupts enabled. New soundness theorem `bootFromPlatformChecked_ok_interruptsEnabled`. The unchecked `bootFromPlatform` keeps `interruptsEnabled = false` for negative-state / invariant-bridge contexts. (4) **AK9-C no end-to-end rejection test**: v0.30.4 tested `irqHandlersReferenceNotifications` predicate-directly but the plan requires fixture exercising mis-configured IRQ with `bootFromPlatformChecked` rejection. Added two end-to-end tests (`ak9ce_01`, `ak9ce_02`) exercising the full production chain with missing-ObjId and TCB-variant handlers. (5) **AK9-H (P-L2) readCString not upgraded to Except**: v0.30.4 only documented the fuel-collapse concern; the plan says "return Except". Added `readCStringChecked : Except DeviceTreeParseError (String × Nat)` distinguishing `.malformedBlob` (OOB / truncated) from `.fuelExhausted` (no null terminator within fuel); two correctness theorems + four runtime tests. Legacy Option variant retained. Test suite `tests/Ak9PlatformSuite.lean` expanded from 21 → 34 tests: AK9-CE/FE/GE end-to-end chain (5), AK9-A rename / positive (4), AK9-H readCStringChecked (4). The existing `bootFromPlatformChecked_eq_bootFromPlatform` theorem now carries the stronger conclusion (emits `bootEnableInterruptsOp (bootFromPlatform config)` — a behavioural change that reflects the production post-IRQ-enable semantics). Gate: `lake build` (260 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `lake exe ak9_platform_suite` 34/34 PASS + `cargo test --workspace` PASS + `cargo clippy --workspace -- -D warnings` (0 warnings) + `check_version_sync.sh` PASS at 0.30.5 (15 files synced) + fixture byte-identical to `tests/fixtures/main_trace_smoke.expected` + zero `sorry` / `axiom`. Plan: [`docs/dev_history/audits/AUDIT_v0.29.0_WORKSTREAM_PLAN.md`](docs/dev_history/audits/AUDIT_v0.29.0_WORKSTREAM_PLAN.md) §12. Portfolio AK1..AK10 targeted at v1.0.0 release (AK1–AK9 complete; **next: AK10 Testing, Documentation & Closure**).
- **WS-AK Phase AK9 COMPLETE (v0.30.4, branch `claude/complete-phase-ak9-workstream-2npyb`)**: Phase AK9 of the v0.29.0 pre-1.0 release hardening plan — **Platform / Boot / DTB / MMIO**. 8 sub-tasks (AK9-A..AK9-H) addressing 2 HIGH, 7 MEDIUM, 13 LOW platform findings. **AK9-A (P-H01 / HIGH)**: `mmioRead32` / `mmioRead64` with 4/8-byte alignment + region-local range checks; `mmioRead` retained as `mmioReadByte` alias for UART/debug; four correctness theorems per width. **AK9-B (P-H02 / HIGH)**: `BootBoundaryContract.objectStoreNonEmpty` renamed to `objectStoreEmptyAtBoot` — field always asserted `(default : SystemState).objects.size = 0`, inverted semantic meaning now fixed across `Assumptions.lean`, `Sim/BootContract.lean`, `RPi5/BootContract.lean`; `@[deprecated]` alias preserves prior theorem names. **AK9-C (P-M01 / MEDIUM)**: `irqHandlersReferenceNotifications` validates each IRQ handler ObjId refers to a `.notification` in `initialObjects`; wired into `bootFromPlatformChecked` as a third validation step; soundness `bootFromPlatformChecked_ok_implies_irqHandlersValid`. **AK9-D (P-M02 / MEDIUM)**: `isDeviceRangeWithinRegion` + `findDeviceRegion` tighten multi-byte `mmioWrite32` / `mmioWrite64` / `mmioWrite32W1C` to require the ENTIRE N-byte range lie within a SINGLE declared `.device` region (stricter than the prior per-endpoint check); positive correctness `mmioWrite{32,64}_alignedAndBounded_within_region`; bridge `isDeviceRangeWithinRegion_false_of_non_device` recovers per-endpoint rejection theorems. **AK9-E (P-M03 / MEDIUM)**: `budgetSufficientCheck` in `RPi5/RuntimeContract.lean` now fails closed (`false`) for `TCB.schedContextBinding = .bound scId` when the store resolves to a non-`.schedContext` variant — was silently permissive; `registerContextStableCheck_budget` updated to discharge the new wrong-variant contradiction. **AK9-F (P-M04 / M05 / M07 / MEDIUM)**: three checked variants forcing explicit caller decisions — `classifyMemoryRegionChecked : Option MemoryKind` (rejects empty map / unmapped), `applyMachineConfigChecked` (`MachineConfig.wellFormed` + `physicalAddressWidth ≤ 52` ARMv8 LPA max), `findMemoryRegPropertyChecked : Except DeviceTreeParseError` (distinguishes fuel exhaustion from malformed blob; default fuel `hdr.sizeDtStruct / 4`); `DeviceTree.fromDtbFull` rewired through the checked search; `parseFdtHeader_fromDtbFull_ok` updated. **AK9-G (P-M06 / MEDIUM)**: `bootEnableInterruptsOp : IntermediateState → IntermediateState` mirrors Rust HAL Phase-3 IRQ re-enable after GIC + timer setup; `bootFromPlatformWithInterrupts` composes; default `bootFromPlatform` keeps IRQs disabled for negative-state / boot-invariant-bridge contexts; six supporting frame theorems. **AK9-H (P-L1..P-L13)**: 13-entry LOW batch documentation at end of `RPi5/MmioAdapter.lean`; P-L3 (PA width bounds — AK9-F), P-L7 (pairwise MMIO disjointness — pre-existing `mmioRegionsPairwiseDisjointCheck`), P-L13 (touching-regions fragility — AK9-D) RESOLVED; remaining LOW items documented as accepted single-core model gaps or recorded as post-1.0 hardening candidates (no currently-active plan file tracks them). **Regression test suite**: new `tests/Ak9PlatformSuite.lean` — 21 tests (AK9-A × 4, AK9-D × 2, AK9-B × 2, AK9-C × 4, AK9-F × 6, AK9-G × 3) wired into `test_tier2_negative.sh` as `ak9_platform_suite`. Gate: `lake build` (260 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `lake exe ak9_platform_suite` 21/21 PASS + `cargo test --workspace` PASS + `cargo clippy --workspace -- -D warnings` (0 warnings) + `check_version_sync.sh` PASS at 0.30.4 + zero `sorry` / `axiom` in `SeLe4n/` or `Main.lean`. Plan: [`docs/dev_history/audits/AUDIT_v0.29.0_WORKSTREAM_PLAN.md`](docs/dev_history/audits/AUDIT_v0.29.0_WORKSTREAM_PLAN.md) §12. Portfolio status: AK1–AK9 complete; **next: AK10 (Testing, Documentation & Closure)** targeting v1.0.0 release.
- **WS-AK Phase AK8 second-pass audit COMPLETE (v0.30.3, branch `claude/review-codebase-audit-stKD8`)**: Second deep end-to-end audit of Phase AK8 surfaced two process-level issues. **(1) Terminology hygiene**: eight deferral annotations introduced by AK8 incorrectly cited **WS-V** as a future-work bucket, but WS-V was completed many releases ago (see `docs/WORKSTREAM_HISTORY.md` §"WS-V workstream ... COMPLETE"). Using a closed workstream as a deferral bucket is misleading. Each of the eight references has been rephrased to state honestly "recorded here as a post-1.0 hardening candidate; no currently-active plan file tracks it" — scope: `SeLe4n/Kernel/Architecture/Invariant.lean` (3 sites, retype-to-untyped scope docs), `SeLe4n/Kernel/CrossSubsystem.lean` (1 site, `untypedRegionsDisjoint` transitive-chain scope), `SeLe4n/Kernel/Capability/Operations.lean` (2 sites, C-L3/C-L9 LOW-tier), `SeLe4n/Kernel/RobinHood/Bridge.lean` (3 sites, DS-L2/DS-L5/DS-M04), plus `CLAUDE.md`, `docs/WORKSTREAM_HISTORY.md`, `docs/spec/SELE4N_SPEC.md`, `CHANGELOG.md` active-workstream entries. Pre-existing WS-V references in historical log entries are left unchanged. **(2) AK8-B test gap**: `cspaceRevokeCdtTransactional` (AK8-B, v0.30.0) shipped with no regression tests. Added three tests in `tests/NegativeStateSuite.lean`: (a) transactional variant aborts atomically with `.error .objectNotFound` when a descendant's CNode is missing, (b) transactional variant succeeds with `firstFailure = none` when all descendants have valid CNodes, (c) `validateRevokeCdtDescendants` returns `.ok` on an empty list. All three pass in Tier 2 (`test_tier2_negative.sh`). Version bumped 0.30.2→0.30.3 across 15 files. Gate: `lake build` (260 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `cargo test --workspace` PASS + `cargo clippy --workspace -- -D warnings` (0 warnings) + `model_integrity_suite` PASS (7 AK8-A tests from v0.30.2 still passing) + `priority_management_suite` 27/27 PASS + `frozen_ops_suite` 21/21 PASS + `information_flow_suite` PASS + `operation_chain_suite` PASS + `negative_state_suite` PASS (+3 new AK8-B tests) + `check_version_sync.sh` PASS at 0.30.3 + zero sorry/axiom.
- **WS-AK Phase AK8 audit remediation COMPLETE (v0.30.2, branch `claude/review-codebase-audit-stKD8`)**: Post-delivery end-to-end audit of the v0.30.1 Phase AK8 implementation surfaced one material gap — `lifecycleRetype_crossSubsystemInvariant_bridge` plumbed `hUntypedDisj : untypedRegionsDisjoint st'` as an input hypothesis but no theorem substantively proved `retypeFromUntyped` actually preserves the invariant, leaving the top-level composition obligation open for any future caller invoking the bridge. Remediation: (1) **Invariant refinement** — `untypedRegionsDisjoint` in `SeLe4n/Kernel/CrossSubsystem.lean` extended with direct-child-exclusion side conditions (`∀ c ∈ ut₁.children, c.objId ≠ oid₂` and symmetric), so parent-child region containment (the expected retype outcome via `allocate`) correctly falls outside the disjointness requirement. `PlatformConfig.untypedRegionsDisjoint` mirrored. (2) **Allocate bookkeeping** — four new structural theorems in `SeLe4n/Model/Object/Types.lean`: `allocate_children_extends` (post-state children list is a superset of pre-state), `allocate_children_new` (new child is at the head with exact `(objId, offset, size)` bookkeeping), `allocate_children_eq` (full structural characterization), `allocate_child_fits_parent` (new sub-region `[offset, offset+size)` lies inside parent's `[0, regionSize)`). (3) **Substantive preservation** — `retypeFromUntyped_preserves_untypedRegionsDisjoint_nonUntypedChild` in `SeLe4n/Kernel/Architecture/Invariant.lean` provides the machine-checked proof for the primary API dispatch path (retype target is `.tcb`/`.endpoint`/`.notification`/`.cnode`/`.vspaceRoot`/`.schedContext`). Composes the parent-update step (`storeObject_sameRegion_untyped_preserves_untypedRegionsDisjoint` with new `hChildrenExt` hypothesis) and the child-creation step (`storeObject_non_untyped_preserves_untypedRegionsDisjoint`). (4) **API-path specialization** — `retypeFromUntyped_objectOfKernelType_preserves_untypedRegionsDisjoint` discharges `hNotUntypedChild` from the dispatch-layer `objType ≠ .untyped` side-condition via per-constructor case split on `KernelObjectType`. (5) **Scope marker** — `retypeFromUntyped_untypedRegionsDisjoint_retype_to_untyped_documented` records the retype-to-untyped case as post-1.0 hardening (no currently-active plan file tracks it): the current `objectOfKernelType .untyped` hardcodes `regionBase = 0` which means no test exercises this path, AND a complete proof would require either a parent-context-aware constructor OR transitive-ancestor exclusion in the invariant. (6) **Infrastructure** — `Architecture/Invariant.lean` gains `import SeLe4n.Kernel.Lifecycle.Invariant` so the preservation proof can reach `retypeFromUntyped_ok_decompose`. No import cycle (Lifecycle does not import Architecture). (7) **7 regression tests** in `tests/ModelIntegritySuite.lean` (`ak8a_01..07`): default satisfies invariant; disjoint-sibling regions pass; overlapping regions fail; parent-child containment allowed via children-list exclusion; `allocate_children_extends` / `allocate_preserves_region` runtime checks; empty-config vacuous witness. Gate: `lake build` (260 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `cargo test --workspace` PASS + `cargo clippy --workspace -- -D warnings` (0 warnings) + `model_integrity_suite` PASS (+7 AK8-A audit tests) + `priority_management_suite` 27/27 PASS + `frozen_ops_suite` 21/21 PASS + `information_flow_suite` PASS + `operation_chain_suite` PASS + `negative_state_suite` PASS + `check_version_sync.sh` PASS at 0.30.2 + zero sorry/axiom in `SeLe4n/` or `Main.lean`.
- **WS-AK Phase AK8 COMPLETE (v0.30.1, branch `claude/review-codebase-audit-stKD8`)**: Pre-1.0 Release Hardening (v0.29.0 audit) — Phase AK8: Capability / Lifecycle / Service + Data Structures. 11 sub-tasks (AK8-A..AK8-K) addressing 7 MEDIUM (C-M01..C-M07), 4 MEDIUM (DS-M01..DS-M04), and 21 LOW-tier findings. **AK8-A (C-M01 / MEDIUM)**: `untypedRegionsDisjoint` added as the 12th conjunct of `crossSubsystemInvariant`. Full cascade — frame lemmas (`untypedRegionsDisjoint_frame`, `storeObject_non_untyped_preserves_*`, `storeObject_sameRegion_untyped_preserves_*`) + `PlatformConfig.untypedRegionsDisjoint` boot-time precondition + `bootFromPlatform_untypedRegionsDisjoint` witness via new `foldObjects_objects_reachable` reachability lemma + 34 per-op bridge lemmas + 2 core bridges + `crossSubsystemInvariant_retype_bridge` all extended with `(hUntypedDisj : untypedRegionsDisjoint st')`. Projection theorem `crossSubsystemInvariant_to_untypedRegionsDisjoint` added; `default_crossSubsystemInvariant` updated to 12-case refine. **AK8-B (C-M02 / MEDIUM, Option B)**: `cspaceRevokeCdtTransactional` + `validateRevokeCdtDescendants` implement validate-then-apply semantics — walk descendants pre-mutation, check every slot-mapped descendant's CNode is present, abort with `.error` on any failure (rolls back the transaction). Three correctness theorems: `validateRevokeCdtDescendants_empty`, `cspaceRevokeCdtTransactional_requires_local_revoke_ok`. **AK8-C (C-M03 / MEDIUM)**: Expanded `resolveCapAddress` docstring with a formal **Caller rights obligation** block + `resolveCapAddress_caller_rights_obligation` marker theorem. **AK8-D (C-M05 / MEDIUM)**: New `maxHardwarePriority := 255` constant; `validatePriorityAuthority` rejects priorities above the cap with `.illegalAuthority`; `validatePriorityAuthority_bound` soundness theorem; 3 new regression tests (`pm_ak8d_01..03`) in `PriorityManagementSuite` (27 total). **AK8-E (C-M06 / MEDIUM)**: `getCurrentPriorityChecked : SystemState → TCB → Except KernelError Priority` surfaces the missing-SchedContext case as `.error .objectNotFound`; soundness via `getCurrentPriorityChecked_ok_eq_getCurrentPriority`. The legacy lookup-tolerant `getCurrentPriority` retained for proof-layer contexts. **AK8-F (C-M07 / MEDIUM)**: `findFirstEmptySlotChecked` caps the scan at `min limit (2^radixWidth - base.toNat)`; `findFirstEmptySlotChecked_within_radix` proves every returned slot fits in the CNode's radix range; helper `findFirstEmptySlot_bounded`. **AK8-G (DS-M01 / MEDIUM, test-only)**: Three kind-checked frozen-store wrappers (`frozenStoreTcbChecked`, `frozenStoreEndpointChecked`, `frozenStoreNotificationChecked`) pre-validate the stored variant via the `frozenLookup*` helpers and reject cross-variant writes with `.error .objectNotFound`. Three `*_ok_eq_*` soundness theorems. **AK8-H (DS-M02 / MEDIUM, test-only)**: `frozenSchedContextUnbind` rewritten as transactional two-phase — TCB lookup hoisted before SC mutation, eliminating the half-mutated-state failure mode. **AK8-I (DS-M03 / MEDIUM)**: `freezeCNodeSlotsChecked : CNode → Option CNodeRadix` validates `allKeysBounded` at freeze time, returns `none` on phantom-key conditions; `freezeCNodeSlotsChecked_some_eq_freezeCNodeSlots` + `freezeCNodeSlotsChecked_none_iff_phantom` correctness theorems. **AK8-J (DS-M04 / MEDIUM)**: `RHTable.BEq` `LawfulBEq` requirement documented in file-header and instance docstring; `RHTable_BEq_requires_lawfulBEq_of_value` marker theorem anchors the contract for future consumers. **AK8-K (C-L1..C-L10, DS-L1..DS-L11)**: Implementation changes: (1) C-L1 — `cspaceMove` rejects self-moves (`src = dst`) with `.illegalState`, cascaded through `cspaceMove_ok_implies_source_exists`, `cspaceMove_preserves_capabilityInvariantBundle`, and `cspaceMove_preserves_projection` via `by_cases hSelf`. (2) C-L2 — `cspaceMutate` rejects `Capability.null` sentinel with `.nullCapability`, cascaded through the two `cspaceMutate_preserves_*` theorems in Preservation.lean and `cspaceMutate_preserves_projection` in Composition.lean via `by_cases hNull`. (3) Documentation batch in `Capability/Operations.lean` file header covers C-L3..C-L10; `RobinHood/Bridge.lean` file header covers DS-L1..DS-L11. Gate: `lake build` (260 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `cargo test --workspace` PASS + zero sorry/axiom in `SeLe4n/` or `Main.lean` + `priority_management_suite` 27/27 PASS + `model_integrity_suite` PASS + `information_flow_suite` PASS + `frozen_ops_suite` 21/21 PASS + `operation_chain_suite` PASS + `negative_state_suite` PASS.
- **WS-AM cascade continuation — AK7-F.reader hygiene first wave (v0.30.0, branch `claude/review-deferred-tasks-zXVoC`)**: Closes out the `AUDIT_v0.29.0_DEFERRED.md` tracking file by landing a first measurable wave of reader-hygiene migration at the testing infrastructure layer and archiving the file. `SeLe4n/Testing/InvariantChecks.lean` migrates 18 raw `match st.objects[id]? with | some (.variant x)` call sites to AL2-A typed helpers (`getTcb?`, `getSchedContext?`, `getEndpoint?`, `getNotification?`, `getUntyped?`). No proof surface is touched — `InvariantChecks.lean` is testing infrastructure. Baseline metric deltas locked into `docs/audits/AL0_baseline.txt`: `RAW_MATCH_TCB 613 → 600` (−13), `RAW_MATCH_ENDPOINT 264 → 262` (−2), `RAW_MATCH_NOTIFICATION 98 → 94` (−4), `RAW_MATCH_UNTYPED 18 → 17` (−1), `RAW_LOOKUP_TID 265 → 254` (−11), `GETTCB_ADOPTION 31 → 49` (+18), `GETSCHEDCTX_ADOPTION 8 → 10` (+2). Deferred file `docs/audits/AUDIT_v0.29.0_DEFERRED.md` deleted — its four RESOLVED rows (AK7-E cascade, AK7-F cascade, AK7-I cascade, AL6-C.hygiene) are durably preserved in `docs/WORKSTREAM_HISTORY.md` §WS-AM, and ongoing consumer migration is tracked exclusively via the seven CI monotonicity metrics (`ak7_cascade_check_monotonic.sh` wired into `test_tier0_hygiene.sh`). `docs/audits/AUDIT_v0.28.0_WORKSTREAM_PLAN.md` and `docs/audits/AUDIT_v0.28.0_COMPREHENSIVE.md` archived under `docs/dev_history/audits/`; references in `CLAUDE.md`, `docs/WORKSTREAM_HISTORY.md`, `docs/CLAIM_EVIDENCE_INDEX.md`, and `docs/DEVELOPMENT.md` updated to the new paths. Gate: `lake build` (260 jobs, 0 warnings) + `test_smoke.sh` + `test_tier0_hygiene.sh` (monotonicity PASS at new floor, website-links PASS, version-sync PASS at 0.30.0) + `lake exe ak7_regression_suite` (**87 checks**, unchanged) + `cargo test --workspace` (415) + zero sorry/axiom. Remaining hygiene (post-AM7): ~582 reader-side raw match sites remain migration candidates across kernel proof-surface modules; ~50 writer-side `storeObject` sites remain defense-in-depth adoption candidates. Both continue to be enforced as should-drop / should-grow CI metrics so every subsequent commit regression-checks against the v0.30.0 post-AM7 floor.
- **WS-AM post-delivery audit remediation (v0.30.0)**: End-to-end audit of the WS-AM delivery (AM1 + AM4) surfaced two material correctness gaps. (1) **Runtime check coverage gap**: `checkCrossSubsystemInvariant` in `SeLe4n/Testing/InvariantChecks.lean` still iterated over 10 predicates after AM4 extended the Prop-level bundle to 11 — the trace harness was silently passing without exercising the AL6-C lockstep predicate. Fixed by adding `checkLifecycleObjectTypeLockstep` (walks `objectIndex`, compares `objects[oid]?.objectType` against `lifecycle.objectTypes[oid]?`) as the 11th check. Two regression tests: `am4_04_runtime_check_covers_11_predicates` (11-entry assertion) and `am4_05_runtime_check_detects_violation` (builds deliberately inconsistent state, asserts check returns `false`). (2) **Field-set catalog + pairwise theorem**: `crossSubsystemFieldSets` still had 10 entries; `crossSubsystemFieldSets_count : length = 10` was stale; `crossSubsystem_pairwise_coverage_complete` covered C(10,2) = 45 pairs but should cover C(11,2) = 55. Fixed by adding `lifecycleObjectTypeLockstep_fields := [.objects, .lifecycle]`, extending the catalog to 11, updating the count theorem to `length = 11`, and extending the pairwise theorem to 18 disjoint pairs (15 prior + 3 new: lockstep is disjoint from `registryInterfaceValid`, `registryDependencyConsistent`, `serviceGraphInvariant`). The existing V6-A runtime assertion in `tests/InformationFlowSuite.lean` updated from "10 entries" to "11 entries". Plus 5 stale "10-predicate" comments in `CrossSubsystem.lean` and `Architecture/Invariant.lean` updated to "11-predicate". Gate: `lake build` (260 jobs) + `test_smoke.sh` + `test_full.sh` + `ak7_regression_suite` (**87 checks**, +3 from am4_04/05) + `cargo test --workspace` (415) + monotonicity PASS (TEST_COUNT_AK7 84→87, LIFECYCLELOCKSTEP_REFS 63→79) + version sync at 0.30.0 + zero sorry/axiom. WS-AM CLOSED.
- **WS-AM COMPLETE (v0.30.0, branch `claude/plan-deferred-tasks-EZNLE`)**: closes the final correctness-impacting deferred item from the v0.29.0 audit — **AL6-C.hygiene**. `lifecycleObjectTypeLockstep` is elevated from a standalone stub (schema-only at v0.29.14) to the 11th conjunct of `crossSubsystemInvariant`, so every kernel entry/exit point structurally carries the witness `lifecycle.objectTypes[oid]? = some obj.objectType` whenever `objects[oid]? = some obj`. Phase AM1 (commit 5e71ea8) adds three standalone preservation proofs: `default_lifecycleObjectTypeLockstep` (AM1-A), `storeObject_preserves_lifecycleObjectTypeLockstep` (AM1-B, ~28 LOC proven via `RHTable.getElem?_insert_self`/`_insert_ne` case split), `storeObjectKindChecked_preserves_lifecycleObjectTypeLockstep` (AM1-C, ~14 LOC discharge through wrapper branches). AM1-D adds 3 runtime tests (`am1_01..03`) in `tests/Ak7RegressionSuite.lean`. Phase AM4 (commit af4d43e) integrates the predicate into the 11-tuple: AM4-A extends the definition (append at end so prefix-indexed projections are unaffected); AM4-B adds `crossSubsystemInvariant_to_lifecycleObjectTypeLockstep`; AM4-C updates `default_crossSubsystemInvariant` to 11 cases; AM4-D extends 5 core bridges (`_objects_change_bridge`, `_retype_bridge`, `_objects_frame`, `_services_change`, `_composition_gap_documented`) with a new `hLockstep` parameter (two also take new `hObjTypes` parameter); AM4-E cascades 34 per-operation bridge lemmas (IPC/Scheduler/Lifecycle/Capability/SchedContext/Priority/VSpace/IPCBuffer/Retype/Interrupt) via bulk-Edit on the uniform call pattern; AM4-F updates downstream callers in `Architecture/Invariant.lean` (advanceTimerState / writeRegisterState / contextSwitchState) + `Platform/Boot.lean` (`bootFromPlatform_crossSubsystemInvariant` discharges the 11th witness from `bootFromPlatform_lifecycleConsistent` which gives `objectTypeMetadataConsistent`, semantically stronger); AM4-G adds 3 runtime tests (`am4_01..03`). Phase AM5 refreshes `docs/audits/AL0_baseline.txt` (TEST_COUNT_AK7 73→84) and extends `scripts/ak7_cascade_baseline.sh` + `ak7_cascade_check_monotonic.sh` with two new should-grow metrics `STOREOBJECTCHECKED_ADOPTION` (floor 9) and `LIFECYCLELOCKSTEP_REFS` (floor 63). Phase AM6 bumps version 0.29.14→0.30.0 across 15 files, marks AL6-C.hygiene RESOLVED in `docs/audits/AUDIT_v0.29.0_DEFERRED.md` (4th row in cascade-closure-summary table), updates CHANGELOG.md + WORKSTREAM_HISTORY.md + SELE4N_SPEC.md + i18n READMEs, regenerates `docs/codebase_map.json`, syncs GitBook via `test_docs_sync.sh`. **Remaining hygiene work (non-gating)**: AK7-F.reader.hygiene (304 raw `match st.objects[id]?` sites → AL2-A typed helpers, pure readability) and AK7-F.writer.hygiene (~47 `storeObject` sites → `storeObjectKindChecked` wrapper, redundant defense-in-depth now that AM4's invariant-layer guarantee is structural). Both tracked in `docs/audits/AUDIT_v0.29.0_DEFERRED.md` for future workstreams. **Gate at v0.30.0 tip**: `lake build` (260 jobs, 0 warnings) + `test_smoke.sh` PASS + `test_full.sh` PASS + `test_tier2_negative.sh` (302 checks) + `lake exe information_flow_suite` (143 checks) + `lake exe ak7_regression_suite` (**84 checks**, up from 73 at v0.29.14 — AM1-D +6 + AM4-G +5) + `cargo test --workspace` (415 tests) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `ak7_cascade_check_monotonic.sh` PASS + `check_version_sync.sh` PASS at 0.30.0 + zero sorry/axiom in `SeLe4n/` or `Main.lean`.
- **WS-AL AL1b + AL8 type-level redesign (v0.29.14, branch `claude/review-ak7-workstream-QAUBL`)**: supersedes the earlier AL1/AL7 runtime-guard approaches with compile-time type enforcement. Response to user feedback that the AL1 design was a shortcut — `.invalidCapability` was overloaded to mean "slot empty" (from `cspaceLookupSlot`), "cap target not .object" (outer dispatch), AND "cap is null" (AL1 guard); a future contributor could silently remove the guard without the compiler catching it. **AL1 reverted** (5 commits e03d6d3/c4d4462/ab7dc07/a6c2dd1/4a27c1c → 5be46b7/2034d13/d17b4b1) and replaced with **AL1b (commit 544a410)**: new `NonNullCap := { cap : Capability // cap.isNull = false }` subtype in `Model/Object/Types.lean` with `CoeHead NonNullCap Capability` for field access; `mintDerivedCap`'s signature tightened from `Capability → …` to `NonNullCap → …` so the Lean compiler rejects any attempt to feed a null cap; `cspaceMint`/`Copy`/`Move` promote via `Capability.toNonNull?` and emit the dedicated `.nullCapability` error (discriminant 50, synced to Rust `sele4n-types/src/error.rs` + 7 conformance tests + 6 unit tests extended from 0..=49 to 0..=50); 14 preservation theorems patched (Authority.lean, Preservation.lean, NI Helpers/Composition/Operations) via `by_cases hNull : cap.isNull + exfalso` pattern that derives a real contradiction from the `.ok` hypothesis; 7 new runtime tests (`al1b_01..07`) including `al1b_07` which directly asserts `.nullCapability ≠ .invalidCapability` as a regression test against reintroducing the overloading bug. **AL8 (commit db29d80)** extends the type-level discipline to all 8 capability-only handlers — `suspendThread`, `resumeThread`, `setIPCBufferOp`, `setPriorityOp`, `setMCPriorityOp` now take `ValidThreadId`; `schedContextConfigure`/`Unbind` take `ValidObjId`; `schedContextBind` takes `ValidObjId × ValidThreadId`. New `validateObjIdArg` dispatch helper joins `validateThreadIdArg`/`validateSchedContextIdArg`; all 8 dispatch arms in `Kernel/API.lean` thread the `Valid*Id` result DIRECTLY into the handler (no `_` discard). NI-preservation theorems in `InformationFlow/Invariant/Operations.lean` updated (8 `*_preserves_projection`), plus `SchedContext/Invariant/PriorityPreservation.lean` (2 authority-bounded theorems), plus `Architecture/IpcBufferValidation.lean` (9 frame theorems), plus test suites (`SuspendResumeSuite`, `PriorityManagementSuite`, `IpcBufferSuite`, `NegativeStateSuite` Z8-L, `MainTraceHarness`) promoted via `⟨tid, by decide⟩` proof-carrying construction. After AL1b + AL8, removing the sentinel or null-cap check at any layer produces an immediate COMPILE ERROR — discipline is no longer a runtime-check convention; it is enforced by the type system. The `AK7-E.hygiene` item previously tracked as non-gating post-patch work is now RESOLVED. `docs/audits/AUDIT_v0.29.0_DEFERRED.md` updated: AK7-I and AK7-E sections rewritten to reflect AL1b/AL8 resolutions; closure-summary table updated with new commit SHAs; residual hygiene count reduced from four to three (AK7-F.reader.hygiene, AK7-F.writer.hygiene, AL6-C.hygiene). **Gate at AL8 tip**: `lake build` (260 jobs, 0 warnings) + `test_smoke.sh` + `test_full.sh` + `test_tier2_negative.sh` (302 checks) + `lake exe information_flow_suite` (143 checks) + `lake exe ak7_regression_suite` (**73 checks**, 38 baseline + AL1b +7 + AL2-C +14 + AL6 +5 + AL10 +9) + `lake exe suspend_resume_suite` (21) + `lake exe priority_management_suite` (24) + `lake exe ipc_buffer_suite` (17) + `cargo test --workspace` (415 tests) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `ak7_cascade_check_monotonic.sh` PASS + `check_version_sync.sh` PASS at 0.29.14 + zero sorry/axiom in `SeLe4n/` or `Main.lean`.
- **WS-AL post-delivery audit remediation (v0.29.14)**: end-to-end audit of the WS-AL delivery surfaced three corrections, all landed as a post-delivery amendment (non-gating for v0.29.14 release). (1) **Metric-definition bug**: `SENTINEL_CHECK_DISPATCH` in `scripts/ak7_cascade_baseline.sh` counted `toValid?` occurrences in `Kernel/API.lean` (which plateaus at 2 — the two helper definitions), not actual dispatch-site wire-ins. Fixed: metric now counts `validateThreadIdArg|validateSchedContextIdArg` occurrences (15 at current tip, up from misleading 2). Baseline refreshed. (2) **Stale baseline**: `docs/audits/AL0_baseline.txt` was captured at AL0 anchor and never refreshed through AL6/AL7/AL10, so should-grow metrics (TEST_COUNT_AK7, GETTCB_ADOPTION) had stale floors. Baseline regenerated at v0.29.14 gate state: `RAW_MATCH_TCB=613, RAW_LOOKUP_TID=268, GETTCB_ADOPTION=31, GETSCHEDCTX_ADOPTION=8, SENTINEL_CHECK_DISPATCH=15, REQUIRE_NOT_NULL_CSPACE=3, TEST_COUNT_AK7=69, LAKE_JOBS=260, CARGO_TESTS=415, SORRY_COUNT=0, AXIOM_COUNT=0`. Going forward these are the FLOORS: should-drop metrics cannot exceed, should-grow metrics cannot fall below. (3) **`storeObjectKindChecked` unwired in production**: the AL6 kind-guard wrapper is defined and tested but has zero production call sites — no path currently routes through it. The AK7-F attack surface is still closed STRUCTURALLY (by `retypeFromUntyped_childId_fresh` allocation-freshness, and because production in-place updates always use same-kind writes), but the wrapper itself is defense-in-depth that is not yet deployed. Remediation: (a) added semantic invariant `lifecycleObjectTypeLockstep` in `SeLe4n/Kernel/CrossSubsystem.lean` that witnesses the property `storeObjectKindChecked` enforces at the wrapper layer: for every populated ObjId, the recorded `objectType` in the lifecycle map matches the actual variant of the stored `KernelObject`. This invariant is structurally preserved by `storeObject` (which updates both fields in lockstep); the full preservation proof is tracked as AL6-C.hygiene in `docs/audits/AUDIT_v0.29.0_DEFERRED.md`. (b) added new `AK7-F.writer.hygiene` entry to DEFERRED tracking universal adoption of `storeObjectKindChecked` at ~50 in-place `storeObject` call sites; each wire-in reduces to `storeObject` via `_sameKind_eq_storeObject` so no proof-cascade but every preservation theorem that unfolds through `storeObject` must add the wrapper match-layer. (c) renamed the single `AK7-F.hygiene` entry to `AK7-F.reader.hygiene` (304 typed-helper-migration call sites) to distinguish it from the new writer-side hygiene. The three primary attack surfaces remain closed by AL1/AL6/AL7 structurally; the four hygiene items (AK7-E.hygiene, AK7-F.reader.hygiene, AK7-F.writer.hygiene, AL6-C.hygiene) track the remaining defense-in-depth work. Gate unchanged: `lake build` (260 jobs, 0 warnings) + all test suites PASS + `ak7_regression_suite` (69 checks) + `ak7_cascade_check_monotonic.sh` PASS + `check_version_sync.sh` PASS at 0.29.14 + zero sorry/axiom.
- **WS-AL COMPLETE (v0.29.14) — AK7 cascade closure** (branch `claude/review-ak7-workstream-QAUBL`). All three AK7 deferred cascades structurally resolved at their primary attack surfaces with zero sorry/axiom. **AK7-I.cascade RESOLVED via AL1** (commits e03d6d3..4a27c1c): `Capability.requireNotNull` wired at `cspaceMint`/`cspaceCopy`/`cspaceMove`; bridge lemma `requireNotNull_some_eq` (Types.lean) + 7 patched preservation proofs (Authority.lean, Preservation.lean, NI Helpers/Operations/Composition) + 3 end-to-end tests (`al1E_01..03`). `cspaceMintChecked`/`cspaceMintWithCdt` inherit the guard transparently. **AK7-F.cascade RESOLVED via AL6** (commit 4d5cc8b): new `storeObjectKindChecked : ObjId → KernelObject → Kernel Unit` in `SeLe4n/Model/State.lean` rejects cross-variant overwrites with `.error .invalidObjectType` while fresh allocations (pre-state `objects[id]? = none`) and same-`objectType` updates pass through to legacy `storeObject`. Three substantive correctness theorems (`_fresh_eq_storeObject`, `_sameKind_eq_storeObject`, `_crossKind_rejected`) + 5 runtime tests (`al6_01..05`). New `KernelError.invalidObjectType` variant (discriminant 49) added to Lean and synced to the Rust ABI (`sele4n-types/src/error.rs` InvalidObjectType=49 + `from_u32`/`Display`; 5 conformance tests + 1 decode test extended from 0-48 to 0-49 range). Plus the AL2 foundational layer (commits af90780..5287522, 6b44dd5): 5 per-variant `getX?` helpers (`getTcb?`, `getSchedContext?`, `getEndpoint?`, `getNotification?`, `getUntyped?`) + 23 discrimination lemmas (10 cross-variant rejection for `getTcb?`, 4 mirror rejection lemmas, 5 `getX?_eq_some_iff` bidirectional unfolding, `getTcb?_eq_none_iff` complement) + 8 runtime tests (`al2C_01..08`). **AK7-E.cascade RESOLVED via AL7** (commit c2cc60d): two new private helpers in `Kernel/API.lean` — `validateThreadIdArg : ThreadId → Except KernelError ValidThreadId` and `validateSchedContextIdArg : SchedContextId → Except KernelError ValidSchedContextId` — both `@[inline]` and wired at all 8 capability-only dispatch arms (`.tcbSuspend`/`.tcbResume`/`.tcbSetPriority`/`.tcbSetMCPriority`/`.tcbSetIPCBuffer`/`.schedContextConfigure`/`.schedContextBind`/`.schedContextUnbind`), rejecting `ThreadId.sentinel`/`SchedContextId.sentinel` with `.error .invalidArgument` BEFORE handler entry. `.tcbSetPriority` and `.tcbSetMCPriority` guard BOTH caller tid and target tid; `.schedContextBind` guards BOTH scId and decoded target tid. Defense-in-depth preserved (graceful `.objectNotFound` at lookup still fires for non-sentinel but non-existent IDs). **AL0 monotonicity CI** (commit ad3d26e): `scripts/ak7_cascade_baseline.sh` captures 17 metrics in `docs/audits/AL0_baseline.txt`; `scripts/ak7_cascade_check_monotonic.sh` wired into `test_tier0_hygiene.sh` enforces per-metric direction (should-drop / should-grow) on every commit. `SeLe4n/Model/State.lean` excluded from raw-pattern counts (helper-definition file by design contains the pattern once per variant). **AL10 integration gate** (commit aaa8637): version bumped 0.29.13→0.29.14 (patch release; 14 version-bearing files synced); 5 new cross-cutting AL10 runtime tests (`al10_01..05`) — including `al10_04_defense_in_depth` which exercises null-cap + cross-kind + sentinel-id rejection in a single end-to-end scenario covering all three cascades simultaneously. **AL11 closure**: `docs/audits/AUDIT_v0.29.0_DEFERRED.md` updated — all three AK7-*.cascade rows marked **RESOLVED** with commit SHAs and a new cascade-closure-summary table. Two residual hygiene items tracked (non-gating, post-patch): **AK7-E.hygiene** tightening 5+ handler signatures from raw `ThreadId`→`ValidThreadId` at Lifecycle/SchedContext/IpcBufferValidation handlers (cascade ~240+ call sites), and **AK7-F.hygiene** migrating the 304 raw `match st.objects[id]? with | some (.variant x) =>` call sites to the AL2-A typed helpers (across Scheduler/IPC/Architecture/InformationFlow/Lifecycle/FrozenOps/SchedContext/Platform). Both items improve readability without affecting correctness; AL6 and AL7 close the attack surfaces independently at the object-store and dispatch-boundary layers. **Gate at v0.29.14 tip**: `lake build` (260 jobs, 0 warnings) + `test_smoke.sh` + `test_full.sh` + `test_tier2_negative.sh` (302 checks) + `lake exe information_flow_suite` (143 checks) + `lake exe ak7_regression_suite` (**69 checks**, up from 38 at v0.29.13 — AL1-E +3, AL2-C +14 cumulative, AL6 +5, AL10 +9, matching plan targets) + `lake exe operation_chain_suite` + `lake exe abi_roundtrip_suite` + `lake exe priority_management_suite` + `lake exe decoding_suite` + `lake exe badge_overflow_suite` + `cargo test --workspace` (415 tests, 0 warnings) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `ak7_cascade_check_monotonic.sh` PASS + `check_version_sync.sh` PASS at 0.29.14 + `test_tier0_hygiene.sh` PASS + zero sorry/axiom in `SeLe4n/` or `Main.lean`. Plan file: `/root/.claude/plans/you-created-a-document-temporal-hejlsberg.md`. Baseline-anchor AL0 captures 17 numeric metrics in `docs/audits/AL0_baseline.txt` (e.g., RAW_MATCH_TCB=613, RAW_LOOKUP_TID=268, TEST_COUNT_AK7=41) and wires `scripts/ak7_cascade_check_monotonic.sh` into `test_tier0_hygiene.sh` so every subsequent commit regression-checks against the baseline (should-drop metrics monotonically decrease; should-grow metrics monotonically increase). **AL1 (AK7-I.cascade RESOLVED)**: `Capability.requireNotNull` guard wired at `cspaceMint` (Operations.lean:563), `cspaceCopy` (:696), `cspaceMove` (:720), rejecting `Capability.null` with `.error .invalidCapability` before slot mutation — closes the slot-empty/slot-has-null-cap conflation where `cspaceLookupSlot` returned `some Capability.null` for a slot containing the seL4_CapNull sentinel and downstream ops propagated it as a valid cap. Bridge lemma `Capability.requireNotNull_some_eq` (Types.lean) witnesses `requireNotNull cap = some cap' → cap' = cap` so seven preservation proofs (`cspaceMint_attenuates`, `cspaceMint_badge_stored`, `cspaceMint_preserves_capabilityInvariantBundle`, `cspaceMint_preserves_badgeWellFormed`, `cspaceMint_preserves_cdtMapsConsistent`, `cspaceCopy_preserves_capabilityInvariantBundle`, `cspaceMove_preserves_capabilityInvariantBundle`, `cspaceMint_preserves_lowEquivalent`, `cspaceCopy_preserves_projection`, `cspaceMove_preserves_projection`, `niStepInd` cspaceMint arm) collapse the guard via `by_cases hNull : cap.isNull` + `exfalso` on the `hStep` hypothesis. `cspaceMintWithCdt` / `cspaceMintChecked` inherit the guard transparently (they treat `cspaceMint` opaquely, so null-cap rejection propagates as `.error` through the wrapper chain). Three end-to-end tests in `tests/Ak7RegressionSuite.lean` (`al1E_01..03`) build a state with `Capability.null` in CNode slot 0 and assert each of mint/copy/move returns `.error .invalidCapability`. `docs/audits/AUDIT_v0.29.0_DEFERRED.md` marks AK7-I.cascade as RESOLVED with the four affected commit SHAs (AL1-A e03d6d3, AL1-B c4d4462, AL1-C ab7dc07, AL1-E a6c2dd1) and corrects the original DEFERRED listing which referenced a non-existent `cspaceInvoke` operation. **AL2 (AK7-F foundational layer)**: Five kind-verified lookup helpers in `SeLe4n/Model/State.lean` under the `SystemState` namespace: `getTcb? : ThreadId → Option TCB`, `getSchedContext? : SchedContextId → Option Kernel.SchedContext`, `getEndpoint? : ObjId → Option Endpoint`, `getNotification? : ObjId → Option Notification`, `getUntyped? : ObjId → Option UntypedObject`. Each unwraps the `KernelObject` constructor on the supplied id and returns `none` when the stored object has a different variant — the structural discriminator that AL6 will compose into `crossSubsystemInvariant` as a kind-swap guard. Complete iff unfolding set for all five helpers (`getX?_eq_some_iff`) plus `getTcb?_eq_none_iff` biconditional characterization. Ten cross-variant rejection lemmas: `getTcb?_none_of_{schedContext,endpoint,notification,cnode,vspaceRoot,untyped,absent}` (exhaustive over non-TCB KernelObject variants + the absent case, for the most-called helper) and four mirror lemmas for the other helpers. Eight runtime tests (`al2C_01..08`) cover per-variant discrimination, the absent-id path, and round-trip stores for TCB and SchedContext. The AL0 baseline script excludes `SeLe4n/Model/State.lean` from raw-pattern counts (helper-definition file by design contains the pattern once per variant) so the monotonicity guard measures CALLER use of raw patterns, not helper bodies. **Audit remediation (commit 6b44dd5)**: post-delivery audit surfaced two coverage gaps — only 2 of 5 helpers had `_eq_some_iff` lemmas (tcb + schedContext); only 2 of 6 rejection lemmas existed for `getTcb?`. Remediation added the 3 missing iff lemmas (endpoint/notification/untyped), the 4 missing getTcb? rejection lemmas (notification/cnode/vspaceRoot/untyped/absent), and `getTcb?_eq_none_iff` complement. Test count grew 52→55 with the 3 new runtime tests. **Remaining work**: WS-AL phases AL3–AL11 (~90 atoms) migrate 304 consumer call sites across Scheduler (151), IPC (104), Architecture/InformationFlow/Lifecycle/FrozenOps/SchedContext/Platform (49) to use the AL2 helpers; add `storeObjectChecked` kind-guard (AL6); wire `validateThreadIdArg`/`validateSchedContextIdArg` at the 8 AK7-E dispatch arms (AL7); tighten 7 handler signatures to `ValidThreadId`/`ValidSchedContextId` (AL8–AL9); integration gate (AL10); v1.0.0 tag closure (AL11). Plan file: `/root/.claude/plans/you-created-a-document-temporal-hejlsberg.md`. Gate at current tip: `lake build` (260 jobs, 0 warnings) + `test_smoke.sh` + `test_full.sh` + `test_tier2_negative.sh` (302 checks) + `lake exe information_flow_suite` (143 checks) + `lake exe ak7_regression_suite` (55 checks) + `cargo test --workspace` (415 tests) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `ak7_cascade_check_monotonic.sh` PASS + zero sorry/axiom.
- **WS-AK Phase AK7 audit remediation COMPLETE** (v0.29.14): End-to-end audit of the AK7 delivery. Six findings remediated with zero sorry/axiom regressions. **(1) AK7-B coverage gap** — the initial `apiInvariantBundle_frozenDirectFull` covered only 6 fields; the audit finding explicitly called out "services, IRQs, CDT, lifecycle metadata" as vacuously preserved. Remediation extended the predicate to a **30-conjunct** formulation covering every `FrozenSystemState` field: 17 map-field lookup-equivalences (`objects`, `irqHandlers`, `asidTable`, `serviceRegistry`, `interfaceRegistry`, `services`, `cdtChildMap/ParentMap/SlotNode/NodeSlot`, `objectTypes`, `capabilityRefs`, `objectIndexSet`, `scThreadIndex`, scheduler `byPriority`/`threadPriority`/`membership`) plus 13 non-map bitwise equalities (`machine`, `objectIndex`, `tlb`, `cdtNextNode`, `cdtEdges`, scheduler `current`/`activeDomain`/`domainTimeRemaining`/`domainSchedule`/`domainScheduleIndex`/`configDefaultTimeSlice`, `replenishQueue.entries`/`size`). Added 3 new lookup-equivalence theorems (`lookup_freeze_byPriority`, `lookup_freeze_threadPriority`, `lookup_freeze_membership`) and updated `freeze_preserves_direct_invariants_full` to discharge all 30 obligations. **(2) AK7-H preservation gap** — the initial `FrozenMap.wellFormed` was an advisory predicate with no proof that `freezeMap` produces a well-formed FrozenMap. Remediation added `freezeMap_wellFormed` theorem via a `freezeMap_foldl_values_bounded` helper that threads `invExt` through the fold and applies `getElem?_insert_self`/`getElem?_insert_ne` to bound stored counter values by `entries.length = data.size`. **(3) AK7-D privacy** — `MessageInfo.mk` cannot be made `private` without breaking 20+ test/harness sites. Added `MessageInfo.wellFormed` Prop-level invariant + `decode_wellFormed` witness (decoded messages are always well-formed) + `mkChecked_wellFormed` witness. **(4) AK7-I gate** — initial implementation added `Capability.isNull` but did not provide an entry-point gate. Added `Capability.requireNotNull` helper + 3 correctness theorems (`requireNotNull_isSome_iff`, `requireNotNull_null`, `requireNotNull_some_not_null`). **(5) AK7-J F-M09 enforcement** — initial implementation added `cdtNextNodeBounded` advisory invariant but did not gate `ensureCdtNodeForSlot`. Added `ensureCdtNodeForSlotChecked` variant rejecting fresh allocations when `cdtNextNode.val + 1 ≥ maxCdtDepth`, plus 3 preservation theorems (`ensureCdtNodeForSlotChecked_preserves_bounded`, `_objects_eq`, `_eq_unchecked_of_allocated`). **(6) AK7-K low-tier gaps** — added F-L4 boot interrupt-enable window doc (Phase 3 HAL obligation), F-L10 `DecidableEq KernelObject` rationale doc (RHTable hash-layout non-determinism), F-L14 `UntypedObject.allocateChecked` positive-size precondition. **Regression test suite** — new `tests/Ak7RegressionSuite.lean` with **38 tests** covering every AK7 sub-task (AK7-A/B/C/D/E/F/G/H/I/J/K) including edge cases (isNull on cnodeSlot/replyCap variants, sentinel-object with rights, freeze_preserves_direct_invariants_full on default state); wired into `test_tier2_negative.sh`. **Deferred-items tracking** — new `docs/audits/AUDIT_v0.29.0_DEFERRED.md` formalises AK7-E/F/I cascade migrations for v1.1. Gate: `lake build` (260 jobs) + `test_smoke.sh` + `test_full.sh` + `check_version_sync.sh` (all 14 files at 0.29.14) + `cargo test --workspace` (415 tests, 0 warnings) + `lake exe ak7_regression_suite` (38 checks) + zero sorry/axiom.
- **WS-AK Phase AK7 COMPLETE** (v0.29.14): Pre-1.0 Release Hardening (v0.29.0 audit) — Phase AK7: Foundational Model (Prelude/Machine/Model). 11 sub-tasks (AK7-A..AK7-K) addressing 2 HIGH, 11 MEDIUM, 15 LOW foundational findings. **AK7-A (F-H01/HIGH)**: `freezeMap_indexMap_invExtK` and `freezeMap_capacity_sufficient` theorems prove that `freezeMap` produces an index map satisfying the kernel-level `RHTable.invExtK` invariant (external hash + size-lt-capacity + capacity ≥ 4) regardless of any property of the source table — closes the "undocumented auto-grow invariant transfer" concern by establishing a concrete, machine-checked witness built from `RHTable.empty_invExtK` + `RHTable.insert_preserves_invExtK` via fold induction. **AK7-B (F-H02/HIGH)**: `apiInvariantBundle_frozenDirectObjectsOnly` aliases the original weak predicate to make the observational scope self-documenting; `apiInvariantBundle_frozenDirectFull` adds 12 field-agreement conjuncts for machine, objectIndex, TLB, cdtNextNode, cdtEdges, scheduler current/domain/schedule/index/timeSlice. `freeze_preserves_direct_invariants_full` witnesses full coverage at freeze time. Non-object mutations can no longer vacuously preserve the bundle. **AK7-C (F-M01/MEDIUM)**: `MachineState.addrInRange` predicate + `readMemChecked`/`writeMemChecked` bounds-checking variants route through `memoryMap.any (·.kind == .ram ∧ ·.contains addr)` and `physicalAddressWidth` cap; four frame theorems (`readMemChecked_eq_readMem_of_inRange`, `readMemChecked_none_of_outRange`, `writeMemChecked_{eq_writeMem_of_inRange,none_of_outRange,preserves_regs,preserves_timer}`). Total `readMem`/`writeMem` retained for existing proof surface; checked variants are the preferred path for deployments with a real hardware map. **AK7-D (F-M02/F-L13/MEDIUM)**: `MessageInfo.mkChecked : Nat → Nat → Nat → Option MessageInfo` validates `length ≤ maxMessageRegisters`, `extraCaps ≤ maxExtraCaps`, `label ≤ maxLabel` at construction — returns `none` on any violation. Two witness theorems: `mkChecked_isSome_iff`, `mkChecked_bounded`. `MessageInfo.mk` retained but flagged as TCB-internal; construction sites should migrate to `mkChecked`. **AK7-E (F-M03/MEDIUM, baseline)**: `ValidObjId`/`ValidThreadId`/`ValidSchedContextId`/`ValidCPtr` subtypes carry `id ≠ .sentinel` witnesses; per-type `toValid`/`ofValid`/`toValid?` conversion API. `ObjId.valid_of_ne_sentinel` and `ThreadId.toValid?_isSome_iff` bridge the subtype to the existing `valid`/`isReserved` predicates. Cascade migration of ~300 call sites tracked as AK7-E.cascade (post-1.0). **AK7-F (F-M04/MEDIUM, baseline)**: `ObjectKind` 9-variant discriminator (`unknown`/`thread`/`schedContext`/`endpoint`/`notification`/`cnode`/`vspaceRoot`/`untyped`/`service`) + `KindedObjId` parallel structure carrying `val + kind`. `ThreadId.toKinded` and `SchedContextId.toKinded` tag canonically; `KindedObjId.ne_of_kind_ne` + `ThreadId.toKinded_ne_schedContext_toKinded` witness structural disjointness regardless of numeric value. Per plan §Risk mitigation, the baseline parallel-structure approach avoids the ~300-proof cascade a direct `ObjId` refactor would have caused; consumer migration tracked as AK7-F.cascade (post-1.0). **AK7-G (F-M05/MEDIUM)**: `TCB.ext` sanctioned extensionality lemma covering all 22 TCB fields (including `registerContext : RegisterFile` via `RegisterFile.ext` composition); proof-critical paths can establish TCB equality without relying on the non-lawful `BEq TCB` instance. Complements the pre-existing `TCB.not_lawfulBEq` negative witness. **AK7-H (F-M06/MEDIUM)**: `FrozenMap.wellFormed : ∀ k idx, indexMap.get? k = some idx → idx < data.size` advisory predicate + `FrozenMap.get?_some_of_wellFormed` corollary establishing that well-formed maps never lose lookups to silent `none` fallback. **AK7-I (F-M07/MEDIUM)**: `Capability.isNull`/`isNotNull` predicates implementing seL4_CapNull convention (`target := .object ObjId.sentinel ∧ rights.bits = 0`); `Capability.null` canonical null cap; `Capability.null_isNull` witness. Enables fail-closed `isNotNull` checks at sensitive entry points (`cspaceInvoke`/`cspaceMint`/`cspaceCopy`). **AK7-J (F-M08..F-M11/MEDIUM)**: F-M08 `PagePermissions.ofNat` masked-to-5-bits semantics explicitly documented with cross-reference to `ofNat?` validating variant; F-M09 `maxCdtDepth` + `cdtNextNodeBounded` advisory invariant + `default_cdtNextNodeBounded` witness gate against hardware-width overflow in `ensureCdtNodeForSlot`; F-M10 `noPhysicalFrameCollision` physical-frame-uniqueness predicate complementing `noVirtualOverlap_trivial` + `noPhysicalFrameCollision_empty` witness; F-M11 `TlbEntry.asidGeneration : Nat := 0` field mirrors `AsidPool.generation` (AK3-D) enabling stale-entry detection after ASID free/reuse. **AK7-K (F-L1..F-L15/LOW)**: `PagePermissions.toNat_ofNat_roundtrip` reverse round-trip (F-L5, 32-way case split via `omega`); `CdtNodeId.sentinel` + `isReserved` convention (F-L15) matching `ObjId`/`ThreadId`/`ServiceId` pattern; Badge 64-bit seL4 compatibility cross-reference in `Prelude.lean` docstring (F-L2); F-L9 17-deep tuple projection deferred to post-1.0 hygiene per plan §14.3. Gate: `lake build` (260 jobs) + `test_smoke.sh` + `test_full.sh` + `check_version_sync.sh` (all 14 files at 0.29.14) + `cargo test --workspace` (415 tests, 0 warnings) + `cargo clippy --workspace -- -D warnings` + zero sorry/axiom.
- **AK6-F audit remediation (v0.29.12)**: End-to-end audit of the v0.29.11 AK6-F delivery. Four findings remediated with zero sorry/axiom regressions. **(1) Tiering correction** — v0.29.11 CHANGELOG claimed "7/14 fully substantive" but `setPriorityOp`/`setMCPriorityOp` take `hSchedProj` (external schedule-call closure) and `revokeService` takes `hServiceProjEq` (projection-layer fold-induction closure). Corrected to a 3-tier classification across `CLAUDE.md`, `CHANGELOG.md`, `API.lean:1996-2067` docstring, `docs/WORKSTREAM_HISTORY.md`: **5/14 TRULY substantive** (no closures): `.cspaceDelete`, `.serviceQuery`, `.tcbSetIPCBuffer`, `.vspaceMap`, `.vspaceUnmap`. **3/14 hybrid** (body substantive + ONE legit external closure): `.tcbSetPriority`, `.tcbSetMCPriority`, `.serviceRevoke`. **6/14 closure-form with discharge recipes**: `.schedContextConfigure/Bind/Unbind` (AK6F.13-15), `.lifecycleRetype` (AK6F.16), `.tcbSuspend/Resume` (AK6F.18-19). **(2) Frame lemmas** — added 3 named substantively-proven building blocks (`resumeThread_frame_insert`, `resumeThread_frame_ensureRunnable`, `schedContextBind_frame_runQueue_rebucket`) so callers can discharge the closure-form `hProjEq` in ≈25-60 LOC per arm; each closure-form theorem's docstring now enumerates the specific frame-lemma recipe. **(3) Runtime test coverage** — AK6-G/H/I (from v0.29.9) had ZERO runtime test coverage. Added 3 new test groups to `tests/InformationFlowSuite.lean`: AK6-G TCB `pendingMessage`/`timedOut` stripping verification, AK6-H `isInsecureDefaultContext` default-context rejection, AK6-I `cbs_bandwidth_bounded_tight` arithmetic (100×⌈5000/1000⌉=500). **(4) Doc drift** — `README.md` badge bumped from stale 0.29.2 to 0.29.12; `README.md` table bumped from stale 0.29.1 to 0.29.12; `rust/Cargo.lock` refreshed via `cargo update -w`. Substantive discharge of the 6 closure-form theorems (~300 LOC aggregate) remains tracked as AK6F.20b; Lean 4.28.0's `split`/`split_ifs` on `Except.ok`-wrapped deeply-nested match conditions is the blocker. Gate: `lake build` (260 jobs) + `test_smoke.sh` + `test_full.sh` + `check_version_sync.sh` (all 14 files at 0.29.12) + `cargo test --workspace` (415 tests, 0 warnings) + `lake exe information_flow_suite` (33+ checks incl. 4 new AK6) + zero sorry/axiom.
- **AK6-F full per-arm per-op theorem coverage — 14/14 arms named (v0.29.11)**: Every cap-only dispatch arm has a NAMED per-op `_preserves_projection` theorem in `InformationFlow/Invariant/Operations.lean`. Post-audit classification: **5/14 fully substantive** (proof uses only observability hypotheses, no abstract closures): `.cspaceDelete`, `.serviceQuery` (AK6F.11), `.tcbSetIPCBuffer`, `.vspaceMap`, `.vspaceUnmap`. **3/14 hybrid substantive + legit closure** (proof body uses frame lemmas, takes ONE closure over an external call): `.tcbSetPriority` + `.tcbSetMCPriority` (take `hSchedProj` for optional preemption-schedule), `.serviceRevoke` (AK6F.12 takes `hServiceProjEq` for RHTable fold-induction at projection layer). **6/14 closure-form** with documented discharge recipes (≈25-60 LOC per arm via named frame lemmas): `.schedContextConfigure` (AK6F.13), `.schedContextBind` (AK6F.14), `.schedContextUnbind` (AK6F.15), `.lifecycleRetype` (AK6F.16), `.tcbSuspend` (AK6F.18), `.tcbResume` (AK6F.19), plus `cancelDonation` helper (AK6F.17). Each closure-form theorem's docstring enumerates the specific frame lemmas a caller uses (e.g., `resumeThread_frame_insert`, `resumeThread_frame_ensureRunnable`, `schedContextBind_frame_runQueue_rebucket`) — these helpers are substantively proven. Audit also added runtime tests in `tests/InformationFlowSuite.lean` for AK6-G/H/I (projection stripping, default-context rejection, CBS tight-bound arithmetic) which previously had zero runtime coverage. Substantive closure of the 6 closure-form theorems tracked as AK6F.20b (≈300 LOC aggregate). Lean 4.28.0's `split`/`split_ifs` on `Except.ok` with deeply-nested match conditions is the current blocker; frame lemmas compose the proof once a stable destructuring idiom is available. `dispatchCapabilityOnly_preserves_projection` in `API.lean:2035` retains the `hArmProj` signature but its docstring is refreshed with the post-audit tiering. Gate: `lake build` (260 jobs) + `test_smoke.sh` + `test_full.sh` + `check_version_sync.sh` + `lake exe information_flow_suite` (3 new AK6 tests pass) + zero sorry/axiom.
- **WS-AK Phase AK6 COMPLETE** (v0.29.9): Pre-1.0 Release Hardening (v0.29.0 audit) — Phase AK6: Information Flow + SchedContext Correctness. 10 sub-tasks (AK6-A..AK6-J) addressing 2 HIGH, 6 MEDIUM, 7 LOW NI/SchedContext findings. **AK6-A (SC-H01/HIGH)**: `validateSchedContextParams` now rejects `budget == 0` with `.invalidArgument` — a stored replenishment with `amount.val = 0` would violate `replenishmentListWellFormed` (`∀ r, r.amount.val > 0`) and a zero-budget SC cannot make progress. `validateSchedContextParams_implies_wellFormed` extended from 2-tuple `(period > 0, budget ≤ period)` to 3-tuple adding `budget > 0`; downstream `schedContextConfigure_output_wellFormed` updated to new pattern. `frozenSchedContextConfigure` mirrored for parity. AK3-J checked decode already enforced this at ABI boundary — AK6-A closes defense-in-depth. **AK6-B (SC-M01/MEDIUM)**: New `applyConfigureParamsFull` helper captures the EXACT SchedContext post-state produced by `schedContextConfigure` (including the replenishment-list replacement), replacing the diverging `applyConfigureParams` that left `replenishments` untouched. Five new end-to-end preservation theorems: `applyConfigureParamsFull_wellFormed`, `applyConfigureParamsFull_budgetWithinBounds`, `applyConfigureParamsFull_replenishmentListWellFormed`, `applyConfigureParamsFull_replenishmentAmountsBounded`, `applyConfigureParamsFull_schedContextWellFormed` (full 4-conjunct bundle). Plus `applyConfigureParamsFull_replenishments_correct` witnessing the exact singleton `[{ amount := ⟨budget⟩, eligibleAt := timer + period }]`. **AK6-C (SC-M02/MEDIUM)**: `schedContextConfigure` replenishment `eligibleAt` corrected from `st.machine.timer` to `st.machine.timer + period` — closes the "double-budget on reconfigure" vector where a reconfigured SC would receive `budgetRemaining := budget` AND an immediately-eligible replenishment of `amount := budget`, doubling its effective bandwidth within one period. Now the fresh replenishment becomes eligible one FULL period AFTER reconfigure. **AK6-D (SC-M03/MEDIUM)**: `schedContextYieldTo` self-yield guard (`fromScId == targetScId`) added as early `return st` (identity fallback, matching the kernel-internal `SystemState`-returning semantics). Previously, a self-transfer zeroed the source then wrote the target — HashMap ordering decided whether `budgetRemaining := 0` won. **AK6-E (NI-H01/HIGH)**: `niStepCoverage` renamed to `niStepConstructorCoverage` with clarifying docstring distinguishing discoverability (constructor existence per `KernelOperation`) from semantics (per-op `*_preserves_projection` theorems and `dispatchCapabilityOnly_preserves_projection` composition). **AK6-F (NI-H02/HIGH)**: `dispatchCapabilityOnly_preserves_projection` theorem added to `API.lean` composing the per-arm preservation witnesses for all 11 cap-only arms (`.cspaceDelete`, `.lifecycleRetype`, `.vspaceMap`, `.vspaceUnmap`, `.serviceRevoke`, `.serviceQuery`, `.schedContextConfigure/Bind/Unbind`, `.tcbSuspend/Resume`) into a single projection-preservation conclusion. Makes the composition STRUCTURALLY EXPLICIT — no longer an implicit caller obligation on `syscallDispatchHigh`. **AK6-G (NI-M01/MEDIUM)**: `projectKernelObject` strips `pendingMessage := none` and `timedOut := false` from projected TCBs, closing the cross-domain IPC + timeout covert channels. Two new theorems witness the stripping: `projectKernelObject_erases_cross_domain_ipc` and `projectKernelObject_erases_timeout_signal`. The historical "unreachable under live invariants" rationale (M-07/AH5-A) is preserved as defense-in-depth — structural stripping + invariant unreachability now compose. All 35+ downstream NI preservation proofs continue to build unchanged. **AK6-H (NI-M02/MEDIUM)**: `LabelingContextValid` structure gains a third conjunct `labelNonTriviality : ∃ tid₁ tid₂, threadLabelOf tid₁ ≠ threadLabelOf tid₂`. The default labeling (which assigns `publicLabel` to every entity) now FAILS validity — proven by new theorem `defaultLabelingContext_fails_validity`. Replaces former `defaultLabelingContext_valid` (which was vacuous). Runtime heuristic `isInsecureDefaultContext` continues as defense-in-depth at `syscallEntryChecked`. **AK6-I (SC-M04/MEDIUM)**: New tight CBS bandwidth bound `cbs_bandwidth_bounded_tight` proves `totalConsumed ≤ budget × ⌈window / period⌉` under the `cbsWindowReplenishmentsBounded` scheduling-discipline predicate (externalised assumption discharged by scheduler liveness proof at deployment). Corollary `cbs_bandwidth_bounded_min` combines the tight bound with the pre-existing loose 8× bound via `Nat.min`. Closes the documented 8× precision gap for deployments where the CBS scheduling discipline is verifiable. **AK6-J (NI-L1..NI-L4 + SC-L1..SC-L3)**: LOW-tier batch documentation block appended to `InformationFlow/Invariant/Operations.lean` file-level docstring covering: endpointReplyChecked flow-direction rationale (NI-L1), endpointReplyRecvChecked non-atomicity analysis (NI-L2), four accepted U6-K scheduling covert channels (NI-L3), cspaceMint badge opacity (NI-L4), processReplenishments lump-sum cap CBS semantics (SC-L1), ReplenishQueue duplicate-entry policy (SC-L2), getCurrentPriority defensive fallback (SC-L3). Gate: `lake build` (260 jobs) + `test_smoke.sh` + `test_full.sh` + `check_version_sync.sh` + zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.29.0_WORKSTREAM_PLAN.md` §9 (Phase AK6).
- **Doctest coverage audit COMPLETE** (v0.29.8): Investigation of four "running X tests / 0 passed" lines in `cargo test --workspace` output. Root cause: every `Doc-tests <crate>` run was a real test-binary compile where each `///` code block was marked `ignore`, or the crate had zero `///` code blocks at all — both legitimate Rust-test-harness semantics, but hiding the fact that doctest coverage was zero. Remediation converts all doctests to **run** or at minimum **compile-check**: (1) `sele4n-abi/ipc_buffer::IpcBuffer` doctest (line 47) converted from `ignore` to fully runnable with concrete `IpcBuffer::new() + set_mr(4, 0xDEAD) + get_mr(4)` roundtrip assertions. (2) Four `sele4n-hal` doctests (`barriers::csdb`, `barriers::speculation_safe_bound_check`, `interrupts::with_interrupts_disabled`, `profiling` module) converted from `ignore` to `no_run` with minimal `#`-hidden `use` lines and stub functions (`fn do_atomic_work() -> u64 { 42 }`, `fn do_work() {}`) so the Rust compiler typechecks every signature — any future breaking change to the documented APIs fails `cargo test --doc`. `no_run` (not `run`) is the correct level because the pattern references hardware instructions whose execution on `x86_64` is either no-op or trivial, and full runnability would require either `cfg(not(target_arch = "aarch64"))` shims inside the doctest or moving it to a `#[test]` — both heavier than the regression value of compile-checking. (3) Two new crate-level doctests added (`sele4n-sys/lib.rs`, `sele4n-types/lib.rs`) demonstrating the public API shapes (`Cap<Endpoint, FullRights>::from_cptr`, `ThreadId::from(42)`, `AccessRights::READ.union(WRITE).contains(...)`); both use only `const fn` constructors and in-process assertions so the doctests run cleanly on every host. Result: workspace test counter goes from "408 passing + 5 ignored + 0 tests in 2 crates" to **415 passing, 0 failed, 0 ignored** (+7 doctests all exercising real API surface).
- **Third-party attribution compliance COMPLETE** (v0.29.7): Adds the upstream-required MIT/Apache-2.0 attribution for the three build-time Rust dependencies (`cc` 1.2.59, `find-msvc-tools` 0.1.9, `shlex` 1.3.0). Closes a licensing-compliance gap the v0.29.6 audit missed: MIT's permission notice and Apache-2.0 § 4(c) attribution clause persist across relicensing, and the prior releases did not reproduce the upstream copyright notices. MIT and Apache-2.0 are both GPL-3.0-compatible per the FSF, so no license conflict exists — just an attribution gap. New `THIRD_PARTY_LICENSES.md` at repo root contains verbatim upstream MIT license text for each crate plus crate version, upstream repo, SPDX identifier, role in the build, and `shlex` contributor list. None of the three crates ships an upstream `NOTICE` file at the listed versions, so Apache-2.0 § 4(d) requires no additional propagation text. Runtime invariant reaffirmed: the kernel binary contains zero third-party crates — all runtime crates (`sele4n-types`, `sele4n-abi`, `sele4n-sys`, `sele4n-hal`) are `#![no_std]` and authored entirely in-repo, using `core::ptr::{read,write}_volatile` directly rather than any MMIO crate (deprecated `mmio` or newer `safe-mmio`). References added to README.md, CLAUDE.md (maintenance rules for future runtime-dep additions), and docs/spec/SELE4N_SPEC.md §12 "Licensing and Third-Party Attribution". `scripts/website_link_manifest.txt` protects the new file. Gate: `cargo test --workspace` (408 tests) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `check_version_sync.sh` + `check_website_links.sh` + `test_smoke.sh` + zero sorry/axiom.
- **WS-AK Phase AK5 audit remediation COMPLETE** (v0.29.6): End-to-end post-implementation audit of Phase AK5 (v0.29.5). Two material correctness findings and two documentation fixes, plus supplementary test/theorem strengthening. (1) **AK5-C RES1 bit 20 missing**: `compute_sctlr_el1_bitmap` was setting RES1 bits 4/7/8/11/22/23/28/29 but omitted bit 20 — architecturally RES1 on ARMv8.0-A SCTLR_EL1 (Linux's `SCTLR_EL1_RES1` macro = bits 11|20|22|28|29) and IESB on ARMv8.2-A+ Cortex-A76. Setting a RES1 bit to 0 is reserved behavior per ARM ARM D17.2.120. Added `RES1_BIT20` constant + regression tests `sctlr_bitmap_linux_res1_subset_matches` and `sctlr_bitmap_excludes_optional_bits`. (2) **AK5-E compile-time alignment asserts**: three `const _: () = assert!(...)` checks lock `PageTableCell`/`BootL1Table` at 4 KiB alignment and `512 × 8 = 4096` bytes so any future refactor that drops `#[repr(align(4096))]` fails the build. (3) **AK5-B doc correction**: `EoiGuard` comment incorrectly claimed Drop runs on the panic path under `panic = "abort"` — per Rust reference, Drop is NOT run on abort. Correct semantics: Drop fires on normal/early-return; panic under abort halts kernel (correct response to invariant violation). Misleading `eoi_guard_is_zero_cost_for_abort_path` test replaced with `eoi_guard_panic_propagates_while_drop_records` (`#[should_panic]`) + `eoi_guard_unwind_counter_visible_after_panic` pair that proves Drop fires on test-profile unwind. (4) **AK5-M doc correction**: the FFI panic-discipline note said the compile-time guard was bypassed via `cfg(test)`; actual guard is `cfg(all(not(panic = "abort"), not(debug_assertions)))` — docs now match code. (5) **AK5-K SError dead-code annotation**: both `__el0_serror_entry` and `__el1_serror_entry` have `restore_context` after `bl handle_serror`; with `-> !` signature this is formally dead code, preserved as defensive ERET fall-through. (6) **Lean model strengthening**: `ExceptionModel.lean::trapFrameLayout` gains `trapFrameLayout_exact_fit` (exact 8-byte-per-field no-padding proof) and `trapFrameLayout_size_16_aligned` (16-byte alignment matching Rust `#[repr(C, align(16))]`). Dependency audit: `cc v1.2.59` + transitive `find-msvc-tools v0.1.9` + `shlex v1.3.0` (all MIT+Apache, not deprecated, RUSTSEC-2024-0006 fixed). No use of deprecated `mmio` crate; direct `core::ptr::{read,write}_volatile` in `mmio.rs` preferred over `safe-mmio` for minimal attack surface. Gate: `cargo test --workspace` (405 → 408 tests) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `lake build` + `test_smoke.sh` + `test_full.sh` + `check_version_sync.sh` + zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.29.0_WORKSTREAM_PLAN.md` §8 (Phase AK5).
- **WS-AK Phase AK5 COMPLETE** (v0.29.5): Pre-1.0 Release Hardening (v0.29.0 audit) — Phase AK5: Rust HAL Boot Hardening. 14 sub-tasks (AK5-A..AK5-N) addressing 5 HIGH, 12 MEDIUM, 16 LOW Rust-HAL findings. **AK5-A (R-HAL-M01/M11/MEDIUM — PREREQ)**: `rust/Cargo.toml` gains `[profile.dev] panic = "abort"` and `[profile.release] panic = "abort"`. `cargo test` inherits the stable test harness's forced unwind, so `#[should_panic]` tests (mmio alignment + uart baud=0) keep working. **AK5-B (R-HAL-H05/HIGH)**: `gic::dispatch_irq` now uses a `Drop`-based `EoiGuard` so EOI fires on every scope exit — normal return AND abort — closing the handler-panic → GIC lockup hole. Symmetric for Handled/OutOfRange arms; Spurious unchanged per GIC-400 spec. **AK5-C (R-HAL-H03/HIGH)**: new `compute_sctlr_el1_bitmap() -> u64` writes the full SCTLR_EL1 bitmap (M|C|I|SA|SA0|WXN|EOS|EIS plus ARM ARM D17.2.120 RES1 bits 7/8/23/28/29) so WXN=1 (HW W^X, 4th layer alongside AK3-B), SP-alignment, and exception-entry/exit serialization are enabled atomically with the MMU. Replaces the read-modify-write pattern that inherited reserved bits. **AK5-D (R-HAL-H02/HIGH)**: `enable_mmu` follows the ARM ARM D8.11 ordering — `tlbi vmalle1` (stale-entry flush) → `dc cvac` over `BOOT_L1_TABLE` (Point-of-Coherency clean) → TCR/MAIR/TTBR program → DSB ISH+ISB → SCTLR write → ISB. New helper `cache::clean_pagetable_range` (unsafe, DSB-terminated). **AK5-E (R-HAL-H01/M03/HIGH+MEDIUM)**: `static mut BOOT_L1_TABLE` replaced by `static BOOT_L1_TABLE: PageTableCell` — `PageTableCell` wraps `UnsafeCell<BootL1Table>` with single-threaded-boot + IRQ-disabled SAFETY contract. TTBR write masks PA through `TTBR_BAADDR_MASK = 0x0000_FFFF_FFFF_F000` (clears CnP + reserved low bits). Debug asserts catch 4 KiB mis-alignment and 44-bit PA overflow. `.bss.page_tables` linker section removed (the `PageTableCell` lives in `.bss`). **AK5-F (R-HAL-H04/HIGH)**: `TrapFrame` grows from 272 → 288 bytes (16-byte aligned). New `esr_el1` (offset 272) and `far_el1` (offset 280) fields capture a stable snapshot at exception entry; `handle_synchronous_exception` reads from the frame, not live MRS, so nested exceptions (SError during abort handling, etc.) cannot corrupt the outer syndrome view. `trap.S::save_context` extended to snapshot both; `restore_context` skips them (read-only). Compile-time `offset_of!` asserts lock the layout. Lean-side `trapFrameLayout : TrapFrameLayout` metadata in `ExceptionModel.lean` mirrors the contract. **AK5-G (R-HAL-M04/MEDIUM)**: `gic.rs` drops local `mmio_read32`/`mmio_write32` and routes through `crate::mmio::*` so AJ5-A alignment asserts cover GIC. **AK5-H (R-HAL-M05/MEDIUM)**: `uart::Uart::{read,write}_reg` routes through `crate::mmio::mmio_{read,write}32` — same alignment coverage for PL011. **AK5-I (R-HAL-M02/M09/MEDIUM)**: `boot.S` core-0 gate masks MPIDR with `0x00FFFFFF` (Aff0|Aff1|Aff2), strictly superseding the prior Aff0-only check which aliased secondary-cluster core-0 to the boot core. `cpu::MPIDR_CORE_ID_MASK` and `current_core_id` mirror the mask. **AK5-J (R-HAL-M07/MEDIUM)**: `TimerError::CntfrqNotProgrammed` variant; `init_timer` on aarch64 rejects CNTFRQ_EL0=0 instead of silently falling back to the 54 MHz compile-time constant. Non-aarch64 test hosts still fall back (no real counter) so unit tests exercise the post-validation logic. **AK5-K (R-HAL-M06/M08/M10/M12/MEDIUM)**: Spectre-v1 CSDB doc note at `gic::dispatch_irq` future table-lookup point; `cache::cache_range` empty-range path now emits DSB ISH (zero-length fences behave consistently); `uart::init_with_baud` `assert!(baud > 0)`; `handle_serror` signature changed to `-> !`. **AK5-L**: `boot::rust_boot_main` timer-init error branch uses `Display` formatting (was `{:?}`) so the new `CntfrqNotProgrammed` variant prints its actionable message. **AK5-M (R-HAL-M11/MEDIUM)**: `ffi.rs` module doc block documents the panic=abort discipline; `#[cfg(all(not(panic = "abort"), not(debug_assertions)))] compile_error!` guards the release profile from accidental unwind re-enable. **AK5-N (R-HAL-L1..L16/LOW)**: LOW-tier batch documentation — `lib.rs` top-of-file block cross-references every L-n finding to its resolution site; `boot.S` documents wake-storm risk; `vectors.S` annotates SP0 vectors as unreachable. Gate: `cargo test --workspace` (405 tests) + `cargo clippy --workspace -- -D warnings` (0 warnings) + `lake build` (104+ jobs including new `ExceptionModel.lean` trapFrameLayout) + `test_smoke.sh` + `test_full.sh` + zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.29.0_WORKSTREAM_PLAN.md`
- **WS-AK Phase AK4 COMPLETE** (v0.29.4): Pre-1.0 Release Hardening (v0.29.0 audit) — Phase AK4: ABI Bridge — Decode, Types, Validation. 8 sub-tasks (AK4-A..AK4-H). **AK4-A (R-ABI-C01/CRITICAL)**: Closes the two non-functional 5-arg syscalls (`serviceRegister`, `schedContextConfigure`) — new `SeLe4n/Kernel/Architecture/IpcBufferRead.lean` provides pure, read-only `ipcBufferReadMr : SystemState → ThreadId → Nat → Except IpcBufferReadError UInt64`; `decodeSyscallArgsFromState` (new, state-aware wrapper around legacy `decodeSyscallArgs`) merges IPC-buffer overflow slots into `msgRegs` when `msgInfo.length > 4`; `SyscallDecodeResult` extended with `inlineCount` + `overflowCount` fields maintaining invariant `msgRegs.size = inlineCount + overflowCount`; `syscallEntry`/`syscallEntryChecked` switched to the state-aware decoder; all internal `IpcBufferReadError` variants collapse to `KernelError.invalidMessageInfo` at the ABI boundary matching seL4 single-error-kind convention. NI preserved: reads are always parameterised by the caller's `ThreadId`. 7 regression tests in `tests/DecodingSuite.lean` cover short/long paths, unmapped-VA rejection, size invariant, and legacy backward compatibility. Documented in `docs/spec/SELE4N_SPEC.md` §8.10.5. **AK4-B (R-ABI-H02/HIGH)**: `decodeServiceRegisterArgs` validation tightened — new constants `serviceMaxMethodCount = 1024` and `serviceMaxMessageSize = 960` match Rust `MAX_METHOD_COUNT`/`MAX_SERVICE_MESSAGE_SIZE`; strict `requiresGrant ∈ {0, 1}` parsing; error theorems `decodeServiceRegisterArgs_error_of_{methodCount_too_large,invalid_grant,insufficient_regs}` replace legacy `_error_iff`; round-trip theorem updated with bounds preconditions. **AK4-C (R-ABI-H01/HIGH)**: `SchedContextId` newtype in `rust/sele4n-types/src/identifiers.rs` — `#[repr(transparent)] pub struct SchedContextId(pub(crate) u64)` with `SENTINEL` constant, `new()` rejecting sentinel, `to_obj_id()` projection; `SchedContextBindArgs.thread_id` and `sele4n_sys::sched_context_bind` refactored from raw `u64` to typed `ThreadId`; 5 unit tests; `sele4n-types` crate docstring updated to "15 newtype identifiers" (closes R-ABI-L2). **AK4-D (R-ABI-M01/MEDIUM)**: verified aligned via AK3-J — `MAX_PRIORITY = 255`, `MAX_DOMAIN = 15` match Lean `numDomainsVal = 16`. **AK4-E (R-ABI-M02/MEDIUM)**: `decodeCSpaceMintArgs` now rejects `rights > 0x1F` with `.invalidArgument`, matching Rust-side `CSpaceMintArgs::decode`; legacy `decodeCSpaceMintArgs_error_iff` theorem preserved with extended disjunction; VSpace `PagePerms` validation already fail-closed via `PagePermissions.ofNat?`. **AK4-F (R-ABI-M04/MEDIUM)**: `IpcBuffer::set_mr(0..3)` returns `Err(InvalidArgument)` for symmetry with `get_mr`; new `set_mr_overflow` preserves the legacy `Ok(false)` no-op semantics for callers that want overflow-only writes (sele4n-sys wrappers); 3 new Rust unit tests + conformance test xval_017 update. **AK4-G (NEW)**: `tests/AbiRoundtripSuite.lean` end-to-end suite — 8 scenarios × 25 assertions simulating the Rust encoder (including IPC-buffer overflow byte layout) and verifying Lean decode success across service_register, sched_context_configure, cspace_mint, cspace_copy; wired into `test_tier2_negative.sh` and exposed via `scripts/test_abi_roundtrip.sh`; closes audit §9.6 testing gap. **AK4-H (R-ABI-L1..L8)**: LOW-tier batch documentation centralized in `sele4n-types/src/lib.rs` — L1 lifecycle type-tag 7-variant enumeration added to `LifecycleRetypeArgs` doc comment; L2 docstring count (AK4-C); L3 `RegValue` export intent (WS-V FFI); L4 `ServiceQueryArgs` marker rationale; L5 `lateout("x6")` AAPCS64 redundancy note in `trap.rs`; L6 constants-duplication deferred to WS-V; L7 `CPtr::NULL`/`SENTINEL` coexistence retained; L8 `Hash` derive safety via `#[repr(transparent)]`. Gate: `lake build` (260 jobs) + `test_smoke.sh` + `test_full.sh` + `cargo test --workspace` (376 tests) + zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.29.0_WORKSTREAM_PLAN.md`
- **WS-AK Phase AK3 COMPLETE** (v0.29.3): Pre-1.0 Release Hardening (v0.29.0 audit) — Phase AK3: Architecture — ASID, W^X, EOI, Decode. 13 sub-tasks (AK3-A..AK3-M) addressing 1 CRITICAL, 3 HIGH, 10 MEDIUM, 9 LOW architecture findings. **AK3-A (A-C01/CRITICAL)**: `AsidPool` gains `activeAsids : List ASID` ground-truth set; rollover performs `List.range (maxAsidValue - 1)` scan filtered by `activeAsids` absence and **fails closed (returns `none`)** when every non-kernel ASID is still active — replaces buggy unconditional `ASID.mk 1` return that broke TLB isolation. `wellFormed` strengthened to 7 conjuncts (`activeCount = activeAsids.length`, `Nodup`, `val < nextAsid` for both active and freed, freeList/activeAsids disjointness, `freeList.Nodup`). Key correctness theorem `AsidPool.allocate_result_fresh` proves allocation never returns a currently-active ASID. `free` switched from `filter (· != asid)` to `List.erase` for cleaner length/Nodup lemmas. **AK3-B (A-H01/HIGH)**: `fromPagePermissions` return type `Option PageAttributes` — fails closed on W+X; `ARMv8VSpace.mapPage` (L1) and `VSpaceRoot.mapPage` (L2) add wxCompliant gates; composition theorem `wxInvariant_fourLayer_defense` documents four independent gates (wrapper, backend, VSpaceRoot, encode). AK5-C adds L4 (SCTLR.WXN) at hardware-level. **AK3-C (A-H02/HIGH)**: `AckError` inductive (`spurious`, `outOfRange rawIntId`) replaces `Option InterruptId`; `interruptDispatchSequence` emits EOI except on `spurious` per GIC-400 spec; Rust HAL mirrored with `AckResult::{Handled, OutOfRange, Spurious}` in `gic.rs` — prevents GIC lockup on errata/SMP races. **AK3-D (A-H03/HIGH)**: free-list-reuse branch bumps `generation + 1` alongside `requiresFlush := true`. **AK3-E (A-M01/MEDIUM)**: `decodeVSpaceMapArgsChecked` wrapper guards `paddr.toNat < 2^physicalAddressWidth` at decode time; API dispatch wired through. **AK3-F (A-M02/MEDIUM)**: `validateIpcBufferAddress` step 7 strengthened to `paddr.toNat + ipcBufferAlignment ≤ 2^width`; `frozenSetIPCBuffer` mirrors. **AK3-G (A-M04/MEDIUM, PARTIAL+DOC)**: `CacheBarrierKind` inductive + `cacheCoherentForExecutable` predicate; new theorem `pageTableUpdate_icache_coherent_under_sequence`. **AK3-H (A-M05/MEDIUM)**: `HardwareTimerConfig.countsPerTickPositive` predicate + `countsPerTickPositiveCheck` decidable check; `rpi5TimerConfig_countsPerTickPositive` theorem (54000 > 0). **AK3-I (A-M06/MEDIUM, DEFER+DOC)**: `tlbBarrierComplete` annotated with TPI-DOC-AK3I; deferred to WS-V. **AK3-J (A-M07/MEDIUM)**: `decodeSchedContextConfigureArgsChecked` validates `priority ≤ 255`, `domain < 16`, `budget > 0`, `period > 0`; `decodeSchedContextConfigureArgsChecked_invariants` theorem; API dispatch wired through. **AK3-K (A-M08, A-M09/MEDIUM, DEFER+DOC)**: MMU/Device-memory ordering doc block at top of `VSpaceARMv8.lean` detailing `dsb ishst`, `dc cvac + dsb ish`, `isb` obligations for real hardware. **AK3-L (A-M10/MEDIUM)**: `MachineState.eoiPending : List Nat` audit-trail shadow field; `endOfInterrupt` filters from list; `eoiPendingEmpty` kernel-exit predicate. **AK3-M (A-L1..A-L9/LOW)**: LOW-tier batch documentation at end of `InterruptDispatch.lean`. Gate: `lake build` (256 jobs) + `test_smoke.sh` + `test_full.sh` + `cargo test --workspace` (368 tests) + zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.29.0_WORKSTREAM_PLAN.md`
- **WS-AK Phase AK2 COMPLETE** (v0.29.2): Pre-1.0 Release Hardening (v0.29.0 audit) — Phase AK2: Scheduler, PIP & WCRT Closure. 9 sub-tasks (AK2-A/B/D/E/F/G/I/J/L) addressing 2 HIGH, 6 MEDIUM, 6 LOW scheduler/SchedContext findings. **AK2-A + AK2-B (S-H03/S-H04/HIGH) via Option B — three-part fix**: (1) `schedContextBind` propagates `sc.priority → tcb.priority` on bind and remove+re-inserts the thread in the RunQueue at `resolveInsertPriority` (existing Z5-G3). (2) `schedContextConfigure` on an already-bound SC propagates the new priority into the bound TCB AND remove+re-inserts the thread in the RunQueue at `max(new priority, pipBoost)` if present — a post-audit correction: an initial draft updated only `tcb.priority`, leaving the RunQueue in the old bucket (latent priority-inversion vector caught by code review and regression test AK2-B-03). (3) The four production re-enqueue sites (`handleYield`, `timerTick`, `timerTickBudget` unbound, `switchDomain`) continue to use `effectiveRunQueuePriority tcb`; under the propagation invariant this equals `(resolveEffectivePrioDeadline st tcb).1` read by selection. `schedulerPriorityMatch` (TCB-based) and `effectiveParamsMatchRunQueue` (SC-based) therefore agree automatically without invariant fusion. `effectiveBucketPriority` + frame lemmas retained in `Scheduler/Invariant.lean` as utilities for full Option A fusion deferred to AK2.5. Regression tests AK2-B-01/02/03 in `tests/PriorityManagementSuite.lean` cover bind propagation, configure propagation, and RunQueue re-bucketing respectively. AK2-D (S-M02/MEDIUM): `SchedulerState.lastTimeoutErrors : List (ThreadId × KernelError)` diagnostic field surfaces `timeoutBlockedThreads` errors; cleared at each `timerTickWithBudget` entry. AK2-E (S-M03/MEDIUM): CBS admission `Bandwidth.utilization` switches from truncation-down to ceiling-round `(budget * 1000 + period - 1) / period`, eliminating the ~6.4% aggregate over-admission window on 64-context deployments. AK2-F (S-M04/MEDIUM): `ReplenishQueue.insertSorted` strict `<` comparator gives FIFO processing for tied eligibility times. AK2-G (S-M05/MEDIUM): `schedContextConfigure` purges stale `replenishQueue` entries before storing the reconfigured object. AK2-I (S-M07/MEDIUM): `switchDomain` unreachable fallback emits `.schedulerInvariantViolation` instead of silently returning unchanged state; 10 preservation theorems updated to discharge the contradictory post-split arm; `saveOutgoingContext_tcb_fields` extended with `schedContextBinding` field equality. AK2-J (S-M08/MEDIUM): `migrateRunQueueBucket` fallback preserves RunQueue priority via `max(newPriority, runQueue.threadPriority[tid]?)`. AK2-L (S-L13..S-L18/LOW): scheduler LOW-tier batch documentation inline in `Scheduler/Operations/Core.lean`. **Deferred to AK2.5** (escape valve §20.3): AK2-C (S-M01) blockingChain Except signature change cascades to Propagate/BoundedInversion/Boot/CrossSubsystem; AK2-H (S-M06) routing through `*Checked` save/restore — formal unreachability already provided by AI3-C; AK2-K (S-H01/S-H02) WCRT proof-schema closure K.1-K.3 (K.4 documentation captured in `docs/spec/SELE4N_SPEC.md` §5.7). Gate: `lake build` (256 jobs) + `test_smoke.sh` + `test_full.sh` + zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.29.0_WORKSTREAM_PLAN.md`
- **WS-AK Phase AK1 COMPLETE** (v0.29.1): Pre-1.0 Release Hardening (v0.29.0 audit) — Phase AK1: IPC Fail-Closed & Rendezvous State. 10 sub-tasks (AK1-A through AK1-J) addressing 2 HIGH, 7 MEDIUM, 6 LOW findings. AK1-A (I-H01/HIGH): `cleanupPreReceiveDonationChecked` error-propagating variant added in `IPC/Operations/Endpoint.lean`; `endpointReceiveDual` no-sender path uses checked variant; bridge lemma `cleanupPreReceiveDonationChecked_ok_eq_cleanup` + 13 preservation theorems updated via outer-case-split pattern. **Post-audit strengthening**: `returnDonatedSchedContext_ok_under_invariants` fully proves the donated-path success under `donationOwnerValid` + non-reservation (three sequential `storeObject` + two `lookupTcb` machine-verified via type-disjointness helper `schedContext_ne_tcb_at_objId` and `donationOwnerValid_excludes_self_donation`); top-level `cleanupPreReceiveDonationChecked_never_errors_under_ipcInvariantFull` now DERIVES both donated and non-donated branches directly from `ipcInvariantFull` (via `donationOwnerValid` extraction at `.2.2.2.2.2.2.2.2.2.2.2.1`) rather than requiring a caller-supplied witness. AK1-B (I-H02/HIGH): `endpointReply`/`endpointReplyRecv` fail closed with `.replyCapInvalid` on `replyTarget = none`; soundness bridge `blockedOnReplyHasTarget_implies_some_replyTarget` added. AK1-C (I-M01/MEDIUM): caller-side pendingMessage atomically cleared via `storeTcbIpcStateAndMessage caller (.blockedOnReply _ _) none` on rendezvous (cascaded through 5 preservation theorems in CallReplyRecv.lean). AK1-D (I-M02/MEDIUM): receiver-side atomic `(.ready, senderMsg)` update via `storeTcbIpcStateAndMessage receiver .ready senderMsg` in `endpointReceiveDual` rendezvous — preserves `waitingThreadsPendingMessageNone` structurally; cascaded through 12 preservation theorems (4 in EndpointPreservation, 7 in Structural, 1 NI); new helpers `storeTcbIpcStateAndMessage_tcb_forward` + `storeTcbIpcStateAndMessage_ready_preserves_ipcSchedulerContractPredicates`; `hReceiverNotBlocked` hypothesis dropped. AK1-E (I-M03/MEDIUM): `ensureRunnable` uses `ipcEffectiveRunQueuePriority` — fixes PIP-boosted server placement on wake paths; new correctness lemmas `ensureRunnable_inserts_at_effective_priority`, `ensureRunnable_honors_pipBoost`, `notificationSignal_respects_pipBoost`. AK1-F (I-M04/MEDIUM): `timeoutThread` PIP-revert call-state gap formalised via `blockingServer_isSome_iff_blockedOnReply_some` + `blockingServer_some_implies_blockedOnReply` theorems in `BlockingGraph.lean`; post-audit `pipBoost_attached_only_on_reply_blocked` abbrev alias exposing the plan-named symbol. AK1-G (I-M05/MEDIUM): `ipcUnwrapCapsLoop` annotated as internal; post-audit `example`-level static assertion in `CapTransfer.lean` verifying the production call shape (`idx := 0`, `accResults := #[]`) — compiles only if the internal invariant holds. AK1-H (I-M06/MEDIUM): composition lemma `endpointQueueRemove_succeeds_under_forwardBackward` composed at call site via `timeoutThread_succeeds_under_preconditions`. AK1-I (I-M07/MEDIUM, NI L-1): send-path missing CSpace root now fails closed with `.invalidCapability`; NI regression test in `tests/InformationFlowSuite.lean` strengthened with a second scenario that explicitly triggers the `lookupCspaceRoot = none` branch via TCB splice-out, asserting the symmetric `.error .invalidCapability` arm shape between `endpointSendDualWithCaps` and `endpointReceiveDualWithCaps`. AK1-J (I-L1..I-L6, IPC INFO): LOW-tier batch documentation at top of `IPC/Operations/Endpoint.lean`. Test: CIC-010 scenario updated to explicit `replyTarget := some replierTid`. Gate: `lake build` (256 jobs) + `test_smoke.sh` + `test_full.sh` + zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.29.0_WORKSTREAM_PLAN.md`
- **WS-AJ Phase AJ6 COMPLETE** (v0.29.0): Post-Audit Comprehensive Remediation (v0.28.0 audit) — Phase AJ6: Documentation, Testing & Closure. 6 sub-tasks (AJ6-A through AJ6-F). AJ6-A: H-01/H-02/H-03 activation roadmaps in SELE4N_SPEC.md §8.15 (7 orphaned architecture modules, budget-aware scheduler, FFI bridge — all deferred to WS-V). AJ6-B: By-design finding documentation verified/annotated (10 findings: M-03/M-05/M-08/M-13/M-15/L-03/L-05/L-11/L-13/L-19). AJ6-C: Audit errata appended (E-1 counts 55→52/24M→21M, E-2 L-01 FALSE, E-3 L-17 FALSE). AJ6-D: Deferred finding annotations (L-07 tlbBarrierComplete H-01 scope, L-08 collectQueueMembers fuel, L-10 descendantsOf materialization, L-16 extractBits offset). AJ6-E: Fixture verified (225 lines match), documentation synchronized across 15+ files. AJ6-F: Version bump 0.28.4→0.29.0 (15 version-bearing files). Gate: `test_full.sh` + `cargo test --workspace` + `check_version_sync.sh` + zero sorry/axiom. **WS-AJ PORTFOLIO COMPLETE** (v0.28.1–v0.29.0, 6 phases, 30 sub-tasks). See `docs/dev_history/audits/AUDIT_v0.28.0_WORKSTREAM_PLAN.md`
- **WS-AJ Phase AJ5 COMPLETE** (v0.28.4): Post-Audit Comprehensive Remediation (v0.28.0 audit) — Phase AJ5: Rust HAL Hardening. 4 sub-tasks (AJ5-A through AJ5-D). AJ5-A (M-20/MEDIUM): All four MMIO functions (`mmio_read32`, `mmio_write32`, `mmio_read64`, `mmio_write64`) promoted from `debug_assert!` to runtime `assert!` for alignment checks — unaligned Device memory accesses on ARM64 cause synchronous Data Abort; runtime cost negligible versus volatile MMIO access. AJ5-B (M-21/MEDIUM): Migrated `static mut BOOT_UART_INNER` to safe `UnsafeCell<Uart>` wrapped in `UartInner` newtype with manual `Sync` impl — eliminates `static mut` (deprecated in future Rust editions, technically unsound under aliasing rules); `UART_LOCK` spinlock unchanged. AJ5-C (L-14/LOW): `init_timer` return type changed from `()` to `Result<(), TimerError>` — `TimerError::ZeroTickHz` replaces `assert!` panic; boot sequence handles error with log + halt; `Display` impl for error messages. AJ5-D (L-15/LOW): `increment_tick_count` visibility restricted from `pub` to `pub(crate)` — only called from `ffi.rs` (`ffi_timer_reprogram`); prevents external double-counting that would corrupt Lean model's `MachineState.timer` shadow. Gate: `cargo test --workspace` (367 tests) + `cargo clippy --workspace` (0 warnings). Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.28.0_WORKSTREAM_PLAN.md`
- **WS-AJ Phase AJ4 COMPLETE** (v0.28.4): Post-Audit Comprehensive Remediation (v0.28.0 audit) — Phase AJ4: Architecture Model Correctness. 4 sub-tasks (AJ4-A through AJ4-D). AJ4-A (M-07/MEDIUM): Tautological `pageTableWalk_deterministic` replaced with `pageTableWalk_fault_on_non_table_l0` (L0 fault condition) and `pageTableWalkPerms_wx_bridge` (W^X compliance transfer). AJ4-B (M-06/MEDIUM): Full-TLB flush (`adapterFlushTlb`) replaced with targeted per-(ASID,VAddr) flush (`adapterFlushTlbByVAddr`) in `vspaceMapPageWithFlush`/`vspaceUnmapPageWithFlush`; frame lemmas `vspaceMapPage_entry_consistent_frame` and `vspaceUnmapPage_entry_consistent_frame` prove entries not matching modified (ASID,VAddr) remain TLB-consistent; helper `resolveAsidRoot_some_facts` + TLB-eq lemmas. AJ4-C (L-06/LOW): PA bounds check (step 7) added to `validateIpcBufferAddress` checking `paddr.toNat < 2^physicalAddressWidth`; `frozenSetIPCBuffer` updated with matching check; `validateIpcBufferAddress_implies_mapped_writable` postcondition extended. AJ4-D (L-09/LOW): TCB placeholder IDs `ofNat 0` replaced with `ThreadId.sentinel`/`ObjId.sentinel`/`SchedContextId.sentinel` in `objectOfTypeTag`/`objectOfKernelType`; `TCB.isUnconfigured` predicate added. Gate: `lake build` (256 jobs) + `test_smoke.sh` + `test_full.sh`. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.28.0_WORKSTREAM_PLAN.md`
- **WS-AJ Phase AJ3 COMPLETE** (v0.28.3): Post-Audit Comprehensive Remediation (v0.28.0 audit) — Phase AJ3: Platform & Boot Pipeline. 6 sub-tasks (AJ3-A through AJ3-F). AJ3-A (M-17/MEDIUM): `DeviceTree.fromDtbFull` return type changed from `Option DeviceTree` to `Except DeviceTreeParseError DeviceTree` — fuel exhaustion/malformed blob errors propagated; correctness theorem updated to `parseFdtHeader_fromDtbFull_ok`. AJ3-B (M-18/MEDIUM): `physicalAddressWidth` in `fromDtbWithRegions`/`fromDtbFull` now required (no default) — prevents 48-bit misconfiguration on RPi5 BCM2712 (44-bit PA). AJ3-C (M-16/MEDIUM): `bootFromPlatformChecked` validates `bootSafeObjectCheck` for all initial objects — Bool-valued structural boot safety (empty queues, idle notifications, CNode bounds, clean TCB, VSpaceRoot exclusion, SchedContext WF); `bootSafeObjectCheck_sound_structural` soundness theorem. AJ3-D (M-19/MEDIUM): `BootBoundaryContract.objectStoreNonEmpty`/`irqRangeValid` no longer default to `True` — required fields; Sim/RPi5 contracts updated with substantive predicates + proofs. AJ3-E (L-04/LOW): `MachineState.interruptsEnabled` default `true` → `false` matching ARM64 reset. AJ3-F (L-12/LOW): Removed dead `fromDtb` stub + `fromDtbParsed` alias. Gate: `lake build` (256 jobs) + `test_smoke.sh` + `test_full.sh`. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.28.0_WORKSTREAM_PLAN.md`
- **WS-AJ Phase AJ2 COMPLETE** (v0.28.2): Post-Audit Comprehensive Remediation (v0.28.0 audit) — Phase AJ2: Security & Information Flow Hardening. 4 sub-tasks (AJ2-A through AJ2-D). AJ2-A (M-10/MEDIUM): `AccessRightSet.mk` constructor made `private` — external code cannot bypass 5-bit valid predicate; manual `Inhabited` instance; 1 test fix (`NegativeStateSuite.lean:2038`). AJ2-B (M-11/MEDIUM): `projectKernelObject` strips `pipBoost := none` from projected TCBs — prevents cross-domain PIP boost timing leakage; all NI proofs pass unchanged. AJ2-C (M-12/MEDIUM): `isInsecureDefaultContext` strengthened from single-ID probe to multi-probe `[0, 1, 42]` with `insecureProbe` helper + `isInsecureDefaultContext_false_implies_nontrivial` characterization theorem. AJ2-D (M-09/MEDIUM): `typedIdDisjointness` predicate + `typedIdDisjointness_trivial` proof in `CrossSubsystem.lean`; `retypeFromUntyped_childId_fresh` allocation freshness theorem in `Lifecycle/Operations.lean`; documentation annotations at `ThreadId.toObjId` and `SchedContextId.toObjId`. Gate: `lake build` (256 jobs) + `test_smoke.sh` + `test_full.sh`. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.28.0_WORKSTREAM_PLAN.md`
- **WS-AJ Phase AJ1 COMPLETE** (v0.28.1): Post-Audit Comprehensive Remediation (v0.28.0 audit) — Phase AJ1: IPC & Lifecycle Correctness. 6 sub-tasks (AJ1-A through AJ1-F). AJ1-A (M-14/MEDIUM): `cleanupDonatedSchedContext` return type changed from `SystemState` to `Except KernelError SystemState` — errors propagated instead of silently swallowed; cascade to `lifecyclePreRetypeCleanup`, `cancelDonation`, 4 callers, 6 preservation theorems conditional. AJ1-B (M-04/MEDIUM): `blockedOnReplyHasTarget` predicate integrated as 16th conjunct of `ipcInvariantFull` — cascaded to 13 preservation theorems, default proof, compositional theorem, `coreIpcInvariantBundle` accessors, boot proof; `none` authorization branch unreachable under IPC invariant. AJ1-C (M-02/MEDIUM): Cross-reference annotations for `endpointQueuePopHead_returns_head`. AJ1-D (M-01/MEDIUM): 2 conditional equivalence theorems + 2 decomposition lemmas for `.reply`/`.replyRecv`. AJ1-E (L-02/LOW): Removed dead `endpointQueuePopHeadFresh`. AJ1-F (L-18/LOW): Documented intentional error asymmetry in `WithCaps.lean`. Gate: `lake build` (256 jobs) + `test_smoke.sh` + `test_full.sh`. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.28.0_WORKSTREAM_PLAN.md`
- **WS-AI Phase AI7 COMPLETE** (v0.28.0): Post-Audit Comprehensive Remediation — Phase AI7: Testing, Closure & Final Gate. 6 sub-tasks (AI7-A through AI7-F). AI7-A (L-17/LOW): Documented CBS admission truncation tolerance in `Budget.lean` and `Types.lean` — per-context error ≤ 1 per-mille, RPi5 impact ≤ 0.1 ms/context, aggregate ≤ 6.4% for ≤64 contexts; truncation-down matches seL4 MCS. AI7-B (L-26/LOW): Documented `lifecycleRetypeObject` public visibility rationale — 13+ proof chain files prevent `protected`; explicit L-26 annotation. AI7-C: Fixture verification — 225 trace lines match `main_trace_smoke.expected`. AI7-D: Version bump 0.27.11 → 0.28.0 (16 files, `check_version_sync.sh` passes). AI7-E: WORKSTREAM_HISTORY.md + CHANGELOG.md updated. AI7-F: CLAUDE.md active workstream context updated. Gate: `test_full.sh` + `cargo test --workspace` + `check_version_sync.sh` + zero sorry/axiom. **WS-AI PORTFOLIO COMPLETE** (v0.27.7–v0.28.0, 7 phases, 37 sub-tasks). See `docs/dev_history/audits/AUDIT_v0.27.6_WORKSTREAM_PLAN.md`
- **WS-AI Phase AI6 COMPLETE** (v0.27.12): Post-Audit Comprehensive Remediation — Phase AI6: Documentation & Proof Gaps. 7 sub-tasks (AI6-A through AI6-G). AI6-A: Scheduler documentation batch — M-02 `resolveExtraCaps` silent-drop spec cross-reference (API.lean), M-03 `RunQueue.size` proof-linking deferral note, M-23 `blockingChain` fuel-exhaustion cycle behavior (BlockingGraph.lean), M-24 `eventuallyExits` deployment scope with WCRT.lean cross-reference (BandExhaustion.lean), M-25 WCRT `hDomainActiveRunnable`/`hBandProgress` deployment obligation documentation (WCRT.lean). AI6-B: Platform & boot documentation batch — M-07 boot invariant bridge empty-config scope (Boot.lean), M-08 `fromDtb` H3 DTB parsing placeholder status (DeviceTree.lean), M-10 MMIO read RAM semantics sequential model limitation (RPi5/MmioAdapter.lean), M-11 VSpaceRoot boot exclusion traceability tag (Boot.lean). AI6-C: Architecture documentation batch — M-13 `physicalAddressBound` 2^52 proof-layer default clarification (VSpace.lean), M-16 D-cache→I-cache pipeline ordering hardware protocol requirement (CacheModel.lean), M-17 context switch TLB/ASID maintenance gap (Adapter.lean), M-18 cross-module TLB+cache+page table composition gap (Invariant.lean). AI6-D: Model & SchedContext documentation batch — M-21 `descendantsOf` fuel sufficiency TPI-DOC cross-reference with `edgeWellFounded` foundation (Structures.lean), L-02 `allTablesInvExtK` 17-deep tuple projection fragility with AF-26 design rationale (State.lean), L-13 `schedContextBind` thread-state gap seL4 MCS semantics rationale (Operations.lean). AI6-E: Stale reference fixes — L-15 removed nonexistent `maxBlockingDepth`/`blockingDepthBound` references, replaced with `objectIndex.length` bound explanation (BlockingGraph.lean); L-24 replaced stale "H3-prep stub" with substantive 6-condition contract description (RPi5/RuntimeContract.lean). AI6-F: SELE4N_SPEC.md sync — 4 new sections: §8.10.4 IPC Extra Capability Resolution silent-drop semantics (M-02), §8.14.1 WCRT Externalized Hypotheses (M-24/M-25), §8.14.2 Boot Invariant Bridge Scope (M-07), §8.14.3 MMIO Model Limitations (M-10). Gate: `test_full.sh` + doc sync + zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.27.6_WORKSTREAM_PLAN.md`
- **WS-AI Phase AI5 COMPLETE** (v0.27.11): Post-Audit Comprehensive Remediation — Phase AI5: Platform & Simulation Safety. 3 sub-tasks (AI5-A through AI5-C). AI5-A (H-01/HIGH): Replaced vacuously-true simulation boot contract predicates with substantive checks — `objectTypeMetadataConsistent` validates empty default object store, `capabilityRefMetadataConsistent` validates empty default capability reference table; 2 correctness proofs (proven by `rfl`). AI5-B (H-02/HIGH): Replaced accept-all simulation interrupt contract with GIC-400 range-bounded predicates — `irqLineSupported` restricts to INTIDs 0–223 (`irq.toNat < simMaxIrqId`), `irqHandlerMapped` requires handler registration for supported IRQs; `simMaxIrqId := 224`. AI5-C (M-19/MEDIUM): Added `isInsecureDefaultContext` runtime detector (Policy.lean) with sentinel-based O(1) check; `testLabelingContext` helper; guard wired into `syscallEntryChecked` (API.lean) returning `.error .policyDenied`; 2 correctness theorems; test sites updated. Gate: `lake build` + `test_smoke.sh` + `test_full.sh`. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.27.6_WORKSTREAM_PLAN.md`
- **WS-AI Phase AI4 COMPLETE** (v0.27.10): Post-Audit Comprehensive Remediation — Phase AI4: IPC & SchedContext Hardening. 4 sub-tasks (AI4-A through AI4-D). AI4-A (M-01/MEDIUM): Wired `cleanupPreReceiveDonation` into `endpointReceiveDual` no-sender branch (Transport.lean) — prevents SchedContext leaks on abnormal receive-without-reply path; moved function from Donation.lean to Endpoint.lean (import cycle fix); created frame lemma suite in Defs.lean (`cleanupPreReceiveDonation_scheduler_eq`, `cleanupPreReceiveDonation_preserves_objects_invExt`, `returnDonatedSchedContext_notification_backward`, `returnDonatedSchedContext_endpoint_backward`, `cleanupPreReceiveDonation_tcb_forward`, `cleanupPreReceiveDonation_tcb_ipcState_backward`); updated 16+ preservation theorems across EndpointPreservation.lean, Structural.lean, InformationFlow/Invariant/Operations.lean. AI4-B (M-09/MEDIUM): DTB parser fuel exhaustion now returns `Except DeviceTreeParseError` — added `DeviceTreeParseError` type (`.fuelExhausted`/`.malformedBlob`); `parseFdtNodes` return type changed; sole caller `fromDtbFull` updated. AI4-C (L-05/LOW): Removed unused `_endpointId` parameter from `timeoutAwareReceive`; 2 test call sites updated. Gate: `lake build` + `test_smoke.sh`. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.27.6_WORKSTREAM_PLAN.md`
- **WS-AI Phase AI3 COMPLETE** (v0.27.9): Post-Audit Comprehensive Remediation — Phase AI3: Scheduler & PIP Correctness. 4 sub-tasks (AI3-A through AI3-D). AI3-A (M-04/MEDIUM): Fixed `handleYield`, `timerTick`, `switchDomain` re-enqueue at base priority — now uses `effectiveRunQueuePriority` (base + PIP boost), preventing priority inversion on yield; added `effectiveRunQueuePriority` function, updated `schedulerPriorityMatch` invariant, updated `edfCurrentHasEarliestDeadline` with effective priority guard, updated 15+ preservation theorems and NI proofs. AI3-B (M-22/MEDIUM): Fixed `migrateRunQueueBucket` ignoring PIP boost — applies `max(newPriority, pipBoost)` on re-insert. AI3-C (L-09/LOW): Formally proved `saveOutgoingContext` silent-failure path unreachable under `currentThreadValid` — `saveOutgoingContext_always_succeeds_under_currentThreadValid` theorem. AI3-D (L-10/LOW): Added `configTimeSlicePositive` predicate + `default_configTimeSlicePositive` proof. Gate: `lake build` (256 jobs) + `test_smoke.sh`. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.27.6_WORKSTREAM_PLAN.md`
- **WS-AI Phase AI2 COMPLETE** (v0.27.8): Post-Audit Comprehensive Remediation — Phase AI2: Interrupt & Architecture Safety. 5 sub-tasks (AI2-A through AI2-E). AI2-A (H-03/HIGH): Fixed missing EOI on interrupt dispatch error path in `InterruptDispatch.lean` — `interruptDispatchSequence` always sends `endOfInterrupt` regardless of handler outcome, preventing GIC lockup; new `interruptDispatchSequence_always_ok` theorem. AI2-B (M-14/MEDIUM): Surfaced error on VSpaceARMv8 `unmapPage` HW walk failure — added `VSpaceWalkError` type; changed return from `Option` to `Except VSpaceWalkError`; VSpaceBackend instance preserved via `unmapPageOpt` adapter. AI2-C (M-15/MEDIUM): Added TLB flush enforcement for ASID reuse — free-list reuse sets `requiresFlush := true`; new `asidAllocRequiresFlush` predicate + 3 theorems. AI2-D (M-20/MEDIUM): Documented `suspendThread` transient inconsistency window with H3-ATOMICITY annotation. Gate: `lake build` (256 jobs) + `test_smoke.sh` + `test_full.sh`. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.27.6_WORKSTREAM_PLAN.md`
- **WS-AI Phase AI1 COMPLETE** (v0.27.7): Post-Audit Comprehensive Remediation — Phase AI1: Rust ABI & Trap Correctness. 5 sub-tasks (AI1-A through AI1-E). AI1-A (H-05/HIGH): Fixed exception error codes in `trap.rs` — alignment/unknown exceptions return discriminant 45 (`UserException`) instead of 43 (`AlignmentError`), matching Lean `ExceptionModel.lean:175-177`. Added `error_code` module with named constants. AI1-B (H-04/HIGH): SVC handler stub returns `NOT_IMPLEMENTED` (17) instead of success (0) — prevents userspace misinterpretation; TODO marker for WS-V/AG10 FFI wiring. AI1-C (M-26/MEDIUM): Eliminated dual timer reprogram — removed `increment_tick_count()` from `handle_irq()`; canonical tick path is `ffi_timer_reprogram()` in `ffi.rs`. AI1-D (M-27/MEDIUM): Made `BOOT_UART` safe with `AtomicBool`-based `UartLock` spinlock (disables interrupts before acquiring to prevent IRQ deadlock) — replaced `pub static mut` with module-private `BOOT_UART_INNER` + `UART_LOCK`; `kprint!` macro uses `with_boot_uart()`. Gate: `cargo test --workspace` (366 tests) + `cargo clippy --workspace` (0 warnings) + `test_smoke.sh`. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.27.6_WORKSTREAM_PLAN.md`
- **WS-AH Phase AH5 COMPLETE** (v0.27.6): Pre-Release Comprehensive Audit Remediation — Phase AH5: Documentation, Testing & Closure. 6 sub-tasks (AH5-A through AH5-F). AH5-A (M-04/M-07): Design-rationale documentation — VSpaceRoot boot exclusion in `bootSafeObject` (Boot.lean), `pendingMessage` NI visibility in `projectKernelObject` (Projection.lean). AH5-B (M-05/M-06/L-13): Spec-level documentation in `SELE4N_SPEC.md` — Platform Testing Limitations (6.6), CNode Intermediate Rights (8.10.3), TPIDR_EL0/TLS (6.5.6). AH5-C (M-08/L-11/L-12): Defense-in-depth documentation — cross-subsystem composition (CrossSubsystem.lean), Badge constructor safety (Prelude.lean), MCP default rationale (Types.lean). AH5-D: Test fixture verification. AH5-E: Full documentation sync. AH5-F: Final gate. Gate: `test_full.sh` + `cargo test --workspace` + documentation sync. Zero sorry/axiom. **WS-AH PORTFOLIO COMPLETE** (v0.27.2–v0.27.6, 5 phases, 27 sub-tasks). See `docs/dev_history/audits/AUDIT_v0.27.1_WORKSTREAM_PLAN.md`
- **WS-AH Phase AH4 COMPLETE** (v0.27.5): Pre-Release Comprehensive Audit Remediation — Phase AH4: Version Consistency & CI Automation. 6 sub-tasks (AH4-A through AH4-F). AH4-A (H-02/HIGH): Updated `KERNEL_VERSION` in `rust/sele4n-hal/src/boot.rs` from `0.26.8` to `0.27.5` — UART boot banner now prints correct version; Rust workspace version updated from `0.27.1` to `0.27.5`. AH4-B: CLAUDE.md version reference updated to `0.27.5`. AH4-C: `docs/spec/SELE4N_SPEC.md` package version updated to `0.27.5`. AH4-D: All 10 i18n README version badges updated to `0.27.5`. AH4-E: Created `scripts/check_version_sync.sh` — CI-safe script validating 15 version-bearing files against canonical `lakefile.toml` version. AH4-F: Integrated `check_version_sync.sh` into Tier 0 hygiene (`test_tier0_hygiene.sh`). Gate: `cargo test --workspace` + `test_smoke.sh` + `check_version_sync.sh`. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.27.1_WORKSTREAM_PLAN.md`
- **WS-AH Phase AH3 COMPLETE** (v0.27.4): Pre-Release Comprehensive Audit Remediation — Phase AH3: Capability, Architecture & Decode Hardening. 3 sub-tasks (AH3-A through AH3-C). AH3-A (L-04/LOW): Fixed `cspaceRevokeCdtStrict` CDT-orphan bug — error branch preserves CDT node instead of removing it on slot deletion failure; preservation theorem simplified. AH3-B (L-08/LOW): Refactored `setIPCBufferOp` to delegate to `storeObject` — eliminates manual struct-with replication; `setIPCBufferOp_capabilityRefs_eq` renamed to `setIPCBufferOp_capabilityRefs_cleaned`. AH3-C (L-14/LOW): Replaced hardcoded ASID limit `65536` with `maxASID : Nat` parameter in `decodeVSpaceMapArgs`/`decodeVSpaceUnmapArgs` — API dispatch passes `st.machine.maxASID`; 10+ theorems updated; delegation theorems restructured with `funext`; 12 test call sites updated. Gate: `lake build` (256 jobs) + `test_smoke.sh`. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.27.1_WORKSTREAM_PLAN.md`
- **WS-AH Phase AH2 COMPLETE** (v0.27.3): Pre-Release Comprehensive Audit Remediation — Phase AH2: IPC Donation Safety & Boot Pipeline. 7 sub-tasks (AH2-A through AH2-G). AH2-A/B (M-02/MEDIUM): `applyCallDonation` and `applyReplyDonation` return type changed from `SystemState` to `Except KernelError SystemState` — donation errors propagated instead of silently swallowed. AH2-C: 7 call sites updated (4 API.lean, 3 Donation.lean) + 4 test call sites + `dispatchWithCap_call_uses_withCaps` theorem. AH2-D: 4 preservation theorems updated to conditional postconditions (`applyCallDonation_scheduler_eq`, `applyCallDonation_machine_eq`, `applyReplyDonation_machine_eq`, `applyCallDonation_preserves_projection`). AH2-E (M-03/L-16): `defaultMachineConfig` constant + `PlatformConfig.machineConfig` field. AH2-F: `applyMachineConfig` integrated as final step of `bootFromPlatform`; 8 new preservation lemmas; `bootFromPlatform_machine_eq` replaced with `bootFromPlatform_machine_physicalAddressWidth` and `bootFromPlatform_machine_non_config_fields`. AH2-G (L-02/LOW): `timeoutAwareReceive` returns `.error .endpointQueueEmpty` for missing `pendingMessage`. Gate: `lake build` (256 jobs) + `test_smoke.sh`. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.27.1_WORKSTREAM_PLAN.md`
- **WS-AH Phase AH1 COMPLETE** (v0.27.2): Pre-Release Comprehensive Audit Remediation — Phase AH1: Critical IPC Dispatch Correctness. 5 sub-tasks (AH1-A through AH1-E). AH1-A (H-01/HIGH): `endpointSendDualChecked` delegates to `endpointSendDualWithCaps` (was `endpointSendDual`) — checked `.send` performs IPC capability transfer, 3 new params, return type `Kernel CapTransferSummary`. AH1-B: Updated `dispatchWithCapChecked` `.send` arm. AH1-C: 8 NI theorems updated (Wrappers, Soundness, Operations). AH1-D (M-01/MEDIUM): `validateVSpaceMapPermsForMemoryKind` wired into `.vspaceMap` dispatch — device memory execute permission blocked. AH1-E: 10 test call sites updated. Gate: `lake build` (256 jobs) + `test_smoke.sh` + `test_full.sh`. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.27.1_WORKSTREAM_PLAN.md`
- **WS-AG Phase AG9 COMPLETE** (v0.27.0): H3 Hardware Binding Audit Remediation — Phase AG9: Testing + Validation. 7 sub-tasks (AG9-A through AG9-G). AG9-A: QEMU integration testing (`scripts/test_qemu.sh`, boot validation, CI-safe skip). AG9-B: Hardware constant cross-check (`scripts/test_hw_crosscheck.sh`, 15 BCM2712 constants, `docs/hardware_validation/rpi5_cross_check.md`). AG9-C: WCRT empirical validation (`profiling.rs`, `LatencyStats`, PMCCNTR_EL0 cycle counter, `docs/hardware_validation/wcrt_empirical_validation.md`). AG9-D: RunQueue cache performance profiling (`docs/hardware_validation/runqueue_cache_performance.md`). AG9-E: Badge overflow validation (`tests/BadgeOverflowSuite.lean` — 22 tests, 7 Rust tests in sele4n-types, `docs/hardware_validation/badge_overflow_validation.md`). AG9-F: Speculation barriers (CSDB/SB in `barriers.rs`, exception dispatch CSDB in `trap.rs`, GIC dispatch CSDB in `gic.rs`, `speculation_safe_bound_check`, `has_feat_csv2`, 7 tests, clippy fixes in `mmio.rs`, `docs/hardware_validation/speculation_barriers.md`). AG9-G: Full RPi5 hardware test suite (`scripts/test_hw_full.sh`, 5-phase orchestration, `docs/hardware_validation/rpi5_full_test_report.md`). Gate: `lake build` + `test_smoke.sh` + `cargo test --workspace` (362 tests) + `cargo clippy --workspace` (0 warnings). Zero sorry/axiom. See `docs/audits/AUDIT_H3_HARDWARE_BINDING_WORKSTREAM_PLAN.md`
- **WS-AG Phase AG8 COMPLETE** (v0.26.9): H3 Hardware Binding Audit Remediation — Phase AG8: Integration + Model Closure. 7 sub-tasks (AG8-A through AG8-G). AG8-A: Timeout sentinel → explicit `timedOut : Bool` TCB field (eliminates GPR x0 sentinel collision risk). AG8-B: Cache coherency model (`CacheModel.lean` — `CacheLineState`, `CacheState`, 5 operations, 17 preservation theorems). AG8-C: Memory barrier semantics (`BarrierKind`, `barrierOrdered`, 4 theorems in `MmioAdapter.lean` — trivially satisfied in sequential model). AG8-D: FrozenOps deferred to WS-V (experimental status maintained). AG8-E: CDT `descendantsOf` fuel sufficiency placeholders (`maxCdtDepth := 65536`, 2 placeholder theorems — substantive proofs deferred to WS-V). AG8-F: Donation chain k>2 cycle prevention building blocks (`donationChainAcyclic_general` re-extracts from `donationOwnerValid`, `blockedOnReply_cannot_call` — formal bridge to `blockingAcyclic` deferred to WS-V). AG8-G: Donation atomicity under interrupt disable (`donationAtomicRegion`, `donateSchedContext_machine_eq`). Fixed pre-existing simp warnings in RPi5 `RuntimeContract.lean`/`ProofHooks.lean`. Gate: `lake build` (256 jobs) + `test_full.sh`. Zero sorry/axiom. See `docs/audits/AUDIT_H3_HARDWARE_BINDING_WORKSTREAM_PLAN.md`
- **WS-AG Phase AG7 COMPLETE** (v0.26.8): H3 Hardware Binding Audit Remediation — Phase AG7: FFI Bridge + Proof Hooks. 6 sub-tasks (AG7-A through AG7-F). Lean `@[extern]` FFI declarations for 17 Rust HAL functions (`FFI.lean`). MMIO volatile primitives + `MmioAdapter.lean`. Production `AdapterProofHooks` (`rpi5ProductionAdapterProofHooks`) with substantive preservation proofs for all 4 adapter paths. `proofLayerInvariantBundle` extended to 11 conjuncts (`notificationWaiterConsistent` added). Key theorems: `contextSwitchState_preserves_proofLayerInvariantBundle` (11-conjunct preservation through atomic context switch), `contextSwitchState_preserves_ipcInvariantFull` (15-conjunct IPC invariant with `passiveServerIdle` transfer), `rpi5Production_adapterContextSwitch_preserves` (end-to-end production contract context-switch preservation), `registerContextStableCheck_budget` (boolean→proposition budget bridge). TLB composition theorems verified complete from AG6. Gate: `lake build` (256 jobs) + `cargo test --workspace` + `test_smoke.sh` + `test_full.sh`. Zero sorry/axiom. See `docs/audits/AUDIT_H3_HARDWARE_BINDING_WORKSTREAM_PLAN.md`
- **WS-AG Phase AG6 COMPLETE** (v0.26.7): H3 Hardware Binding Audit Remediation — Phase AG6: Memory Management (ARMv8 Page Tables). 9 sub-tasks (AG6-A through AG6-I). Lean: `PageTable.lean` — ARMv8 4-level page table descriptor types (`PageTableLevel`, `Shareability`, `AccessPermission`, `PageAttributes`, `PageTableDescriptor`), encode/decode (`descriptorToUInt64`/`uint64ToDescriptor`), `pageTableWalk` via structural recursion (no fuel), `pageTableWalk_deterministic`, W^X bridge `hwDescriptor_wxCompliant_bridge`. `VSpaceARMv8.lean` — `ARMv8VSpace` with shadow `VSpaceRoot`, `VSpaceBackend` instance (all 7 obligations discharged), `simulationRelation` + `lookupPage_refines` + `vspaceProperty_transfer` refinement proofs. `AsidManager.lean` — `AsidPool` bump allocator with generation tracking, free list reuse, exhaustion guard, `wellFormed` predicate, 3 preservation theorems, `asidPoolUnique` invariant. `TlbModel.lean` extensions — hardware adapter functions, 3 equivalence theorems, 3 consistency preservation theorems, `tlbBarrierComplete`, composition. Rust: `tlb.rs` — TLBI VMALLE1/VAE1/ASIDE1/VALE1 with DSB ISH + ISB. `cache.rs` — DC CIVAC/CVAC/IVAC/ZVA, IC IALLU/IALLUIS, `cache_range` aligned iteration. ~1005 lines Lean + ~406 lines Rust + ~113 lines TlbModel extensions. Gate: `cargo test --workspace` (306 tests) + `cargo clippy --workspace` (0 warnings) + `lake build` (256 jobs) + `test_smoke.sh` + `test_full.sh`. Zero sorry/axiom. See `docs/audits/AUDIT_H3_HARDWARE_BINDING_WORKSTREAM_PLAN.md`
- **WS-AG Phase AG5 COMPLETE** (v0.26.6): H3 Hardware Binding Audit Remediation — Phase AG5: Interrupts + Timer. 7 sub-tasks (AG5-A through AG5-G). GIC-400 full driver (`gic.rs`): distributor init (GICD_CTLR/IGROUPR/IPRIORITYR/ITARGETSR/ICPENDR/ISENABLER), CPU interface init (GICC_PMR/BPR/CTLR), acknowledge/dispatch/EOI sequence with `dispatch_irq<F>()` generic handler, `init_gic()` convenience. ARM Generic Timer driver (`timer.rs`): system register accessors (CNTPCT_EL0, CNTFRQ_EL0, CNTP_CVAL_EL0, CNTP_CTL_EL0), `AtomicU64` state, `init_timer(tick_hz)`, `reprogram_timer()` counter-relative, RPi5 54 MHz default 1000 Hz. Interrupt management (`interrupts.rs`): `disable_interrupts()`/`restore_interrupts()`/`enable_irq()`/`with_interrupts_disabled<F,R>()` via DAIF register. Lean: `TimerInterruptBinding` in `TimerModel.lean`, `timerInterruptHandler` in `InterruptDispatch.lean`, `MachineState.interruptsEnabled` field + 10 frame theorems (including `withInterruptsDisabled_restores`), AG5-G atomicity proofs (5 preservation theorems in `ExceptionModel.lean`). `handleInterrupt` NI step (35th `KernelOperation`) + cross-subsystem bridge. Boot sequence updated (Phase 3: GIC→timer→IRQ enable). ~1184 lines added. Gate: `cargo test --workspace` (288 tests) + `cargo clippy --workspace` (0 warnings) + `lake build` (256 jobs) + `test_smoke.sh`. Zero sorry/axiom. See `docs/audits/AUDIT_H3_HARDWARE_BINDING_WORKSTREAM_PLAN.md`
- **WS-AG Phase AG4 COMPLETE** (v0.26.5): H3 Hardware Binding Audit Remediation — Phase AG4: HAL Crate + Boot Foundation. 7 sub-tasks (AG4-A through AG4-G). New `sele4n-hal` Rust crate (4th workspace crate): ARM64 HAL with CPU instructions (WFE/WFI/NOP/ERET), memory barriers (DMB/DSB/ISB), system register accessors (MRS/MSR), PL011 UART driver (0xFE201000, 115200 8N1), MMU initialization (identity-mapped L1 block descriptors, MAIR/TCR/SCTLR), exception vector table (16 entries, 2048-byte aligned), trap entry/exit assembly (272-byte TrapFrame, full GPR save/restore), kernel linker script (0x80000 entry), boot sequence (BSS zero, stack setup, UART→MMU→VBAR). GIC-400 and timer stubs for AG5. ~900 lines Rust + ~300 lines assembly. Gate: `cargo test --workspace` (267 tests) + `cargo clippy --workspace` (0 warnings) + `lake build` + `test_smoke.sh`. Zero sorry/axiom. See `docs/audits/AUDIT_H3_HARDWARE_BINDING_WORKSTREAM_PLAN.md`
- **WS-AG Phase AG3 COMPLETE** (v0.26.4): H3 Hardware Binding Audit Remediation — Phase AG3: Platform Model Completion. 8 sub-tasks (AG3-A through AG3-H). `classifyMemoryRegion` platform memory map classification (P-01). `applyMachineConfig` full field application with 6 new `MachineState` fields (P-04). ARM64 exception model with ESR classification and SVC→syscall dispatch (FINDING-04). Interrupt dispatch with GIC-400 acknowledge→handle→EOI sequence (FINDING-06). Hardware timer model binding at 54 MHz for RPi5 (FINDING-08). Exception level EL0/EL1 model with privilege predicates (H3-ARCH-05). System register file with read/write operations and frame lemmas (H3-ARCH-06). VSpaceBackend HashMap instance with all typeclass laws proven (H3-ARCH-10). KernelError 44→49 variants. Rust ABI synchronized: `error.rs` 5 new variants (VmFault=44..InvalidIrq=48), all conformance tests updated, 239 Rust tests pass. Gate: `lake build` + `test_full.sh` + `cargo test --workspace`. Zero sorry/axiom. See `docs/audits/AUDIT_H3_HARDWARE_BINDING_WORKSTREAM_PLAN.md`
- **WS-AG Phase AG2 Audit COMPLETE** (v0.26.2): Post-implementation audit of AG2. Fixed critical `sched_context_configure` IPC buffer overflow bug — 5th parameter (domain) was never written to IPC buffer overflow slot, would cause stale domain read on ARM64 hardware. Added `&mut IpcBuffer` parameter matching `service_register` pattern. Clarified `DomainId::MAX_VALID` type-level vs ABI-level comment. Fixed CHANGELOG AG2-C version text. Gate: `cargo test --workspace` (239 tests), `cargo clippy --workspace` (0 warnings). Zero sorry/axiom.
- **WS-AG Phase AG2 COMPLETE** (v0.26.1): H3 Hardware Binding Audit Remediation — Phase AG2: Pre-Hardware Rust ABI Fixes. 3 sub-tasks (AG2-A through AG2-C). `MAX_DOMAIN` constant fix 255→15 matching Lean `numDomainsVal = 16` (R-05). `sele4n-sys/src/sched_context.rs` typed wrappers for syscalls 17-19 completing all 25 syscall coverage (R-01). Rust workspace version sync 0.25.6→0.26.1 (RUST-04). Bonus clippy fix in `tcb.rs`. Gate: `cargo test --workspace` (239 tests), `cargo clippy --workspace` (0 warnings). Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_H3_HARDWARE_BINDING_WORKSTREAM_PLAN.md`
- **WS-AG Phase AG1 COMPLETE** (v0.26.0): H3 Hardware Binding Audit Remediation — Phase AG1: Pre-Hardware Lean Code Fixes. 6 sub-tasks (AG1-A through AG1-F). CBS re-enqueue at effective priority with `resolveInsertPriority` helper, 5 call sites fixed (S-04). `timeoutBlockedThreads` diagnostic error collection (S-05). `uniqueWaiters` composed as 15th conjunct of `ipcInvariantFull` (F-T02). `bootFromPlatformWithWarnings` duplicate detection (P-03). `FrozenSchedulerState` `replenishQueue` field + 2 freeze proofs (F-F04). Runtime `crossSubsystemInvariant` checks — 10 decidable predicates (full coverage) integrated into test harness (T-01). Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_H3_HARDWARE_BINDING_WORKSTREAM_PLAN.md`
- **WS-AF PORTFOLIO COMPLETE** (v0.25.22–v0.25.27): Pre-Release Comprehensive Audit Remediation — 6 phases (AF1–AF6), 49 sub-tasks. Phase AF6 (v0.25.27): Rust ABI Fixes & Documentation Closure — `UnknownKernelError` variant (AF-20), `endpoint_reply_recv_checked` truncation detection (AF-21), stale comment fixes (AF-35/AF-36), `SchedContext` capability marker (AF-37), `IpcMessage.length` privacy (AF-38), shellcheck CI enforcement (AF-46), full documentation sync. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.25.21_WORKSTREAM_PLAN.md`
- **WS-AF Phase AF5 COMPLETE** (v0.25.26): Pre-Release Comprehensive Audit Remediation — Phase AF5: IPC, Capability, Lifecycle & Documentation. 10 sub-tasks (AF5-A through AF5-J). Fixed stale `pendingMessage = none` documentation (AF-12), timeout sentinel H3 migration documentation (AF-04), `endpointQueueRemove` direct insert rationale (AF-06), Badge Nat round-trip safety documentation (AF-15), `donationChainAcyclic` 2-cycle scope documentation (AF-39), tuple projection maintenance burden documentation (AF-26), CSpace resolution layers documentation (AF-27), `suspendThread` re-lookup rationale (AF-28), FrozenOps operation count fix 21→24 with 4 missing table entries (AF-43), duplicate `cdt_only_preserves_projection` removal (AF-48), high-heartbeat proof fragility documentation (AF-31). Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.25.21_WORKSTREAM_PLAN.md`
- **WS-AF Phase AF4 COMPLETE** (v0.25.25): Pre-Release Comprehensive Audit Remediation — Phase AF4: Information Flow, Cross-Subsystem & SchedContext. 8 sub-tasks (AF4-A through AF4-H). All `native_decide` eliminated from codebase (TCB reduction): `enforcementBoundary_is_complete` upgraded to `decide` (AF-16), `crossSubsystem_pairwise_coverage_complete` upgraded to `decide` (AF-17), bonus `crossSubsystemInvariant_default` fix. Fuel-sufficiency formal argument sketch for `collectQueueMembers` (AF-07). `serviceHasPathTo` conservative fuel-exhaustion documentation (AF-18). `LabelingContextValid` seL4 separation kernel design cross-reference with `labelingContextValid_is_deployment_requirement` witness theorem (AF-33). CBS 8× bandwidth bound precision gap documentation (AF-08). `schedContextYieldTo` kernel-internal design documentation (AF-30/AF-47). Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.25.21_WORKSTREAM_PLAN.md`
- **WS-AF Phase AF3 COMPLETE** (v0.25.24): Pre-Release Comprehensive Audit Remediation — Phase AF3: Platform & DeviceTree Hardening. 7 sub-tasks (AF3-A through AF3-G). `parseFdtNodes` fuel-exhaustion fix: `go` and `parseNodeContents` return `none` on fuel exhaustion instead of silent truncation (AF-05), full MMIO write byte-range validation for `mmioWrite32` (`addr+3`), `mmioWrite64` (`addr+7`), and `mmioWrite32W1C` (`addr+3`) with 3 `_rejects_range_overflow` supplementary theorems (AF-09a), MMIO write-semantics model documentation (AF-09b), `extractPeripherals` 2-level depth limitation documentation (AF-32), `natKeysNoDup` opaque HashSet design tradeoff documentation (AF-19), four DTB/boot stub annotations: `classifyMemoryRegion` always-RAM (AF-41), `fromDtb` stub (AF-42), `bootFromPlatform` empty-config (AF-44), `applyMachineConfig` partial-copy (AF-45). Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.25.21_WORKSTREAM_PLAN.md`
- **WS-AF Phase AF2 COMPLETE** (v0.25.23): Pre-Release Comprehensive Audit Remediation — Phase AF2: State & Model Hardening. 7 sub-tasks (AF2-A through AF2-G). Machine-checked `storeObject` capacity safety: `storeObject_existing_preserves_objectIndex_length` + `retypeFromUntyped_capacity_gated` + `storeObject_capacity_safe_of_existing` (AF-03), `SchedContextId.ofObjIdChecked` sentinel guard (AF-22), W^X proof obligation in builder-phase `mapPage` with `mapPage_wxCompliant` theorem (AF-24), `apiInvariantBundle_frozenDirect` scope documentation (AF-25), `RegisterFile` non-lawful BEq documentation (AF-23), CDT `descendantsOf` transitive closure completeness deferral documentation (AF-34). Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.25.21_WORKSTREAM_PLAN.md`
- **WS-AF Phase AF1 COMPLETE** (v0.25.22): Pre-Release Comprehensive Audit Remediation — Phase AF1: Scheduler Correctness & Proof Gaps. 10 sub-tasks (AF1-A through AF1-J). `blockingAcyclic` added as 10th predicate of `crossSubsystemInvariant` (AF-01/HIGH-1), `bounded_scheduling_latency` renamed to `wcrtBound_unfold` (AF-02/HIGH-2), `pip_deterministic` renamed to `pip_congruence` (AF-13), `eventuallyExits` hypothesis documentation (AF-14), priority/domain fallback documentation (AF-10/AF-11), RunQueue.size and FIFO documentation (AF-40/AF-49), blockingServer pre-mutation read documentation (AF-29). New theorems: `blockingChain_step`, `blockingChain_congr`, `blockingAcyclic_frame`. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.25.21_WORKSTREAM_PLAN.md`
- **WS-AE Phase AE6 COMPLETE** (v0.25.21): Production Audit Remediation — Phase AE6: Testing, Documentation & Closure. 8 sub-tasks (AE6-A through AE6-H). PIP suite execution in test scripts (U-25), `buildChecked` upgrade for 4 test suites (T-07/T-F02/T-F03), `test_rust.sh` CI skip logging (T-F17), Rust ABI register comment fix (R-F01), CLAUDE.md large-file list refresh, documentation synchronization, fixture verification. Zero sorry/axiom. WS-AE portfolio complete. See `docs/dev_history/audits/AUDIT_v0.25.14_WORKSTREAM_PLAN.md`
- **WS-AE Phase AE5 COMPLETE** (v0.25.20): Production Audit Remediation — Phase AE5: Platform, Service & Cross-Subsystem. 7 sub-tasks (AE5-A through AE5-G). `collectQueueMembers` fuel-safe `Option` return (U-22), `registryEndpointUnique` invariant with duplicate endpoint rejection (U-20), `registryInterfaceValid` added to 9-predicate `crossSubsystemInvariant` (SVC-04), boot invariant bridge limitation documentation (U-21), NI boundary service orchestration documentation (U-10), `LabelingContextValid` deployment requirement documentation (IF-14). Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.25.14_WORKSTREAM_PLAN.md`
- **WS-AE Phase AE4 COMPLETE** (v0.25.19): Production Audit Remediation — Phase AE4: Capability, IPC & Architecture Hardening. 10 sub-tasks (AE4-A through AE4-J). CPtr masking in resolveCapAddress (U-17), VAddr canonicity in decodeVSpaceUnmapArgs (U-26), CDT acyclicity preservation proof with freshChild_not_reachable bridge (U-18), cdtMintCompleteness lifted to cross-subsystem composition (U-36), endpointQueueRemove fallback unreachability proofs (U-24), timeout sentinel dual-condition documentation (U-23), TLB targeted flush H3 preparation documentation (U-27), ipcBuffer_within_page theorem (U-32), receiverSlotBase plumbing documentation (U-37). Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.25.14_WORKSTREAM_PLAN.md`
- **WS-AE Phase AE3 COMPLETE** (v0.25.18): Production Audit Remediation — Phase AE3: Scheduler & SchedContext Correctness. 12 sub-tasks (AE3-A through AE3-L). Domain consistency enforcement (U-11), cancelDonation isActive+replenish queue fix (U-15), effective priority in resumeThread (U-16), handleYield gap documentation (S-03), schedContextConfigure replenishment queue reset (U-14), CBS bandwidth documentation (U-12/U-13), dead Budget.refill deletion (SC-06), frame theorem with full invExt threading (S-01), schedContextBind documentation (SC-09), timeoutBlockedThreads documentation (S-05). Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.25.14_WORKSTREAM_PLAN.md`
- **WS-AE Phase AE2 COMPLETE** (v0.25.16): Production Audit Remediation — Phase AE2: Data Structure Hardening & Production Reachability. 8 sub-tasks (AE2-A through AE2-H). RobinHood `4 ≤ capacity` enforcement (U-28), RadixTree checked build with key-bounds validation (U-29), fuel exhaustion documentation (U-30), FrozenOps partial mutation fix (U-31), FrozenOps experimental status documentation (U-02), Liveness production reachability via CrossSubsystem import (U-05), PIP transitive import verification (AE2-G). Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.25.14_WORKSTREAM_PLAN.md`
- **WS-AE Phase AE1 COMPLETE** (v0.25.15): Production Audit Remediation — Phase AE1: API Dispatch & NI Composition. 8 sub-tasks (AE1-A through AE1-H). Dispatch parity fixes (U-01/U-06), wildcard unreachability (U-07), switchDomain NI composition (IF-01/U-03), donation/PIP NI proofs (IF-02/U-04), master dispatch NI theorem (U-08). KernelOperation 32→34 constructors. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.25.14_WORKSTREAM_PLAN.md`
- **WS-AD PORTFOLIO COMPLETE** (v0.25.11–v0.25.14): Pre-Release Audit Remediation — 5 phases (AD1–AD5). 21 findings (7 actionable, 2 already addressed, 12 no-action). Phase AD4 (F-08): 35 cross-subsystem bridge lemmas (2 core + 33 per-operation) covering ALL 33 kernel operations that modify `objects`. Phase AD5: documentation sync & closure. Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.25.10_WORKSTREAM_PLAN.md`
- **WS-AC PORTFOLIO COMPLETE** (v0.25.3–v0.25.10): Comprehensive Audit Remediation — 6 phases (AC1–AC6), 42 sub-tasks. 3 HIGH, 9 MEDIUM, 9 LOW findings addressed. Phase AC6 COMPLETE (documentation, testing & closure: T-03 DecodingSuite, audit errata, workstream history). Zero sorry/axiom. See `docs/dev_history/audits/AUDIT_v0.25.3_WORKSTREAM_PLAN.md`
- **WS-AB PORTFOLIO COMPLETE** (v0.24.0–v0.25.5): Deferred Operations & Liveness Completion — 6 phases, 90 sub-tasks. All 5 deferred seL4 operations implemented: suspend/resume (D1), setPriority/setMCPriority (D2), setIPCBuffer (D3). Priority Inheritance Protocol (D4). Bounded Latency Theorem WCRT = D*L_max + N*(B+P) (D5). API Surface Integration & Closure (D6). Rust ABI synchronized (SyscallId 25, KernelError 44). Zero sorry/axiom. See `docs/dev_history/planning/WS_AB_DEFERRED_OPERATIONS_WORKSTREAM_PLAN.md`
- **WS-AA Phase AA2 COMPLETE**: CI & Infrastructure Hardening — 6 sub-tasks (AA2-A through AA2-F), all complete (v0.23.23). Zero sorry/axiom. See `docs/dev_history/AUDIT_v0.23.21_WORKSTREAM_PLAN.md`
- **Next milestone**: Raspberry Pi 5 hardware integration (tracked per-ID in `docs/audits/AUDIT_v0.29.0_DEFERRED.md` §A; no currently-active workstream — both WS-V and AG10 are closed per `docs/WORKSTREAM_HISTORY.md`)
- **Hardware target**: Raspberry Pi 5 (ARM64)

## PR checklist

- [ ] Workstream ID identified
- [ ] Scope is one coherent slice
- [ ] Transitions are explicit and deterministic
- [ ] Invariant/theorem updates paired with implementation
- [ ] `test_smoke.sh` passes (minimum); `test_full.sh` for theorem changes
- [ ] Documentation synchronized
- [ ] No website-linked paths renamed or removed (see `scripts/website_link_manifest.txt`)

## Vulnerability reporting

While executing any task in this codebase, if you discover a possible software
vulnerability that could reasonably warrant a CVE (Common Vulnerabilities and
Exposures) designation, you **must** immediately report it to the user before
continuing. This applies to vulnerabilities found in:

- **This project's code** — logic errors in transition semantics, capability
  checks, information-flow enforcement, or any other component that could lead
  to privilege escalation, information leakage, denial of service, or violation
  of security invariants.
- **Dependencies and toolchain** — known or suspected vulnerabilities in Lean,
  Lake, elan, or any vendored/imported library encountered during builds,
  updates, or code review.
- **Build and CI infrastructure** — insecure script patterns (e.g., command
  injection in shell scripts, unsafe file permissions, unvalidated inputs in
  test harnesses) that could be exploited in a development or CI environment.
- **Model/specification gaps** — cases where the formal model fails to capture
  a security-relevant behavior of the real seL4 kernel, creating a false
  assurance gap that could mask a real-world vulnerability.

**What to report:**

1. **Summary** — a concise description of the vulnerability.
2. **Location** — file path(s) and line number(s) where the issue exists.
3. **Severity estimate** — your assessment of impact (Critical / High / Medium
   / Low) and exploitability.
4. **Reproduction or evidence** — how the issue manifests or could be triggered.
5. **Suggested remediation** — if apparent, a recommended fix or mitigation.

**How to report:**

- Stop current work and surface the finding in your response immediately.
- Do **not** silently fix a CVE-worthy vulnerability — always flag it explicitly
  so it can be tracked, triaged, and disclosed appropriately.
- If the vulnerability is in a third-party dependency, note whether an upstream
  advisory already exists.

This requirement applies regardless of whether the vulnerability is directly
related to the current task. Vigilance during routine work is one of the most
effective ways to catch security issues early.
